# 心臓の分け直し（2026-08-04）

`useAppWiring`（1,226行・上限1,250まで残り24）をどうにかしたい、から始めた。
**結論から書く: 配線側は割れない。今日やったのは、割れるようにするための前段。**

---

## 測り直した（前の数字は過小だった）

2026-08-02 の記録は「どこで横に切っても 106〜413個またぐ」だった。
今日、**TypeScript の compiler API で測り直した**（`scratchpad` の使い捨て）。
開発部 CLAUDE.md が名指ししている「使い捨てスクリプトで3回とも嘘の数字が出た」3つの穴を、
記号解決がまとめて避ける:

```
① 片方向しか数えない      → 中→外・外→中の両方を出す
② top-level しか見ない     → 識別子を全部歩いて、宣言の位置で判定する
③ 同名の再宣言を1つ目だけ  → getSymbolAtLocation は**その場のスコープ**を解く
```

### **4つ目の穴を、自分で踏んだ**

最初の結果は「切る位置によっては またぐ数 2」と出た。**束の literal
（`{ orderedTabs, TAB_DEFS, … }`）が「何も受け取っていない」と出るはずがない。**

原因: **省略記法のプロパティは、記号解決すると「値」ではなく「プロパティ」を指す。**
`checker.getShorthandAssignmentValueSymbol` を通さないと、
84個の名前がまるごと「この文の中で宣言された」と数えられる。

→ 直したら数字は**大きく**なった。前の 106〜413 は**過小**だった。

```
切る位置（146文・145通り全部）  またぐ数
  上から少し                      10〜39
  中盤                           300〜506
  末尾（束だけ残す）              153
```

**横には切れない。** この判断は 2026-08-02 のまま生きている。

---

## 外へ出せる塊も、機械で探した

推測で切り口を決めない（このリポジトリは切り口の見立てを2回外している）。
60行以上の**連続した範囲を全部**試して、またぐ数の少ない順に並べた:

```
  行の範囲     行数  受け取る  返す  合計
  769〜831      63      32      5    37   ← 唯一 40 の内側
  312〜373      62      22     31    53
  230〜291      62      14     40    54
```

**63行を出すのに37個受け取る**——引数37個のフックができるだけで、
リファクタリング部が「40個ルールを撤回した」理由（書く側の手間しか測っていない）に
まっすぐ当たる。**採らなかった。**

---

## 記録が指していた本筋（B）をやった

`引き継ぎ-App分割.md` の段階4・5 はこう書いてある:

> 割れないのは配線が悪いからではなく、**画面側が「8つの大きな束」で受けているから**。
> 束を小さくしていけば、集める先が分かれて配線も分かれる。
> 1. 一番大きい `rightPanel`（約60個）を、タブ単位の小さな心臓へ割る

`rightPanel` は**81個に育っていた**（記録の「約60」から）。

### 途中で見立てを2回外した（測って分かった）

| 案 | 測った結果 |
|---|---|
| 右パネルの配線を丸ごと外へ出す | 31 + 38 = **69**。目安40超え |
| タブごとに5本へ割る（各5〜17に見えた） | **足し合わない**——5つが同じ文（`useLibraries` `useLibraryOrganize` `useBandDrag`）を取り合っている。タブ固有の文は**2〜8行**しかない |

→ **配線側を割るのではなかった。**

### 本当の問題は props の数ではなかった

`RightPanelArea`（410行）は、心臓から81個を取り出して5つのタブへ配る役だが、
**配るだけでなく、タブごとの糊まで JSX の中に直書きしていた**——
アイコンタブだけで52行のうち35行が「選んでいるテロップに付ける」
「右クリックの品書き」の中身だった。

困るのは行数ではなく、**「そのタブに何が要るか」がどこにも書いていない**こと。
81個の束から拾う所と、名前の付け替えと、糊と、JSX が同じ場所で混ざっていた。

### やったこと: タブ1つにつきフック1つ

```
state/useProjectBinTab.ts    96行
state/useTelopTab.ts         56行
state/useIconTab.ts          91行
state/useSeTab.ts            82行
state/useTransitionsTab.ts  125行
```

区画の側はこれだけになった:

```tsx
{rightTab === 'project'    && <ProjectBinTab      {...projectBinTab} />}
{rightTab === 'telop'      && <TelopTemplatesTab  {...telopTab} />}
{rightTab === 'icon'       && <IconLibraryTab     {...iconTab} />}
{rightTab === 'se'         && <SeLibraryTab       {...seTab} />}
{rightTab === 'transition' && <TransitionsTab     {...transitionsTab} />}
```

| | 前 | 後 |
|---|---|---|
| `RightPanelArea` | 410行 | **114行** |
| 心臓から受け取る物 | **81個** | **8個**（タブ帯に要る分だけ） |

### **`useAppWiring` は1行も増えない**

タブのフックは**自分で心臓を見に行く**（`useRightPanel` / `useDoc` / `useSel` …）。
配線の大元を経由しないので、あちらは太らない——**ここが肝**で、
「画面から出した物を配線へ押し込む」形にすると本末転倒になる。

---

## 途中で捕まった2つ

- **`{cond && <Tab {...useIconTab()} />}` と書いた。**
  タブを切り替えるたびに**フックの数が変わる**（React の規則違反）。
  **型検査では出ない**ので、`RightPanelArea` の真上にコメントで残した
- **`mode: 'toggle' | 'range' | 'set'` と推測で書いた。** 実際は `'one'`。
  型検査がその場で指した——**渡す側が実物なので、手で書いた型は必ず照合される**

---

## **記録が言っていた理屈を測ったら、成り立たなかった**

`引き継ぎ-App分割.md` はこう書いていた:

> 割れないのは配線が悪いからではなく、画面側が「8つの大きな束」で受けているから。
> **束を小さくしていけば、集める先が分かれて配線も分かれる。**

右パネルを割った直後に、**この理屈そのものを測った**（同じ道具で、束ごとに
「その束の配線を外へ出す費用」を両方向で数えた）:

```
             連れて行く文  行数   外から受け取る   **残る側へ返す**
dialogs         77        347        72             325
menus           60        259        60             312
previewCtx      75        331        70             310
timelineOps     63        290        62             280
timelineView    30        107        27             180
```

**どの束も、外へ出そうとすると 200〜400個を残る側へ返すことになる。**

理由: 束の入力を作っているのは、**他の全部の束にも配っている共通のフック**。
束を消費側でいくつに割っても、**作っている側は1つのまま**なので配線は分かれない。
2026-08-02 が「絡まりは輪ではなく**網**だった」と書いたとおりで、
**網は消費側からはほどけない。**

→ **`dialogs` と `menus` に同じ手を当てても、本丸には1ミリも近づかない。**
  やらなかった（`AppDialogs` は315行で、大きさの決まりにも触れていない）。

### では何が要るのか

**「フックが値を返し、`useAppWiring` が配り直す」形そのものが原因。**
各フックが**自分の心臓へ直接書く**形にすれば、配り役が要らなくなる——
それが本当の分け直しで、今日やった範囲よりずっと深い。

**先に測ること。** 上の表を作り直す道具の作り方は、この文書の
「4つ目の穴」を必ず読む（**省略記法を解かないと嘘が出る**）。

### いま `useAppWiring` は上限まで残り24行

割れないことが3つの測定で確定しているので、次に何か足すときは
**a) 各フックが自分の心臓へ書く形へ変える**（本筋・大きい）か、
**b) `fileSize.test.ts` の DEBT で上限を上げる**（最後の手）かのどちらか。
**「良い切り口を探す」はもうやらなくてよい。全部試して無い。**

### 測り直しの道具

`scratchpad` の使い捨てで消える。必要になったら作り直すこと。

---

## `main/exportGraph.ts`（946行）— 取説が最初から答えを書いていた

18項目の取説が、**話題を4つ宣言していた**（共通の小道具／重ねる段／本編の切片／
音を混ぜる）。しかも本文は**その順に連続して並んでいた**。
つまり切り口は 08-03 の時点で書いてあり、**誰も読み返さなかっただけ**。

### 5つ目の穴: **型の宣言も「両側に置ける」**

最初に測ったら、どの群も「返す」が 2〜27 で割れなさそうに見えた。原因は道具側:

| 直した順 | 数えてはいけなかった物 | なぜ |
|---|---|---|
| 4つ目 | `import` 文 | 両側で書ける（前日に直した） |
| **5つ目** | `interface` / `type` の宣言 | **同じく両側で書ける**（`ExportCrop` など） |

**原則が見えた: 両側に置ける物は「連れて行く」に数えない。**
数えると、どの群も同じだけ膨らんで**全部が割れないように見える**。

直したあとの実測:

| 群 | 連れて行く文 | 行 | 受け取る | **返す** |
|---|---|---|---|---|
| 小道具 | 3 | 28 | 0 | **0** |
| 重ねる段 | 8 | 245 | 0 | **1** |
| 本編の切片 | 13 | 239 | 0 | **1** |
| 音を混ぜる | 2 | 80 | 0 | **0** |

`useTimelineEdit` と同じで、**先に土台（小道具）を出すと上の3つが独立する**。

### 割った形（依存は一方向・横のつながり無し）

```
exportRun ─┬→ exportOverlays  316 ─┐
           ├→ exportSegments  336 ─┼→ exportFilters 53 → exportTypes 169
           ├→ exportAudioMix  142 ─┘
           └→ exportSpawn     268
```

**組み立ての3つは互いを知らない。** 共通の物（型・小道具）は下へ落としてあるので、
「重ねる段が切片を見に行く」という横の線が生えない。

### 検査側も1つ直した（`readability.test.ts`）

取説に `ExportPayload` や `RAW_BASE_A` を並べたら「**本体が無い**」と**嘘の赤**が出た。
照合が**関数しか実体と認めていなかった**ため（`canon.test.ts` の `declares()` で
前日に踏んだのと同じ穴）。直し方は**義務と許可を分ける**:

- 並べる**義務**は top-level の関数だけ（今までどおり）
- 型・目印の定数は「**並べてもよい**」（`topLevelNamed`）。ただし嘘は赤

※ 型まで**必須**にしたら12ファイルが赤くなった（フックの `*Deps` が全部並ぶ）。
   目次が読めなくなるので、そこは義務にしない。

---

## 確認

`npm run verify` 934件緑／`npm run build` 通る／
`--only=アイコン,見本帳,テンプレ,SE,効果音,トランジション,素材 --fast` **44件緑**。
**振る舞いは1つも変えていない**（配線の置き場を移しただけ）。

`exportGraph` を割ったあと: `npm run verify` 緑／`--only=書き出 --fast` **22件緑**。
こちらも**1文字も書き写していない**（行範囲をそのまま運び、頭だけ新しく書いた）。

---

## 500超を片付けた（2026-08-04）

| ファイル | 前 | 後 | 出した物 | 測った値（受け取る／返す） |
|---|---|---|---|---|
| `main/exportGraph.ts` | 946 | **消滅** | 型・小道具・重ねる段・切片・音 の5本 | 0〜1 |
| `state/useTimelineEdit.ts` | 959 | **176** | 6区画（心臓を1つも見なくなった） | 1〜12 ／ 0〜1 |
| `state/useTimelineDrag.ts` | 596 | **295** | `useTrackSelectTool` `useTelopDrag` | 1・7 ／ 0 |
| `state/useProjectFile.ts` | 542 | **223** | `useProjectApply`（**読む側／書く側**で割れた） | 24 ／ 0 |
| `lib/telopStyle.ts` | 534 | **439** | `telopFill`（色とグラデ） | **0 ／ 0** |
| `state/useMediaDrop.ts` | 533 | **243** | `useMediaPlace` | 5 ／ 1 |
| `state/usePreviewManip.ts` | 526 | **413** | `usePreviewAnchor`（土台） | 3 ／ 0 |
| `state/usePlaybackEngine.ts` | 521 | **282** | `useSegClock`（A面/B面） | 4 ／ 2 |

**残る500超は3本だけ。どれも「割らない」が測ったうえでの結論。**

| ファイル | 行 | なぜ割らないか |
|---|---|---|
| `useAppWiring.tsx` | 1226 | 3つの測定で確定（横に切ると300〜506またぐ／束を外へ出すと200〜400返す） |
| `lib/telopSvg.ts` | 594 | 448行が1本のパイプライン。**94行を出すのに導管21本**。割るなら作り直し＋画素の確認 |
| `state/useClipDrag.ts` | 524 | **わざと。** 3種類は同じことをしていて、バラバラに置いた頃に「端の伸ばし方が2通り」の事故が起きている |

### 繰り返し出た形

1. **またぐ物こそが土台。** 「どこで切っても導管になる」と書いてある所ほど、
   先にそれを出すと残りが 0 になる（`mapContentTimes` `maybeTrackSelect`
   `cropFilter` `基準点` の4回とも）。
2. **取説が切り口を先に書いている。** `exportGraph` の18項目は話題を4つ
   宣言していて、本文もその順に並んでいた。1年分の目で見落としていただけ。
3. **読む側と書く側で割れることがある。** `useProjectFile` は setter を
   1つも持たない側と、値を1つも読まない側に分かれた。
4. **測る道具の穴は5つ見つかった**（名前照合／省略記法／分割代入／import／型の宣言）。
   原則は1つ: **両側に置ける物は「連れて行く」に数えない。**
5. **deps の型は手で書かない。** `Pick<親の型, …>` で引く。手で書いて3か所ズレた。

---

## 残り3本を「中身を読んでから」決め直した（2026-08-04・同日夜）

前の判断は**測っただけで、通しでは読んでいなかった**。読んだら2本ひっくり返った。

| ファイル | 前の判断 | 読んだ結果 |
|---|---|---|
| `useClipDrag` 524 | わざと割らない | **写しを消した → 431**（判断は合っていたが理由が違った） |
| `telopSvg` 594 | 割らない（導管21本） | **割れた → 445 + 268**（導管を1本にする道があった） |
| `useAppWiring` 1226 | 割れない | **割れない**（読んでも同じ。ただし地図が取れた） |

### `useClipDrag` — 並べて置くことは、写しを1本にする代わりにならない

頭は「決め事は共通」と宣言していた。**中は3通りに割れていた:**

| | レザーの分け方 | 右端 | 左端 |
|---|---|---|---|
| 効果音 | `splitAt` | `trimRight` | `trimLeft` |
| **画像** | **手書き** | **手書き** | **手書き** |
| 映像 | 手書き | `trimRight` | `trimLeft` |

「バラバラのファイルに置くと片方だけ直る」は正しい。**が、同じファイルに
3回書いても同じことが起きる。** 直したのは置き場所ではなく写しの数:

```
lib/clipDragLoop     掴んでから離すまでの段取り（3回 → 1回）
shared/lanes         canDropOn … 落としてよい段か（3回 → 1回）
shared/clipEdit      shiftGroup … 束をずらす（3回 → 1回）
```

### `telopSvg` — **測った数字は正しく、そこから引いた結論が間違っていた**

「94行を出すのに導管21本」は本当。だが**導管を1本にする道があった**——
組版の下ごしらえを1つの値（`TelopLayout`）にまとめれば、段を積む側は
それだけを受け取ればよい。21本の名前は、まとめていなかったから21本だった。

**先に網を張った。** `npm run frames` は動きと prfpset しか見ておらず、
**テロップの絵には試験が1つも無かった**（頭の「突き合わせは npm run frames」は嘘）。
`telopSvg.golden.test.ts`（21ケース・SVG 文字列をそのまま固定）を書いてから動かし、
**出力が1バイトも変わらない**ことを確かめた。

> 網が無いまま動かしていたら、「画面では出るのに書き出すと違う」を静かに作っていた。
> **作り直しの前に網を張る**——それが「割れる／割れない」より先に来る。

### `useAppWiring` の地図（読んだうえで、やはり割れない）

```
  頭 40 ／ import 107 ／ **配線の本体 957** ／ 束の組み立て 113 ／ return 9
```

束113行は**名前を並べているだけ**で、宣言しているのは8個（timelineOps /
timelineView / previewCtx / leftPanel / rightPanel / header / menus / dialogs）。
つまり**切る余地があるのは配線957行だけ**で、そこが「網」。行を動かしては割れない。

**短くする道は1つだけ。** いま各フックは値を返し、ここが配り直している。
フックが**自分の心臓へ直接書く**形にすれば、配り役（束）が要らなくなる。

※ 08-04 の `RightPanelArea` の変更は**ここを1行も短くしていない**。
  タブのフックが配線を経由しないだけで、タブ帯が要る物は `rightPanel` に残るため。
  **区画側を直すだけでは短くならない**——直すのは値を返す側。

順にやれば1つ変えるごとに束が数行ずつ減る。**一度に全部やる必要はない。**

### この日いちばんの教訓

**測るのは、読まない言い訳にならない。** 3本とも「測って見送り」にしていたが、
通しで読んだら2本が動いた。数字は切り口の有無を教えるが、
**「その数字をどう小さくするか」は中身を読まないと出てこない。**
