# 引き継ぎ：リファクタリング（2026-07-31 〜 08-04）

**これ1本にまとめてある。** 前は5本に分かれていて、しかも**互いに矛盾していた**
（`引き継ぎ-App分割.md` は「配線は割れない」、`引き継ぎ-心臓の分け直し.md` は
「3つの測定で確定」と書き、**どちらも 08-04 に覆った**）。
古い4本は畳んで消した（`引き継ぎ-App分割` / `引き継ぎ-読める形` /
`引き継ぎ-受け口の型` / `引き継ぎ-2026-08-03-300行リファクタ`。git 履歴には残る）。

---

## 0. 結論だけ先に

```
App.tsx        11,404 → 200行   （07-31 〜 08-02）
useAppWiring    1,229 → 362行   （08-04）
src/ の 500行超   多数 → **0本**
```

**`src/` に 500行を超えるファイルは1本も無い。** 残るは e2e の2本
（`run.mjs` 1,024 / `09-モーション.mjs` 1,131）。

`npm run verify` **970件緑**／`npm run e2e` **256 / 256**（13.3分・初の全緑）。

### 次に触る人が守ること（これだけ守れば元に戻らない）

> **フックを1本足すとき、`useAppWiring` には書かない。囲い（`state/*Context.tsx`）を作る。**

足した直後に `npm run passthrough` を叩く。「いま囲いへ上げられるフック」に
**自分が出てきたら、書く場所を間違えた合図**。
見張りは `state/wiringSize.test.ts`（自分で呼ぶフック12本・450行が上限）。
**赤くなったら上限を上げない。囲いを作る。**

---

## 1. いちばん大事な教訓：**3回「割れない」と測って、3回とも間違っていた**

| いつ | 測った物 | 出した結論 |
|---|---|---|
| 08-02 | 横に切ったときにまたぐ名前 **106〜413個**（目安40） | 割れない |
| 08-04 昼 | 測り直したら **300〜506個**（前のは過小だった） | やはり割れない・**確定** |
| 08-04 昼 | 束を外へ出す費用 **200〜400個を残る側へ返す** | 3つの測定で確定・**「良い切り口を探す」はもうやらなくてよい** |
| 08-04 夜 | 通しで読んだ（測らずに読んだ） | 内訳が取れた。**やはり割れない** |
| **08-04 深夜** | **フックを1本ずつ囲いへ上げた** | **362行になった** |

**数字は全部正しい。** またぐ名前は本当に300〜506個ある。
間違っていたのは**測った物と、決めたい事が一致していなかった**こと——

```
測っていた   「この1,229行を、横線1本でどこで切れるか」
決めたかった 「この1,229行を、どうやって小さくするか」
```

正解は横ではなく**縦**だった。フックを1本ずつ囲い（`*Context.tsx`）へ上げて、
使う側に自分で見に行かせる。**配線を経由しなくなるので、またぐ数は関係なくなる。**

> **測るのは、読まない言い訳にならない。** そして
> **「測ったから正しい」でもない。測った物が、決めたい事と一致しているか。**

同じ形で 08-04 夜に2本ひっくり返っている:

| ファイル | 前の判断 | 読んだ結果 |
|---|---|---|
| `telopSvg` 594 | 割らない（**94行を出すのに導管21本**） | **445 + 268 に割れた。**導管を1本にする道（下ごしらえを1つの値にまとめる）があった |
| `useClipDrag` 524 | わざと割らない（3種類は同じことをしている） | **431。**頭は「決め事は共通」と言っていたが**中で3通りに割れていた** |

`useClipDrag` の教訓は特に効く:
**同じ場所に並べて置くことは、写しを1本にする代わりにならない。**

| | 段の分け方 | 右端 | 左端 |
|---|---|---|---|
| 効果音 | `splitAt` | `trimRight` | `trimLeft` |
| **画像** | **手書き** | **手書き** | **手書き** |
| 映像 | 手書き | `trimRight` | `trimLeft` |

直したのは置き場所ではなく写しの数（`lib/clipDragLoop` / `shared/lanes` の
`canDropOn` / `shared/clipEdit` の `shiftGroup` へ、3回 → 1回）。

---

## 2. 何を目標にするか（**行数ではない**）

不具合が来たとき・要件が増えたときに**的確に直せる／足せる**こと。測り方は3つ:

| | 何ができれば成功か | 実績 |
|---|---|---|
| ① 直す場所が一発で分かる | grep が1か所に着地する | — |
| **② 直し漏れが出ない** | **2か所目が存在しない** | 08-03 の不具合**8件、全部これ** |
| ③ 足す場所が分かる | 新しい物をどこに置くか迷わない | — |

**8件は1件も「見つけられなかった」型ではない。全部「片方だけ直した」型。**
だから優先は ② > ① > ③。

**「428行」を目標にしないこと。** あれはこのリポジトリの90%点なので、
定義上いつも約31本が超える。効く基準は2つだけ:

1. **1,250を超えない**（AIが1回で読めなくなる。機械で止まっている）
2. **1つのファイルが2つ以上の話題を持たない**（これは人が判断する）

---

## 3. なぜ 500 行なのか（実測）

AI は行数によって**読み方を変える**:

```
〜500 行        頭から通しで読む
500〜1,250 行   冒頭コメントを読んで、あとは grep で当たりを付ける ← ここが本番
1,250 行〜      読めない（fileSize.test.ts が止める）
```

**500 を超えた瞬間から、AI は「そのファイルを読んだ」と言いながら読んでいない。**

| | |
|---|---|
| **害になった例** | `main/exportRun.ts`（1,220行）。冒頭コメントだけ読んで「ffmpeg を直に spawn していない」と**断言して間違えた**。**頭のコメントが「読んだ気」を作った** |
| **効いた例** | `state/useClipDrag.ts`（534行）。冒頭に事故の記録があったので、読んで割るのをやめた |

→ **冒頭コメントは「割るか・危ないか」の判断には効く。「どこが壊れているか」を
探すのには効かない。** だから「1話題だから 800 行でよい」は成り立たない。

### 撤回した基準：「またぐ名前 40個以下なら割る」

これは**書く側の手間（継ぎ目の汚さ）しか測っておらず、読む側の負担を
1ミリも測っていなかった**:

| 払う物 | いつ |
|---|---|
| 導管が +5 増える | **1回だけ**（切るとき） |
| 892行を部分読みする | **触るたび毎回** |

※ 40個ルールは**横に割るときの目安としては今も使える**。ただし
**縦（囲いへ上げる）ときは数えなくてよい**——配線を通らなくなるので。

---

## 4. 配線を剥がす：道具と手順

### 何をしているのか（1行で）

配線が抱えていた 459個の名前のうち **396個（86%）が配管**だったので、
**フックを「囲い」へ上げて、使う側が心臓を直に見に行く**形へ移した。

### 道具（これが本体。目で探さない）

| | |
|---|---|
| `npm run passthrough` | 名前の使われ方を数え、**次に何をやるか**を順番で出す |
| `node scripts/mkctx.mjs <フック名> [名前]` | 引数ゼロのフックの囲いを作る |
| `npm run passthrough -- --gen <フック> [名前]` | 引数を取るフックの囲いを、解いた心臓の対応から生成する |

`passthrough` が出す4つ:

1. **素通し** … 心臓から出してフックへ渡すだけ → そのフックに見に行かせる
2. **往復** … 作った物を束へ詰め直し、また心臓へ入れるだけ → 作った側に書かせる
3. **いま上げられるフック** … 要る物が全部 心臓から取れる物。**「先を何本解くか」順**
4. **詰まりの根** … 上げられる物が尽きたときだけ出る。配線が自分で書いた物待ち

**③が空になったら④を見る。** ④は「その物を心臓へ出せば、また③が増える」の意味。

### 実際の進み方（玉ねぎを剥く形）

```
        名前   行     待ち   根
開始    506   1229    39     -
        459   1194    27     9
        ...
完了     55    362     0     0
```

**「詰まりの根」を返すのが効く。** `videoDurationRef`（ただの `useRef`）1つが
**16本のフックを止めていた**。持ち主（`useMedia`）へ返した瞬間に16本が上がった。
根はほぼ全部「配線が自分で `useState` / `useRef` を持っていた物」で、
**持ち主のフックへ返せば済む**（`rightTab` → `useAppChrome`、
`PANE_LABEL` → `usePanelLayout`、`padTop`/`padBottom` → `useLaneHeights` …）。

### 上げただけでは名前は減らない

上げるのは**準備**。名前が減るのは次の段階——**束と deps から外して、
使う側に直に見に行かせる**とき。

### 束の組み立ても囲いへ寄せた（最後の仕上げ）

8つの束（`timelineOps` / `timelineView` / `previewCtx` / `leftPanel` /
`rightPanel` / `header` / `menus` / `dialogs`）は、配線が81個の名前を集めて
手で組んでいた。**組み立てを各 `*Context.tsx` の `use*Value()` へ移した**ので、
配線側は1行になった:

```tsx
const timelineOps = useTimelineOpsValue({ openClipMenu })
```

**配線は返す係のまま**にしてある（`Wired<'キー'>`・受け口の型・`ctxTypes` を
1つも書き換えずに済む）。

> **`<Provider>` の中で組んではいけない。** 囲いの中で作ると、描き直しの
> たびに作り直されて、掴んでいる途中の状態が消える。組むのは囲いの**外**。

### 残った12本（これは正しい）

配線が自分で呼ぶフックは12本。**走らせるだけで戻り値を持たない効果**ばかり。
うち3本は**走る順に意味がある**ので、ここに残すのが正しい:

```
useSessionMemory → useHistoryCoalesce → useAutosaveDraft
```

効果（`useEffect`）は**宣言した順**に走る。セッション保存は「1つ前の描画の ref」を
見て中身があるか判断しているので、写し取りをする `useHistoryCoalesce` を先に呼ぶと
**起動直後に空でセッションを上書きしうる**。
e2e が `7613 → 0` で捕まえた実績がある（`16-仕上げ`）。

**囲いを重ねると効果の順は逆（子が先）になる。** `App.tsx` の `nest()` は
配列の順にネストするので、並べ替えるときはこの3本の相対順を必ず確かめること。

---

## 5. 入れた検査（**書いただけの決まりは守られない**）

| 止めている失敗 | 検査 |
|---|---|
| ファイルが気づいたら1万行 | `src/shared/fileSize.test.ts`（上限1,250＋1,200で予告） |
| **配線がまた太る** | `src/renderer/src/state/wiringSize.test.ts`（フック12本・450行） |
| 同じ物を別の場所に二重に書く | `src/shared/noDuplicate.test.ts`（名前で見る＋**式で見る**） |
| 500行超なのに取説が無い | `src/shared/readability.test.ts` |
| **取説が腐る**（引っ越したのに説明が残る） | 同上。並べた名前と実体を照合 |
| **説明が指しているファイルが実在しない** | 同上（08-04 追加。1件本物が出た） |
| **返済済みが免除の名簿に残る** | 同上（08-04 追加。2件本物が出た） |
| 心臓の受け口が `any` で開く | `src/shared/ctxTypes.test.ts`（R1〜R4） |
| 時間計算がバラバラになる | `src/shared/timeline.test.ts` |
| テロップの絵が変わる | `src/renderer/src/lib/telopSvg.golden.test.ts`（21ケース） |

### 「式で見る」重複検査が要る理由

名前で見る検査は、08-03 の8件のうち**5件を素通しした**（名前が違い、式が
関数の中に埋まっていた）。そして**「よく読めば気づく」では直らない。2個目は
別のファイルにあるから。**

足すときに起きているのはこれ:

1. 「動画の長さが要る」 2. **`vcLen` があることを知らない**
3. 生の式を書く 4. **動く。試験も緑。誰も気づかない**

**同じ関数は使われない。使われていたら重複にならない。** 知らない理由は
**props で配られていて import では取れない**から。決定的な証拠:
`LeftPanel.tsx` は `vcLen` を props で受け取っておきながら、**同じ名前の
ローカル定数に生の式を書いて影を作っていた**（手に持っているのに書き直した）。

→ **検査と、正典を `shared/` へ移すことの両方が要る。**

### 取説（`// ## 中身`）を機械で照合する理由

腐った例が実際にあった——`lib/telopAnim.ts` の末尾に**本体の無い説明**が
残っていて（中身は `telopStyle.ts` へ引っ越し済み）、誰も気づいていなかった。
**腐った取説は無いより悪い**（AI がそれを信じて「このファイルにはこれしか無い」
と判断する）。

- 並べる**義務**は top-level の関数だけ。型・目印の定数は「**並べてもよい**」
- **1行に2つ書くと片方しか拾われない。1名につき1行**
- **取説が書けない形が1つある**——ファイル全体が1つの関数で包まれていると、
  並ぶ名前が1〜2個になって「これしか無い」という嘘の案内になる。
  そのときは**先に割る**（`exportRun` は 1,229行中 1,012行が1コールバックだった）

---

## 6. 心臓の受け口を `any` から実体へ（`Wired`）

区画は心臓を自分で見に行くので、**受け口が `any` だと存在しない物を触っても
型検査が素通りする**。341件あった。

### 手で341件書き直さない——**実体から引く**

```ts
// state/wiredValue.ts
export type Wired<K extends keyof ReturnType<typeof useAppWiring>> =
  ReturnType<typeof useAppWiring>[K]

// 各 *Context.tsx
type W = Wired<'previewCtx'>
export interface PreviewValue { currentTime: W['currentTime'], ... }
```

**1行の派生型（`interface PreviewValue = W`）にしなかった理由**: それだと
「この心臓が何を配っているか」がファイルを開いても分からない。
名前を並べておくと、**配線から名前を外した瞬間に受け口が赤くなる**。

**結果: 363件が本物の型／`any` は 0。** 隠れていた本物を3件見つけた。
どれも「手で書いた注釈が実体と違う」型で、**受け側が `any` である限り
永久に見つからない**（`TimelineArea` の注釈に `kind` が無く、書き直した瞬間に
段の種類が消える形／`setState` の updater が2件）。

### deps は同じ手が使えない（輪になる）

フックの `deps` を `Wired` から引くと型が輪になる。代わりに
「**呼ぶ側が実物を渡すので、手で書いてもズレたら呼び出し側で落ちる**」を使う。
型は推測せず、TypeScript の compiler API で**実引数を読んだ**。
親の型があるなら `Pick<親の型, …>` で引く（手で書いて3か所ズレた）。

### 検査 R1〜R4（`src/shared/ctxTypes.test.ts`）

| | |
|---|---|
| R1 | 受け口に `any` を書かない |
| R2 | `W['同じ名前']` で引く（別名で引かない） |
| R3 | **配線側で束に型注釈を付け直さない**（付けると輪になって型が消える） |
| R4 | **受け口の型を部品の props 型に別名付けしない** |

R4 は 08-04 に実際に踏んだ穴を塞いだ物: `headerContext` が受け口を
`AppHeaderProps`（= `{ [k: string]: any }`）に別名付けしていて、
**配線から名前を6個外しても型検査が1件も出ず、画面で `undefined` になった**
（ファイルメニューが開かない）。捕まえたのは `App.behavior.test.tsx`。

R3 も 08-04 に踏んだ: `useTimelineOpsValue(deps: { openClipMenu: W['openClipMenu'] })`
と書いたら `Wired<'timelineOps'>` が自己参照になった。**実体の型
（`OpenClipMenu`）で受ければ輪にならない。**

---

## 7. 測る道具の穴 5つ（**同じ物を繰り返さないこと**）

使い捨てスクリプトは3回とも嘘の数字を出した。記号解決（compiler API）が
まとめて避ける。それでも自分で5つ踏んだ:

| 穴 | 何が起きたか |
|---|---|
| ① 片方向しか数えない | 中→外・外→中の両方を出す |
| ② top-level しか見ない | 識別子を全部歩いて、宣言の位置で判定する |
| ③ 同名の再宣言を1つ目だけ | `getSymbolAtLocation` は**その場のスコープ**を解く |
| ④ `import` 文を数えた | **両側で書ける** |
| ⑤ `interface` / `type` の宣言を数えた | **同じく両側で書ける** |

**原則は1つ: 両側に置ける物は「連れて行く」に数えない。**
数えると、どの群も同じだけ膨らんで**全部が割れないように見える**。

さらに `passthrough` 自身で3つ:

| 穴 | 何が起きたか |
|---|---|
| 心臓の一覧を**名前の形**で判定 | `useLaneHeights` を心臓と数え、**上げられない物を「上げられる」**と出した |
| `const sel = useSel()` **経由**を見ていない | そこから取った名前を「配線が書いた物」と数え、**上げられる物を「待ち」**に見せた |
| **関数宣言（`function`）を見ていない** | 配線の中の `blurActiveInput` が見えず、`useTimelineDrag` を**誤って「上げられる」**と出した |

**原則は同じ: 名前で判断せず、実体から引く。**

### もう1つの原則：**またぐ物こそが土台**

「どこで切っても導管になる」と書いてある所ほど、**先にそれを出すと残りが 0 になる**
（`mapContentTimes` / `maybeTrackSelect` / `cropFilter` / `基準点` の4回とも）。

### そして：**取説が切り口を先に書いている**

`exportGraph`（946行）の18項目の取説は**話題を4つ宣言していて、本文も
その順に並んでいた**。切り口は前日の時点で書いてあり、誰も読み返さなかっただけ。
記号解決で測ったら連れて行く名前は **0〜1**。

---

## 8. 割った物（記録）

### 500行超を片付けた（08-03 〜 08-04）

| ファイル | 前 | 後 | 出した物 |
|---|---|---|---|
| `state/useAppWiring.tsx` | 1,229 | **362** | 囲い32本（縦に上げた） |
| `main/exportRun.ts` | 1,229 | **499** | `exportGraph` / `exportSpawn` |
| `main/exportGraph.ts` | 946 | **消滅** | 型・小道具・重ねる段・切片・音 の5本 |
| `state/useTimelineEdit.ts` | 959 | **176** | 6区画（心臓を1つも見なくなった） |
| `components/RightPanelArea.tsx` | 410 | **114** | タブ5つのフック（受け取る物 81 → 8個） |
| `state/useTimelineDrag.ts` | 596 | **295** | `useTrackSelectTool` / `useTelopDrag` |
| `lib/telopSvg.ts` | 594 | **445** | `telopLayout`（組版の下ごしらえ） |
| `state/useProjectFile.ts` | 542 | **223** | `useProjectApply`（**読む側／書く側**で割れた） |
| `lib/telopStyle.ts` | 534 | **439** | `telopFill`（色とグラデ） |
| `state/useMediaDrop.ts` | 533 | **243** | `useMediaPlace` |
| `state/usePreviewManip.ts` | 526 | **413** | `usePreviewAnchor`（土台） |
| `state/useClipDrag.ts` | 524 | **431** | 写しを3→1へ（割らずに） |
| `state/usePlaybackEngine.ts` | 521 | **282** | `useSegClock`（A面/B面） |
| `main/projectFiles.ts` | 497 | **214** | 4本 |
| `components/AppMenus.tsx` | 492 | **30** | `menus/` 3本 |

### `exportRun` の割り方（依存は一方向・横のつながり無し）

```
exportRun ─┬→ exportOverlays  316 ─┐
           ├→ exportSegments  336 ─┼→ exportFilters 53 → exportTypes 169
           ├→ exportAudioMix  142 ─┘
           └→ exportSpawn     268
```

**組み立ての3つは互いを知らない。** 共通の物（型・小道具）は下へ落としてあるので、
「重ねる段が切片を見に行く」という横の線が生えない。
組み立てと実行を混ぜないのが、「グラフが悪いのか ffmpeg が悪いのか」を
切り分けられる形の条件。

### 直した実害

**`main/exportRun.ts` が `shared/timeline.ts` を手書きしていた:**

```
shared/timeline.ts:95   Math.max(0, srcEnd - srcStart) / 速度
main/exportRun.ts:438        (srcEnd - srcStart) / 速度      ← Math.max(0,) が無い
```

**片方だけ直した跡がそのまま残っていた。** 壊れた切片で画面は0で止まるのに、
書き出しだけ負の長さになる。

**`xfOf`（ディゾルブ長）を映像側と音声側が1つの閉じ込みで共有していた。**
そのまま切ると片方だけ別の式を持ち、**音と絵の尺がずれて concat の位置が
食い違う**。→ top-level の純関数 `xfadeDurOf(segs, i)` にして両方が同じ物を通る形に。

---

## 9. 危なかった所・やってはいけない事

### **作り直しの前に網を張る**（「割れる／割れない」より先に来る）

`telopSvg` を割る前、`npm run frames` は**動きと prfpset しか見ておらず、
テロップの絵には試験が1つも無かった**（頭の「突き合わせは npm run frames」は嘘）。
`telopSvg.golden.test.ts`（21ケース・SVG 文字列をそのまま固定）を書いてから動かし、
**出力が1バイトも変わらない**ことを確かめた。

> 網が無いまま動かしていたら、「画面では出るのに書き出すと違う」を静かに作っていた。

### **ファイルをスクリプトで切り貼りしない**

08-04 に**行を消しすぎた**。終端の正規表現 `^\s*\}\s*=\s*use...` が
1行の分割代入に当たらず、**最大40行を巻き込んで関数の途中で切った**。
→ 書き直したスクリプトは**中括弧の釣り合いで終端を探し、消す行を全部
先に表示する**。それでも原則は「**行番号を目で確かめてから1か所ずつ編集する**」。

### **`git checkout <ファイル>` で一時的な変更を戻さない**

戻る先は**コミット済みの版**なので、その日の作業ごと消える。しかも**気づきにくい**
——続けて走らせた検査が「対象が無い」ので素通りして緑になる。
→ わざと壊すときは **`cp` で控えを取って、控えから戻す。**

同じ形で `--gen` が既存の `exportContext.tsx` を**黙って上書きした**。
→ `existsSync` で止めるようにした。

### **コードを heredoc で書かない／`node -e` にバッククォートを渡さない**

シェルに食われて、説明文が空のまま通る。08-04 に2回。

### `{cond && <Tab {...useIconTab()} />}` と書かない

タブを切り替えるたびに**フックの数が変わる**（React の規則違反）。
**型検査では出ない。**

### 一度に何本も上げない

1本ごとに `npm run verify`。効果の順が変わる。

---

## 10. e2e で最後まで残った赤（**アプリは正しく、検査が間違っていた**）

`動きをコピーしたあとでも、クリップのコピー＆貼り付けは普通に効く` が
「貼ってもテロップが増えない」で落ちていた。二分探索で `39e4e22`
（貼り付け・重なり修正）まで遡ったが、**直すべきはアプリではなかった。**

調べ方はアプリ内に一時的な表示を出す（`貼=6.0-8.0@V2 元=1.0-3.0@V2,6.0-8.0@V2 結果=2`）。
**貼り付け先に、まったく同じ範囲を占めるテロップが既に居た**ので、
上書き（`shared/overwrite`）が負けた方を消し、数が増えなかった。

**検査が「貼り付けは上書きしない」に黙って寄りかかっていた**（`39e4e22` 以前は真）。
直したのは検査側:

- 空いている所（`seekTo(3.5)`）へ貼る。**なぜそこが空いているかをコメントで書く**
- 数える前に横スクロールを戻す（`toLeft()`）——**帯は見えている範囲にしか作らない**
- **成立しなければ落ちる**を2つ足した（`.mo-row.mo-picked` が0本、
  `.telop-clip.clip-selected` が1本）。前は「消えても緑」だった

> **測る側は「成立しなければ落ちる」に倒すこと。**
> 消えた物を `?? ''` や `if (count())` で守ると、落ちない代わりに**黙って死ぬ**。

---

## 参考

| | |
|---|---|
| `npm run index` | 目次（約400ファイル・自分で名乗っている1行） |
| `npm run measure` | verify が出さない数字（`any` の残り・正典の呼び出し数・借金） |
| `npm run passthrough` | 配線の名前を数え、次にやることを順で出す |
| `引き継ぎ-秘書エージェント計画.md` | 「それ、もうある？」に答える係の作り |
