# 引き継ぎ：決まりの逸脱を洗い出して潰した（2026-08-17）

**`npm run verify` は 1,139件すべて緑だった。** その状態から「機械が見ていない型の
逸脱」を探して、**本物の不具合を2件**見つけた。以下は見つけ方と、直した場所。

**この文書のいちばんの持ち帰り:**

> **借金（`shared/canon.ts` の `debt`）は「まだ直っていない不具合」かもしれない。**
> 台帳には「正典の式を写している場所が2か所」としか書いていない。だが写した先が
> **写した時点の古い式**なら、それは重複ではなく**バグそのもの**。今回の2件が
> まさにそれで、**2週間、使う人が毎日触る所に残っていた。**

---

## 1. 反転した映像を掴むと、左右が真逆に動いた（本物・本線）

`state/usePreviewFrame` が、映像に掛ける CSS transform を

```
[回転・反転] [動かす・寄せる]      ← 2026-08-17 まで
```

の順で繋いでいた。CSS は**右にある物から先に**当たるので、これは
「反転した座標系の中で動かす」＝**右へ掴むと左へ動く**。

同じ間違いは 2026-08-03 に `lib/clipXform` で直っている。**が、あちらが持つのは
重ねた物（映像レイヤー・画像）だけ**で、**再生ヘッド位置の本線はこちら**だった。

```
直した後   [演出(slide)] [動かす・寄せる] [回す] [反転]
```

- 見た目の文字列は**正典 `clipXform` 1本**で作る（写しを消した）
- 演出との繋ぎは `clipXform.ts` の **`moveThen`** が持つ（3行）。
  台帳には「直すには下位の物が要る」と書いて見送ってあったが、**要ったのはこれだけ**

## 2. つなぎ目の間だけ、入ってくる映像の回転・反転が外れた（本物）

`state/useSegClock` の `xfBStyle`（B面の見た目）が **ズームしか受け取っていなかった**。
しかもその式は正典の写し。→ 受け取る物を `bXform`（`clipXform` が作った完成品）に
変えて、**式も繋ぎ方も正典1つ**にした。

**症状の出方が意地悪い**: 切り替わり切った瞬間に A面（正しい）へ変わるので、
「一瞬パッと変わる」としか見えない。**絵が違うのではなく、絵が戻る。**

## 3. 切片の長さの写しに、下限が無かった（借金の3件目）

`state/useSegmentDrag` が `(srcEnd - srcStart) / segSpeed(...)` を手書きしていた。
正典 `segTLen` は `Math.max(0, ...)` で下を止めている。**08-03 に `exportRun` で
同じ写しが実害になっている**（画面は0で止まるのに書き出しは負）。
ここは端を掴んだあと**後ろを詰める量**なので、負になるとテロップ・SE が逆へずれる。

→ **借金は 3 → 0。** `noDuplicate.test.ts` の上限も 0 へ下げた
（**返しただけでは効かない。上限を一緒に下げて初めて効く**——08-04 の教訓）。

---

## 4〜6. 測る側が「黙って通す」（CLAUDE.md 7番）

**3件とも、同じファイルの中に「これをやってはいけない」と書いてあった。**

| どこ | 何が起きていたか |
|---|---|
| `e2e/stutter.mjs` | `(r.audio?.gaps.length ?? 0) > 0` ＝**音を測れなかった回が「抜け0回」に化ける**。明細には「測れませんでした」と正しく出るのに、**まとめの1行と終了コードだけが「音も抜けませんでした」**と言っていた |
| `e2e/bench-play.mjs` | `秒へ(最初.tc) ?? 0`。**すぐ上の関数に「読めなければ null（黙って0にしない）」と書いてある。** 読めないと進んだ量が絶対時刻に化け、遅れ率が負（実時間より速い）で素通り |
| `e2e/bench-ops.mjs` | `boundingBox()?.x ?? NaN`。**同ファイルの20行上に「これは CLAUDE.md 7番そのもの」と書いて再生ヘッド側だけ直した跡**があり、横スクロール側が残っていた |

直し方はどれも同じ：**成立しなければ落ちる**へ倒す。ただし `stutter` の音だけは
「素材が無音」と「経路に繋げなかった」を区別できないので、

- 測れた回だけで合否を出す（無音素材を素材のせいで × にしない）
- **測れなかったことを ○ に混ぜない**（`○ 絵は滑らか（**音は測れていない**）`）
- **1つも測れなかったら落とす**（結果が並ぶぶん、緑に見える分だけ質が悪い）

### ついでに、測る道具の穴が1つ出た

`clipXform.test.ts` の `toMatrix` が **`translateX` / `translateY` を読めなかった**
（`translate` だけ見ていた）。知らない関数は黙って素通りするので、今回足した
「演出は画面の向きへ流れる」の検査が**両方 0 になり、順番の違いが消えていた**。
——**測る側が読めない形は、測れていないのと同じ。**

---

## 7. 文書が腐っていた（数字を2か所に持った代償）

| どこ | 書いてあった | 実物 |
|---|---|---|
| `CLAUDE.md`「いまの状態」 | verify **1,113件** / 版 **v0.1.35** | 1,139件 / **v0.1.43** |
| 同・別の節 | 版は **v0.1.29** | **同じファイルの中で版が2つ食い違っていた** |
| 同 | 借金は**全部空** | 冒頭コメント無しが**9件**残っていた |
| `shared/canon.ts` | deps の穴が **221件残っている** | **08-04 に返済済み**（14件・全部「正しい `any`」） |

→ **「いまの状態」から数字を全部抜いて、見に行く先（コマンド）だけにした。**
その節は自分で「この表は書いた日から腐り始める」と警告しており、
**警告しながら、いちばん腐っていた。**

## 8. 行数

`やること.md` が **840行**（600行の決まり超え。`npm run verify` は毎回名指ししていた）。
→ **491行**へ。移し先は「既にその話題を持っているファイル」:

```
負荷の調査          → やること-済み-負荷.md（242 → 351）
リファクタの決まり  → 引き継ぎ-心臓の分け直し.md（483 → 553）
書き出しの高速化    → **引き継ぎ-書き出しを速くした.md**（新設・188行）
```

書き出しの高速化だけ新しい入れ物にしたのは、既存の
`やること-済み-書き出しと重さ.md`（485行）へ足すと **667行**＝また決まり超えになるため。
中身も「何をどう変えたか・測った数字」＝**引き継ぎの定義そのもの**だった。

---

## 9. 出す途中で、もう2件出た（`check:swap`）

**この版は JS だけの変更＝使う人には「差し替え」で届く。** `npm run release` は
`check:swap` を通らないので手で叩いたら、**書いてあるとおりに叩いて2回落ちた。**

```
① npm run dist は差し替えの荷物（bundle-*.zip）を作らない
   → 案内の `npm run dist && npm run check:swap` が ENOENT の生スタックで落ちる。
     **`npm run release:check` の最後も同じ並び**なので、同じ所で落ちていたはず
② 3件とも緑を出した直後、**片付け（temp の削除）だけ EPERM** で終了コード 1
   → 閉じた直後の Electron が置き場をまだ掴んでいる。**嘘の赤**
```

→ ①は無ければ自分で作る、②は片付けで判定を覆さない（消し損ねは1行言うだけ）。
**案内どおりに叩いて落ちる道は、案内が嘘なのと同じ。**

※ 3件（効く・戻れる・土台違いを弾く）は**全部緑**。中身は無罪だった。

## 次に同じことをするなら（探し方）

1. `npm run verify` が緑でも、**`npm run measure` の③（借金）を読む**。
   借金は「重複」ではなく「**まだ直っていない**」かもしれない
2. コードの中の**自己申告**を grep する（`残っている` `直していない` `未確認`）。
   今回の2件は `canon.ts` に「**同じ間違いが2つ残っている**」と書いてあった
3. 測る側の `?? 0` / `?? ''` / `?? NaN` を全部見る。
   **真上のコメントと矛盾している物**が3件あった
4. 文書の数字（版・件数）は `package.json` と実行結果に突き合わせる

## まだ確かめていないこと

- **1と2は絵の話なので、機械で押さえたのは「順番」まで。**
  回した物: `npm run verify` **1,143件緑**（今日足した4件を含む）／
  `npm run e2e -- --only=プレビュー --fast` **26緑・0赤**（1.4分）。
  **ただしどちらも「反転した映像を掴む」を通っていない**——
  プレビュー章は拡大・上書き・切り抜きは見るが、**反転して掴む道が無い**。
  ＝ 直したことは確かめたが、**症状が消えたことは人がまだ見ていない。**
  触って見るなら「映像を左右反転 → プレビューで掴んで右へ」と
  「反転したクリップへつなぎ目の演出を掛けて、境目を再生」の2つ
- **通しは回していない**（指示があったときだけ。CLAUDE.md 1番）
