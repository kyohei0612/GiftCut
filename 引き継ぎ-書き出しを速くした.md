# 引き継ぎ：書き出しを速くした（2026-08-09〜08-10）

**60分の動画の書き出しが 104.6分 → 55.4分。** 実時間の 1.74倍から **0.92倍**へ。
絵と音は framemd5 で完全一致（速くしただけで、出来上がりは1バイトも変えていない）。

**読む順**: まず下の「実測（08-10）」で結果を見て、そこから段階1・段階2へ降りる。
**犯人の見つけ方**（内訳をどう取ったか）がいちばん再利用が効く。

※ 2026-08-17 に `やること.md` から移した。あちらが 840行になり、600行の決まりを
  超えたため——**中身は1文字も変えていない**（見出しの階層もそのまま）。
  ここは「何をどう変えたか・測った数字」を置く場所（CLAUDE.md の記録の置き分け）。

---

### 実測（08-10）: 60分の書き出し **55.4分（0.92倍実時間）**。**60分を切った**

```
2日前   104.6分（1.74倍実時間）
今      **55.4分（0.92倍実時間）** ＝ 1.89倍の高速化・実時間より速い
条件    tv 基準の全部乗せ（xfade 80・カット600・テロップ1200枚）・健全な機械
        （物差しが全項目 23.9〜24.4ms に張り付いた回。触る8項目もオール緑）
音・絵  -17.9dB / 無音 0.0秒 / 明るさ156、全部合格
```

見立て（62分）より良かったのは、ノード減が音側や muxer の干渉まで
道連れで軽くしたため（「ノード総数が全体を道連れにする」の逆回転）。

#### 取り直しの過程が「物差しの使い方」の実例になった

1回目の実測は **98.6分**——だが物差しが 28〜36ms（+25〜50%）を示し続け、
**ffmpeg のプロキシ焼きまで遅かった**ので「機械の悪い回」と判定して**捨てた**。
回復を node 版の物差しで見張り（15分おき・3回連続で底に張り付いたら回復）、
健全になってから取り直したのが 55.4分。
**数字を信じる前に、物差しを見る**——この2日で作った測定系の型がそのまま効いた。

※ **やらかしも1つ**: preset 標本の土台に、混在 feeder 時代の**デッドロックする
  graph**（検証用に保存してあった物）を使い、首輪も無く**6時間空回りさせた**。
  地雷の控えは消した。**保存した「壊れる版」は名前で分かるようにするか消すこと。**

### 済：段階2を実装した（08-09）。**split を segment に（全か無か）。xfade なしで +20%**

本編の切片が入力を使うとき**窓**（source 時間）を添え（`filterLabels` の `useVAt`）、
その入力の使い方が**全部「窓付き・時間順・重なりなし」なら** split（全コマを全枝へ
コピー）ではなく **segment**（該当する枝へだけ送る）で配る。1つでも配れなければ
丸ごと従来の split。

```
関門1 同一性  xfade なしの本物 graph・旧等価（split へ逆変換）vs segment 版
              → framemd5 **2,401コマ完全一致**（映像+音）
関門2 速さ    658.7 → 546.9秒（**+20%**）
関門3 退避    tv（xfade あり）は segment 0個・**段階1とバイト単位で一致**
関門4 検査    routing 試験6件（殺すと赤・確認済み）/ verify 1,126件緑 / e2e 緑
```

**踏んだ地雷: 混在 feeder（split=2 で二股）は構造的にデッドロックする。**
split は両方の出口が受け取れるまで次のコマを出さない → 従来側の枝が出番待ちで
詰まる → split ごと停止 → デコーダ停止 → segment 側の枝が飢える → concat が永遠に
待つ（実測: 60秒の照合が**85分空回り**。最小再現 M3 系で確定）。
逃がし弁の `fifo` フィルタは **ffmpeg 7 で削除済み**（同梱版に無い）。
→ だから「全か無か」。経緯は `pickRouted` のコメントにも焼き込んだ。

**+20% が骨組み（13.9→3.2秒）より小さい理由**: 残りの費用（音の asplit=800・
overlay 1400段・graph 全体のノード数）が支配的。**音を畳まないのは**、連続カットの
境目では音フレームを前後の枝が**半分ずつ**使う（atrim はフレームの中を割れるが
asegment は割れない）から。

残る設計課題（上の表の項目2）: xfade の食い込み窓を「前のスライスを局所 split して
尻を継ぐ」形で拾えば xfade 混じりでも効く見込みだが、デッドロック解析を
やり直す必要がある。**測ってから**。

### 済：段階1を実装した（08-09）。**concat の直列を1ノードへ。graph 1.96倍・絵は完全一致**

`exportSegments.ts` の映像・音の連結を「連続する concat は concat=n=K の1ノード」へ
（xfade / acrossfade はそのまま）。**n=2 の直列と数学的に同じ**なので絵は変わらない。

```
関門1 同一性  旧 vs 新 graph・同じ入力・同じPNGで framemd5 → **2,401コマ完全一致**（映像+音）
関門2 速さ    実アプリが吐いた graph 同士・-f null 300秒 → 710.1 → 362.8秒（**1.96倍**）
関門3 検査    形の試験6件を新設（exportSegments.test.ts・**直列に戻すと赤**を確認済み）
              verify 1,120件緑 / e2e 書き出し2章 12緑
```

60分の書き出しの見立て: **104.6分 → 約60分**（エンコード17%は不変のため）。

**段階の見極めも測って決めた**: P6（split 残し・concat 1ノード）13.9秒 vs
P1 52.9秒 vs P2 2.4秒 → 出口の直列が扇の大きい方だと分かったので、
**意味が完全に同じ小工事（段階1）を先に**入れた。段階2は下のメモ。

### 済：合成83%の犯人は「カット600の組み立て」だった（08-09・試作で5.3倍を実証）

**部品を1つずつ測ると全部シロ**だった（下の表）。犯人は部品ではなく**ノードの総数**
——ffmpeg のスケジューラは毎コマ全ノードを歩くので、組み立ての約1,200本が
graph に**居るだけで**全体が道連れになる。単体では53秒の組み立てが、
全体から抜くと**382秒**返ってくるのはそのため。

```
容疑者を消した順（全部 -t 300 の標本・アプリ抜き）:
  overlay 1200段の通過        素の再現で 10.8秒 → シロ（rgb でも 4.7秒）
  スレッド数                  threads=1 で変化なし → シロ
  特定の演出の場所            時系列プロファイルが真っ平ら → シロ
  zoompan（動き100本）        +5.2秒 → シロ
  一時フォルダ・プロキシ・冷え （前日に潰済み）
決め手:
  P1 今の組み立て（split=600 → 枝 → concat=n=2 直列600連）   52.9秒
  P2 concat demuxer（範囲リスト1入力）                        **2.4秒（22.5倍）**
  D  本物の映像 graph                                        470.6秒
  V2 本物から組み立てだけ畳んだ物（overlay 以降そのまま）      **88.6秒（5.3倍）**
```

**「映像+音を一緒に流すと部分の和(624秒)より遅い(1085秒)」もこれで説明が付く**
（合計ノードが増えるほど1コマの巡回が高くつく）。音側も同じ型
（`[0:a]asplit=800`＋concat直列）なので、同じ畳み方が効くはず。

#### 実装の設計メモ（着手時に読む）

- 触る所: `main/exportSegments.ts`（組み立て）・`exportRun.ts`（入力の並び）・
  `exportAudioMix.ts`（音側の同じ畳み）。overlay 側（`exportOverlays.ts`）は**無傷**
- 素のカットの連なり → **concat demuxer**（`file/inpoint/outpoint` のリスト・
  出力順に書く。並べ替えも重複もリスト側で自由）
- **動き付き（zoompan）と切り替え（xfade）の両端だけ**個別入力（`-ss/-to`）で残す
  （zoompan は per-segment の式なので merged stream には掛けられない）
- 入力の合流は **concat=n=K を1ノード**で（n=2 の直列に戻さないこと。
  直列がまさに今回の犯人）
- Windows の引数上限（32,767字）に注意——リストはファイルなので効かないが、
  個別入力が増える形（動き・切り替えだらけのプロジェクト）では当たり得る。
  当たったら「その区間だけ従来の split 型」へ落とす逃げ道を残す
- **絵と音の同一性が最優先**: `npm run frames`・耳の章・`filterGraph` 検証・
  可能なら新旧 graph の framemd5 突き合わせを検査に足す
- 見込み: 映像 5.3倍・音側も同型・エンコード 211秒は不変
  → **60分の書き出し 104.6分 → 35〜50分の見立て**（実装後に実測すること）
- 試作の道具は `scripts/export-abc.mjs` と、このセッションの scratchpad
  （v2-proto: filter-video.txt から組み立て一族を落として [vcat] を [0:v] に
  付け替え・concat リストは trim の範囲を出力順に）

### 済：書き出しの内訳（08-09）。**犯人は合成（83%）。「ほぼエンコーダ」は覆った**

本物の命令（控え: `e2e/.cache/export-dump/`）の**頭5分だけ**を3通りで回した
（道具: `scripts/export-abc.mjs`。10分版プロジェクトを作らないのは密度が変わるから）:

```
C 準備（1261入力を開く）    50.8秒   （4%）
B−C **合成（フィルタ）**  1034.0秒   （**83%**）
A−B エンコード（nvenc p4）  211.0秒   （17%）
```

- **08-04 の「遅さはほぼエンコーダだけ」は、tv 規模では成り立たない。**
  あれは軽い条件の結論。テロップ1200枚では**合成がエンコードの5倍**食う
- 1コマ115ms ＝ **出番が過ぎたテロップの overlay も、コマは全段（1200段）通る**
  のが見立て。効くなら「時間で区切って、その区間に出る物だけの graph を concat」
  型の再設計（`filterGraph` の大工事）
- preset（p4→p1）で縮むのは最大17%。**掘るなら合成側**
- ※ 頭5分は平均より濃い（換算250分 vs 実測104.6分）。**比率だけを読む**こと

### 済：書き出しの実測（08-09）。**60分ぶんで 104.6分（1.74倍実時間）**

```
ffmpeg 単体   01:17:01 → 03:01:36 ＝ **104分35秒**（h264_nvenc p4・720p30・3.16GB）
出来上がり    尺 1:00:00.34 ✓ / 音 -18.0dB ✓ / 絵 YAVG157・幅232 ✓（全部合格）
```

**測れたのは事故の副産物。** 運転席（node）が途中で SIGTERM（終了コード15・
**犯人不明**。スクリプト自身の出口は 0/1/2 のみ・エラー文言も無し）で死んだが、
アプリと ffmpeg は孤児のまま焼き続けた。それを見届けて回収した。

### その過程で、嘘の赤の穴をもう1つ塞いだ

**保存ダイアログの差し替えでは、焼く先は変わらない。** 窓（export overlay）が
「どこへ・どの名前で」を決めて `payload.outPath` で渡すので、main の
`showSaveDialog` は呼ばれない（`exportRun.ts` の「画面側で決まっていれば聞かない」）。

bench-export は差し替えだけに頼っていたので:

```
焼いた物は全部 **Downloads** へ（bench-60min(n).mp4 として溜まる。7本・7.2GB あった）
確認は temp を見る → 「完走しなかった」の**嘘の赤**（完走していても）
```

**昨夜の通しの唯一の赤も、これだった可能性が高い**——昨夜の `(5)` と今回の `(6)` は
**バイト数まで同一**（3,164,910,090）。昨夜も完走していて、見る先が違っただけ。

→ 直した: 手順の正典 **`e2e/lib/exportTarget.mjs`** を bench-export も呼ぶ
  （localStorage へ置き場・窓の欄へ名前。効く仕組みは `useExport.ts:100` の
  「開くたびに localStorage を読み直す」）。
  「正典はあるのに、その道を通っていない」型の**6件目**。
  ※ **直しはまだ通しで検証していない。** 次に `bench:export` を回す日に、
    temp へ落ちて1発で緑が出るかを見ること（約105分＋触る2分）。
  ※ Downloads の焼き残し 7本・7.2GB は**本人の確認待ち**（消してよいか）。

