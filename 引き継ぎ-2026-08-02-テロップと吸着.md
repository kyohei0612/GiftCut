# 引き継ぎ: テロップ・吸着・重さの直し（2026-08-02）

`やること.md` のプレテスト分と、その日に本人から出た指摘をまとめて片付けた回。
**項目ごとの詳しい話は `やること.md` の各行**（なぜそうしたか・やってはいけない形まで
そこに書いてある）。ここは全体像と、次に触る人が知っておくべき所だけ。

## 何が入ったか（13コミット）

| コミット | 中身 |
|---|---|
| `3eed868` | ◎ 拡大の中心（アンカー） |
| `62c8d30` | テロップの吸着3件／段が無ければ足す／打ち直しが左右パネルで消えない |
| `b9c5322` | 重さの調べ（実プロジェクトを数えた） |
| `7ec4e81` | 控えに上限／吸着の抜けを効果音・画像・映像レイヤーでも塞ぐ |
| `2c4688e` | テロップの重なり上書き／新しい段で効かない物／段の名前／書き出しの窓 |
| `cfde9ea` | スクショが黙って失敗しないようにする |
| `a7f882b` | 掴んだまま枠の外へ引っ張るとタイムラインが送られる（全種類） |
| `4531a9a` | リップル削除で詰めた先が枠の外なら見せる |
| `b684f5f` | 音の置き先を A2 に固定しない |
| `57064cf` | 隣り合うテロップの間を掴めるようにする（帯の幅の式を1つに寄せる） |

単体試験 **673 → 735件**。絞った e2e は触った章すべて緑
（プレビュー33・タイムライン15・動か56・クリップ33・効果音10・文字37・段25・
トラック20・書き出し22・リップル13・再生15・目で見る15）。

## この回で3回出た「同じ式が何か所にも書き写されていた」

**直した不具合の半分は、これが原因だった。** 見つけ方も直し方も同じなので、
次に「片方だけ動きが違う」と言われたら、まずこれを疑うこと。

| 症状 | 実際 | 寄せ先 |
|---|---|---|
| テロップの吸着がケツに効かない | テロップだけ「時刻を1つ寄せる」道具を通っていた | `shared/snap` へ（束の頭と尻） |
| 隣り合うテロップの間を掴めない | 帯の幅の式が6か所にあり、**テロップだけ 1px を引き忘れ**ていた | `lib/bandGeom` の `bandWidth` |
| 掴んで端まで運んでも送られない | 再生ヘッド側にだけ同じ仕掛けが直書きされていた | `shared/edgeScroll` |

同じ型はまだ残っている疑いがある。`'V2'` `'V3'` の名指し（トラック選択で
1件見つけて直した）と、`snapTime` / `snapClipStart` の使い分けは特に。

## 測った数字（次に触るときはこれと比べる）

本人が1本編集した実物（`project.gcproj`）を数えた:

| | |
|---|---|
| ファイル全体 | 0.87MB |
| テロップ | 242個（限界は4000枚。**桁が違う**） |
| 切片 29 / 効果音 21 / 素材 3 | どれも限界のはるか手前 |
| `iconAssign` + `laneIconAssign` | **0.594MB**（PNG を dataURL で4枚） |

**プロジェクトの68%が絵4枚。** 「編集すればするほど重くなる」の原因は
**数の多さではない。** 見つけて直したのは `inkCache`（鍵に文字の中身が入っていて
捨てられない＝打つたびに増える）で、上限2000件を付けた。
**ただし、これが主因かは測っていない。** 次はタイムラインの描き直しの回数を測ること。

## 直していないが、調べて分かっていること

- **スクショが保存されない** … 再現できなかった。変更前のコードで確認を回しても緑。
  こけたら理由が赤い帯に出るようにしてあるので、次に起きたら文言を聞くこと
- **消えた画像がプレビューに出る** … 実物を開いて数えたが再現せず。
  帯は「見ている範囲に入ると出る」作りで、テロップの帯もまったく同じ絞り方を通っている。
  残る見立ては「縦に隠れていた」
- **縦書き** … プレビューと書き出しは同じ `buildTelopSVG` を通っているので、
  そこを直せば両方に効く。ただし**横に並べる前提の自前レイアウト**なので、
  列組みをもう1本作る話になる（`やること.md` に見積もりを書いた）
