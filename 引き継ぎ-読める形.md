# 引き継ぎ：AI が読める形に保つ（2026-08-03）

**目的は行数ではない。** 不具合が来たとき・要件が増えたときに**的確に直せる／足せる**こと。

測り方は3つ:

| | 何ができれば成功か | この日の実績 |
|---|---|---|
| ① 直す場所が一発で分かる | grep が1か所に着地する | — |
| **② 直し漏れが出ない** | **2か所目が存在しない** | **不具合8件、全部これ** |
| ③ 足す場所が分かる | 新しい物をどこに置くか迷わない | — |

**この日に見つけた不具合8件は、1件も「見つけられなかった」型ではない。全部
「片方だけ直した」型。** だから優先は ② > ① > ③。

ルート `CLAUDE.md` に「428行を目標にしないこと」と書いてあるが、**では何を
目標にするのか**が書かれていなかった。それが上の3つ。

---

## なぜ 500 行なのか（実測）

AI は行数によって**読み方を変える**:

```
〜500 行        頭から通しで読む
500〜1,250 行   冒頭コメントを読んで、あとは grep で当たりを付ける ← ここが本番
1,250 行〜      読めない（fileSize.test.ts が止める）
```

**500 を超えた瞬間から、AI は「そのファイルを読んだ」と言いながら読んでいない。**

この日に実際に起きた:

| | |
|---|---|
| **害になった例** | `main/exportRun.ts`（1,220行）。冒頭コメントだけ読んで「ffmpeg を直に spawn していない」と**断言して間違えた**。本文を見ていない。**頭のコメントが「読んだ気」を作った** |
| **効いた例** | `state/useClipDrag.ts`（534行）。冒頭に「バラバラに置いて片方だけ直した事故」と書いてあったので、読んで割るのをやめた |

→ **冒頭コメントは「割るか・危ないか」の判断には効く。「どこが壊れているか」を
探すのには効かない。** だから「1話題だから 800 行でよい」は成り立たない。

### 撤回したこと

それまでの基準は「またぐ名前 40個以下なら割る」だったが、これは**書く側の手間
（継ぎ目の汚さ）しか測っておらず、読む側の負担を1ミリも測っていなかった**:

| 払う物 | いつ |
|---|---|
| 導管が +5 増える | **1回だけ**（切るとき） |
| 892行を部分読みする | **触るたび毎回** |

`useTimelineEdit`（896行）を「deps が 24→29 に増えるから割らない」と判断して
いたが、**これは撤回した**。

---

## 入れた検査（`npm run verify` で走る）

### `src/shared/readability.test.ts`（新規・5件）

| 見張る物 | 中身 |
|---|---|
| 冒頭コメントがある | 先頭の非空行が `//` か `/*` か |
| **500行超は割るか取説を付ける** | 取説＝`// ## 中身` に関数名を並べる |
| **取説が腐っていない** | 並べた名前と実体を照合。**引っ越して説明だけ残せない** |

**取説を機械で照合する理由**: このリポジトリに既に腐った例があった——
`lib/telopAnim.ts` の末尾に本体の無い説明が残っていて（中身の `textRectInFrame`
は `telopStyle.ts` へ引っ越し済み）、**誰も気づいていなかった**。
`noUnusedLocals` は説明を見ない。**腐った取説は無いより悪い**（AI がそれを
信じて「このファイルにはこれしか無い」と判断する）。

**取説は「中の関数」まで並べる。** 大きいファイルはほぼ全部フックで、中身が
1つの関数の中に入っている（`useProjectFile` は537行のうち約500行が
`useProjectFile()` の中）。top-level だけ見ると取説が1行になって役に立たない。
線は「`function` と書いたか」に引いてある（中の矢印 `const` まで数えると
3行のヘルパーが全部並んで読めなくなる）。

### `src/shared/noDuplicate.test.ts` に「式で見る」を追加（+5件）

それまでの2つは**名前で見ていた**ので、この日の8件のうち**5件がすり抜けた**
（名前が違い、式が関数の中に埋まっていた）。

そして**「よく読めば気づく」では直らない。2個目は別のファイルにあるから。**

```
段の音量      2ファイル
色の計算      2ファイル
素材の置き場  3ファイル
時間の計算    exportRun と shared/timeline
動画の長さ    15ファイル
```

足すときに起きているのはこれ:

1. 「動画の長さが要る」 2. **`vcLen` があることを知らない**
3. 生の式を書く 4. **動く。試験も緑。誰も気づかない**

**同じ関数は使われない。使われていたら重複にならない。** 知らない理由は
**props で配られていて import では取れない**から。決定的な証拠:
`LeftPanel.tsx:166` は `vcLen` を props で受け取っておきながら、**同じ名前の
ローカル定数に生の式を書いて影を作っている**（手に持っているのに書き直した）。

→ **検査と、正典を `shared/` へ移すことの両方が要る。** 検査だけだと、赤く
なったとき「何を import すればいいか」が分からない。

**入れた最初の1回で本物を1件見つけた**（`useTimelineEdit:474`。私の grep が
取りこぼした場所で、正典にある `Math.max(0, …)` と「速度0以下なら等速」が
両方抜けていた＝速度0で Infinity になり後ろの素材が消し飛ぶ）。

---

## 直した実害

### `main/exportRun.ts` が `shared/timeline.ts` を手書きしていた（3か所）

```
shared/timeline.ts:95   Math.max(0, srcEnd - srcStart) / 速度
main/exportRun.ts:438        (srcEnd - srcStart) / 速度      ← Math.max(0,) が無い
```

**片方だけ直した跡がそのまま残っていた。** 壊れた切片（`srcEnd < srcStart`）で
画面は0で止まるのに、書き出しだけ負の長さになる。`src/main` は既に
`../shared/*` を14か所で引いているので、引かない理由は無かった。

音声側の `extLen` も `segTLen` に通した。ここは「映像 extLenN と一致」と
**コメントで約束していただけ**で、機械の保証が無かった。

確認: `npm run e2e -- --only=書き出` で **22件緑**（実際に ffmpeg を回して
尺・音量・色・動きを測っている）。

---

## 割った7本（全部「またぐ名前0」。`StylePanel` だけ5）

| ファイル | 前 | 後 | 出した先 |
|---|---|---|---|
| `main/projectFiles.ts` | 497 | **214** | `allowProjectMedia` / `projectPackIpc` / `projectTemplates` / `srtExport` |
| `components/AppMenus.tsx` | 492 | **30** | `menus/TabMenus` / `menus/ClipMenus` / `menus/OrganizeMenus` |
| `state/useSessionMemory.ts` | 415 | **273** | `useHistoryCoalesce` / `useAutosaveDraft` |
| `lib/telopStyle.ts` | 623 | **510** | `telopMeasure` ＋取説 |
| `state/useLibraries` | 530 | **210** | `useLibraryOrganize` |
| `state/useProjectFile.ts` | 667 | **537** | `useProjectTemplates` ＋取説 |
| `components/StylePanel.tsx` | 679 | **451** | `StyleAppearance` ＋冒頭コメント |

**7本とも、冒頭コメント自身が2つ以上の話題を宣言していた**（または1行も無かった）。
切り口を数える前に「頭のコメントの宣言と、実体の関数の並びが合っているか」を
見ると当たる、という関係がこの日5回連続で出た。

### ついでに直った物

- **配信を許すかの判断**（`allowProjectMedia`・安全に関わる）が、テンプレートの
  置き場と一覧の**間に挟まって外から名前が見えなかった**
- **心臓が部品から型を借りる逆向き**を1本消した（`AppMenusProps` →
  `menusContext` の `MenusValue`）。しかも `AppMenus` は props を1つも
  受け取っていないので「Props」という名前自体が実体と合っていなかった
- `useLibraries` は **`.tsx` → `.ts` に変わった**（JSX が全部整理側へ行った）。
  話題が本当に別だった、いちばん分かりやすい証拠
- `telopSvg → telopStyle` の輪が細くなった（SVG 側が借りていたのは測る2つで、
  スタイルの定数とは無関係だった）

---

## 順番に依存する所（`useSessionMemory` を割るときに踏んだ）

効果（`useEffect`）は**宣言した順**に走る。`useAppWiring` では

```
useSessionMemory → useHistoryCoalesce → useAutosaveDraft
```

の順を**必ず保つこと**。セッション保存は「1つ前の描画の ref」を見て中身が
あるか判断しているので、写し取りをする `useHistoryCoalesce` を先に呼ぶと
**起動直後に空でセッションを上書きしうる**。

同じ型の事故を1度やっていて、e2e が `7613 → 0` で捕まえている
（`16-仕上げ` の「前回の続きを開くと、タイムラインの横位置も戻る」）。
割ったあと実測で `0 → 7581` に戻ることを確認済み。

---

## `exportRun` を3つに割った（2026-08-03 夜。最後の1本）

**当初の計画は「まず取説を作る」だった。実測したら、その道は塞がっていた。**

取説に並ぶのは top-level の callable（`function` と、中身が矢印の `const`）だが、
`exportRun.ts` にはそれが**2つしか無かった**:

```
189: export function isExporting()
196: export function registerExportHandlers()
```

矢印の `const` は top-level に0個。**1,229行のうち 1,012行が `export:run` の中の
1つのコールバック**だったため。取説を書いても2行で終わり、
「このファイルにはこれしか無い」という**ほぼ嘘の案内**になる
——`readability.test.ts` 自身が「腐った取説は無いより悪い」と言っている型に、
書いた瞬間に入る。**だから先に割った**（リファクタ部の R6「ファイル全体を
1つの関数で包まない」がまさにこれ）。

### 話題は3つあった

| ファイル | 行 | 話題 | 取説 |
|---|---|---|---|
| `main/exportRun.ts` | **1,229 → 479** | 受け口（IPC・出す先・PNGを焼く・引数を組む） | **要らない**（500未満） |
| `main/exportGraph.ts` | 893（新） | 組み立て（型・切片・音のミックス・重ねる段） | 18項目 |
| `main/exportSpawn.ts` | 258（新） | 走らせる（起動・進捗・中止・GPU失敗→CPU） | 8項目 |

**依存は一方向**（`exportRun → exportGraph` / `exportRun → exportSpawn`、
あの2つは互いを知らない）。組み立てと実行を混ぜないのが、
「グラフが悪いのか ffmpeg が悪いのか」を切り分けられる形の条件。

### 切るときにいちばん危なかった所

**`xfOf`（ペア (i,i+1) のディゾルブ長）を、映像側と音声側が1つの閉じ込みで
共有していた。** そのまま切ると片方だけ別の式を持ち、**音と絵の尺がずれて
concat の位置が食い違う**——`fileSize.test.ts` が
「`filter` 文字列を書き換えて返す形なので、出すときは戻り値で組み直しが要る」と
警告していたのはここ。→ top-level の純関数 `xfadeDurOf(segs, i)` にして、
`buildSegmentVideo` と `buildSegmentAudio` が**同じ物を通る**形にした。

### 途中で見つかった本物（どれも「片方だけ直せる」型）

1. **進捗の読み取りが2か所にあった。** GPU 用と CPU やり直し用で同じ11行。
   `pipeProgress` 1つに寄せた
2. **`vcLen` の式が生で書いてあった（2か所）。** `noDuplicate.test.ts` が
   「正典は `shared/timeline` だが、実体は `useTrackGeom` にあって **props で
   配られている＝書く瞬間に見えない**」と自分で書いていたので、
   **正典を本当に `shared/timeline` へ置いてから** import した。
   書き出し側からは props すら届かないので、ここが最初の返済になる
3. **`wasCanceled` が参照0の export になっていた。** `noUnusedLocals` は
   export を見ないので、これは人が消すしかない

### 取説の照合が3回とも仕事をした

`wasCanceled` を消したのに取説へ残した／`cropFilter` などを載せ忘れた／
`buildAudioMix` を載せ忘れた——**全部その場で赤くなった。**

※ **1行に2つ書くと片方しか拾われない。** `- \`motionIn\` / \`motionOut\` …` と
書いたら `motionOut` が「取説に無い」で赤くなった。**1名につき1行。**

### 確かめ方

`npm run verify` 緑。`--only=書き出` **22件緑**（1回目・重ねる段と走らせるを出した所）、
`--only=書き出,トランジション,ディゾルブ` **23件緑**（2回目・切片と音のミックスも出した後）。
どちらも実際に ffmpeg を走らせる確認（ダッキング・色調整・9:16・mp3・音の途切れ）を含む。

---

## 残り

| 行 | | |
|---|---|---|
| **896** | `useTimelineEdit` | 判断を撤回した物。まだ割っていない |
| 1,223 | `useAppWiring` | 割れない（またぐ名前106〜413。心臓の分け直しが先） |

借金（増やしてはいけない。減らしたら検査の数字も一緒に下げること）:

```
取説なしで500行超   7本   readability.test.ts の DEBT_INDEX（exportRun を返して 8 → 7）
冒頭コメント無し   10本   readability.test.ts の DEBT_HEAD
正典のある式の写し 16件   noDuplicate.test.ts の CANON.debt（vcLen 2件を返して 18 → 16）
```

**`vcLen` の残り11か所は画面側**（props で受け取れる場所）。
`shared/timeline` に正典ができたので、触るついでに1つずつ寄せる
——寄せたら `noDuplicate.test.ts` の debt も一緒に減らすこと。
