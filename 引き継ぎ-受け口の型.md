# 心臓（context）の受け口を `any` から実体へ（2026-08-03 夜）

**区画が心臓を `any` で受け取っていた。341件。** それを全部、詰めている実体から
機械で引く形にした。手では1件も型を書いていない。

---

## なぜやったか

秘書エージェントの検証（`引き継ぎ-秘書エージェント計画.md`）で出た3件目の指摘:

> `draggingTemplateRef` / `draggingEmphasisRef` が `any` で宣言されている。
> 今日事故った「型だけあって実体が無い」型が、まだ同じ形で残っている

実物を見たら**単発ではなかった**。`rightPanelContext.tsx` は `rightBodyRef: any`
`importSeInto: any` … と受け口がほぼ全部 `any`。数えたら6ファイルに **341件**。

`any` は「何でも通る」なので、こうなる:

- 存在しない物を触っても通る
- 引数の数を間違えても通る
- **手で書いた型が実体とズレていても、ズレたまま通る**

2026-08-03 の不具合11件のうち2件がこの型（②「宣言だけあって実体が無い」）。

---

## 手で341件書き直さない——**実体から引けるか、先に測った**

詰めているのは `useAppWiring` の1か所だけ:

```ts
const previewCtx: PreviewCtxValue = { orderedTabs, TAB_DEFS, monitorTab, … }
```

省略記法なので、**注釈を外せば実体の型がそのまま推論される**はず。
本当にそうなるかを、TypeScript の compiler API で数えてから決めた:

```
previewCtx   全84件 / any 1件 / 型が付く 83件
rightPanel   全81件 / any 0件 / 型が付く 81件
```

**手書きの `any` は情報を捨てていただけだった。**
（唯一 `any` のままなのは `xfNextBUrl`。ここは実体側が `any`＝正直な `any`）

**測ってから決めたのが効いた。** 「たぶん型が付く」で341件書き換え始めると、
半分が `any` のままだったときに引き返せない。

---

## 形

```ts
// state/wiredValue.ts   ← 「なぜ・どう腐らないか」はここ1か所
export type Wired<K extends keyof ReturnType<typeof useAppWiring>> =
  ReturnType<typeof useAppWiring>[K]

// state/previewContext.tsx
type W = Wired<'previewCtx'>

export interface PreviewCtxValue {
  /** 映している枠。掴んだ位置はここを基準に測る */
  screenRef: W['screenRef']
  …
}
```

配線側は注釈を外す（`const previewCtx = { … }`）。

### **1行の派生型（`= W`）にしなかった理由**

`export type PreviewCtxValue = Wired<'previewCtx'>` と書けば1行で済む。
**採らなかったのは、キーごとの説明が全部消えるため。**

受け口の説明（`/** 映している枠。掴んだ位置はここを基準に測る */`）は
そのキーの真上にあるべきで、消すと「何が入っているか」を毎回実体まで辿る羽目になる。
**型だけを引いて説明は残す**——それが `W['名前']` の形。

### 腐らない

実体が引っ越した・名前が変わったら `W['名前']` がその場で赤くなる。
**説明だけ取り残されることが原理的に起きない**（`telopAnim.ts` に本体の無い取説が
残っていた失敗と同じ型を、ここでは踏めない）。

---

## 出た数字

| ファイル | 引いた型 | 残り `any` |
|---|---|---|
| `previewContext.tsx` | 84 | 0 |
| `rightPanelContext.tsx` | 81 | 0 |
| `dialogsContext.tsx` | 59 | 0 |
| `menusContext.tsx` | 50 | 0 |
| `timelineOpsContext.tsx` | 58 | 0 |
| `timelineViewContext.tsx` | 31 | 0 |
| **合計** | **363** | **0** |

`state/*Context.tsx` 21本すべてで `any` は 0 件（残り15本は元から 0）。

---

## 見つけた本物（`any` が隠していた）

**`components/timeline/TimelineArea.tsx`** の段名の変更:

```ts
setTracks((prev: { id: string; name: string }[]) => …)   // ← 手で書いた注釈
```

実体は `Track[]` で、**`kind` が抜けていた**。`{ ...t, name }` で広げていたので
実害は出ていなかったが、`{ id: t.id, name }` と書き直した瞬間に段の種類が黙って消える。

→ 注釈を外して実体から推論させた。

※ **これ1件だけだった**（型を本物にしても他の赤は 0）。
  つまり**いまのコードは正しく使えていた**が、間違えても誰も気づけない状態だった。

---

## 決まりには検査を付けた（`src/shared/ctxTypes.test.ts`）

| | 決まり |
|---|---|
| R1 | `state/*Context.tsx` に `any` を書かない |
| R2 | `Wired` を使う受け口は、型が `W['同じ名前']` だけ（**名前のズレも赤**） |
| R3 | `useAppWiring` 側で型注釈を付け直さない（付けると輪になって型が消える） |

**3つとも、わざと破って赤くなることを確かめてある。**

R2 に「名前が一致すること」まで入れたのは、`togglePlay: W['skipSec']` のように
**別のキーの型を当てて辻褄を合わせられる**と検査の意味が無いため。

---

## 危なかった所

### `git checkout <ファイル>` で一時的な変更を戻すと、**コミット前の作業ごと消える**

歯の確認（わざと壊して赤くなるか）のあと `git checkout previewContext.tsx` で
戻したら、**その日の作業が全部消えた**（戻る先はコミット済みの版＝全 `any`）。

しかも消えたことに気づきにくい: 続けて走らせた R2・R3 の確認は
「対象ファイルに `Wired` が無い」ので**素通りして緑**になり、
**歯を確かめたつもりで何も確かめていなかった。**

→ **一時的に壊すときは `cp` で控えを取って、控えから戻す。**
  git は「コミット済みの状態へ戻す」道具で、「さっきの状態へ戻す」道具ではない。

※ 気づけたのは、直前に別の目的で控えを取っていたから。運が良かっただけ。

### 機械で置き換えた所と、手で直した所を分けた

341件は1行ずつ手では直せないのでスクリプトを使ったが、
**`^  <名前>: any$` に完全一致する行だけ**を触る形にして、
一致が想定と違ったら**書かずに止める**ようにした。

複数行にまたがる signature（`placeDropped` と `removeKeyAtTime`）は
**目で確かめて手で直した**（閉じ括弧の探し方が甘くて関数の途中で切った前例がある）。

CRLF で `\n` の検索が当たらず1度止まったが、**書く前に止まったのでファイルは無傷**だった
——「一致が1件でなければ書かない」を先に入れておいたのが効いた。

---

## 次の穴（測っただけ。着手していない）

受け口は 0 になったが、**一段深い所（フックが受け取る `deps`）にはまだ 221件ある。**

漏れの形も1つ確認できている:

```
useProxy.ts:69          const previewUrl = (path: string, orig: string): string  ← 実体
usePreviewFrame.ts:41   previewUrl: any                                          ← ここで落ちる
```

受け口を実体から引いても `xfNextBUrl` だけ `any` のままだったのは、これが元。

**ただし context と同じ手が使えるとは限らない。** context が引けたのは
**詰める場所が1か所（`useAppWiring`）だったから**で、deps は呼び出し側が
複数のことがある。**「詰める場所が1か所か」を数えてから**決めること。

---

## 確認

- `npm run verify` **925件 緑**（うち今回の検査3件）
- `npm run build` 通る
- **実行時の動きは1行も変えていない**（型だけ）。
  唯一のコード変更 `TimelineArea.tsx` も、注釈を外しただけで意味は同じ
