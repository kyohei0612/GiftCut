# GiftCut — 第三者診断用の概要（2026-08-05 / v0.1.22）

このファイルは**外部のAIに診断してもらうために書いた**。リポジトリを見なくても
判断できるよう、数字と根拠を中に入れてある。**良く見せる書き方はしていない。**
弱い所は「弱い」と書いてあるので、そこも含めて評価してほしい。

---

## 1. 何のアプリか

**切り抜き動画（YouTube/配信の切り抜き）に特化したテロップ編集アプリ。** Windows デスクトップ。

- 個人が1人で作っている。利用者も現時点では作者本人＋少数
- 競合は Premiere Pro / Vrew / CapCut。**狙いは「テロップを速く大量に載せる」一点**
- 技術構成: Electron 31 + React 18 + TypeScript + FFmpeg（同梱）
- 配布: GitHub Releases から**自動更新**（起動時に確認し、黙って入れ替わる）

### 規模

```
本体（src、テスト除く）   54,658 行
単体テスト                11,010 行（1,000件）
e2e                       17,465 行（254項目）
```

外部依存は極端に少ない（実行時依存は `electron-updater` / `yauzl` / `yazl` の3つだけ）。
**UIライブラリも状態管理ライブラリも使っていない**（React の素の機能だけ）。

---

## 2. できること（e2e が実際に確認している範囲）

254項目の自動確認が、下の全部を**実際に画面を触って**通している。

### 編集の土台
- タイムライン（複数トラック・段の追加/削除/高さ変更・縦横スクロール）
- カット、リップル削除（消して後ろを詰める）、リップルトリム
- クリップの移動（**プレミア流の上書きドラッグ**）、吸着（マグネット）、範囲選択（投げ縄）
- 元に戻す/やり直す（50回連続でも並びと絵が完全に復元することを確認済み）
- 複数素材（マルチソース）、ネスト（組）、クリップの色分け

### テロップ（このアプリの本丸）
- SRT 読み込み、プレビュー上での直接ドラッグ配置・その場で打ち直し
- 縁取り（最大3枚）・影（複数）・グラデーション塗り・縦書き
- **文字単位の装飾**（一部の文字だけ色/サイズ/縁/影/グラデ）
- アイコン（顔アイコン等）の添付、見本帳（テンプレート）とD&D適用
- **重なったら置いた方が勝つ**（上書き）— 移動・貼り付け・複製・端の伸ばし、全経路で統一
- 出入りのアニメーション、キーフレーム（ベジェ）による動き

### 音
- 波形表示、無音カット（自動検出して一括で切る）
- **ダッキング**（声に合わせてBGMを自動で下げる。プレビューと書き出しで同じ曲線）
- トラックごとの音量/ソロ/ミュート、ラウドネス正規化

### 素材・映像
- 素材ビン、フォルダ丸ごと読み込み、素材パック（ZIP）
- 映像レイヤー（ピクチャインピクチャ）、画像の重ね
- 拡大/位置/回転/反転/不透明度/色調整/切り抜き、切り替え効果（トランジション）
- **プロキシ**（低画質の焼き直しを裏で作って編集を軽くする）

### 書き出し・運用
- H.264（NVIDIA/Intel/AMD の GPU を自動判別、無ければ CPU、最後の砦は OpenH264）
- 音だけ書き出し（mp3）、ショート（9:16）対応
- プロジェクトの保存/自動下書き/復元、**別PCへ素材ごと持ち出し**
- パネルの切り離し（別ウィンドウ・別モニター）
- 自動更新、**測定レポートの書き出し**（不具合報告用）

---

## 3. 品質の実測値

### 自動確認

```
単体テスト   1,000件 緑
e2e         254項目 / 256チェック  全緑（約13分・実際に画面を操作）
```

e2e は「画面を撮って明るさを測る」「書き出した動画の音量と無音区間を ffmpeg で測る」
「元に戻した後の画面を画素で比較する」まで含む。**モックではなく実物**を通している。

### 負荷（「テレビの編集マン1時間ぶん」を基準にした実測）

基準の中身: 60分の素材／カット600／テロップ1200枚／効果音200／画像120／
映像レイヤー80／動き100／切り替え効果80／めじるし150／素材ビン40。

```
                        2026-08-04 朝   同日夜（v0.1.22）
クリップを掴んで動かす   95% 174.9ms  →  緑（33ms以下）
再生してみる             95% 312.7ms  →  緑
タイムラインを拡大・縮小 95% 162.4ms  →  緑
全部選んでまとめて動かす  操作不能     →  緑
総合                     13良好/5問題 →  **19良好 / 0問題**
```

判定は**平均ではなく95パーセンタイル**（引っかかりは平均に出ないため）。

### 書き出し速度

```
エンコーダ設定の見直し        1.27倍（PSNR 66dB＝画質は同一、ファイルサイズも同一）
テロップ合成の作り直し        重ねる段が 2.85倍
```

---

## 4. 設計上、特徴的な判断

**他AIに見てほしいのはここ。** 良し悪しの判断を仰ぎたい。

### 4-1. 「決まりには必ず機械の検査を付ける」

書いただけのルールは守られないという前提で、以下を `npm run verify` で強制している:

- ファイルの行数（上限1,250／警告800／600超は名指し）
- **1ファイルに2つ以上の話題を持たせない**（500行超は取説コメントを強制）
- **取説が腐っていないか**（コメントが並べた名前と、実際の実装を照合）
- **説明が指すファイルが実在するか**（案内が嘘だと読む人が時間を落とす）
- 同じ物を2か所に書いていないか
- 状態管理の受け口が `any` で開いていないか
- 時間計算・書き出しフィルタ・テロップ描画の出力そのものを固定（ゴールデンテスト）

### 4-2. コメントに「なぜ」と「実測値」を書く

コード上のコメントは説明ではなく**記録**として書いている。例:

```
// **`willReadFrequently: true` を外さないこと。GPU 描画に戻ると重くなる。**
//   レイヤー800枚  Layerize＋合成の更新に 17.8秒（1回 79ms）→ 95% 200ms
//   レイヤー160枚  同 1.3秒（1回 1.7ms）                    → 95%  25ms
```

外れた仮説も**名指しで残している**（誰かが再挑戦しないように）。

### 4-3. 「測る側が壊れている」を最大の敵とみなしている

このプロジェクトで繰り返し起きた事故は**アプリの不具合ではなく、確認する側の欠陥**だった。

- 確認用の素材が「Downloads の一番小さい動画」から作られていて、**中身が運まかせ**
  → 無音ゼロの動画に入れ替わった日、3項目が同時に赤になり切り分けに半日
- 負荷測定中にウィンドウが裏に回ると Chromium が rAF を毎秒1コマに絞る
  → **「アプリが致命的に重い」という顔の赤**が6件出た
- 「テロップが全部読み込まれている」が**数だけ見ていて、1本も描かれていなくても緑**
- 消えたUI要素を `?? ''` で守っていたため、**4か所が何も測らないまま緑**

→ 原則を「**成立しなければ落ちる**」に統一し、上記は全部塞いだ。

### 4-4. 大きいファイルは「話題」で割る（行数では割らない）

`App.tsx` が11,404行、配線が1,229行まで膨らんだ事故を2回起こしている。
原因は毎回同じで「**足しただけで元を消さない**」。1回ずつは無害なので誰も気づかない。

→ 構造で防ぐ形にした（新しい機能は配線に足さず、自前の Context を作らせる）。
現在 600行超のファイルは **0本**。

---

## 5. 弱い所・やっていない所（正直に）

- **Windows のみ。** macOS/Linux は未対応（ビルド構成はあるが未検証）
- **署名なし配布。** SmartScreen の警告が出る
- **利用者が実質1人。** 他人の環境・他人の操作で壊れる余地は未知数
- **e2e の確認用素材が Downloads 依存。** 無音は自前で仕込むようにしたが、
  絵の中身は「そこにある一番小さい動画」のまま。他のPCでは違う素材になる
- **`e2e` の項目に順番依存がある。** 手前が残した状態に寄りかかる確認があり、
  並べ替えると偽の赤/緑が出る（ファイル冒頭に明記して運用でしのいでいる）
- **アニメーション付きテロップの書き出しは、まだ全画面PNGのまま**
  （静止テロップだけ切り詰めた。連番の大きさを揃える必要があるため）
- **自動更新の公開手順が不安定。** `npm run release` が1回目に
  「添付が1つだけ・終了コードは0」で終わる事象が3回連続で起きている（GitHub側の挙動）
- **国際化なし**（日本語のみ）。**アクセシビリティ未検討**
- **クラッシュレポートの自動送信なし**（手動で測定レポートを出す形）

---

## 6. 診断してほしい観点

1. **この規模（本体5.5万行・1人開発）に対して、検査の量と質は妥当か。**
   過剰か、それとも足りないか
2. **「測る側の正しさ」への投資が重すぎないか。** e2e 1.7万行・254項目は、
   個人開発として維持できる量か
3. **状態管理をライブラリなしで組んでいる**ことのリスク
   （Context を多数作る方式。配線の肥大は検査で止めている）
4. **Electron + FFmpeg 同梱**という構成の、配布・保守面での落とし穴
5. 上の「弱い所」のうち、**優先して潰すべきはどれか**
6. **見落としている大きなリスクはないか**（この文書に書かれていない観点）

---

## 補足: 判断に使える生データ

- 公開先: https://github.com/kyohei0612/GiftCut （公開リポジトリ・コードは全部読める）
- 版: v0.1.22（2026-08-05 公開）
- コミット履歴に、各変更の**測定値と外れた仮説**が日本語で残してある
