# 差分更新（更新を 十数秒 → 1秒 にする）

**2026-08-06 に入った**（読み込み係 v0.1.27 → 差し替えの本体 v0.1.28）。

> **最初の見立ては外れていた。** 「落とす量が多いから遅い」と思って始めたが、
> 測ったら**落とす量は 1.2 MB（1.0%）で既に速かった**。
> 遅いのは**インストーラが 263MB を書き直す所**だけ。設計は変えないが、
> **やる理由が「落とす量を減らす」から「インストーラを走らせない」へ変わった。**

## いまどう動くか（v0.1.28 以降）

```
新しい版を見つける
  └ bundle-<版>.json（数百バイト）を見に行く
      在って土台が合う   → bundle-<版>.zip（**361 KB**）を落として userData へ展開
                            再起動は「閉じて開く」だけ＝**1秒**
      無い／土台が違う   → いままでどおりインストーラ（Electron や ffmpeg が
                            変わった版はこちら。**そういう版があるのが普通**）
```

| どこ | 何をする |
|---|---|
| `boot.js` | どの JS を動かすかを決める。**指紋と版を下ろす**（環境変数） |
| `bootGate.js` | 通す・断るの判断だけ（副作用なし＝**試験できる**）。`bootGate.test.ts` |
| `scripts/make-bundle.mjs` | 配る側。`out/` を固めて荷札を付ける |
| `src/main/bundleUpdate.ts` | 受け取る側。荷札→中身の照合→展開→印 |
| `scripts/check-swap.mjs` | **配布物で本当に効くかを見る**（`npm run check:swap`） |

**`out/` は 1,477KB、固めて 361KB。** node_modules は入れない——あれは土台の側で、
指紋が同じなら中身も同じなので運ぶ意味がない。差し替えから見えるようにするのは
`boot.js` の仕事（`NODE_PATH` に asar の node_modules を足す）。

## 戻れることの方が本体

通す道より**戻る道**に手数をかけてある。一度でも悪い物を配ると、使う人には
手で入れ直す以外の道が無くなるため。

```
壊れた差し替え    1回だけ試す → 起動できなければ**中身ごと捨てて同梱へ**
土台が違う        読まずに捨てる（新しい本体を古いコードが呼ぶ形を作らない）
同梱の方が新しい  捨てる（後からインストーラを当てた形。読むと過去へ戻る）
```

**印は「書く側」ではなく「消す側」に倒してある。** 起動できたら `verified` を書き、
書かれていなければ次で捨てる。電源断・強制終了で書けなかった場合も安全側へ落ちる。

## 本物の更新を通した（2026-08-06・v0.1.28 → v0.1.29）

`npm run release` で出した本物の 0.1.29 へ、退避しておいた 0.1.28 の配布物を
更新させた。道具は `node scripts/measure-swap.mjs <古い版のexe>`。

```
18:04:30.343  見に行く（起動から4.0秒後。ここは待ちの設定どおり）
18:04:31.408  Found version 0.1.29
18:04:31.589  **差し替えを置いた（361 KB）**   ← 落として照合して展開まで 0.18秒
18:04:32.071  「今すぐ再起動」を押す
18:04:32.409  --- 起動 v0.1.29（同梱 v0.1.28）---
18:04:32.603  差し替え v0.1.29 は無事に起動できた（＝画面が出た）
```

**いままで側も実測が取れた**（本人の 0.1.27 → 0.1.29・インストーラの道）:

```
23:58:49.441  [経過] 入れ替え開始（押した）
23:59:06.367  --- 起動 v0.1.29（同梱 v0.1.29）---
```

| | いままで（実測） | いま（実測） |
|---|---|---|
| 押してから画面が出るまで | **16.9秒** | **0.5秒** |
| 落とす量 | 1.2 MB | **361 KB** |

**34倍。**「十数秒」は体感だけが根拠だったが、当たっていた。

**落とす量の計算も裏が取れた。** electron-updater 自身が同じ更新でこう吐いている:

```
File has 53 changed blocks
Full: 118,671.68 KB, To download: 1,202.59 KB (1%)
```

`scripts/update-diff.mjs` が blockmap から出した **1.2 MB / 1.0%** と一致。
**次の更新を待たずに出せる**ことが、これで確かめられた。

**同梱は 0.1.28 のまま**で、動いているコードだけ 0.1.29 になっている。
題名の横に出る版も 0.1.29（`runningVersion()`）——ここが同梱の版を返すと
「直したはずが直っていない」の切り分けができなくなる。

※ 測り方でつまずいた記録: 最初は「`--- 起動` を含む行」を待っていたが、
  **先頭から探すので古い版の起動行に当たり**、押した時刻より前を掴んで
  所要が **-6.7秒** になった。待っているのは「これから出る行」なので後ろから見る。

## 実物で確かめた（2026-08-06）

単体試験は判断しか見ない。**asar の中から userData の JS を読めるか**は実物でしか
出ないので、配布物を起動して見る:

```
差し替えで起動し、確認済みになった      **1.0秒**
壊れた差し替えを捨てた（中身ごと）      0.5秒
土台違いを読まずに捨てた                0.6秒
```

**開発中は必ず通ってしまう確認が1つある。** 差し替えた JS が `electron-updater` を
見つけられるかは、開発中は node_modules がそこら中に在るので成功する。
配布物では asar の中にしか無い。**だから実物で見る。**

---

## **測ったら、狙いどころが違った**（2026-08-06 に確定）

最初はこう書いていた——「263MB 全部を入れ替えている。**落とす量**を 2.6MB にしたい」。

**落とす量は、最初から 1.2 MB だった。**

```
v0.1.26 → v0.1.27    全体 115.9 MB のうち **落とすのは 1.2 MB（1.0%）**
v0.1.22 → v0.1.27    5つ前の版からでも **1.2 MB（1.0%）**
```

electron-updater の差分（blockmap）は**とっくに効いていた。**
道具は `scripts/update-diff.mjs`（`node scripts/update-diff.mjs 0.1.26`）。

### 待たずに済んだ

`src/main/updateLog.ts` を入れて「次に更新が出たら読む」つもりでいたが、
**待つ必要が無かった**——blockmap が両方あれば計算で出る。
実際 v0.1.27 が当たった直後にログを見ても、落とした量は入っていない。
**記録を持っているのは新しい側**で、落とす仕事をしたのは古い側（v0.1.26）だから。
「次の更新で分かる」と決めた時点で、**1回ぶん無駄に待つことが確定していた。**

→ 手元にあるデータで先に出せないかを、待つと決める前に考えること。

### では十数秒は何か

**インストーラが 263MB を書き直している時間。** 落とす所ではない。

```
落とす      1.2 MB   ← 既に速い。触っても意味がない
入れ替え    263 MB を展開して置き換える   ← **ここだけが遅い**
```

だから下の設計は**そのまま有効**だが、**理由が変わる。**
「落とす量を減らす」ではなく、**「インストーラを走らせない」**。
JS を userData に置いて開き直すだけなら、263MB の書き直しが丸ごと消える。

※ 内訳（展開後 263MB）: 自前のコード 2.6MB ／ Electron 本体 119MB ／
  ffmpeg・whisper 93MB ／ その他 48MB。**変わるのは 2.6MB だけ。**

---

## 設計（案）

### 形

```
app.asar（同梱・めったに変わらない）
  └ main.js …… **読み込み係だけ**。
       userData/bundle/current.json を見て、
       あれば userData/bundle/<版>/main/index.js を require、
       無ければ同梱の物へ落ちる

配る物
  GiftCut-Setup-x.y.z.exe        いままでどおり（本体が変わるとき）
  bundle-x.y.z.zip（約2.6MB）    **JS だけ変わったとき**
  bundle-manifest.json           版・ハッシュ・**土台の指紋**
```

### 土台の指紋

JS だけ差し替えてよいのは、**土台が同じとき**だけ。

```
Electron の版 / ffmpeg の版 / whisper の版 / native モジュールの有無
```

これを固めた文字列（指紋）を manifest に入れ、**一致しなければ差分をやめて
いままでどおりインストーラへ**。ここを緩めると「新しい JS が古い本体を呼ぶ」
という一番たちの悪い壊れ方をする。

### 壊れたときに戻れること（**ここが本体**）

```
1  落とす        userData/bundle/<版>/ へ展開。ハッシュを確かめる
2  印を書く      current.json = { 版, verified: false }
3  開き直す
4  読み込み係    verified が false でも一度は読む
5  無事に起動    ready まで来たら verified: true
6  次の起動      verified が false のまま来た＝**前回この版で起動できなかった**
                → 同梱の物へ戻し、その版を捨てる
```

**「落ちない代わりに黙って死ぬ」を作らないこと**（CLAUDE.md 7番）。
読み込みに失敗したら、黙って同梱へ落ちるのではなく**記録を残す**。

---

## なぜ 2026-08-06 に一気に出さなかったか

**確かめる手段が、1回のリリースでは作れないから。**

差分更新が本当に動くかは「**古い版から新しい版へ実際に更新してみる**」でしか
分からない。片方だけ作っても、動いた証拠が手に入らない。

そして失敗の形が悪い:

```
書き出しの不具合   出来上がりを見れば分かる。作り直せる
差分更新の不具合   **アプリが起動しなくなる**。使う人は何もできない
```

自動更新は**全員に配られる**ので、壊れた読み込み係を配ると
「開かないアプリ」が全員の手元に残る。手で入れ直してもらうしかない。

**測れていない・確かめられない・失敗すると全員が困る**、が揃っている。
この3つが揃ったものを、確認できない状態で出さないこと。

---

## 進め方（案）

```
1  更新のログを入れて出す                    ← **済（v0.1.27）**
2  落としている量を出す                      ← **済。1.2 MB（1.0%）＝既に速い**
   → 狙いどころが決まった: **入れ替え側だけ**
3  読み込み係だけ作る（同梱の物を読むだけ）   ← **済（v0.1.27）**
4  bundle の配布と差し替えを作る              ← **済（v0.1.28）**
5  配布物で確かめる                          ← **済。`npm run check:swap`**
6  **実際に2つの版で更新してみる**            ← v0.1.28 → v0.1.29 で見る
   ※ v0.1.27 からの更新は**インストーラ**になる（受け取る口が無いため）。
     差し替えの道を通るのは v0.1.28 以降どうし
7  入れ替えの秒数を取る（下）
```

### 4 は、次の更新で勝手に出る

v0.1.27 は「今すぐ再起動」を押したとき `入れ替え開始` の時刻を書く。
新しい版は起動したとき `--- 起動 vX ---` を書く。**その差が入れ替えの秒数。**

```
2026-…T…  [経過] 入れ替え開始（ここから先はインストーラの仕事）
2026-…T…  --- 起動 v0.1.28 ---        ← この差
```

※ **黙って閉じたときは出ない**（`autoInstallOnAppQuit` が当てる道には
  こちらの記録が挟まっていない）。測りたいときは帯の「今すぐ再起動」を押すこと。
  遅いと言われているのもそちらの道なので、測る対象としては合っている。

**3 を先に配るのが要点。** 読み込み係が全員に行き渡ってからでないと、
4以降を試せない（古い版には読み込み係が無いので差分を受け取れない）。

---

## 参考: 調べた事実

- `quitAndInstall` は「インストーラを起動 → **すぐ閉じる**」（`BaseUpdater.js`）
- NSIS は動いているアプリを**強制終了させる**。その前に
  `Sleep 300 → 探す → Sleep 1000 → KILL_PROCESS`（`allowOnlyOneInstallerInstance.nsh`）。
  こちらから閉じなければ **1.3〜1.6秒は窓が生きる**
- 入れ替えはアプリが終了してからでないと始められない
  （動いている実行ファイルは Windows が置き換えさせない）。
  **「入れ替え完了 → 再起動ボタン」は作れない**
