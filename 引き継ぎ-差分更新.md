# 差分更新（更新を 十数秒 → 1秒 にする）

**2026-08-06 に着手。差し替えの本体はまだ入っていない**（読み込み係だけ v0.1.27 で配った）。

> **最初の見立ては外れていた。** 「落とす量が多いから遅い」と思って始めたが、
> 測ったら**落とす量は 1.2 MB（1.0%）で既に速かった**。
> 遅いのは**インストーラが 263MB を書き直す所**だけ。設計は変えないが、
> **やる理由が「落とす量を減らす」から「インストーラを走らせない」へ変わった。**

---

## **測ったら、狙いどころが違った**（2026-08-06 に確定）

最初はこう書いていた——「263MB 全部を入れ替えている。**落とす量**を 2.6MB にしたい」。

**落とす量は、最初から 1.2 MB だった。**

```
v0.1.26 → v0.1.27    全体 115.9 MB のうち **落とすのは 1.2 MB（1.0%）**
v0.1.22 → v0.1.27    5つ前の版からでも **1.2 MB（1.0%）**
```

electron-updater の差分（blockmap）は**とっくに効いていた。**
道具は `scripts/update-diff.mjs`（`node scripts/update-diff.mjs 0.1.26`）。

### 待たずに済んだ

`src/main/updateLog.ts` を入れて「次に更新が出たら読む」つもりでいたが、
**待つ必要が無かった**——blockmap が両方あれば計算で出る。
実際 v0.1.27 が当たった直後にログを見ても、落とした量は入っていない。
**記録を持っているのは新しい側**で、落とす仕事をしたのは古い側（v0.1.26）だから。
「次の更新で分かる」と決めた時点で、**1回ぶん無駄に待つことが確定していた。**

→ 手元にあるデータで先に出せないかを、待つと決める前に考えること。

### では十数秒は何か

**インストーラが 263MB を書き直している時間。** 落とす所ではない。

```
落とす      1.2 MB   ← 既に速い。触っても意味がない
入れ替え    263 MB を展開して置き換える   ← **ここだけが遅い**
```

だから下の設計は**そのまま有効**だが、**理由が変わる。**
「落とす量を減らす」ではなく、**「インストーラを走らせない」**。
JS を userData に置いて開き直すだけなら、263MB の書き直しが丸ごと消える。

※ 内訳（展開後 263MB）: 自前のコード 2.6MB ／ Electron 本体 119MB ／
  ffmpeg・whisper 93MB ／ その他 48MB。**変わるのは 2.6MB だけ。**

---

## 設計（案）

### 形

```
app.asar（同梱・めったに変わらない）
  └ main.js …… **読み込み係だけ**。
       userData/bundle/current.json を見て、
       あれば userData/bundle/<版>/main/index.js を require、
       無ければ同梱の物へ落ちる

配る物
  GiftCut-Setup-x.y.z.exe        いままでどおり（本体が変わるとき）
  bundle-x.y.z.zip（約2.6MB）    **JS だけ変わったとき**
  bundle-manifest.json           版・ハッシュ・**土台の指紋**
```

### 土台の指紋

JS だけ差し替えてよいのは、**土台が同じとき**だけ。

```
Electron の版 / ffmpeg の版 / whisper の版 / native モジュールの有無
```

これを固めた文字列（指紋）を manifest に入れ、**一致しなければ差分をやめて
いままでどおりインストーラへ**。ここを緩めると「新しい JS が古い本体を呼ぶ」
という一番たちの悪い壊れ方をする。

### 壊れたときに戻れること（**ここが本体**）

```
1  落とす        userData/bundle/<版>/ へ展開。ハッシュを確かめる
2  印を書く      current.json = { 版, verified: false }
3  開き直す
4  読み込み係    verified が false でも一度は読む
5  無事に起動    ready まで来たら verified: true
6  次の起動      verified が false のまま来た＝**前回この版で起動できなかった**
                → 同梱の物へ戻し、その版を捨てる
```

**「落ちない代わりに黙って死ぬ」を作らないこと**（CLAUDE.md 7番）。
読み込みに失敗したら、黙って同梱へ落ちるのではなく**記録を残す**。

---

## なぜ 2026-08-06 に一気に出さなかったか

**確かめる手段が、1回のリリースでは作れないから。**

差分更新が本当に動くかは「**古い版から新しい版へ実際に更新してみる**」でしか
分からない。片方だけ作っても、動いた証拠が手に入らない。

そして失敗の形が悪い:

```
書き出しの不具合   出来上がりを見れば分かる。作り直せる
差分更新の不具合   **アプリが起動しなくなる**。使う人は何もできない
```

自動更新は**全員に配られる**ので、壊れた読み込み係を配ると
「開かないアプリ」が全員の手元に残る。手で入れ直してもらうしかない。

**測れていない・確かめられない・失敗すると全員が困る**、が揃っている。
この3つが揃ったものを、確認できない状態で出さないこと。

---

## 進め方（案）

```
1  更新のログを入れて出す                    ← **済（v0.1.27）**
2  落としている量を出す                      ← **済。1.2 MB（1.0%）＝既に速い**
   → 狙いどころが決まった: **入れ替え側だけ**
3  読み込み係だけ作る。**差分は使わない**（同梱の物を読むだけ）
   → 出しても挙動は1ミリも変わらない＝安全に土台を配れる  ← **済（v0.1.27）**
4  **入れ替えに何秒かかっているかを取る**（次の更新で自動的に出る。下）
5  bundle の配布と差し替えを作る。**手で staging に置いて動くことを確かめる**
6  実際に2つの版で更新してみる（3 が配られている必要がある）
7  問題が無ければ既定で有効にする
```

### 4 は、次の更新で勝手に出る

v0.1.27 は「今すぐ再起動」を押したとき `入れ替え開始` の時刻を書く。
新しい版は起動したとき `--- 起動 vX ---` を書く。**その差が入れ替えの秒数。**

```
2026-…T…  [経過] 入れ替え開始（ここから先はインストーラの仕事）
2026-…T…  --- 起動 v0.1.28 ---        ← この差
```

※ **黙って閉じたときは出ない**（`autoInstallOnAppQuit` が当てる道には
  こちらの記録が挟まっていない）。測りたいときは帯の「今すぐ再起動」を押すこと。
  遅いと言われているのもそちらの道なので、測る対象としては合っている。

**3 を先に配るのが要点。** 読み込み係が全員に行き渡ってからでないと、
4以降を試せない（古い版には読み込み係が無いので差分を受け取れない）。

---

## 参考: 調べた事実

- `quitAndInstall` は「インストーラを起動 → **すぐ閉じる**」（`BaseUpdater.js`）
- NSIS は動いているアプリを**強制終了させる**。その前に
  `Sleep 300 → 探す → Sleep 1000 → KILL_PROCESS`（`allowOnlyOneInstallerInstance.nsh`）。
  こちらから閉じなければ **1.3〜1.6秒は窓が生きる**
- 入れ替えはアプリが終了してからでないと始められない
  （動いている実行ファイルは Windows が置き換えさせない）。
  **「入れ替え完了 → 再起動ボタン」は作れない**
