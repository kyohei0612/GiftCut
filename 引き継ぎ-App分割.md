# 引き継ぎ：大きいファイルを割る

最終更新 2026-08-02。App.tsx **11,404 → 198行**。
併せてリポジトリ全体を片付け、**上限（1,250行）超えを0にした**。

---

## 全体でやったこと（2026-08-02）

| ファイル | 前 | 後 | 出した先 |
|---|---|---|---|
| `src/main/index.ts` | 3,352 | **758** | exportRun / assetLibrary / mediaProbe / projectFiles / ffmpegRun / assetRoots / allowList |
| `lib/telopStyle.ts` | 1,730 | **660** | telopSvg / telopAnim / telopMotion |
| `e2e/bench.mjs` | 1,641 | **977** | bench-limits / lib×4 |
| `e2e/run.mjs` | 1,457 | **1,215** | lib/e2eFixture / lib/changedArea |
| `App.tsx`（前回まで） | 11,404 | **198** | components / state |

308ファイルの中央値は121行。**上限超えは0、借金（DEBT）も空。**

**通しは 222 / 224 で、リファクタの前後で1件も変わっていない**（落ちる2件も同じ2件・
同じ数値）。＝これだけ動かして、振る舞いは1つも変えていない。

### 「428行」を目標にしないこと

428 はこのリポジトリの**90%点**。定義上いつも約31本が超えるので、
追いかけても終わらない。効く基準は2つだけ:

1. **1,250を超えない**（AIが1回で読めなくなる。`src/shared/fileSize.test.ts` が見張る）
2. **1つのファイルが2つ以上の話題を持たない**（人が判断する）

大きくても話題が1つなら正しい大きさ。`main/exportRun.ts` 1,133行は
「書き出し」1つで、渡す物を数えたら**0個**だった。

### 残っている構造的な指摘（数字ではなく話題の話）

**`state/useProjectFile.ts`（773行）は2つの話題を持っている**——
プロジェクトの保存・読み込みと、雛形（テンプレート）。
ただし雛形の関数が **3か所に散っている**（L101 / L227-368 / L669-773）ので、
まとめて出すのは危険。やるなら**先に1か所へ寄せてから**測ること。

---

## いまの形（配線まわり）

  App.tsx (198)              画面は何でできているか（心臓を積む・区画を並べる）
  state/useAppWiring (1,206) それらをどうつなぐか（どのフックに何を渡すか）
  state/use*.ts (約60個)     話題ごとの中身
  components/                区画・帯・ダイアログ

### 絡まりはほどけた（「呼ぶときに見に行く」は 0 か所）

以前は 21 か所で `(...a) => X(...a)` の形を使って前方参照を逃げていた。
**全部消した。** 呼ぶ順が本当の依存の順と一致している。
内訳は「完了までの道」の段階1〜3。

### **ただし、配線は話題ごとには割れない**（実測で確定）

ほどけば割れると見ていたが、**外れだった**。どこで割っても最低106個をまたぐ。
理由と数字は下の「段階4・5」を読むこと。**再挑戦する前に必ず読む。**

### 受け取り方は全部そろえた

区画（左・右・プレビュー・タイムライン）・品書き・画面上端は、
**すべて心臓（context）から自分で見に行く**。prop で受けるものは無い。
新しい区画を足すときも同じ形にすること。

### 使っていない物を見つける検査を入れた

tsconfig に `noUnusedLocals`。入れた時点で**493個**の死んだ import と変数が
見つかった（PerfHud は別ファイルへ移った後も宣言だけ残っていた。
useClipDrag は import されたまま一度も呼ばれていなかった）。
切り出すたびにこれがたまるので、**外さないこと**。

---

# 絡まりをほどいた記録（2026-08-02）

**「呼ぶときに見に行く」形（`(...a) => X(...a)`）を 21か所 → 0 にした。**
手順は4段階。同じことをまたやるなら、この順を崩さないこと
（1と2を飛ばして3をやると、どれが本当の輪か分からないまま大手術になる）。

各段の終わりに `npm run verify`。実行時の順番を動かした段だけ
`npm run build` → `npm run e2e -- --only=<語> --fast`。**通しは最後に1回だけ。**

---

## 段階0 ── 掃除

`noUnusedLocals` を入れたとき、**中身だけ抜けて器が残っていた**。

- App.tsx 380行のうち **179行が空行**。`import type {} from '…'` のような
  空の器も残っていた
- useAppWiring 1,294行のうち **132行が空行**。持ち主が別ファイルへ移ったのに
  説明だけ残っている塊が10か所以上

**説明は捨てる前に、持ち主側のファイルに同じ話があるか確かめた。**
全部あった（1つを除く）。無かった「V と A の境目を真ん中に残そうとして壊した」
記録だけ `useTimelineBox` の頭へ移した——**あの決まりが何のためにあるか**の
唯一の記録なので、消してはいけない。

結果: App.tsx 380 → **198行**、useAppWiring 1,294 → 1,164行。

---

## 段階1 ── 輪でない「見に行く」を外す（地図を出す）

**まず数え間違いを直した。** 引き継ぎには「5か所」とあったが、
`Parameters<typeof` で数えると15か所、矢印の形（`() => X()`）も入れると
**21か所**あった。片方の書き方だけで数えると足りない。

数え直したら、**21か所のうち9か所は輪ですらなかった**。渡す相手が既に
上で定義済みで、そのまま渡せる。並び替えの後に外し忘れた物。

外した9か所（→ 相手はすべてこれより上で定義済み）:

| 渡していた物 | 渡し先 |
|---|---|
| `setTime` / `fallbackTrack` / `kindOf` / `setOpenAccSec` | useMediaOps |
| `fallbackTrack` | useCopyPaste |
| `stopPlayback` / `setTime` / `fallbackTrack` / `kindOf` | useProjectFile |

**9か所外して型検査が通った＝どれも前方参照ではなかった、の証明。**
ここで初めて本当の輪だけが残って地図が見える。21 → 12 か所。

---

## 段階2 ── 順番を入れ替えるだけで解ける片道参照を解く

3か所は輪ではなく、**ただ呼ぶ順が逆**だった。相手はこちらを要らない。

| 渡していた物 | やったこと |
|---|---|
| `stopPlayback`・`seekTo` → useSubtitles | useSubtitles を usePlaybackEngine の後ろへ |
| `setSegRotate` → usePreviewManip | usePreviewManip を useTimelineEdit の後ろへ |
| `razorSegment` → useSegmentDrag | useSegmentDrag を useTimelineEdit の後ろへ |

**移す前に2つ確かめた。** ① 相手が本当にこちらを要らないか（渡す物の一覧を目で見る）
② 移す物が `useEffect` を持っていないか。3つとも効果ゼロの「関数の工場」だったので、
**順番を変えても実行時の順序は動かない**（＝e2e を回すまでもない）。

12 → 9 か所。残りが本当の輪。

---

## 段階3 ── 本当の輪を解く（山場）

**輪は4つ。1つ解くごとに verify を回した。**
解き方は毎回同じ形になった——**輪の結び目は、たいてい「置き場所を間違えた小さな物」**。
それを正しい持ち主へ移すと輪が切れる。大手術は1度も要らなかった。

### 輪A：useHistory ↔ useProjectFile

履歴は控えを戻す `restore` を要り、開く側は `resetHistory` を要っていた。

**`restore` を `useHistory` へ移した。** 使っていたのは undo/redo だけで、
中身も心臓（`useDoc` / `useTracksCtx` / `useSel` / `useExportCtx`）を
書き換えるだけ。**開く処理を一切要らない**——ただ置き場所が違っただけだった。
ついでに `setHistTick` / `suppressHistoryRef` / `Snap` が useProjectFile から不要になった。

### 輪B：useMediaOps → useMediaDrop → useSegmentPlace → useMediaOps

読み込む側が置く側を要り、置く側が読み込む側を要る三角。

**結び目は `addMediaAtPlayhead`（6行）だけだった。** 置き方を3種類知っているだけの
関数で、これを配線側（置く物が3つとも揃った位置）へ出したら輪が消えた。

### 輪C：usePlaybackEngine ↔ useViewNav ／ ↔ useMediaOps

二重の輪。**結び目は2つとも「引くだけ・動かすだけ」の小さな関数**だった。

- `revealPlayhead`（連れてくる）は飛ばす側を一切要らない
  → `useTimelineBox` へ。**縦の追従を持っている所が、横の追従も持つ**のが筋
- `seekAndReveal`（飛ばして見せる）は飛ばす本人が持てばよい → `usePlaybackEngine` へ
- `srcOfSeg`（切片の元動画を引く）は `sourcesRef` を引くだけ
  → 素材の心臓 `useMedia` へ。**読む人が一番多いのに、読み込む側に置いてあった**

これで `usePlaybackEngine` を `useMediaOps` より**前**に呼べるようになった。

### 輪D：useProjectFile ↔ useProjectIO

**`confirmDiscard` / `rememberProject` / `hasUnsavedChanges` を
`state/useProjectGuard` へ出した**（新しいファイル）。3つとも開く処理も保存処理も
要らず、出し入れ側に置いてあったのは topic のなりゆきだけ。両方より先に作れば済む。

### おまけ：useMediaMeta

`historySnaps` が `undoStackRef` を待っていた（段階2と同じ「順番だけ」）。
`useHistory` の後ろへ移した。効果は2つあるが、片方は ref の写し、
もう片方は `setTimeout` の中なので**順序に敏感ではない**ことを確かめてある。

---


## 段階4・5 ── **測ったら「割れない」と出た。やらないこと**

段階3が終わった時点で数え直したところ、**配線は話題ごとに割れない**と分かった。
「輪をほどけば割れる」という上の見立ては**外れていた**。以下は実測。

### 測り方

境目を1本引いて、**前半で生まれて後半で使われる名前**を数える
（＝そこで割ったときに渡すことになる物）。目安は40個。

### 結果：どこで割っても 3〜10倍を超える

| 割る位置 | またぐ物 | 束を各グループへ配ったと仮定しても |
|---|---|---|
| 心臓の受け取りの直後 | 136 | 106 |
| 画面まわりの後 | 293 | 177 |
| 再生と素材の後 | 325 | 197 |
| 編集の後 | 413 | 133 |
| 全部の後（＝何も出さない） | 359 | 55 |

**最小でも 106 個。** 40 の 2.6 倍で、これは「一番おいしい所で割った」場合の数字。

### なぜ割れないか

**配線は「最後に一点へ集まる」形をしている。** 宣言される 579 個の名前のうち、
ほとんどが末尾の8つの束（`timelineOps` / `timelineView` / `previewCtx` /
`leftPanel` / `rightPanel` / `header` / `menus` / `dialogs`）へ流れ込む。
どこで横に切っても、この流れを全部またぐ。

段階5（束を持ち主へ配る）で直るかとも思ったが、**直らない**。上の表の右列がそれで、
配っても 55〜204 個。束を配れないのは、1つの束が10個以上のフックから
少しずつ集めているため（例：`rightPanel` は `useLibraries` だけでなく
`useTelopEdit` / `useTransitions` / `useMotion` / `useBandDrag` … から集めている）。

### フック単位でも同じ

「戻り値が束にしか行かないフック」＝**他のフックに渡らない葉**を数えたら、
**65個中6個だけ**（`useIconLibrary` / `usePreviewManip` / `useLabelsPresets` /
`useSubtitles` / `useSegmentDrag`、戻り値は合わせて21個）。
残り59個は本当に互いへ渡っている。**絡まりは輪ではなく、網だった。**

### では何が要るのか（やるとしたらこの順）

割れないのは配線が悪いからではなく、**画面側が「8つの大きな束」で受けているから**。
束を小さくしていけば、集める先が分かれて配線も分かれる。
すでに2回それをやって成功している（`DragPreviewProvider`、
`TimelineOpsProvider` / `TimelineViewProvider`）。同じ手を続けるなら:

1. 一番大きい `rightPanel`（約60個）を、タブ単位の小さな心臓へ割る
2. `dialogs`（約50個）を、ダイアログ1つにつき1つへ割る
3. そのぶん配線から流れ出す先が減るので、**そこで初めて**上の表を測り直す

**先に配線を割ってはいけない。** 106〜204個の導管ができるだけで、
次の人は「どこから来た値か」を追えなくなる。JSX を一度取り下げたのと同じ理由。

---

## そのあと（リファクタの外）

**リファクタとしてやれることは、ここで一区切り。** 次は `やること.md`
（プレテストで出た30件ほど）。段の太さの「引っ越し」の件もそちら。

### 先に知っておくこと：通しは 222 / 224（既存の落ちが2件）

2026-08-02 の通し（12.1分）。落ちる2件は**どちらもリファクタ前から落ちている**
——作業を退避して、同じ2件を同じ数値で落ちることを確かめた。

- **プレビューで文字を動かすと、選んである他の文字も一緒に動く**
  … 掴んだ文字が動いていない（0px）
- **文字を2つ選んで拡大すると、両方が大きくなる（元の差は保つ）**
  … 選んである他の文字が大きくなっていない（67 → 67）

どちらも**プレビュー上で複数選んだテロップをまとめて操作する**話。
落ちた時の記録はどちらも「選択中: 1」なので、まとめ操作の入口まで
届いていない可能性がある。持ち主は `state/useTelopBox` と `state/usePreviewManip`。

### この試験群には、順番に依存する物がある

**「落ちたときに『1つ前の状態』でも復元できる」は、`--only` で絞ると落ちるのに
通しでは通る。** 下書きの世代交代が、前の項目が残した状態に依存しているため。

つまり **`--only` で緑／赤が出ても、それが通しの結果とは限らない。**
逆も同じ。絞った確認で赤が出たら、**通しでも同じか**を見てから騒ぐこと
（この2件は絞っても通しでも落ちるので、本物）。

---

## これまでの経緯

---

## 数え方を間違えないこと（この作業の要点）

**「大きい関数を1個ずつ」で数えると桁を見誤る。**

一度そう数えて「切り出せるのは 1,200〜1,500行」と見積もったが、
**話題ごとにまとめて**数え直したら、166個の関数が13の話題に1個残らず収まり、
実際には 2,100行以上出た。

理由は、話題の中どうしの呼び合いが「渡す物」から消えるから。
`cutAtPlayhead` を単体で出すと切片・選択・履歴・磁石…と数十個要るが、
**編集25個をまとめて出せば27個**で済む。

---

## App.tsx が 4,721行だった頃の内訳（記録）

| 行 | 何 | 出せるか |
|---|---|---|
| 315 | import と型 | — |
| 116 | App の外（定数・小さな部品） | — |
| 約2,250 | state / ref の宣言、心臓からの受け取り、残りの小さな関数 | 心臓へ移さないと出せない分が多い |
| 317 | `useEffect` 37個 | 1個8行と細かい。大きい塊は出し終えた |
| 1,720 | JSX | **要設計** → 済（下記） |

---

## 出した先

### 帯（クリップ）… `components/timeline/`

`TelopBands` / `MainClipBands` / `SeBands` / `OverlayClipBands` /
`TransitionBands` / `DropGhosts` / `Ruler` / `ClipBand` / `KeyMarks`。
**App.tsx に `ClipBand` の直書きは無い。**

### 話題ごとのロジック… `state/use*`

| ファイル | 行 | 渡す物 |
|---|---|---|
| `useTimelineEdit` | 733 | 23 |
| `usePlaybackEngine` | 425 | 24 |
| `usePreviewManip` | 347 | 17 |
| `useMediaDrop` | 345 | 26 |
| `useTracksAdmin` | 326 | 12 |
| `useLibraries` | 281 | 1 |
| `useSessionMemory` | 239 | 28 |
| `useMotion` | 232 | 10 |
| `useVideoSync` | 221 | 23 |
| `useTelopLook` | 191 | 0 |
| `useProjectIO` | 185 | 21 |
| `useTransitions` | 178 | 9 |
| `useDiagnostics` | 150 | 9 |
| `useIconLibrary` | 149 | 14 |
| `useAppLayout` | 143 | 22 |
| `useSegmentPlace` | 137 | 6 |
| `usePreviewFrame` | 133 | 6 |
| `useViewNav` | 71 | 4 |
| `useSnap` | 69 | 2 |
| `useMarkers` | 68 | 4 |
| `useVisibleRange` | 55 | 1 |
| `useSelectionCleanup` | 42 | 0 |

### 画面… `components/`

`AppMenus`（右クリックの品書き6種）。

### 心臓（context）

`DragPreviewProvider` を足した（影・吹き出し・吸い付きの線・囲い・上書き先）。
**JSX を割る前に、掴んでいる最中の状態を心臓へ移す**のが要点。
props で配ると、区画を切り出したときに渡す物が一気に増える。

---

## 手順（次も同じでよい）

1. **話題ごとに「渡す物」を数える。** 心臓から取れる分を引く。目安は40個まで
2. **関数を、直上の説明1ブロックと一緒に**切り出す
   （続けて遡ると、既に別ファイルへ移った物の**取り残された説明**まで巻き込む）
3. deps を空で作って `npm run typecheck` を回す。`Cannot find name 'X'` が全部出るので、
   context / import / deps に振り分ける。**この往復が一番速い**
4. App から元の範囲を消し、呼び出しを入れる
5. `npm run verify` → `npm run build` → `npm run e2e -- --only=<語> --fast`

**一括置換はしないこと。** `(prev) =>` のような形を機械的に置き換えると、
関係ない所（別の setState のコールバック）まで巻き込む（実際に2回起きた）。
型を付けるときは行を見て1つずつ。

---

## 呼ぶ位置が最大の落とし穴

`function` 宣言は巻き上げられるので、どこから参照しても動いていた。
**`const`（フックの戻り値）に変わった瞬間に初期化前参照になる。**
型検査が捕まえてくれるが、順番の組み替えが要る。逃げ道は2つ。

1. **呼び出しを、渡す物が揃った後ろへ移す** ← いまは全部これ
2. 相互参照なら「**呼ぶときに見に行く**」形で渡す
   （`razorSegment: (...a) => razorSegment(...a)`）

**2 は現在 0 か所。** 2026-08-02 に 21か所すべて外した（上の「絡まりをほどいた記録」）。
**新しく増やさないこと。** 増やす前に、まず疑う:

- 相手は本当にこちらを要るか（要らないなら、ただ呼ぶ順が逆なだけ）
- 輪の結び目が「置き場所を間違えた小さな物」ではないか
  （4つ解いて、4つとも**これ**だった。輪そのものを設計し直す必要は一度も無かった）

**呼ぶ順は依存の順に並んでいる。** 上から順に読めば、下は上しか参照していない。

---

## 残っていること

### 1. `useEffect`（配線に約10個）── **細かい物だけ**

大きい塊（映像・音の追従／セッションと保存／計測）は出し終えた。
残りはほとんどが「state を ref へ写す」1〜3行の同期。
**まとめても読みやすくならない。**

### 2. 小さな関数 ── 残りは小粒

`changeRatio` / `shiftAfter` / `hasProjectContent` / `blurActiveInput` /
`addMediaAtPlayhead` / `openClipMenu` / `resetCount` ほか。
どれも配線の中で1〜2か所から呼ばれるだけで、出す先が無い。

### 3. JSX（1,720行）── **済（2026-08-02）**

素直に「タイムライン区画」を部品にすると**渡す物が約130個**になり、目安40個を
大きく超えた。プロパティの導管を作るだけで読みやすくならないので**一度取り下げ**、
先に下ごしらえの心臓を3つ作ってから割った。

- `DragPreviewProvider` … 済
- `TimelineOpsProvider` … 済（掴む操作の入口。約50個）
- `TimelineViewProvider` … 済（タイムライン自身の見え方。約30個）

結果、区画の部品は「自分で見に行く」形になり、prop で渡す物は無くなった。
出した先は `components/panels/`（`Workspace` / `AppHeader` / `AppDialogs` /
`PreviewArea` / `PreviewLayers` / `PreviewOverlays` / `PreviewBars` /
`RightPanelArea` / `PropertiesPanel` ほか）。**App.tsx の JSX は約70行。**

**この順番が要点。** 下ごしらえ無しに部品化していたら、130個の導管ができていた。

### 4. 出せない・保留 ── **どちらも済（2026-08-02）**

- **本編映像の `<video>` 常設**（109行）… `components/panels/PreviewLayers.tsx` へ
- **履歴（元に戻す）**（69行）… `state/useHistory.ts` へ。`restore` との相互参照は
  「呼ぶときに見に行く」形で解いてある（`useAppWiring.tsx:508`）。
  **これは上の「完了までの道」段階3の輪Aとして残っている**

---

## 掃除の宿題 ── **済（2026-08-02）**

説明の取り残しも、空の import の器も片付けた（段階0）。
切り出すたびにまたたまるので、**次に何か出したら同じことをすること**。決まりは1つ:

**説明は捨てる前に、持ち主側のファイルに同じ話があるか確かめる。**
「なぜそうしたか」の記録なので、両方から消えると理由が失われる。

数えるなら「import 文の行を除いて本文を探す」形にすること
（`import './x.css'` のような from の無い行で素朴な探し方は壊れる）。
