# 計画：「部長秘書」エージェント（2026-08-03 夜）

**聞けば「それ、もうある？ どこ？」に答えられる係を作る。**

本人の言葉:

> 全ファイルどこに何があるか把握してる優秀な秘書エージェント。そいつに一度聴いたら
> 「これは初めての実装です」とか「その機能はこのファイルのここに書いてあるので
> ここを修正したらいい」とか。また更新のたびにそれ覚えてるみたいな。

---

## なぜ要るか（今日の実データ。11件を分類した）

2026-08-03 に直した**11件**（朝の e2e 赤4件＋本人指摘7件）を、原因の型で分けた:

| 型 | 件数 | 実例 |
|---|---|---|
| **① 正典・仕組みはあるのに、その道を通っていなかった** | **5** | `overwriteCues` は移動だけ／`EMPTY_DRAG_IMG` は素材ビンだけ／全体表示の率が3か所バラバラ／`placeDropped` は正しいのに写しが古い／保存する道としない道 |
| ② 宣言だけあって実体が無い | 2 | `rightPanelContext` の `draggingEmphasisRef`（`any` なので型検査も素通り）／`telopAnim.ts` の本体の無い取説 |
| ③ 見張りが塞がれて一度も走っていない | 2 | `orderDependent` と `skipHere` の二重で、書かれてから一度も走っていなかった |
| ④ 見送りの判断が古い | 1 | 「札を増やすと `useAppWiring` が上限ぎりぎり」→ **測ったら壁に当たらなかった** |
| ⑤ 参照0の export | 1 | `wasCanceled`（`noUnusedLocals` は export を見ない） |

**①が半分。** そして①はどれも「探せば見つかった」物で、実際に毎回 grep で探し当てている。
**秘書の本命は①を消すこと。**

---

## 秘書が答えるべき問い（優先順）

```
Q1  それ、もう実装ある？                 ← ①（5件）。最重要
Q2  あるならどこ？（正典はどれ？）        ← ①
Q3  同じことをする別の場所は？            ← ①②
Q4  その「やらない」判断、まだ生きてる？   ← ④
```

Q1〜Q3 は**索引**の問題。Q4 は**判断の鮮度**の問題で、性質が違う。

---

## 設計の原則（この組織の第一原則をそのまま継ぐ）

### 1. **照合できない索引は書かない**

`readability.test.ts` の R1b と同じ論理。**腐った索引は、無いより悪い**——
AI はそれを信じて「このリポジトリにはこれしか無い」と判断する。
実際 `telopAnim.ts` には本体の無い説明が残っていて、**誰も気づいていなかった**。

→ 索引は**機械で生成するか、機械で照合するか**のどちらか。人が手で書いて放置しない。

### 2. **リポジトリ側に置く**

`.company/` は .gitignore ＝ clone に付いてこない。秘書の知識をそこへ置くと travel しない。
**`.claude/`（追跡されている）と `src/shared/` に置く。**

### 3. **全ファイルの目次は「作らない」ではなく「機械で作る」**

**当初この計画では「391ファイル全部の要約は作らない（腐るから）」と書いた。撤回した。**

本人の指摘:

> 秘書は把握しといて欲しいな流石に

そのとおりで、**私（1回のセッション）が全部読めない**ことと、**秘書が把握している**ことは
別の話だった。人が手で書く索引なら腐るが、**機械で作るなら腐らない**。

そしてこのリポジトリには、それを作れる根拠が既にある——
`readability.test.ts` が「**全ファイルに冒頭コメントがある**」を機械で保証している
（例外は `DEBT_HEAD` の10本だけ）。

**実測した（2026-08-03）**:

```
ファイル数        398（src / e2e / scripts）
冒頭コメントあり  348（87%）
目次の大きさ      約 23,000字   ← 秘書が毎回まるごと読める
```

出てくる中身の質（実際の抜粋）:

```
state/dragPreviewContext.tsx  ——  掴んでいる最中に出す物を、どの区画からでも触れるようにする。
shared/alignCues.ts           ——  聞き取った字幕の時刻を、**実際に喋っている所へ合わせ直す**。
state/exportContext.tsx       ——  書き出しの設定を、どの区画からでも触れるようにする。
```

**これだけで「何のファイルか」が分かる。** 秘書は全部を頭に入れる必要はなく、
この目次で当たりを付けて、必要な物だけ実物を読めばよい。

※ 説明が無い50本の内訳は `src/shared` 27 ／ `src/renderer` 12 ／ その他11。
  `readability.test.ts` の対象は `src/` の非テストファイルだけなので、
  **e2e / scripts / テストは検査の外**にいる。目次には「説明なし」として出す
  ——無い物を無いと出すのは正しい（次に触る人が足せる）。

---

## 段階（上から順に。各段階だけで役に立つ）

### 段階0：目次を機械で作る（いちばん安く、いちばん広く効く）

`npm run index` で、398ファイルの「パス ＋ 冒頭コメントの1行目」を出す。約23,000字。

**git に入れない。呼ばれるたびに作る。** 生成は数百ms なので、
**原理的に腐らない**（取説が腐った `telopAnim.ts` と同じ失敗をしない）。

これだけで答えられるようになる問い:

```
「テロップの見た目を決めているのはどこ？」   → lib/telopStyle.ts
「掴んでいる最中の物はどこに置いてある？」   → state/useBandDrag.ts
「字幕の時刻合わせは？」                     → shared/alignCues.ts
```

**足りないのは「同じ問いに2つの答えがあるとき、どちらが正典か」だけ**——それが段階1。

### 段階1：正典台帳（今日の①5件がこれで防げた）

**「この問いの答えはこれ」の一覧をリポジトリに持つ。**

原型は既にある——`noDuplicate.test.ts` の `CANON`（4件）。ただし
**式の一致でしか見ていない**ので、名前が違う二重実装は捕まらないし、
「答えの一覧」としては読めない。

作る物: `src/shared/canon.ts`（データ）＋ `canon.test.ts`（照合）

```
問い                          正典
------------------------------------------------------------
重ねた動画の長さは？           shared/timeline の vcLen
切片のタイムライン長は？       shared/timeline の segTLen
テロップが重なったら？         shared/overwrite の overwriteCues
全体が収まる拡大率は？         shared/zoomBar の fitZoom
掴んだときカーソルを消すには？ lib/dragChip の EMPTY_DRAG_IMG
書き出しの窓（enable）は？     shared/filterGraph の overlayEnableExpr
…
```

**照合すること（全部いま書ける）**:

- 正典が**実在する**（引っ越したら赤）
- 正典が**参照0でない**（誰も使っていない正典は正典ではない）
- 生の式で書いた場所の数が**増えていない**（`CANON.debt` の仕組みをそのまま使う）

**やらないこと**: 「全部の関数」を載せる。載せるのは**2回以上書かれた実績がある物**だけ。
書いていない物は grep で足りる。

### 段階2：秘書エージェント本体

置き場: `.claude/agents/secretary.md`（追跡される＝travel する）

**探す順を決め打ちにする。** ここが肝で、決め打ちが無いと結局 grep から始まる:

```
1. src/shared/canon.ts        「その問いに正典はあるか」
2. CLAUDE.md の持ち場の表      「触るならどの部門か」
3. 各 *.test.ts の冒頭コメント 「なぜそう作ったか・過去に何が起きたか」
4. やること.md                 「やった／やらないと決めた／見送った」
5. それでも無ければ grep       「本当に初めての実装か」
```

**答えの形を決める**（これが無いと毎回書き方が違って読めない）:

```
・もうある     → 正典: <file:line>。通っていない道: <file:line>（あれば）
・部分的にある → ある: … ／ 無い: …
・初めて       → 近い物: …（無ければ「近い物も無い」と言い切る）
・やらないと決めた物 → いつ・誰が・測った数字（やること.md より）
```

**「初めてです」と言い切れることが価値**。曖昧に濁すと、結局こちらが grep する。

### 段階3：判断の鮮度（Q4。今日 実際に引っかかった）

`やること.md` には「測って、やらないと決めた」判断が並んでいる。
**その根拠が古くなっても、誰も気づかない。**

実例（2026-08-03）: 「エフェクトのD&Dは、札を増やすと `useAppWiring` が上限ぎりぎりなので
リファクタで場所を空けてから」→ **実際に測ったら壁に当たらなかった**（札は `useBandDrag` が
持っていて `useAppWiring` は受け渡すだけ）。この見送りのまま何日か眠っていた。

→ 見送りには**測った日と数字**を必ず書く。秘書は聞かれたら
「その判断は 08-03 の測定に基づく。いまの数字は◯◯」と**再測定できる形**で答える。

→ 機械で守るなら: 数字付きの見送り（例「残り19行」）を書いた行に印を付け、
**いまの数字とズレたら知らせる**。これは段階1・2が回ってから。

### 段階4：覚え直し

「更新のたびに覚えてる」の実装は、**人が索引を更新しない**こと。

- 正典台帳は**人が足す**（2回書かれた実績が出たときだけ。年に数回）
- 照合は `npm run verify` が毎回やる（＝腐りが1日以上生きられない）
- 段階3の数字は**測り直しの口**を用意する（`npm run measure` のような物）

---

## この計画が踏まないようにしている穴

| 穴 | どう避けるか |
|---|---|
| 索引が腐る | **目次は git に入れず、呼ばれるたびに作る**（原理的に腐らない）。台帳は照合3つ |
| `.company/` は travel しない | `.claude/` と `src/shared/` に置く（両方とも追跡されている） |
| 台帳が肥大して誰も直さなくなる | 台帳に載せるのは**2回以上書かれた実績がある物**だけ。それ以外は目次と grep で足りる |
| 目次に説明の無いファイルが混ざる | 「説明なし」と出す。**e2e / scripts / テストは `readability` の対象外**なので、そこが多い |
| 効かない見張りが増える | 各段階で**わざと壊して赤くなるか**を確かめる（今日と同じ手つき） |
| 秘書が「たぶん無い」と濁す | **答えの形を決める**。言い切れないときは「近い物も無い」と書かせる |
| 決まりが願望のまま増える | 段階1・2は検査とセット。段階3は「まず人が使えるようにしてから機械化」 |

---

## 見積もり（段階ごとに独立して役に立つ）

| 段階 | 中身 | 目安 |
|---|---|---|
| **0** | **目次の生成（`npm run index`）。398ファイル・23k字** | **1時間** |
| 1 | 正典台帳＋照合3つ。既存の `CANON` を移して育てる | 半日 |
| 2 | エージェント定義。探す順と答えの形 | 1〜2時間 |
| 3 | 見送りに測定日と数字を入れる（`やること.md` の棚卸し） | 半日 |
| 4 | 測り直しの口 | 段階0〜3が回ってから決める |

**段階0だけで「どこに何があるか」には答えられる。**
段階0〜2で、今日の11件のうち①②（7件）は次から防げる。
③は今日入れた検査（走らない見張りの名指し）が既に効く。④は段階3。

---

---

## 実施した（2026-08-03 夜。段階0〜3）

| 段階 | 作った物 | 場所 |
|---|---|---|
| 0 | 目次（`npm run index`。401ファイル・約43k字） | `scripts/gen-index.mjs` |
| 1 | 正典台帳9件＋照合4件 | `src/shared/canon.ts` / `canon.test.ts` |
| 2 | 秘書エージェント本体 | `.claude/agents/secretary.md` |
| 3 | 見送りの鮮度（下） | `やること.md` |

### 段階3で分かったこと（**芯はここだった**）

見送りの数字が古くなる原因は「忘れる」ことではなく、**同じ数字を2か所に持っていた**こと。

`やること.md` に

```
| src/renderer/src/state/useAppWiring.tsx | 1,250 | 残り0 |
| e2e/run.mjs                             | 1,250 | 残り0 |
| src/main/exportRun.ts                   | 1,246 | 残り4 |
```

と書いてあったが、**同じ日のうちに全部嘘になった**（exportRun 493 ／ e2e/run.mjs 1,024 ／
useAppWiring 1,232）。`npm run verify` が毎回同じ数字を出しているのに、
それを書き写していたから。

→ **verify が出す数字は書かない。** コードでずっとやっている「同じ物を2か所に
  持たない」を、記録にも当てる。

→ verify が出さない数字（「またぐ名前106〜413個」など）は書くしかないので、
  **「測った日」と「測り直し方」を必ず添える**。今日、その型の見送りが1件
  古くなっていた（エフェクトのD&D＝実際は壁に当たらなかった）。

---

## 段階4：測り直しの口（2026-08-04）

**`npm run measure`（`scripts/measure.mjs`）。** verify が出さない数字だけを出す:

```
① any の残り        state/ 配下。心臓の受け口は 0 で固定（ctxTypes.test.ts が止める）
② 正典を呼んでいる場所の数   台帳は在り処しか書かない。**呼び忘れはここでしか見えない**
③ 借金              上限に対していくつか（赤くなる前に「近づいているか」が分かる）
④ 自動では出せない物 「またぐ名前の数」は**割らないと測れない**と明記する
```

**設計は目次（`npm run index`）と同じ**——git に入れない／呼ぶたびに数える／腐らない。
**verify が出す数字（行数）は出さない。** 同じ物を2か所に持たない。

### 作った初日に3つ見つけた

- **借金の上限を下げ忘れていた。** `vcLen` を13件返したのに `noDuplicate.test.ts` の
  上限が `16` のままで、**12件ぶん書き戻しても緑**だった。
  → **借金は「返す」だけでは終わらない。上限を一緒に下げて初めて効く。**
  measure が `3 / 16` と並べて出したので一目で分かった（→ `3` に下げた）
- **`npm run audit` が存在しなかった。** `e2e/panel-audit.mjs` `e2e/dismiss.mjs`
  `.gitignore` の**3か所がそう案内している**のに `package.json` に無く、
  書いてある通り打つと落ちる。実体はあるので入口を1行足した（**秘書が見つけた**）
- **注意書きが要ることが実物で確認できた。** ②は「名前が出てくるファイル数」なので
  **コメント内の言及も1件に数える**。`overwriteCues` が2件と出たが、実際に呼んで
  いるのは `cueOverwrite` の**1つだけ**（もう1件は `useTimelineDrag` のコメント）。
  → 出力に「少ない物は実物を見ること」と書いてある

### この段で秘書に聞いてから作った

**追加なので、着手前に秘書へ投げた**（「measure のような物はもうあるか」）。
返ってきたのは「**静的に数えて出す口は `npm run index` だけ。初めて**」＋近い物3つ＋
**上の `audit` の欠落**。`package.json` の26スクリプトを全部見た上での答えで、
こちらは1つも grep していない。**このためにいる係が、そのとおりに働いた。**

---

## 検証した（2026-08-03 夜）

**3問聞いて、3問とも当てた。しかも聞いた側（私）が見落としていた物を2つ見つけた。**

聞いたのは今日と同じ型の問い:

1. テロップが重なったとき、どちらを残す？ **呼んでいる場所は全部でいくつ？**
2. 掴んだときカーソルの絵を消す仕組みはある？ 使っていない掴み方は残っている？
3. 縦スクロールで段見出しがついてくる機能は、もうある？ 初めて？

### 秘書が見つけて、私が見落としていた物

- **貼り付け（`useCopyPaste.ts:251`）と複製（`useTimelineEdit.ts:300`）が
  `overwriteCues` を通っていない。** 今日直した⑥（端を伸ばす）と**同じ型が2つ残っていた**。
  実物を読んで裏を取った——どちらも `[...prev, ...pasted].sort()` で並べるだけ。
  → `やること.md` に控えた（**まだ再現していない**ことも明記）
- **`draggingTemplateRef` / `draggingEmphasisRef` が `any` で宣言されている。**
  今日事故った「型だけあって実体が無い」型が、まだ同じ形で残っている
- **私の memory が古かった。** 「縦スクロールについていく側は3つ」→ いまは2つ
  （左端の高さ調整の丸は廃止済み）。秘書が現物で確かめ直して指摘した。memory を直した

### 手順の弱点も分かった（秘書が正直に報告した）

| 段 | 実際 |
|---|---|
| 1 台帳 | **決め手。** Q1・Q2 はここだけで在り処が確定した |
| 2 目次 | **ほぼ効かなかった**——台帳に載っていれば1で終わるため。台帳に無い問い（Q3）向け |
| 6 grep | **必ず要る。** 台帳は在り処しか書かないので、「**呼んでいる場所を全部数える**」には grep が要る |

→ 指示書を直した: 目次は「台帳に無い問いのとき」、grep は「初めてかの確認」と
  「**呼んでいる場所を全部数える**」の2つの役目があると明記。

### 制約（正直に）

**`.claude/agents/*.md` はセッション開始時に読み込まれる。** 作った直後のセッションでは
`secretary` として呼べなかった（`Agent type 'secretary' not found`）。
今回は `general-purpose` に指示書を読ませて代役させ、手順そのものを検証した。
**次のセッションからは `secretary` で直接呼べるはず**（まだ確かめていない）。

## 先に決めておきたいこと

1. **正典台帳を `src/shared/canon.ts` に置いてよいか**
   （`shared` は「画面にも main にも要る計算」の場所。台帳はデータなので少し毛色が違う。
   ただし `noDuplicate.test.ts` が既に同種の物を持っているので、隣に置くのが素直）
2. **秘書は毎回リポジトリを読むか、索引だけ読むか**
   → 索引を入口にして、足りなければ読む（腐りに気づける）
3. **段階1をどこまで埋めるか**
   → 今日の11件に出た正典（6〜8件）から始める。**最初から全部埋めない**
