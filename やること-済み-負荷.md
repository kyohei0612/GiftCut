# やること・済み（負荷まわりの調べた跡）

**`やること.md` から移した（2026-08-08）。** あちらは「これからやること」だけにしてある。

ここに入っているのは**終わった調査の記録**。同じ物を追い直す前に、まず grep すること
——「その見立ては既に潰してある」が何度もあった。

| 節 | 何が分かったか |
|---|---|
| 20秒流してずれを見る | 同期は合っている。**赤を3回出して3回とも測り方が原因**だった |
| 横にスクロール -37% | 犯人は**目盛りの置き場所**。遠回りの原因は**道具の分解能** |
| 合成レイヤーの枚数 | 800枚 → 22枚。**「JS が重いはず」で探すと見つからない** |
| 測る条件は項目が決める | 前の項目が残した状態で測ると、**アプリを触らずに数字が動く** |
| 引いたときに帯を1本ずつ作る | 全部の段を1枚の絵にした |

### 20秒流してずれを見る項目を足した。**同期は合っている**（2026-08-07・本人の指定）

```
ずれ 最大 0.5〜0.6コマ（丸めで1コマまでは出る） / 遅れ 0.0〜0.1% /
絵の固まり 0秒 / 裏でプレビュー最適化中 62% → 84%
```

**3秒の項目では「合っているか」を見ていなかった**（あれはコマ落ちを測る物）。
ずれは時間をかけて開くので、20秒流して1秒ごとに突き合わせる。

**赤を3回出して、3回とも測り方が原因だった。** 3つともアプリの不具合に見える形で
出たので、**そのまま報告していたら存在しない不具合を3つ作っていた**:

| 出た数字 | 正体 |
|---|---|
| 443ms のずれ | プレビューの `<video>.currentTime` は**素材側の時刻**。タイムラインの時刻とは `tToSource`（`state/useVideoSync`）で対応付いている。直に引き算していた |
| 50.7ms のずれ | `幅 ÷ (分×60)` で px/秒 を出したが、**アプリの尺はそこちょうどではない**（テロップの終わり+3秒）。47.8秒の地点で 40ms の誤差 |
| 33.5ms のずれ | **時刻表示はコマ単位に丸めてある**ので、連続値の再生ヘッドと比べれば最大1コマ差は必ず出る。線を1コマに引いていた |

**手掛かりは毎回「進み方は正しいのに絶対値だけずれている」だった。**
本当にずれているなら時間とともに開く。→ **比べるのは同じ物差しの物どうしだけ。**
いまは 再生ヘッド ←→ 時刻表示（どちらもタイムラインの時刻）。
映像は絶対値では比べず、**進み方**と**固まっていないか**の2つで見る
（「音だけ進んで絵が止まる」は絶対値の比較では原理的に見つからない）。

**px/秒 は尺を仮定せず標本から出す**（傾き＝再生ヘッドが1秒で動いた px）。
見込みと10%以上離れたら落とす。

#### 画面写真を見て、もう1つ見つけた

プレビュー左上に「**プレビュー最適化中… 62% → 84%**」。**測っている最中も裏で
プロキシを作り続けている。** 今日いちばん最初の通しで `再生してみる（3秒）` が
1回だけ落ちた（再生開始が2秒遅れた）のは、これが早い段階＝いちばん重い所に
当たったため、という説明が立つ。→ **進み具合を測定結果に出す**ようにした。
数字だけ見て「その日は重かった」と誤読しないための手掛かり。

### 横にスクロール 33.2 → **20.8ms（-37%）。犯人は「目盛りの置き場所」だった**（2026-08-07）

```
A 元のコード                    33.1 / 33.3ms   引っかかり 7回・2回
B 目盛りを一切更新しない（実験）  20.9 / 20.9ms   ← 到達しうる下限
C 目盛りを自分の所へ移した        25.1 / 29.0ms
D ＋半画面ぶん動いたら作り直す    **20.7 / 20.8 / 20.8ms・引っかかり 0〜1回**
```

**横に1px 送るたびに、タイムライン全体（全部の段・全部の帯）を描き直していた。**

引き金は目盛り。「見えている範囲だけ作る」設計は正しく、**間違っていたのは置き場所**
——作り直しの合図が長さの心臓（`useTimelineSpan`）に住んでいたので、囲いの値が変わって
見に行っている側が全員巻き添えになっていた（段にも帯にも `memo` は無い）。
→ `state/useRulerTicks` へ移し、目盛りを出す部品だけが呼ぶ形にした。

さらに `visibleTicks` は**前後1画面ぶん多めに**作り、位置は中身の座標なので送っても
一緒に動く。**毎コマ作り直しても絵は1ドットも変わらない**ので、半画面ぶん動いたら
作り直す形にした。**隣の `useVisibleRange` が既に同じ作法**で、片方だけ空いていた。

**副産物: 拡大・縮小も 16.7 / 20.8ms**（元 20.7〜29.2）。長さの囲いが `zoom` を
見なくなったので、寄るときの余計な描き直しも消えた。同じ根。

**危険に網を張り、網が効くことも確かめた**（`16-仕上げ-3-見え方` の「遠くまで送っても、
目盛りが尽きない」）。間引きを半画面→3画面に緩めると**1,038px の空白**を見つけて赤くなる。

#### **遠回りの本当の原因は、道具の分解能だった**

見立てを3つ外したが（下の表）、それより効いたのは**測り方を直したこと**。

```
往復1回（前）   24.9 / 29.1 / 29.2 / 33.3ms   幅 8.4ms ← 1〜2割の差は判定できない
往復5回（いま） 33.1 / 33.3ms                 幅 0.2ms
```

**同じアプリ・同じコード。** 「ばらつきが大きい項目」だと思っていたのは、
**標本が少なすぎたことによる見かけ**だった。緑が出ていた回があったのも、
低い方を引いていただけで、実力は最初から 33ms 台。
→ **1〜2割を追うなら、まず道具がその差を見分けられるかを確かめる。**

※ 所要時間が「その回が汚れたか」の目印になる（通常 8.9〜10.1秒。
  12.4秒の回だけ 37.5ms が出た＝外的な負荷）。**数字だけ見て慌てないこと。**

#### 潰した見立て3つ（同じ所をもう一度疑わないため）

| 疑った物 | 疑った理由 | 測った結果 |
|---|---|---|
| めじるし | 合成レイヤーの内訳で `div.marker-line ×17` が最多 | `--minus=marks` で **37.5ms**。効かない |
| `--tl-scroll` の書き直し | `onScroll` は横でも呼ばれる。継承する変数なので約9,000節を巻き込むはず | 同じ値なら書かない形にして **33.3 / 29.2ms**。変化なし → **戻した** |
| テロップの枚数 | 1200枚がいちばん多い | `--minus=telops` で **24.9ms**（下端）。ほぼ効かない |

**3つとも「中身を減らす」方向だった。** 効いたのは「**描き直す範囲を狭める**」方。
引き金が中身に依存しなかったので、3つが空振りしたのは振り返れば一貫していた。

### 犯人は「合成レイヤーの枚数」だった（2026-08-04）。**全項目で速くなった**

**同じ物差しで両方測った**（入口を固定したあとの bench。下の「測る条件」も読むこと）:

```
                        GPU描画（元）      CPU描画（いま）
クリップを掴んで動かす   154.3ms・154回  →  **70.7ms・69回**
タイムラインを拡大・縮小 162.4ms         →  **120.9ms**
再生ヘッドを掴む          66.7ms ✗       →  **緑**
全部選んでまとめて動かす   83.5ms ✗       →  **37.6ms**
再生してみる             316.7ms         →  **99.9ms**
合成レイヤー             **800枚**       →  **22枚**
合計                     13良好/5問題    →  **15良好/3問題**
```

**拡大は「CPU で描き直すぶん遅くなるのでは」と疑ったが、逆だった。**
レイヤーを組み直す方がずっと高い。**得だけ数えて損を見ない、をしないこと。**

### 測る条件は、項目が自分で決めること（08-04 に4か所直した）

bench は**項目どうしが状態を引き継ぐ**。前の項目が残した拡大率のまま測ると:

```
全体表示のまま掴む      帯が **2px**。押しても当たらず範囲選択が始まって選択が消える
                        → 「全部選んでまとめて動かせない」の正体（アプリは無罪）
拡大率が毎回違う        コードを変えていないのに 拡大・縮小 が 45.9 → 108.3ms
前の項目が寄せて終わる  アプリを1行も変えずに 再生が 104.2 → 25.1ms（**嘘の改善**）
```

**直した先の次が壊れる**ことも実際に起きた（拡大を固定したら、次の再生ヘッドが
全体表示のまま掴むことになって落ちた）。触る項目は自分で寄せてから始める。

**直したのは1行**（`components/WaveformCanvas` の `getContext('2d', { willReadFrequently: true })`）。

見つけ方の順（`npm run bench -- --cpu` / `--cpu-deep`）:

```
1 内訳を出す        レイアウト 849 / スタイル 392 / JS 2,759 / **その他 22,094ms（85%）**
                    → React でもレイアウトでも DOM の数でもない
2 標本器を切る      それでも 85%。**自分の測り方が作った数字ではない**
3 記録（トレース）  **Layerize 17.8秒（1回 79ms）** と名前で出た
4 レイヤーを数える  **800枚→赤 / 160枚→緑 が完全に一致**
5 正体を出す        **canvas ×427 ＋ 巻き添えの div.audio-clip ×347**
```

Chromium は **GPU で描く 2D canvas を1枚ずつ独立した合成レイヤーに載せる**。
波形は音声クリップの数だけあるので、カット600 で 800枚になっていた。
親も重なり順を保つために巻き添えで昇格する。

**次に大きいのは別の玉**（`GPUTask` / `CommandBuffer` 系 約11秒、`RunPaintLifecyclePhase` 7.6秒）。
レイヤーの話ではないので、また内訳から取り直すこと。

### 残り3件は**同じ根**。「引いたときに帯を1本ずつ作る」のをやめる話（08-04 に測定）

```
拡大・縮小  レイアウト 585 / スタイル 1,115 / JS 2,539 / **その他 7,087ms**
            合成レイヤー **23枚**（＝もう直っている）／ canvas 描き直し 741回（安い）
            **節の数 14,320**  ← 全項目でいちばん多い
            BeginMainFrame **1コマ 47ms**
```

**全体表示ではテロップ1200枚がすべて画面内**なので、1ノッチごとに約14,000個の
要素を描き直している。**レイヤーの話ではない**（23枚まで落ちている）ので、
波形を CPU 描画にしたのと同じ手は効かない。

**効くのは構造の変更**——引いた状態（帯が2〜3px 未満）では**1段まるごと1枚の絵**に
まとめ、寄せたら今の帯へ戻す。**テロップの段だけ入れた（2026-08-04）:**

```
                        入れる前    テロップだけ1枚の絵に
クリップを掴んで動かす   70.7ms ✗ →  **52.1ms △**
再生してみる             99.9ms ✗ →  **51.5ms △**
タイムラインを拡大・縮小 120.9ms ✗ →  108.5ms ✗（まだ赤）
結果                     15良好/3問題 → **15良好/1問題**
```

**選べなくはしていない。** プレビューと同じで、押した x → 時刻 → どの帯か を
**データ側で引く**（`components/timeline/TrackSummary` の `pickBandAt`）。
掴んで動かすのは寄せてから（2px の帯は元から実質掴めない）。

**拡大があまり下がらないのは、寄せる途中で 3px/秒 を跨いで DOM の帯に戻るから。**
20ノッチの後半は今までどおり1200個作っている。**残りはカット・効果音・画像・
めじるしの段**——同じ手を当てれば下がるはずだが、まだやっていない。

※ 入れた日に**検査の穴が1つ出た**。「テロップが全部読み込まれている」は数だけ見て
  いたので、**帯が1本も出ていなくても緑**だった（描けているかを何も見ていなかった）。
  帯とまとめた絵のどちらかが出ていることを判定に入れた。

**賞金を測った（`--minus=telops`。2026-08-04）:**

```
                    全部乗せ   テロップ1200枚を抜く
タイムラインを拡大   120.9ms →  **70.9ms**（-41%）
再生してみる          99.9ms →  **58.3ms**（-42%）
クリップを掴む        70.7ms →  **58.4ms**（-17%）
```

**テロップを丸ごと消しても 58〜71ms**（緑の線は 33ms）。つまり:

- テロップは**単独で最大の1本**（拡大と再生の約4割）
- だが**テロップだけ直しても緑にはならない**。残り6割はカット・効果音・画像・めじるし
- ＝ **全部の帯に同じ手を当てる**規模の話。1つずつやっても途中では ✗ のまま

**先に決めること（見た目と操作が変わる）:**

1. 引いた状態で、帯を**1本ずつ選べなくてよいか**（2px の帯は今も実質掴めない）
2. 選べるべきなら、絵に対する当たり判定（時刻→どの帯か）を別に持つ必要がある
3. 帯そのものは既に最小（細いときは文字も摘み手も出さない `div` 1個）なので、
   **削る脂肪は帯側には無い**——構造を変えるしかない

※ 帯を1本ずつ作るのをやめる前に、**この3つを決めてから**着手すること。

- [x] **拡大・縮小 / 掴む / 再生** → **済（2026-08-05）。負荷チェックが初の全緑。**

      **全部の段（テロップ・本編の映像・本編の音声・効果音）を1枚の絵にした。**

      ```
                     朝           テロップだけ    全部の段
      掴んで動かす    174.9ms ✗ →  52.1ms △  →  **緑**
      再生してみる    312.7ms ✗ →  51.5ms △  →  **緑**
      拡大・縮小      162.4ms ✗ → 108.5ms ✗  →  **緑**
      まとめて動かす  動かせない →  39.9ms △  →  **緑**
      結果            13良好/5問題 → 15良好/1問題 → **19 良好 / 0 問題**
      ```

      **音声段は波形を描かない。** 2px の帯の波形は形が読めないうえ、
      切片の数だけ canvas を作ることになる（そこが重い当人だった）。

      ※ 入れた日に検査が1つ落ちた——`クリップを掴んで動かす` が
        「掴める帯が画面に無い」。**仕様どおり**（引いた状態に帯は無い）なので、
        他の項目と同じく**入口で寄せる**ようにした。

~~全部選んでまとめて動かせない~~ **済（08-04）。アプリは無罪**——2px の帯を掴めて
いなかっただけ（上の「測る条件」）。いま 37.6ms。

~~元に戻したら映像も戻る：一致度 0.745~~ **済（08-04）。1.000。** 測る側が
違う時刻どうしを見くらべていた（`lib/benchView` の `seekAt` で時刻指定にした）。

---

※ ここから下は 2026-08-17 に `やること.md`（840行）から移した（同ファイルが 600行の決まりを超えたため）。

## 負荷（テレビ基準で出た本物）

### 通しは**一度も最後まで行けていなかった**（2026-08-08 に分けて解決）

夜通しで回して分かった。**唯一の赤は測る側のせいだった:**

```
**60分を過ぎました。ここで止まっています: （まだ何もしていない）**
✗ 60分ぶんの書き出しが完走する  完走しなかった
```

`watchdog(60, ...)` を限界さがしで使い切り、**書き出しに入る前にアプリごと落として**
いた。「完走しなかった」はアプリのせいではない。しかも `step()` が bench では一度も
呼ばれておらず、**「（まだ何もしていない）」という嘘**まで出ていた。まとめも時間の
内訳も印刷されずに死んでいる。

**書き出しは60分ぶんの映像を焼くので、そもそも60分の枠に収まらない**
——つまり構造的に一度も到達できなかった。

→ **3つに分けた**（本人の指定）。頭打ちも中身に合わせた（固定にすると同じ事が起きる）:

```
npm run bench          触ったときのもたつき  **実測 2.0分**（日常はこれ）／頭打ち 20分
npm run bench:limits   ＋限界さがし（19軸）                    ／頭打ち 110分
npm run bench:export   ＋書き出し（60分ぶんを丸ごと焼く）        ／頭打ち 200分
```

**夜通しの結果（頭打ちに当たるまで）:**

```
緑 37 / 要注意 2 / 赤 1（赤は上記＝測る側）
素の重さ 4.2ms（機械は空いていた＝この回の数字は信じられる）
掴んで動かす 12.6ms ✓ ／ 拡大・縮小 16.5ms・引っかかり 0回 ✓
20秒流して、ずれを見る: ずれ 1.1コマ・遅れ 0.1%・絵の固まり 0秒 ✓
横にスクロール 34.0ms △（線は33ms）／ 同時に1000枚 △（500枚までは平気）
```

### 【未解明】`全部選んでまとめて動かす` が同じコードで3倍振れる（2026-08-07）

```
20.8ms / 引っかかり  3回 / 8.3秒間   ← 20良好0問題の回
62.3ms / 引っかかり 84回 / 11.6秒間  ← 13良好5要注意2問題の回
```

**どちらも素の重さ 4.2ms**（＝機械は空いている）。`src` は触っていない。

### 08-08 に14回まわして追った。**容疑者3つとも潰れた**

**当てにいく道は3回続けて外した。** どれも**先に予測を書いてから測った**ので、
後から話を作った物ではない（外れた記録として残す価値がある）。

| 説 | どう潰したか |
|---|---|
| ~~裏のプロキシ作り~~（前の本命） | **`--wait-proxy` を作って A/B。** 焼き直しを完全に止めても、<br>直後の素の回との差は**全項目 4.2ms（＝ちょうど1コマ）ぶん**だけ。<br>素のまま最適化 53→62% を抱えた回が 20.9ms で通っている |
| ~~一時フォルダの掃除~~ | 「9.6GB 消した回が遅いはず」と書いてから測って**逆**。<br>掃除なしの回が 17良好/3、9.6GB 消した回が **20良好/0** |
| ~~冷え（間を空けた1回目）~~ | 5回ぶん綺麗に揃って見えたので、冷ます120秒＋暖機20秒で対照。<br>**暖機あり・なしのどちらも 20良好/0**。並びの偶然だった |

### 残った事実は1つだけ。**アプリの仕事は増えていない**

```
                    素の重さ   50回続けて切る      結果
良い回（12回）       4.2ms     111〜113ms        20良好/0
悪い回（ 2回）       4.2ms     **168〜171ms**    15〜17良好
```

`50回続けて切る` は**映像を一切触らない、純粋な JS と DOM**。それが 1.5倍に
なるので、**機械が出している能力が落ちている**方の話。全項目が揃って悪くなる
のもこれで説明が付く。

**素の重さが 14回とも 4.2ms を返したのは、道具の欠陥ではなく原理**
——4.2ms は 240Hz の**床**なので、何もしていないときの仕事は能力が半分でも
床に収まる。**あれで機械の能力は測れない。**

### 入れた道具（`e2e/lib/benchClock.mjs`）

| | |
|---|---|
| `周りを測る` / `周りを書く` | 項目ごとに**前後で挟む**。周りの重さ＋最適化%＋まとめ% |
| `物差し` | **決まった量の計算にかかる時間。**床が無いので能力に比例する |
| `焼き終わるまで待つ` ＋ `--wait-proxy` | 焼き直しを止めて測る口。プロキシ説を潰したのはこれ |

※ **`.proxy-badge` は2つある**（プレビュー最適化／素材ごとまとめ）。
  `querySelector` で先頭を取ると、最適化が終わった後は**まとめの数字が
  「最適化」の顔をして出る**。文言で分けてある。

※ **物差しがどこまで動くかも測ってある。** わざと計算を4本ぶつけると、項目は
  20良好 → 17良好/3要注意 まで悪化するのに、物差しは **+11%（24.0→26.7ms）**
  しか動かない。詰めた足し算は L1 に収まって1コアで完結するので**取り合いに
  強い**。つまりこれが見るのは「本スレッドが出せる素の速さ」であって、
  「機械全体が混んでいるか」ではない。

※ **記憶側の物差しは作って捨てた。** 16MB を素数の歩幅で歩く形にしたら、
  **1回の中で 16.6 → 104.4ms（6倍）に散った**（同じ回の計算側は 4%）。
  3回の中央値でも収まらない。理由は `benchClock.mjs` に書いてある。
  **作り直す前にそこを読むこと。**

### 次にやるなら

**物差しは、悪い回を1度も掴んでいない**（入れた後の12回が全部良い回だった）。
＝「効くはずの見張り」であって、**効くと確かめた物ではない**。

→ 次に悪い回が出たら、**まず物差しを見る**。24ms のままなら物差しも外れ、
  36ms 前後まで上がっていれば当たり。**どちらでも一歩進む**ので、
  悪い回を1つ捕まえるまでは新しい説を立てないこと（今日3回外した道）。
