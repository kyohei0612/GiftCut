// 通しで回す章の並び。**ここが唯一の一覧。**
//
// 読む側は2つある（`run.mjs` と `solo-audit.mjs`）。書き写すと、
// **章を1つ足した日に、片方だけが古い一覧を見続ける**——
// 「独立しているか調べた」と言いながら、新しい章だけ調べていない状態になる。
//
// ## 並び順は意味を持つ（いまのところ）
//
// 通しは1つの Electron の中でこの順に回す。**前の章が残した状態に
// 寄りかかっている章がある**（どれかは `npm run solo` で数える）。
// 並べ替えると偽の赤・偽の緑が出るので、勝手に動かさないこと。
//
// ここを独立させ切れたら、章ごとに別の窓で並列に回せる（13分 → 約4分の見込み）。

export const CHAPTERS = [
  '01-起動と復元',
  '02-タイムライン編集',
  '03-音と書き出し',
  '04-素材の配置',
  '05-カットと色',
  '06-書き出しの設定と表記',
  '07-保存とプロジェクト',
  '08-右パネル',
  '09-モーション',
  '09b-Premiere取り込み',
  '10-更新と素材整理',
  '11-編集の残り',
  '12-プレビュー',
  '13-属性ペーストと色',
  '14-字幕と音',
  '15-パネルと見た目',
  '16-仕上げ',
  '17-目と耳の確認'
]

/**
 * **他と同時に回してはいけない章。**
 *
 * 測っている物が「時間」なので、隣で別の Electron が CPU を使うと数字が狂う。
 * このリポジトリでは実際に、別のセッションが Electron を起動しただけで
 * **6項目が同時に赤**になったことがある（アプリには何も無かった）。
 *
 *   12-プレビュー      … 再生して**絵が止まらないか**（1コマの遅れを見る）
 *   17-目と耳の確認    … 画面を撮って画素を測る／書き出した**音**を測る
 *   03-音と書き出し    … 書き出した音の大きさと無音を測る
 *
 * **速さのために正しさを捨てない。** この3章は1つずつ回す。
 * 残り15章を並列にするだけで、下限は十分に下がる。
 */
export const SERIAL_ONLY = ['03-音と書き出し', '12-プレビュー', '17-目と耳の確認']

/**
 * 1章あたりの実測（秒）。**荷分けにだけ使う**（`npm run solo --fast` の実測・2026-08-05）。
 *
 * 正確である必要はない——**長い順に空いている担当へ配る**ので、
 * 多少ずれても偏りは自動で吸収される。知らない章は真ん中あたりとして扱う。
 */
export const COST_SEC = {
  '01-起動と復元': 3,
  '02-タイムライン編集': 90,
  '03-音と書き出し': 16,
  '04-素材の配置': 26,
  '05-カットと色': 22,
  '06-書き出しの設定と表記': 12,
  '07-保存とプロジェクト': 64,
  '08-右パネル': 11,
  '09-モーション': 118,
  '09b-Premiere取り込み': 15,
  '10-更新と素材整理': 21,
  '11-編集の残り': 75,
  '12-プレビュー': 82,
  '13-属性ペーストと色': 49,
  '14-字幕と音': 23,
  '15-パネルと見た目': 83,
  '16-仕上げ': 83,
  '17-目と耳の確認': 162
}
