// 起動時の配置・保存・プロジェクトの切り替え（**入口**）。
//
// 章: 1・3. 起動直後と素材の配置 / 2. 保存とプロジェクトの切り替え
//
// ## なぜ分かれているか（2026-08-05）
//
// 1本で 704行あった（決まり: 600超は500以下に割る）。章の境目と、
// 章2の中の話題の変わり目（保存まわり／持ち込む素材）で切った:
//
//   07a-起動と配置       起動直後の設定・素材を置く・お知らせ
//   07b-保存と下書き     下書き・「＊」・最近使った・更新で消えない置き場
//   07c-開き直しと素材   開き直し・SE・テンプレート・素材パック・赤いボタン
//
// ## **呼ぶ順は1つも変えていない**
//
// e2e には**手前の項目が残した状態に寄りかかっている確認**がある
// （選ばれている物・再生位置・拡大率・置いた素材）。並べ替えると、アプリは
// 正しいのに赤くなる／本当は壊れているのに緑になる。
//
// 章を名乗るのは 07a と 07b。**07c は名乗らない**——07b の続きとして並ぶ。
import startup from './07a-起動と配置.mjs'
import save from './07b-保存と下書き.mjs'
import reopen from './07c-開き直しと素材.mjs'

export default async function (C) {
  // ↓ 元のファイルの並びそのまま。**入れ替えない**（上の「呼ぶ順」を読むこと）
  await startup(C)
  await save(C)
  await reopen(C)
}
