// 画面を撮って測る／書き出した音を測る／画面の記録（**入口**）。
//
// 章: 目で見る確認（画面を撮って中身を測る） / 耳で聴く確認（書き出した音を測る） / 画面の記録
//
// ## なぜ分かれているか（2026-08-05）
//
// 1本で 669行あった（決まり: 600超は500以下に割る）。**頭のコメントが3つの章を
// 宣言している**＝話題が3つ、という一番はっきりした形だったので、そこで切った:
//
//   17a-触った見た目   数値欄を振る・横だけ拡大・2つ選んで拡大・動かした跡
//   17b-流したときの絵 画質ごと／カット多め／文字多め／つなぎ目のちらつき
//   17c-印と音         マウスの印・上書きの赤・耳で聴く確認・画面の記録
//
// ## **呼ぶ順は1つも変えていない**
//
// e2e には**手前の項目が残した状態に寄りかかっている確認**がある
// （選ばれている物・再生位置・拡大率・置いた素材）。並べ替えると、アプリは
// 正しいのに赤くなる／本当は壊れているのに緑になる。
//
// 章を名乗るのは 17a（目で見る）と 17c（耳で聴く・画面の記録）。
// **17b は名乗らない**——17a の続きとして並ぶ。
import look from './17a-触った見た目.mjs'
import play from './17b-流したときの絵.mjs'
import markAudio from './17c-印と音.mjs'

export default async function (C) {
  // ↓ 元のファイルの並びそのまま。**入れ替えない**（上の「呼ぶ順」を読むこと）
  await look(C)
  await play(C)
  await markAudio(C)
}
