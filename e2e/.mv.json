{"names": ["applyIconToCue", "trackForNewTelop", "addTelop", "updateCueText", "patchCueAnim", "applyTelopAnimSide", "nextCueAfter", "resolveTelopTransDrop", "applyTelopTransDrop", "selectTelopTrans", "updateTelopTransDur", "setTelopTransType", "deleteSelectedTelopTrans", "toggleTelopEmphasis", "alignTelop"], "out": "src/renderer/src/state/useTelopEdit.ts", "hook": "useTelopEdit", "deps": ["cueTrack", "telopLocked", "idCounter", "trackNum", "insertTrackOrdered", "motionLabel", "draggingTelopAnimRef", "setRightTab"], "depTypes": ["cueTrack: (c: import('../lib/srt').Cue) => string", "telopLocked: (c: import('../lib/srt').Cue) => boolean", "idCounter: React.MutableRefObject<number>", "/** 段の名前から番号を取る（V2 → 2） */", "trackNum: (id: string) => number", "/* eslint-disable @typescript-eslint/no-explicit-any */", "/** 段を正しい並びで足す */", "insertTrackOrdered: any", "motionLabel: any", "/** 出入りの演出を掴んでいる最中か */", "draggingTelopAnimRef: any", "setRightTab: any", "/* eslint-enable @typescript-eslint/no-explicit-any */"], "head": "// テロップを足す・書き換える・出入りの演出を付ける。\n//\n// ## 新しく足すときの段\n//\n// **本編（V1）には置かない。** そこは動画のカット列なので、テロップを置くと\n// 映像と取り合いになる。V2 から上を使い、埋まっていれば段を足す。\n//\n// ## 出入りの演出（アニメ）\n//\n// クリップの頭と尻に別々に付く。掴んで落とした位置で頭か尻かが決まるので、\n// 中央より手前なら頭、奥なら尻として扱う。", "imports": "import { defaultTelopStyle, sanitizeMotion } from '../lib/telopStyle'\nimport { DEFAULT_LABEL } from '../lib/labels'\nimport { useDoc } from './contentContext'\nimport { useSel } from './selectionContext'\nimport { useTracksCtx } from './tracksContext'\nimport { useToastCtx } from './toastContext'\nimport { useProjectStateCtx } from './projectStateContext'\nimport { usePlaybackCtx } from './playbackContext'\nimport { useExportCtx } from './exportContext'", "hearts": {"useDoc()": ["cues", "setCues"], "useSel()": ["selectedIds", "setSelectedIds", "selectedTelopTrans", "setSelectedTelopTrans", "setEditingId", "isSelected"], "useTracksCtx()": ["tracks", "setTracks", "trackStates"], "useToastCtx()": ["showToast"], "useProjectStateCtx()": ["newTelopStyle", "transDur"], "usePlaybackCtx()": ["currentTimeRef"], "useExportCtx()": ["ratio"]}}