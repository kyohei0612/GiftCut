# やること（プレテストで出た修正・追加）

2026-08-01 に洗い出した分。**リファクタリングが全部終わってから着手する。**

順番は付けていない（着手時に決める）。ひとまず消えないように控えるのが目的。

済んだ物は消さずに `[x]` にして、いつ・何をしたかを一行足す
（同じ話が出たときに「前に見送った理由」まで辿れるように）。

---

## プレビュー

- [ ] **拡大機能**を付ける
- [ ] **テロップのスタイルを D&D でテロップへ**持っていけるように
- [ ] **モーションのリセットがプレビューから消えている**
      → **調べた結果、消えてはいなかった**（2026-08-02）。履歴を `resetVideoZoom` と
        「リセット」で追ったが、消された跡は無い。**いまも出ている**——ただし
        **動画・画像・映像レイヤーを選んだときだけ**（プレビュー下の枠のバーに
        「リセット（◯個）」。中身は等倍へ戻す＋打った動きも消す）。
      → **本当に無いのはテロップ側。** テロップを選んでプレビューで動かしても、
        プレビューには戻す口が無い（モーションタブの各行の戻すボタンしかない）。
        足すなら「テロップを選んでいるときのバー」を新しく作る話になるので、
        **どういう見た目にするか決めてから**着手する
- [ ] モーションと、プレビューで手で動かしたときの**連動を確認**
- [x] 再生バーの下の UI が**2段**なのが気に入らない。まとめられる物はまとめて1段にする
      （そのぶんプレビュー画面が大きくなる）
      → 済（2026-08-01）。画質・fps・尺を見出し行へ出して1段に。実測で映像が 273→303px。
        経緯は `引き継ぎ-プレビューとタイムラインの大きさ.md`
- [x] テロップをダブルクリックすると文字を打てるが、**他をクリック・再生したときに閉じない**。
      Enter を押したときと同じ挙動で閉じる。
      **「どこか他を押した瞬間に完了」と同じ扱いにする**（押した先を問わない）。
      → 済（2026-08-02）。**素直に click を見張っても直らない**（タイムラインの
        クリップは pointerdown を自分で止めるので window まで届かない）。
        capture で拾い、押した場所が `.telop-editor` の中かどうかを自分で見る形にした
        （`state/useDismissOnOutside` に `inside` を追加。閉じ方は1か所のまま）。
        再生を始めたときは押した先が無いので、別に見張りを足した
- [x] **複数選んだテロップを、プレビュー上でまとめて操作できていない**（e2e が2件落ちていた）。
      → 済（2026-08-02）。**まとめ操作の側は最初から正しかった。**
        原因は手前の1行で、**テロップを Ctrl+クリックで足せていなかった**
        （何個押しても常に1個だけ選ばれる）。だから「選択中: 1」だった。
        `state/useTimelineDrag.ts` が `clearAll` を **`clearSegSel` という名前で
        受けていた**のが元。実際はテロップの選択も消すので、すぐ下の
        Ctrl+クリックが「同じバッチで空になった配列」に足していた。
        効果音・画像・映像レイヤーは同じ罠を `toggleSelect`（絶対値）で
        避けており、テロップだけ関数updater のまま取り残されていた。
        → 名前を `clearAll` に戻し、テロップも `toggleSelect` に揃えた。
        ※ **型検査にも `noDuplicate` にも掛からない型。**「名前の付け間違い」は
          いまも機械で止まらない（`engineering/CLAUDE.md` の「まだ検査が無い」）

## タイムライン

- [x] **Ctrl+ホイールでも拡大**できるように（限界まで拡大したいことがある）
      → 既にできていた（Alt でも可）。限界が足りていなかっただけ
- [x] 拡大バーの範囲を**いまの倍くらい**に広げる（テロップ設定などで広く使いたい）
      → 済（2026-08-01）。120 → 240。限界は `state/useView.ts` の ZOOM_MIN / ZOOM_MAX
- [ ] **レーンからレーンへ移動**したい
- [ ] テロップが下まで行ってから次を展開すると、**1からではなく途中から**表示される
- [ ] テロップ同士が隣り合っているとき、**間を調整すると両方が反応**する
- [ ] **ネスト**が欲しい。動画を複数選んで（Ctrl で複数選択）右クリック →「ネストする」。
      テロップなら1つのテロップとして、動画なら1つの動画として扱う。
      ※ **Ctrl で複数選ぶ方は既にできる。** 動画・効果音・画像・映像レイヤーは
        元から効いており、テロップだけ効いていなかったのを 2026-08-02 に直した
- [ ] 置いたときに上書きしない設定：**Shift を押しながら離すと、置いてあるクリップが
      上書きされず後ろへずれる**（いまは Ctrl+Shift でずれる）
- [ ] **吸着（マグネット）が弱い。範囲を広げる。**
      いまのしきい値は **8px**（`state/useSnap.ts` の `8 / zoomRef.current` が2か所）。
      画面px で持っているので、拡大しても「見た目の距離」は変わらない作り。
      当て先の選び方そのものは `shared/snap.ts`（画面なしで確かめられる）

## 効果音（SE）

- [ ] SE は音声トラックに入るのはよいが、**レーンまで固定しないで**ほしい
      （いまは一番上のレーンに固定）。判定は mp3 かどうかだけなので、置き場所は自由に
- [ ] 映像トラックにマウスがあるときだけ出る影は、そこにテロップ等が入っていたら1段ずらす

## モーション・キーフレーム

- [x] ~~**付けた動きが、保存して開き直すと消えている**~~ **不具合ではなかった**
      （2026-08-02 に確認）。保存する側は正しい。
      通しでは元々緑で、**絞って回したときだけ赤くなる確認**だった:
      ・その確認は動きを自分で付けず、前の項目が付けた物に寄りかかっていた
      ・自分で付けるようにしたら、今度は準備の最後で `.mo-head` を押していて、
        そこが「動きを消す」ボタンの当たり所だった＝打った印を自分で消していた
      → 確認を自分で完結するようにし、手を離すのは blur に変えた（押さない）。
      **「保存に動きが入っていない」という文言は、保存漏れにも準備の失敗にも
      同じように出る。** 赤を見たら、まず準備が成立しているかを見ること
- [ ] **キーフレームを付けていないとモーションをコピペできない**。関係なくコピーさせてほしい
- [ ] モーションを**1つずつコピー**したいときもある
- [ ] **アンカーを出すボタン**が欲しい（どこへ向かって拡大するのかを決められない）
- [ ] **キーフレームの削除**ができない。触っていることが分かるマウスの形を作って、
      テロップや動画に付いた印を消せるように

## 入力・操作まわり

- [x] XY などの**無限マウス調整で、触った瞬間に数字が飛ぶ**ことがある
      → 済（2026-08-02）。原因は**ポインタをロックした瞬間の1回**。ロックが効くと、
        その最初の1回だけ「元のカーソル位置から画面中央まで」の距離が movementX として
        まとめて届く（数百px）。**その1回だけ捨てる**ようにした。
        ※ 「大きい値を全部捨てる」やり方にしてはいけない。速く振った1回まで捨てて
          しまい、上限まで一気に持っていく操作ができなくなる（実際に確認が落ちた）。
        置き場は `components/ScrubNumber.tsx`。
        **戻すと本当に落ちることを確かめた確認**を2件足してある
- [ ] **クリックが多い。** 右パネルの要素をタイムラインへ入れるとき、クリックは据え置きで
      **D&D でも持ってこられる**ように（アプリ全体でこの方針）
- [x] テロップや動画に D&D で要素を入れたら、**それを選択状態にする**
      → 済（2026-08-02）。見本帳（テンプレート）・アイコン・出入りの動きの3つとも、
        落とした先を選んだ状態にする。落とした直後は微調整に入るので、選ばれて
        いないと右パネルが別の物を出したままで、もう一度クリックが要る

## 音

- [x] 調整していないなら **Windows の音量と揃える**
      → **既にそうなっていた**（2026-08-02 に確認）。既定は 1.0＝アプリ側で1ミリも
        絞らない＝Windows で聞こえる大きさそのまま。Windows 側の数値を読みに行くのは
        別の話（ネイティブが要る）なので、やっていない
- [x] **音量バーをアプリの中**に置く
      → 済（2026-08-02）。プレビューの見出し行に 🔊＋つまみ。**ミキサーのマスターと
        同じ値**なので片方を動かすと両方動く。🔊 で消す／消す前の大きさに戻す

## 右パネル

- [ ] テロップと同じで、**触っていない所は畳む**方式にする（お気に入りは除く）

## 段（トラック）

- [ ] **段の太さの「前の既定からの引っ越し」が効いていない。**
      前の既定（映像34/音声52）がそのまま保存されている人を、新しい既定
      （一番細い）へ移すつもりのコードが App.tsx にあったが、**誰も呼んでいない
      死んだコードだった**（読む場所が state/useLaneHeights へ移ったときに
      置き去りになった）。2026-08-02 の切り出しで消してある。
      いま復活させると、その太さに慣れた人の画面が黙って変わる。
      **やるなら「初期化」ボタンを足す方**が筋がよい。

## 書き出し

- [x] **同じ名前があっても上書きしない**（2026-08-02）。
      名前の後ろに `(1)` `(2)` … を付けて避ける。消えるのは「前に何分もかけて
      作った物」で、しかも**消えたことに気づくのは探しに行ったとき**なので、
      黙って上書きしてはいけない。決めるのは書き出しを始める直前
      （窓を開けてから押すまでの間に、同じ名前が増えていることがある）。
      作り方は `shared/exportDefaults` の `uniqueName`、使う所は `main/exportRun`。
      ※ 選択の窓を通ったときは避けない（その窓が既に「上書きしますか」を
        聞いており、そこで「はい」と答えた人の意思まで覆すと狙って上書きできない）

- [x] **書き出しが遅い。犯人は「テロップ1枚＝ffmpeg の入力1つ＋重ね1段」の作り。**
      → 済（2026-08-02）。**等間隔で全区間を刻んでいるテロップを連番1入力にまとめた。**
        重ねる段が1本で済むので、枚数が増えても重くならない。
        **実機で ffmpeg 6.1秒 → 1.2秒**（e2e の素材・テロップ121枚・同条件）。

        まとめられるのは等間隔の物だけ（頭と尻の演出だけのテロップは真ん中に
        長い静止区間があり、連番にすると尺が合わない）。**判定は
        「刻みの並びが本当に等間隔か」を見て決める**——`animBreakpoints` の中の
        条件を書き写すと、同じ規則が2か所になって片方だけ直したときに気づけない。

        置き場: 画面側が `telopSeqs` を組んで（`state/useExport.ts`）、
        `main/exportRun.ts` が `-framerate` 付きの連番入力1つ＋重ね1段にする。

        ※ **`MAX_TELOP_PNGS = 2000` の理由が変わった。** コマンドライン長の崖は
          消えたが、画面側が base64 で抱える量が残る。増やすなら、貯めずに
          1枚ずつ書き出す形へ変えてから。

      **測った数字（次に触るときはこれと比べる）**

      GPU は元から効いていた（この機械は NVENC が通る。QSV と AMF は一覧に
      あっても実際は失敗する＝「1枚焼いて確かめる」が正しく働いている）。
      1080p30・60秒を、重ねる枚数だけ変えて測った（枚数以外は同条件）:

      | 重ねる枚数 | GPU（NVENC） | CPU（x264 medium） |
      |---|---|---|
      | 0枚 | 5.1秒 | 6.1秒 |
      | 60枚 | 6.8秒 | — |
      | 200枚 | 10.7秒 | 12.9秒 |
      | 600枚 | **23.3秒** | — |

      **1枚あたり約 0.03秒** ずつ増える。焼く物を GPU→CPU に変えても
      1〜2秒しか動かない＝**エンコーダは犯人ではない。**

      **連番にしたあと**: 同じ600枚を「1本の連番入力＋重ね1段」で渡すと
      **5.2秒**（重ねない 5.1秒とほぼ同じ）。実機でも ffmpeg 6.1秒 → 1.2秒。

      ※ 内訳は書き出し完了時に出る（`テロップ◯枚の画像化 ◯秒 ／ ffmpeg ◯秒`）。
        **次に「遅い」と思ったら、まずこの行を見て切り分ける。**
        この機械の画像化は1枚 約16ms（121枚で1.3秒）

- [ ] **【要調査】プレビューでテロップをダブルクリックした状態が、書き出しにも
      乗っているかもしれない。**
      いまの作り上は乗らないはず（書き出しはテロップの中身から `renderCueToPng` で
      焼き直す経路で、画面の編集枠 `.telop-editor` は通らない）。
      **確かめ方**: ダブルクリックで編集を開いたまま Ctrl+M で書き出し、
      `npm run frames` で1コマを見る。乗っていれば絵に出る。
      ※ 上の「編集がクリックで閉じない」を先に直すと、この状態自体が作れなくなる

- [x] 書き出すと画像がデフォ位置に戻る（v0.1.9 で直した）。
      **拡大していないと X/Y が効かなかった。** 移動を「大きくした絵のどこを
      切り抜くか」で表していたので、等倍だと切り抜く余地がゼロ＝位置が 0 に
      丸められていた。動き（モーション）の位置キーも同じ理由で止まっていた。
      → 先に余白ごと台紙を広げてから切り出す形にした。動き側は足りるぶんだけ
        広げる（広げる必要が無ければ今までどおりの重さ）。
      → 判定は `shared/clipMotion` の `zoomPanChain` と `zoompanFilter`。
        画面（CSS の translate+scale）と同じ画素を指すか、試験で突き合わせてある。

## テロップ

- [x] **字間が全体的に広すぎる**。もっと狭くしてよい
      → 済（2026-08-02）。既定の字間を 0 → **-40**（1/1000em＝字の大きさの4%ぶん）。
        `lib/telopStyle.ts` の `defaultTelopStyle`。**保存済みのテロップは変わらない**
        （それぞれ自分の字間を持っている）ので、変わるのはこれから作る分だけ
- [x] 書き出した動画でテロップがチカチカした（v0.1.8 で直した）。
      表示窓の終わりを半フレーム詰めていたせいで窓と窓の間に隙間ができ、
      出力フレームがそこへ落ちるとテロップが消えていた。半開区間に変更。
      併せて、動きの刻みを 15fps 固定から書き出し fps に合わせた。
      → 判定は `shared/filterGraph` の `overlayEnableExpr` と、その試験にある。
      ※**書き出しは長くなる**（60fps なら画像は 15fps の4倍焼く）。
        遅すぎるという話が出たら、刻みに上限を付けるのが次の手。

## 更新（自動アップデート）

- [x] **更新のたびに再起動を促され、セットアップ画面まで出るのが面倒**（2026-08-02）。
      原因は2つあった:
      ・落とし終わると「5秒後に再起動します」と**こちらから迫っていた**
      ・当てるときに `quitAndInstall(false, …)` を呼んでおり、**NSIS が対話モードで
        立ち上がっていた**（置き場所を選ぶ画面まで毎回出る）
      → **こちらからは促さない**（`shared/updatePolicy` は必ず「次に閉じたとき」を返す）。
        当てるのは黙って走る（`quitAndInstall(true, true)`）。
        帯は「用意しました」と出るだけで、待てない人のために
        「今すぐ更新して再起動」を置いてある（押されたときだけ動く）。
      ※ 初回の導入は今までどおり置き場所を選べる（`oneClick: false` のまま）。
        **黙らせたのは更新のときだけ。**

## 測定・報告（製品版）

- [ ] **製品版に不具合報告の口が無い。** 開いている間ずっと測り続けて、
      「測定を書き出す」ボタンを邪魔にならない所に置く
- [ ] 画面が重いのか、**動かしていないのにぶるぶる震え続ける**ことがある
      （起動し直すと直る）。この手の映像側の症状も測れるようにする。
      ※**測る手段が無いので、これは上の「測定報告ボタン」待ち**（大きさの件で唯一残った物）
- [ ] 要するに、**測定報告ボタン1つであらゆる事象の状態を出せて、それを解析できる**形にする
