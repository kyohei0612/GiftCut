# やること

## **これからやること**（2026-08-16 現在）

**ここだけ見れば、次に何をするか分かる。** 下は全部「終わった話の記録」。

| | 何を | どこまで分かっているか |
|---|---|---|
| 1 | **プレビューのかくつき（再生中）の犯人を名指しする** | **記録に名札を入れた（08-16・`perf.measure`）。次は本人の実物で30秒録るだけ。** 30ms を超えた回だけ `⏱ 名前 123ms` で出る（設定の写し／下書きの自動保存／再生位置の控え／波形を描く）。**分かっていること**: 再生中58%の秒に 100〜190ms の主スレッド停止（平均146ms・止まらない秒は33〜50ms）。**潰した容疑者**: デコード（落コマ10分で28）／GPU／テロップの出現（前後0.6秒 81ms・54% vs それ以外 86ms・60%＝差が無い）／`bench --cpu-deep` は 19良好・合成レイヤー22枚で健全（**見本の形が違うので実物の犯人は出ない**——見本はソース1本・`<video>`2枚／実物は8本・17枚） |
| 2 | **帯の持ち手が細い**（掴みにくさの実体） | つなぎ目の演出の帯の端に付いている持ち手は CSS 7px だが、親の `overflow:hidden` で1px欠けて**実質6px**（`styles/overlays.css` の `.ttrans-resize`）。`shared/hitArea.test` の対象外なので機械も見ていない。**帯そのものが最小12px**なので、太くすると今度は「選ぶ」所が無くなる——**幅の下限と持ち手の幅は一緒に決めること**（`components/timeline/TransitionBands`） |
| 3 | **余白ゼロのつなぎ目を、画面の操作から作る e2e が無い** | 08-16 に「素材の頭が無くても掛かる」を入れたが、確認は **ffmpeg 単体の実測＋単体試験9件**（`timeline.test` 3・`exportSegments.test` 6）だけ。画面から作るには「素材ビンから末尾へ落とす」流れが要る（`e2e/lib/dropChip.mjs` と 04章の落とし方が使える） |

**これ以外に、開いている物は無い。**

### 済：決まりの逸脱を洗い出して潰した（08-17）

本人の指示で「いま決まりを破っている所」を機械が見ていない型まで探した。
**verify は全部緑のまま、本物の不具合が2件出た**（詳細は
`引き継ぎ-決まりの逸脱-2026-08-17.md`）。

```
本体2件   反転した映像を掴むと左右が真逆／つなぎ目の間だけ反転が外れる
          どちらも `canon.ts` の**借金に載っていた**。借金 3 → 0
測る側3件 音が測れなかった回が「抜け0回」に化ける（stutter）ほか2件
文書5件   版・件数・借金が腐っていた（CLAUDE.md の中で版が2つ食い違っていた）
行数1件   このファイルが 840行（600行の決まり超え）→ 491行
```

**いちばんの学びは「借金＝まだ直っていない不具合」。** 台帳には
「式の写しが2か所」としか書いていなかったが、実物は**写しであると同時に
本物の不具合**だった。写しを見つけたら、**中身が同じかどうかまで見ること。**

### 済：人物ごとの縁の色と、テロップの見た目（08-16・v0.1.43）

**人物＝ラベルの色**（アイコンの割り当てが元からその形）。アイコン設定の1行に、
アイコン・**縁の色**・**テロップの見た目**を並べた。

```
縁の色      入っていなければラベル色（今までの見え方）。「縁を戻す」で空へ戻せる
見た目      テンプレートを選ぶと、**その人物のテロップ全部にその場で当たる**。
            あとから足したぶんは「当てる」で追いつかせる
```

**勝手には当てない。** 選んだ時と押した時だけ。自動にすると、後から色を
付け替えた瞬間に、手で直した見た目が黙って上書きされる。

#### 触った所（次に似た物を足すときの地図）

```
入れ物   state/useProjectState（iconRing / iconTemplate）＋ App.tsx の初期読み
保存     useProjectFile（書く）/ useProjectApply（読む）/ useAutosaveMark（＊の見張り）
                                 ↑ **3つ揃えないと「設定したのに開き直したら消えた」**
縁の色   state/cueIconContext の `ringForCue` 1つを、**画面と書き出しの両方が通る**
         （PreviewLayers ／ useExport → lib/rasterize）。別々に決めると焼くまで気づけない
当てる   useTelopTemplate の `applyTemplateToLabel`（位置と大きさは残す）
画面     dialogs/SettingsDialogs の人物の行 ＋ styles/library.css
```

### 済：つなぎ目の演出とアイコンの大きさ（08-16・v0.1.42）

本人からの4件のうち3件。

```
長さが 2秒までしかない    上限 2秒 → **5秒**。スライダーと帯のドラッグが
                          `lib/transitions` の `TRANS_MAX_SEC` 1つを見る形にした
ケツに置くと黒になる      **後ろにクリップがあるなら、尻でも「間」**にする。
                          尻（transOut）は黒／白へ沈む演出で相手が要らないので、
                          途中のクリップの尻へ落とすと次があるのに黒へ落ちていた。
                          黒へ沈めたいのは最後か、後ろが空白のときだけ
1行を2行にすると          アイコンの高さを**行数で変えない**（1行のときの大きさで固定）。
アイコンが 1.64倍になる    前は `行の高さ × 行数 × 係数`（1行1.4／他1.15）
```

#### ついでに見つけた「画面と書き出しで別々の式」

アイコンの高さの式が **`TelopText.tsx`（画面）と `lib/rasterize.ts`（書き出し）の
2か所**に書かれていて、コメントで「TelopText.tsx と同式」と約束していただけだった。
`lib/telopLayout` の `autoIconHeight` 1つへ寄せた（計算・書き出し部の決まり
「画面と書き出しで別々の式を持たない」）。

**寄せる時に1つ踏みかけた**: 書き出し側は出力の画素で書くので `scale` が要る
（共通の式は 1080基準px を返す）。掛け忘れると **1080p だけ合って 720p で狂う**——
書き出すまで気づけない形だった。

### 見送り：書き出しの追加削り（preset p4→p1）は**やらない**（08-16・任された判断）

在庫は3つあったが、**いま取るべき物は無い**と判断した。

```
① preset p4 → p1     −19%（約10分）だが**絵が変わる**（PSNR 44dB・サイズ +8%）
② xfade 混じりでも segment を効かせる   設計仕事。**混在 feeder はデッドロックする**
③ 音の asplit=800                       境目のフレーム共有の解決が前提
```

**①を見送る理由**: 書き出しは既に **0.92倍実時間**（60分の素材が55.4分）で、
待ち時間が仕事を止める水準ではない。そこへ**出す絵の質を落として**10分買うのは
釣り合わない——p5→p4 のときは 66dB＝実質同一だったから迷わず取ったが、
44dB は質が違う。しかも +8% のファイルが付いてくるので、正味の得はもっと小さい。

**気が変わる条件**（そのときは①をそのまま取ればよい。準備は済んでいる）:
- 1本の書き出しを待つ時間が実際に仕事を止めるようになった
- 配信・アップロード側で再圧縮されるので元の質が意味を持たない、と分かった

**②③は日を改める**。どちらも「デッドロック解析をやり直す」が前提の設計仕事で、
中途半端に着手すると今日の3%停止と同じ形（**動くが止まる**）を作る。

### 済：**`16-仕上げ` の赤7件は機械の取り合いだった**（08-16）

通しで赤7件が出て、単独でも再現した。**が、静かな状態で回したら 29/29 全部緑。**
赤かった間は**本人がアプリを使っていて、私が同時にビルドと e2e を回していた**。

```
取り合いのある回  赤7件（1件目「空きが入ったまま書き出す」が240秒たっても終わらず、
                  以降どの項目も「窓が開いたまま」→ click が8秒切れ＝連鎖）
静かな回          **29/29**
```

**末尾の余白（`VIEW_TAIL`）は無罪と実測で確定済み**（0 にしても同じ7件が同じ理由で落ち、
逆に「遠くまで送っても目盛りが尽きない」が新たに落ちた＝300 が正しい）。
CLAUDE.md の「別セッションが Electron を起動しただけで6項目が同時に赤」と同じ型。
**測る e2e は、本人がアプリを触っていない時に回すこと。**

### 済：`全部選んでまとめて動かす` の3倍振れ（08-16 に因果が閉じた）

外因は **08-12 にゲーム同居と実測で確定**していて、残っていたのは「ゲームを閉じた
直後に回して 24ms 帯へ戻るのを見る」だけだった。**08-16 の bench がその回**:

```
物差し 24.3〜25.3ms（全項目）/ 19 良好 / 1 要注意 / 0 問題あり
全部選んでまとめて動かす  中央値 4.2ms / 95% 20.9ms ＝ 良好
```

**アプリは無罪で、外因はゲーム同居。因果が閉じた。**

### 済：記録の「絵の遅れ」が壊れていた（08-16）

報告の**いちばん上に「絵が再生ヘッドから遅れている（平均587ms・最大96秒）」という
嘘が出ていた**（1秒ごとの表の方は中央値24ms＝正常）。

`useSegClock` の待ちが真偽値だったのが原因。**カットで面を入れ替えると出ていった面は
`pause()` される**ので、その面に頼んだ `requestVideoFrameCallback` は二度と呼ばれず、
待ちが立ちっぱなしで**以後1回も頼み直さない**——記録の全行が同じコマ（実物で1.47秒）を
指し続けていた。→ 待ちを「**どの面に頼んだか**」で持つ形へ。

### 済：書き出しが3%で止まる（08-16・v0.1.38）

入力を枝へ配る `segment` の**境目を「次の窓の始まり」に置いていた**ので、編集で捨てた
隙間のコマが手前の枝へ落ち、誰も汲まずに詰まって全体が飢えた。**カット編集した実物には
隙間があるのが普通**なので、最初のカットで必ず止まる。境目を**手前の窓の終わり**へ移した
（隙間は次の枝へ落ち、次の `trim` が `start` まで汲んで捨てる）。詳細は `shared/filterLabels`。

#### **書き出しが止まったときの追い方**（これが今回いちばん効いた）

アプリを疑う前に、**控えからアプリ抜きで再現する**。控えは `%APPDATA%\GiftCut\`:

```
last-export-args.txt     引数（# の行はコメント）。**`-/filter_complex filter.txt` は
                         ffmpeg 7 の書式**（`-filter_complex_script` は削除済み。直すと逆に落ちる）
last-export-filter.txt   graph そのもの。先頭185行が # のコメント
```

1. `#` の行を捨てて `filter.txt` に、引数を1行1個で組み直す（**CRLF を落とすこと**）
2. テロップPNGは作業フォルダごと消えているので、**同じ大きさの透明PNGで代用**して構わない
   （見たいのは graph の構造。`ffmpeg -f lavfi -i color=...@0.0:s=1920x1080 -frames:v 1 t.png`）
3. 叩く。**止まるなら ffmpeg 側、通るならアプリ側**

今回の切り分けはこれで15分だった:

```
アプリで        3%で停止・出力0バイト・作業フォルダは片付け済み・エラーの控えも無い
外で同じ graph  time=00:00:15.44 で frame=906 のまま停止・**CPU だけ回る**＝空回り
                → 止まる時刻を窓と突き合わせて「最初のカット」と判明
segment→split   通る（＝配り方が犯人）／境目を手前の終わりへ 通る（＝速さを捨てずに直せる）
```

**「CPU を食っているのに出力が1バイトも増えない」は詰まりの顔。** 落ちているのでも
遅いのでもない。`ffmpeg` のプロセスが居るか・出力が伸びるかを30秒見れば区別できる。

### 済：つなぎ目の演出が「2秒に設定しても 0.2秒」だった（08-16）

報告は3つに見えたが**全部1つの原因**だった。

```
長さを変えられない        右パネル 2.00s ／ 帯 → 0.2s（次のクリップが 00:00.1〜）
めっちゃ早く切り替わる    上と同じ。効いていたのは 0.2秒
後ろに貼ったクリップに    「間トランジションが効きません」＝実効長 0
掛からない
```

**受け側は本来 `srcStart` より d 秒**手前**から流す**約束なので、素材の頭が無いと
物理的に足りない。だから実効長を「頭の余白（`srcStart/速度`）」で頭打ちにしていた
——**貼ったばかりのクリップは必ず `srcStart: 0`** なので、**後ろに置いた素材へは
構造的に一度も掛からなかった。**

→ **足りないぶんは最初のコマを止めて埋める**（`main/exportSegments` の `headPadOf`。
映像 `tpad=start_mode=clone` ／音は同じ長さの無音）。プロの編集ソフトも素材が
足りないときはコマを繰り返す。**アプリを通さず ffmpeg 単体で先に確かめた**:

```
3秒 + 3秒 に 2秒の重なり（余白ゼロ）→ 出力 **6.000秒・180コマ＝尺は不変**
溶け終わりの絵 = 次クリップの1コマ目（33.8dB）／カット直後 = その 0.10秒（38.6dB）
```

**尺が変わらないことが要**（変わるとテロップと効果音の時刻が全部ずれる）。

一緒に直した3つ:

- **プレビューも同じ絵にする**（`useVideoSync`）。止めるコマを流すと、要求位置が
  頭に張り付いたまま2本目だけ進み、ズレを直す度に引き戻される＝
  **先頭0.25秒の繰り返し**という、書き出しに無い絵になっていた
- **⚠「効きません」の帯と知らせを削除**。もう起きないので、残すと案内が嘘になる
- **帯の持ち手を左端へ**（係数 2 → −1）。帯は `[カット-d, カット)` なので**右端は
  カットに釘付け**で、引っぱっても持ち手が付いてこなかった（カット中心に
  描いていた時代の名残）。テロップ側は元から ±1 で正しかった

#### 遠回りしたので、その分も残す

**「プレビューの2本目が温まっていないから一瞬に見える」という見立ては外れ**だった。
測るために e2e を1件足して（`17b` に「再生中、つなぎ目のディゾルブは帯の長さぶん
"動いた絵"で溶ける」）、再生しながら毎コマ記録したら:

```
溶け 0.94秒 / 2本目が進んだ 0.90秒 / 動き出すまで 21〜53ms / 止まっていたコマ 13/223
```

**2本目はちゃんと回っていた。** ついでに分かったこと2つ:

- **最初の判定は甘くて素通りした。** `paused` は `play()` を呼んだ瞬間に false に
  なるので、立ち上げ待ちを1ミリも捕まえられない。「コマが実際に動き出すまで」へ変えた
- 既存の確認（09c）は**止めた状態で透け具合(opacity)だけ**を見ていた。opacity は
  CSS なので**2本目が止まっていても綺麗に 0→1 になる**＝この経路は無検査だった

**残っている穴**: 余白ゼロの状態を**画面の操作から作る** e2e が無い（素材ビンから
末尾へ落とす流れが要る）。いまの守りは ffmpeg 単体の実測＋単体試験9件
（`timeline.test` 3・`exportSegments.test` 6）。

### 済：08-08 に3つ閉じた（**3つとも前提の方が間違っていた**）

記録は下の各節に（横スクロールと限界さがしは、このすぐ下）:

| 閉じた物 | 何が違ったか |
|---|---|
| 限界さがしの軸ごとの時間 | 「5分超の軸がある」→ **最長 46.8秒**。まとめが嘘をついていた |
| 横にスクロールが線ぎわ | 「直した後も 34.0ms」→ **14回まわして一度も再現せず**（12.5〜20.8ms） |
| 拡大バーの縦の当たり判定 | 「24 にすると食い込む」→ **20 でも既に 2px 食い込んでいた**。下の節へ |

### 済：拡大バーの●を縦も 24px にした（08-08・本人の指定で決着）

```
前  バー 12px / 余白 上2px 下4px   判定 24 × 20 → 上下へ 4px はみ出す
                                   ＝ **上へ 2px、すでに段へ食い込んでいた**
後  バー 12px / 余白 上6px 下6px   判定 24 × 24 → **余白の中に収まる**
```

**境目は「はみ出すか」ではなく「はみ出した先に段があるか」だった。**
はみ出す分を余白として先に確保すれば、押しても段には当たらない。
それが「バーを高くする＝タイムラインを削る」の中身で、要ったのは **6px**。
見た目のバーは 12px のままで、変わるのは触り心地だけ。

検査は `src/shared/hitArea.test.ts`。高さの数字ではなく **「はみ出し ≦ 余白」**
を見る（冒頭の「大きさ ≦ 隣との間隔」と同じ考え方）。上しか見ていなかったので
下も足した。**両方ともわざと余白を戻すと赤くなる**ことを確認済み。

### 見張り：横にスクロールの 34.0ms は**再現しない**（08-08 に14回）

```
08-07 に直した記録（やること-済み-負荷.md）  **20.7 / 20.8 / 20.8ms・引っかかり 0〜1回**
08-08 の良い回12回                          12.5 / 16.8 / 16.8 / 20.6 / 20.7 / 20.7 /
                                            20.8 / 20.8 / 20.8 …（**線 33ms に一度も届かない**）
08-08 の悪い回 2回                          41.8 / 46.0
夜通しの回（34.0 の出どころ）                **1回だけ**
```

**「直した後も 34.0ms」は、夜通しの1回だけを見て書かれていた。**
直した記録の方（20.8ms）と食い違っていて、**片方だけが「これから」に残っていた**
——同じ話を2か所に書くと片方が古くなる、といういつもの型。

※ **【未解明】の振れ（`全部選んでまとめて動かす` の3倍）のせいだとは言い切れない。**
  その回は `掴んで動かす 12.6ms`
  `拡大・縮小 16.5ms` で、**08-08 のどの良い回よりも良かった**。機械は健全なのに
  その項目だけ跳ねている。一様な悪化ではないので、別の何かではある。

→ **再現しない物は追えない**ので、ここは見張りに落とす。次に 33ms を割ったら、
  そのときは**内訳から取り直す**（`npm run bench -- --cpu`）。08-07 の遠回りの
  原因は**道具の分解能**で、往復1回では 24.9〜33.3ms に散って1〜2割を判定できな
  かった（5往復で収束した）。**1〜2割を追うなら、まず道具がその差を見分けられるか
  から。**

### 済：限界さがしの軸ごとの時間（08-08）。**「5分超の軸がある」は嘘だった**

`findLimits` に軸ごとの区切りを入れて測った（作り直さず本体の `時計を作る` を借り、
題名だけ受け取れるようにした）。

```
19軸ぜんぶで 8.9分。**いちばん長い軸で 46.8秒**（同時に見えている数）
いちばん短い軸で 19.0秒。**5分どころか1分を超える軸が1つも無い**
bench:limits 全体で 11.5分 / 38良好 1要注意 0問題
```

要注意の1件は既知（`同時に見えている数` が 1000枚同時で 95% 58.3ms）。
**残り18軸は傾きがほぼゼロ**で、前の測定と同じ。

**なぜ「5分超」と思っていたのか——まとめが嘘をついていた。**
`区切る('限界さがし（19軸）')` が **`if (DO_EXPORT)` の中**にあったので:

```
bench:limits   **印が一度も打たれず**、8.9分が「最後の確認 82%」に化ける ← これを見た
通し（両方オン） 正しく出る                    ← **通しを見ている限り気づけない**
```

**限界さがしのための口で、限界さがしの時間だけが迷子になる**形だった。
印を `findLimits` の直後へ出して直した。

※ 軸ごとの時間は**「長い＝重い」ではない。** 打ち切った軸は途中で `break` して
  短く終わるので、**最後まで試し切った軸ほど長くなる。** 各行（どこで崩れたか）と
  突き合わせて読むこと。コードにも同じ注意を書いてある。

---

## 読み方（このファイルの構成）

**上が「これから」、下は全部「終わった記録」。** 済んだ話は消さずに残してある
——「なぜそうなっているか」を調べた跡が入っていて、**同じ物を作り直す前に
grep する所**だから。

| ファイル | 中身 |
|---|---|
| **このファイル** | これからやること／依頼の記録 |
| `やること-済み-負荷.md` | **負荷の調査**（同期・横スクロール・合成レイヤー・測る条件・3倍振れ） |
| `やること-済み-機能.md` | プレビュー・タイムライン・テロップ・SE・モーション・音・波形・素材ビン・段 |
| `やること-済み-書き出しと重さ.md` | 書き出し・テロップ・更新・重さ・吸着・ズレ・スクショ・測定 |
| `やること-済み-検査と通し.md` | リファクタの経緯・通しで落ちた件の決着 |
| `引き継ぎ-書き出しを速くした.md` | **08-09〜10 の高速化**（104.6分 → 55.4分。段階1・2と内訳の取り方） |
| `引き継ぎ-心臓の分け直し.md` | リファクタ（**リファクタの決まりもこちら**へ移した） |

**2026-08-17 に 840行 → 491行へ割った**（600行の決まりを超えていた）。移した先は
上の表の下3つ。**話題ごとに、既にその話題を持つファイルへ寄せた**——新しい
入れ物を作ると、次に探す人が「どちらに書いてあるか」を毎回考えることになる。

※ 切るときは**行の範囲で切って、並べ替えて突き合わせ**、1行も欠けていないことを
  確かめること。`やること.md` は前に Python で置換して**0バイトにした**ことがある
  （CLAUDE.md 9番）ので、**必ず控えを取ってから触る**。

---

## 依頼（2026-08-07・本人）

- [x] **まっさらなタイムラインへ置いた物は 0秒から**（プレミアと同じ）。
      本人の言葉:「いっちゃん最初の時で、なにもタイムラインにない状態の時を指す。
      タイムラインにあるやつをすべて消したらその状態になるし、プロジェクト開始のときも
      そのルールなら、何かしら落としたら 0秒から配置されるってだけ」
      → 判定と規則は **`src/shared/emptyTimeline.ts`** に1つ（`timelineIsEmpty` / `placeStartSec`）。
        置く道**4つ全部**を通した: 落とす（`placeDropped`）／端で受ける（同じ道）／
        ビンのダブルクリック（`addMediaAtPlayhead`）／♪＋ の BGM（`addBgm`）。
        **影（落とす前の目安）も同じ規則で出す**——ここだけ落とした位置のままだと、
        「見えていた所と違う所へ入った」になる。
      → **既にあった `hasProjectContent` を作り直した。** 8項目のうち**5項目が同じ**
        だったので、帯の5種類はあちらから引く形にした（種類が増えた日に片方だけ直る
        のを防ぐ。このリポジトリで4回起きた型）。
        ※ 混ぜてはいない——あちらは**素材ビンも数える**（保存が要るかの判断）。
          こちらは**タイムラインの上だけ**。同じにすると、素材を読み込んだ時点で
          「空ではない」ことになり、規則が効かなくなる
      → **空のときの案内も出した**（本人・同日「プレミアプロみたくここにドラッグ＆
        ドロップで開始的な文言」）。`.tl-empty`（timeline.css）。
        **見た目は作らず、プレビューの `ScreenEmpty` を借りた**——同じ形の案内が
        2つになると、片方の言い回しだけが変わっていく。あちらは文言を受け取る形にした。
        ※ `pointer-events: none` は必須。案内が落とす操作を横取りすると
          「書いてあるとおりにやったのに何も起きない」になる。e2e で当たり判定を見ている。
        ※ **最初 `z-index: 0` で書いて、在るのに見えなかった。**
          後ろに来る `.track-inner`（position: relative）が上に描かれる。
          数を上げて直したが、**画面を撮るまで気づけない型**だった
      → **1本目を置くときは、影（置き先の予告）を出さない**（本人・同日
        「どうせどこ置いても0秒開始やから影消していい。0から1置くときだけ」）。
        **一度は影を 0秒へ出す形で作ったが、それは捨てた**——カーソルから離れた所で
        影だけが動き、何が起きているのか読めない。指しても指した所には入らないので、
        指さないのが正しい。代わりに案内が先に「0秒から並びます」と言っている。
        ※ 影を出す側の確認（外へ出ても消えない・置いたら消える）は**非空で動く**ので
          そのまま生きている。空のときだけの分岐
      → 網は `src/shared/emptyTimeline.test.ts`（種類ごとに1つずつ）と、
        e2e `04-素材の配置` の「まっさらなタイムラインへ落とすと〜」。
        **右の方（幅の6割）へ落とす**こと。左端に落として 0秒になっても、
        規則が効いたのか元から 0秒だったのか区別が付かない

## 依頼（2026-08-03 夜・本人）

- [x] **貼り付け・複製したテロップが重なっても、上書きされない**（秘書エージェントの検証で発見。
      **2026-08-03 に再現してから直した**）。
      → 呼び方を `state/cueOverwrite` に1つ寄せて、**掴む・貼り付け・複製の3つ**が
        同じ所を通るようにした（3か所目になったので、書き写す前に寄せた）。
      → e2e 2件追加。**どちらも直しを外すと本当に赤くなる**ことを確認済み。
      ※ **複製の方が見立てより危なかった。** 秘書は「選択が複数で長さがまちまちだと」と
        条件付きで言ったが、実際は `start: c.end`＝**自分の直後**に置くので、
        同じ段に次のテロップが並んでいれば**1つ選んだだけで必ず食い込む**。
      ※ **確認を3回書き直した。** 1回目は緑だったが**何も試していなかった**
        （プロジェクトの1つ目と2つ目が離れていて、複製が届かない）。
        経緯と教訓は検査部の `CLAUDE.md` の 4e。

- [x] （元の記録）**貼り付け・複製したテロップが重なっても、上書きされない**
      同じ段で重なったときの正典は `shared/overwrite` の `overwriteCues` だが、
      **呼んでいるのは `useTimelineDrag` の2か所だけ**（落として重ねる／端を伸ばす）。
      通っていないのは:
      ・`state/useCopyPaste.ts:251` 貼り付け … `[...prev, ...pasted].sort()` で並べるだけ
      ・`state/useTimelineEdit.ts:300` 複製 … `start: c.end` に置くので普通は隣接だが、
        **選択が複数で長さがまちまちだと後続に食い込む**
      → **まだ再現していない。** コードを読んで「呼んでいない」ことを確かめただけで、
        アプリでも e2e でも症状を見ていない。**まず手で再現すること**（今日、
        再現せずに「不具合」と記録して丸1日むだにした前例がある）。
      → 直すなら、包み役 `overwriteOverlappedCues` が `useTimelineDrag` の中にあるので
        **置き場所を先に決める**（掴む処理の中にあるが、貼り付け・複製は掴んでいない）。
      ※ 2026-08-03 に⑥（端を伸ばす）を直したとき、**この2つは視野に入っていなかった**。
        `やること.md` の 08-02 の記録にも一言も出てこない。

- [x] **★を押したときに、プロジェクト由来の物まで一緒に保存される**（⑦の残り）
      → **直した（2026-08-04）。**
      → **見立てと違う直し方になった。** ここには「アプリ側の物とプロジェクト由来を
        **別々に持つ**必要があり、整理の心臓（`useLibraryOrganize`）まで手が入る」と
        書いてあったが、**その道は採らなかった**。代わりに**保存する物を変えた**:

        ```
        前： saveFavorites(画面に出ている一覧)   ← プロジェクト由来が混ざっている
        後： persistFav(押した1件, on/off)       ← 保存済みへ、その1件だけを当てる
        ```

        **画面の一覧を渡さないので、混ざりようがない。** 別々に持つ必要も、
        心臓を触る必要も無くなった。読むのは毎回 localStorage で画面の状態は見ない。
        置き場は `lib/telopTemplates`（`persistFav` / `persistCat` / `persistCustomCat` /
        `persistDropCat` / `persistUserTemplateAdd` / `persistUserTemplateRemove`）。
      ※ **見送りに書いた「手が入る範囲」は、当てにならないことがある。**
        測ったのではなく見立てで書いた物だったので、実際に触ったら別の道があった。
      ※ **直したあと grep で数えて1件見つけた。** `useLabelsPresets` に
        **同じことをする2つ目の道**があった（`saveCurrentAsTemplate` と `savePreset` が
        どちらもテンプレを足す）。直した所だけ見ていたら、**こちらから足したときだけ
        焼き付く**状態が残っていた。今日ずっと出ている型（片方だけ直す）がまた出た。
      → 検査は `lib/telopTemplates.test.ts`（9件）。**本命は②の
        「`lib/telopTemplates.ts` の外から丸ごと保存する関数を呼んでいない」**
        ——①（persist* の振る舞い）が通っていても、1か所丸ごと書けば元に戻るため。
        **わざと丸ごと保存へ戻すと赤くなる**ことを確認済み。
      → `--only=テンプレ,見本帳,お気に入り` 11件緑。

- [x] **心臓（context）の受け口が `any` だった**（秘書エージェントの検証で発見した3件目。
      **2026-08-03 に潰した**）。指摘は `draggingTemplateRef` / `draggingEmphasisRef` の2つ
      だったが、実物を見たら**単発ではなく341件**（6ファイルの受け口がほぼ全部 `any`）。
      → **手で書き直さず、詰めている実体から引く形にした**（`state/wiredValue.ts` の
        `Wired<'キー'>`）。手で書いた型は1件も無い。**363件が本物の型になり、`any` は 0。**
      ※ **測ってから決めた。** 注釈を外すと本当に型が付くかを compiler API で数えたら
        84件中83件。「たぶん付く」で341件書き換え始めていたら引き返せなかった。
      ※ **本物を1件見つけた**——`TimelineArea.tsx` が手で書いた `setTracks` の注釈から
        `kind` が抜けていた（`{ id, name }` と書き直した瞬間に段の種類が黙って消える形）。
      → 検査は `src/shared/ctxTypes.test.ts`（3件。**わざと破って赤くなるのも確かめた**）。
        経緯・危なかった所は `引き継ぎ-心臓の分け直し.md` の「6」

- [x] **同じ穴が一段深い所（フックの `deps`）にもある** → **潰した（2026-08-04）。293 → 19件。**
      残る19件は**全部「正しい `any`」**（ディスクから読んだJSONを解く所）。
      → **context と同じ手は使えなかった**（`useAppWiring` が呼ぶので輪になる）。
        代わりに使えたのは「**deps は呼ぶ側が実物を渡すので、手で書いてもズレたら
        呼び出し側で落ちる**」という性質。型は compiler API で実引数を読んで写した。
      → **本物を2件見つけた。どちらも同じ型**——「setState の updater に手で書いた
        注釈が実体と違う」（`TimelineArea` の `setTracks` から `kind` が抜けている／
        `usePlaybackEngine` の面が `string` になっている）。**受け側が `any` である
        限り永久に見つからない**型。両方とも注釈を外して推論させた。
      → **同じ形を書き写さない**を7つ（`Ask` `TemplatePickerState` `RightTab`
        `MediaMeta` `SegLayout` `useEdit` の返り値 `Snap`）。
        途中で `noDuplicate` が**私を捕まえた**（12行そっくり）。
      → 経緯は `引き継ぎ-心臓の分け直し.md` の「6」。残数は `npm run measure`
      （元の記録）

- [x] （元）**同じ穴が一段深い所（フックの `deps`）にもある**（2026-08-03 に測った 221件）
      → **済（2026-08-04）。221件 → 14件。残りは全部「正しい `any`」。**

      減った大半はリファクタの副産物（受け口を実体から引く形にしたぶん）。
      最後に残っていた**本物**は `applyLayout` で、**根が1か所**だった:

      ```
      useAppLayout.ts が (l: any)  ← 根
        └ useProjectApply / useProjectTemplates が「借りている側も any だから」と any
      ```

      根を `unknown` にしたら3か所とも消えた。中身は1つずつ `typeof` で確かめてから
      使っているので `unknown` で足りる——**形が保証されないことと「何でも触れる」ことは別。**

      **残る14件はすべて「ディスクから読んだ物」**（`data: any` と、その中を歩く
      `map`/`filter`）。ここは `any` が正しい（形が保証されない入口）。

      ※ 元の実害だった `previewUrl` は**もう直っている**
        （`usePreviewFrame.ts:42` に `(path: string, orig: string) => string`）。
      → **数はここに書かない。`npm run measure` が毎回出す**（多い順・上位8本）。
        前は「221件・useProjectFile 32…」と書いていたが、**同じ物を2か所に持つと
        片方が必ず古くなる**（今日それで2回書き直した）。
        ※ measure は `as any` や `Record<string, any>` も数えるので
          `grep -c ": any"` より多く出る。**両方の数字を突き合わせないこと。**

- [x] **「部長秘書」エージェントを作る**（**段階0〜3を 2026-08-03 に作った**）。
      目次（`npm run index`）／正典台帳（`src/shared/canon.ts`）／
      エージェント本体（`.claude/agents/secretary.md`）／見送りの鮮度。
      ※ 前回「次のセッションからは `secretary` で直接呼べるはず（まだ確かめていない）」と
        書いた件は、**このセッションで一覧に `secretary` が載っているのを確認した**
        （＝登録はされている）。**呼び出してはいない**ので、そこはまだ未確認。
      計画・検証・段階4（測り直しの口。未着手）は `引き継ぎ-秘書エージェント計画.md`。
      （元の依頼は下）
      狙い: **どのファイルに何があるかを把握していて、聞けば答えてくれる**係。
      「これは初めての実装です」「その機能は◯◯.ts のここにあるので、そこを直せばよい」
      と即答でき、**更新のたびに覚え直す**。
      → **今日の作業がそのまま根拠になっている。** 6件のうち3件が「既にあるのに
        効いていない」型で、毎回 grep で探し当てるのに時間を使った
        （`placeDropped` は実装済み／`overwriteCues` は移動時だけ／
        `EMPTY_DRAG_IMG` は素材ビンだけ）。**二重実装を防ぐのが本命。**
      → 計画のときに一緒に考えること: いまのリポジトリの穴（`.company/` が
        .gitignore で travel しない／取説は1ファイル内しか照合していない／
        「同じ物が2か所」は式の一致でしか見ていない）。


2026-08-01 に洗い出した分。**リファクタリングが全部終わってから着手する。**

順番は付けていない（着手時に決める）。ひとまず消えないように控えるのが目的。

済んだ物は消さずに `[x]` にして、いつ・何をしたかを一行足す
（同じ話が出たときに「前に見送った理由」まで辿れるように）。
