# GiftCut

切り抜き動画に特化したテロップ編集アプリ。Electron + React + TypeScript + FFmpeg。

**このファイルは AI が毎回自動で読む。** 長くしないこと（長いと読み飛ばされる）。
詳しい話は各リンク先にある。

---

## 最初に：組織がある

このプロジェクトは `.company/` に**秘書室と開発部**を持っている。
`.company/` は自動では読まれないので、**この2行だけ覚えて入口を踏むこと**:

| いつ | 何を読む |
|---|---|
| コードを触る前 | 下の表から**担当部門の CLAUDE.md** |
| 相談・TODO・記録・雑談のとき | `.company/secretary/CLAUDE.md`（秘書室） |

どちらか1つを読めば `.company/CLAUDE.md`（組織のルール）も一緒に付いてくる。

**オーナーが「秘書」と言ったら秘書室のこと。** `/company` でも入れるが、
打ち忘れても抜けないようにここへ書いてある。

### 作業の終わりに必ず確かめること

**`.company/` は .gitignore されている**（clone しても付いてこない）。
コードと一緒に残すべき記録は**リポジトリ側**に置くこと:

- **なぜそう作ったか** → そのコードの真上のコメント
- 何をどう変えたか・測った数字 → `引き継ぎ-*.md`
- これからやること → `やること.md`

2026-07-31〜08-02 の作業は `.company/` に1行も残らなかった（秘書のメモは 07-29 で
止まっていた）。**実害が無かったのは、travel すべき物が全部リポジトリ側にあったから。**

---

## 書く前に：**もうあるか**を見る

**`npm run index`**（数百ms）。496ファイルの「パス＋そのファイルが自分で名乗っている1行」が出る。

2026-08-03 に直した11件のうち**5件が「正典・仕組みはあるのに、その道を通っていなかった」型**
だった（`overwriteCues` は移動だけ／`EMPTY_DRAG_IMG` は素材ビンだけ／全体表示の率が3か所バラバラ…）。
**どれも探せば見つかった。** 目次はそのために作ってある。

```
掴んだときカーソルの絵を消すには？  → lib/dragChip.ts  「カーソルへ何を握らせるか」
テロップが重なったら？              → shared/overwrite.ts「置いた物を勝たせる」
素材を落とす所は？                  → state/useMediaDrop.ts
```

**git に入れていない**（呼ぶたびに作る＝腐らない）。中身は各ファイルの冒頭コメントの
1行目そのままで、書き起こしではない。詳しくはそのファイルを開くこと。
計画と、この先の段階は `引き継ぎ-秘書エージェント計画.md`。

---

## 触る前に：担当を決める

**触ったファイルのパスで、持ち主が一意に決まる。**「直すところが違う」を構造で消すため。

| パス | 部門 | 決まり |
|---|---|---|
| `src/renderer/src/state/**` | 心臓・配線部 | `.company/engineering/departments/state/CLAUDE.md` |
| `src/renderer/src/components/**` | 画面部 | `.company/engineering/departments/ui/CLAUDE.md` |
| `src/renderer/src/lib/**`<br>`src/shared/**`<br>`src/main/**` `src/preload/**` | 計算・書き出し部 | `.company/engineering/departments/core/CLAUDE.md` |
| `e2e/**` `scripts/**` `**/*.test.ts` | 検査部 | `.company/engineering/departments/qa/CLAUDE.md` |

**持ち場を持たない部門が1つある。** 割る・整理する・重複を消す指示は**開発部ではなく
こちらが出す**（パスで分けると、部門をまたぐ整理が誰の物にもならないため）:

| | |
|---|---|
| リファクタリング部 | `.company/engineering/departments/refactor/CLAUDE.md` |

**目的は行数ではない。** 不具合が来たとき・要件が増えたときに**的確に直せる／足せる**こと。
測り方は「①場所が一発で分かる ②**直し漏れが出ない** ③足す場所が分かる」。
08-03 の不具合8件は**全部②**（片方だけ直した型）。だから**②が最優先**。

**取説（`// ## 中身`）が書けない形が1つある。** ファイル全体が1つの関数で
包まれていると、並ぶ名前が1〜2個になって「これしか無い」という嘘の案内になる。
そのときは**先に割る**（`exportRun` が実例。1,229行中1,012行が1コールバックだった）。

**2つ以上にまたがる作業は、着手前にどちらへ置くか決める**
（またがったまま進めると、両方に半分ずつ書かれて二重実装になる）。

※ `.company/` は .gitignore なので、clone しただけでは無い。無ければこのファイルの決まりだけで進めてよい。

---

## **フックを1本足すとき、`useAppWiring` には書かない**

**囲い（`state/*Context.tsx`）を作って、使う側に自分で見に行かせる。**

配線へ書き足すと必ず元に戻る。同じ事故を2回やっている:

```
App.tsx        11,404 → 200行（2026-07-31〜08-02）
useAppWiring    1,229 → 362行（2026-08-04）
```

どちらも「フックを呼んで名前を配る」が積み上がっただけで、**1回ずつでは
誰も気づけない大きさ**でしか増えない。剥がすのに丸1日かかっている。

- 足した直後に **`npm run passthrough`**。「いま囲いへ上げられるフック」に
  **自分が出てきたら、書く場所を間違えた合図**
- 囲いの雛形は `node scripts/mkctx.mjs <フック名>`（引数を取らない葉の場合）
- 見張りは `state/wiringSize.test.ts`。**赤くなったら上限を上げない。囲いを作る**

---

## 決まりには検査が付いている

**書いただけの決まりは守られない**ので、機械で止めている。破ると `npm run verify` が赤くなる。

| 止めている失敗 | 検査 |
|---|---|
| ファイルが気づいたら1万行 | `src/shared/fileSize.test.ts`（上限1,250行＋借金の据え置き） |
| **上限に当たってから慌てる** | 同上。**800行を超えたら名前を出して知らせる**（止めない）。<br>1,200 だと**残り50行で知らされる**ので、気づいた時にはもう詰んでいる<br>——`bench.mjs` は警告から上限超えまで**1回の編集で飛び越えた** |
| **割った拍子に、検査が黙って死ぬ** | `e2e/lint-checks.mjs`。あれは `run.mjs` の宣言を正規表現で拾うので、<br>道具を `lib/` へ出すと**分割代入になって1つも拾えず、書き忘れがあっても緑**になる。<br>2026-08-04 に実際そうなった（分割代入も拾うよう直した）。<br>**割ったら、その検査がまだ効いているかを確かめること** |
| **配線（`useAppWiring`）がまた太る** | `state/wiringSize.test.ts`。**自分で呼ぶフック12本・450行が上限。**<br>同じ事故を2回やっている（App.tsx 11,404 / useAppWiring 1,229） |
| **返済したのに免除の名簿に残る** | `readability.test.ts`。名簿は載せた日しか更新されないので<br>**減った側は誰も直さない**。08-04 に実際に2件残っていた |
| **説明が指しているファイルが実在しない** | 同上。**案内が嘘だと、読む人はその先を探しに行って時間を落とす**。<br>入れた初日に本物を1件見つけた（消えた `lib/people.ts` を指していた） |
| 同じ物を別の場所に二重に書く | `src/shared/noDuplicate.test.ts`（shared の作り直し／12行そっくり） |
| **500行超なのに取説が無い** | `src/shared/readability.test.ts`。**500 を超えると AI は通しで読まず<br>grep に切り替わる**ので、割るか「`// ## 中身`」を付けるかを迫る |
| **取説が腐る**（引っ越したのに説明が残る） | 同上。並べた名前と top-level の実体を照合する。<br>実例: `telopAnim.ts` に本体の無い説明が残っていた（誰も気づかなかった） |
| 冒頭コメントが無いファイルが増える | 同上。**コメントが無いファイルほど話題が多い**（実測） |
| **心臓の受け口が `any` で開く** | `src/shared/ctxTypes.test.ts`。区画は心臓を自分で見に行くので、<br>受け口が `any` だと**存在しない物を触っても型検査が素通りする**。<br>型は手で書かず `Wired<'キー'>` で実体から引く（`state/wiredValue.ts`） |
| 使っていない物がたまる | tsconfig の `noUnusedLocals` |
| 時間計算がバラバラになる | `src/shared/timeline.test.ts` |
| テロップの絵が黙って変わる | `lib/telopSvg.golden.test.ts`（21ケース・SVG をそのまま固定） |
| **一時フォルダが際限なく貯まる** | `e2e/lib/e2eFixture.mjs` の `cleanBigTemp`（**5GB を超えたら捨てる**）。<br>通し・測定・音の確認の入口で毎回通る。**82GB まで貯まった実績あり** |
| **確認用の素材が運まかせ**（＝アプリの<br>不具合の顔をした赤が出る） | 同ファイル。素材は「Downloads の**いちばん小さい動画**」から作るので、<br>中身は選べない。**無音を3か所こちらで仕込み、数えて足りなければ落とす**。<br>08-04、無音ゼロの動画に入れ替わって**3項目が同時に赤**になり、<br>切り分けに半日かかった（コードには何も無かった） |
| **窓が裏に回ったまま負荷を測る** | `e2e/bench.mjs` の `measure`。前に出られると Chromium は<br>rAF を**毎秒1コマ**に絞るので、中央値が 1000ms になり所要時間も4倍に伸びる。<br>08-04、別セッションが Electron を起動して**6項目が同時に赤**になった。<br>**中央値 500ms 超は「測れていない」で落とす**（アプリのせいにしない） |
| 画面と書き出しで絵がズレる | `shared/clipMotion`・`shared/filterGraph` の試験 |

**検査が無い決まりは「願望」**として `.company/engineering/CLAUDE.md` に分けて書いてある。
決まりを増やすときは検査とセットで。

---

## やってはいけないこと（実際にやらかした物だけ）

1. **通しの e2e（`npm run e2e`、約13分）を勝手に回さない。** 普段は `--only=<語> --fast` で絞る。
   知らないフラグを渡すと**無視されて全件走る**（`e2e/run.mjs` は `includes` で拾うだけ）
2. **`--only` の赤／緑を通しの結果と同じだと思わない。** 順番に依存する項目がある
3. **赤を見たら、まず変更前と比べる。**
   `git stash push -u` → `npm run build` → 同じ `--only` → `git stash pop` → `npm run build`。
   既存の不具合を自分のせいだと思って直しにいかない
4. **大きいファイルを機械的に割らない。** 割る直前に「境目をまたぐ名前」を数える（目安40個）。
   数え方は `引き継ぎ-心臓の分け直し.md`。
   ※ **「40個」は横に割るときの目安。** フックを囲い（`state/*Context.tsx`）へ上げる
   （縦）ときは数えなくてよい——配線を通らなくなるので、またぐ数が消える。
   **この取り違えで `useAppWiring` を3回「割れない」と誤判断している**
5. **一括置換をしない。** `(prev) =>` のような形を機械で置き換えると、
   関係ない所（別の setState のコールバック）まで巻き込む（実際に2回起きた）
6. **コードを heredoc で書かない。** バックスラッシュが1つ消えて、試験ごと壊れて素通りする
7. **消えた物を `?? ''` や `if (count())` で守らない。** 落ちない代わりに**黙って死ぬ**。
   拡大UIを下のバーへ移した日から、e2e と負荷チェックの**4か所が測らないまま緑**だった
   （`.tl-zoom input[type=range]` が消えたのに気づけなかった）。
   測る側は「**成立しなければ落ちる**」に倒すこと
8. **e2e を手前で待たない。** 絞った確認でも数十秒〜数分あり、その間ずっと返事ができない。
   **裏で走らせて、終わったら結果を見る**（待っている間も話が続けられる）
9. **ファイルをスクリプトで切り貼りしない。** 2026-08-03〜04 に4回失敗した:
   `やること.md` を Python で置換して**0バイトにした**／`node -e` の中でバッククォートが
   シェルに食われて**説明文が空になった**（2回）／終端の探し方が甘く**最大40行を
   巻き込んで関数の途中で切った**。
   **切り出すときは、行番号を目で確かめてから1か所ずつ編集する。**
10. **測る e2e を裏で走らせている間に、ファイルを編集しない。**
   このリポジトリは**編集のたびにフックが `npm run verify`（型検査＋970件）を自動で回す**ので、
   絵の止まり・音の抜けを測っている裏で CPU を丸ごと使うことになる。
   2026-08-03 に実際に赤を1つ作り、**それを本物と誤判定しかけた**
11. **`git checkout <ファイル>` で一時的な変更を戻さない。** 戻る先は**コミット済みの版**なので、
   その日の作業ごと消える（2026-08-03、受け口の型で実際に消した）。しかも**気づきにくい**——
   続けて走らせた検査が「対象が無い」ので素通りして緑になり、
   **歯を確かめたつもりで何も確かめていなかった。**
   → わざと壊すときは **`cp` で控えを取って、控えから戻す。**
   git は「コミット済みへ戻す」道具で、「さっきの状態へ戻す」道具ではない

---

## よく使うコマンド

```
npm run index                         **目次**（496ファイル・約56k字）。「それ、もうある？」に最初に使う
npm run passthrough                   **配線の名前を数え、次にやることを順で出す**。フックを足した後に叩く
npm run measure                       **測り直しの口**。verify が出さない数字（any の残り・
                                      正典を呼んでいる場所の数・借金）
npm run verify                        型 + 単体（数秒。編集のたびにフックが自動で回す）
npm run build                         e2e の前に必ず要る
npm run e2e -- --only=<語> --fast     絞った確認（**裏で走らせる**。手前で待たない）
npm run e2e                           通し256項目・約13分（指示があったときだけ）
npm run bench                         **負荷チェック**。既定はテレビの編集マン1時間ぶん。
                                      過去の数字と比べるときは `-- --profile=light`
npm run bench -- --cpu                **重い理由の内訳**（レイアウト/スタイル/JS/その他）。軽い
npm run bench -- --cpu-deep           ＋記録と合成レイヤーの枚数・正体。**犯人を名指しする用**
                                      （重いので、数字を過去と比べないこと）
npm run dev                           開発起動
```

**書き出しが遅い／絵が違うと言われたら、まず控えを見る**（`%APPDATA%\GiftCut\`）:
`last-export-args.txt`（引数・同じ素材を何回開いたか）と `last-export-filter.txt`。
測定で使うときは `GIFTCUT_EXPORT_DUMP=<フォルダ>` を渡す（一時 userData だと消えるため）。

---

## いまの状態（2026-08-04）

### リファクタは一区切り。**通し 256 / 256 で初の全緑**

```
npm run verify   979件緑
npm run e2e      **253 / 256** → 赤3件は**アプリではなく確認用の素材**だった。
                 素材を作る側（`e2e/lib/e2eFixture.mjs`）を直して 3件とも緑
                 （`--only=無音カット,ダッキング` で確認。経緯は `やること.md` の頭）
500行超         **`src/` 0本。`e2e/` も大物は片付いた**（最大は 12-プレビュー 720）
                 run.mjs 1,024 → 430 ／ 02-タイムライン編集 1,078 → 43（入口）
                 09-モーション 1,131 → 428 ／ bench.mjs 1,303 → 497
版               **v0.1.21**（書き出しが 1.27倍・画質はそのまま）
```

- **`useAppWiring` 1,229 → 362行**（抱える名前 459 → 55）。囲いを32本足して、
  フックが自分で心臓を見に行く形にした。**「割れない」と3回測って3回とも
  間違っていた**——測っていたのは「横に切ったとき」だけで、実際の道は縦。
  → 経緯・道具・撤回した判断は **`引き継ぎ-心臓の分け直し.md`**（リファクタはこれ1本）

- **`.md` を13本 → 8本に畳んだ（08-04）。** リファクタの引き継ぎが5本に分かれていて、
  しかも**互いに矛盾していた**（片方が「割れない」と書き、もう片方が「確定」と書き、
  どちらも覆った）。**古い判断が残っていると次の担当が同じ結論を引く**ので消した

- **最後の赤1件はアプリの不具合ではなかった。** `動きをコピーしたあとでも、
  クリップのコピー＆貼り付けは普通に効く` が落ちていたのは、**検査が
  「貼り付けは上書きしない」に黙って寄りかかっていた**から（`39e4e22` 以前は真）。
  空いている所へ貼るよう直し、**成立しなければ落ちる**判定を2つ足した

- **借金（DEBT）は全部空。** `fileSize.test.ts` の `DEBT` も
  `readability.test.ts` の `DEBT_INDEX` も 0

### 負荷と書き出しを測り直した（08-04 夜）

**測る道具に穴が2つ重なっていて、限界さがし（`bench-limits.mjs` 19軸）は
書かれてから一度も走っていなかった。**

| 穴 | いつから |
|---|---|
| `const video` が `else` の中に閉じ、最後の `findLimits` から見えず落ちる | **最初のコミット（`87e5234`）から** |
| `setZoom` が消えたUI（`input[type=range][title*=拡大率]`）を触る | 08-03（`ZoomBar` へ移行）。**CLAUDE.md 7番の5か所目** |

直したら**全軸で傾きがほぼゼロ**だった（＝`bench-limits` の設計思想どおり）:

```
テロップ4000枚 / 文字1000字 / カット2000 / 効果音1000 / 画像1000枚 /
めじるし8000 / 素材ビン2000（別ファイル）/ 動き1000本 / エフェクト1000 …全部平気
画像1枚 4096px でも +0.3ms・メモリ +0.0MB
**1000枚が同時に見えていても 1操作 10.0ms**
```

- 基準を **`--profile=tv`（テレビの編集マン1時間ぶん）**にした。
  前の基準（テロップ200・カット1）は**編集していないのとほぼ同じ**だった。
  過去の数字と比べるときだけ `--profile=light`
- ~~**「平気」は平均の話**~~ → **直した（08-04）。95% で判定し、ドラッグも 240ミリ秒 → 1.3秒へ。**
  測り方を変えたら**重い軸が1本出た**:

```
**同時に見えている数**  200枚同時まで平気 / **500枚で 95% 58.3ms・引っかかり67回**
                        傾き **+92.4ms / 1000**
動画クリップの数         +25.6ms / 1000   元ファイル数 +21.7ms / 1000
切り替え効果             +12.8ms / 1000   テロップの枚数 +2.2ms / 1000（4000枚まで平気）
ほか全部                 ほぼ変わらない
```

  **「いくつ置いたか」ではなく「同時に何個見えているか」が効く。**
  前の測り方では、この軸すら「1000枚同時で +0.0ms」と出ていた。

**跳ねの内訳を測った（1種類ずつ抜いて8回）。犯人は1つではない:**

```
light（カット1・他ゼロ）   95% 4.4ms / 引っかかり 0回 / 切る 144ms
カット600だけ（他ゼロ）     95% 54.1ms / 64回        / 切る 521ms   ← 12倍
tv 全部乗せ                 95% 162.5ms / 165回      / 切る 903ms   ← さらに3倍
```

**カットが半分・残り全部の積み上がりで半分。** テロップは1200枚抜いても
162.5ms のまま＝**無関係**。抜いていちばん効くのは動画クリップだが -20%。
道具は `npm run bench -- --minus=<種類>`（tv から1種類だけ抜く）。

- **赤4件のうち3件は測定側だった**（`light` でも同じように落ちる）。→ **3件とも直した（08-04）。**
  原因は3つとも別だった:

```
テロップを掴む      寄せ方が固定10ノッチ。60分だと帯が **3.5px** にしかならない
文字を打ち直す      同上（細い帯の真ん中を押すと、時刻が数秒ずれる）
**再生してみる**    引いた状態だと **3秒＝1.2px**。しきい値5pxに届かない
                    → **再生は動いていた。** 判定を「動くはずの量に対する割合」へ
```

  **`headX` の `?? NaN` も外した**——再生ヘッドが画面外だと NaN になり、
  `Math.abs(NaN - x0) < 5` は **false ＝「動いた」**で素通りする（CLAUDE.md 7番）

**書き出しは 1.27倍速くした**（`preset p5 → p4`。画質は PSNR 66dB＝同じ、
ファイルの大きさも同じ）。分解した結果、**遅さはほぼエンコーダだけ**:

```
-hwaccel cuda      効かない      scale/pad/fps 除去  効かない
カットの数         効かない      入力の重複          既に1本
**テロップPNGが全画面（1920x1080）** … 100枚重ねると 3.6秒 → 7.8秒（切り詰めれば 4.0秒）
```

**テロップPNGの切り詰めを通した（08-04 夜）。重ねる段が 2.85倍。**

見送っていた2つが、どちらも片付いた。

```
1 全画面・yuv420（いままで）  3,668ms      1と2の絵の差  46.09dB
2 全画面・rgb                 3,019ms      1と3の絵の差  46.09dB ← 2と同じ
3 切詰め・rgb                 1,288ms      2と3の絵の差  **inf** ← 完全一致
```

- **ばらつき（71秒と126.8秒）はアプリ側だった。** ffmpeg だけ取り出して同じ物を
  3回ずつ回すと **±1%**。**書き出しの速さはアプリを通さずに測ること**
- **PSNR 43.8dB の正体は「重ねるときの色空間」。** `overlay` は既定で
  `format=yuv420`＝重ねる絵の色差を 2x2 に間引いてから混ぜる。網が絵の大きさで
  変わるうえ swscale は 2x2 の外まで見るので、**偶数に揃えても一致しない**（41.4dB）
- **絵の変化はすべて色空間ぶん。切り詰めは1ミリも足していない**（上表の 46.09 が同じ）
- 色空間を変えてよい根拠は**手で出した**: 青に赤を50%で重ねると canvas は 128,0,127。
  **yuv420 は 122,1,126（赤が6ずれる）／ rgb は 127,0,127。**
  プレビューは canvas＝rgb なので、`format=rgb` は**画面と書き出しのズレを消す方向**

見張りは `main/exportOverlays.test.ts`。**動きのあるテロップ（連番）はまだ全画面**
（大きさを揃える要があるので、やるなら「その連番ぜんぶの和集合で切る」形）。

### もたつきの犯人は「合成レイヤーの枚数」だった（08-04 夜）。**1行で半分〜3分の1**

```
                        直す前              直したあと
クリップを掴んで動かす   95% 174.9ms・167回  →  **91.8ms・94回**
再生してみる             95% 312.7ms         →  **104.2ms**
合成レイヤー             **800枚**           →  **22枚**
```

Chromium は **GPU で描く 2D canvas を1枚ずつ独立した合成レイヤーに載せる**。
波形は音声クリップの数だけあるので、カット600 で 800枚になり、毎フレームの
`Layerize`（1回 79ms）が**掴んでいる28秒のうち 17.8秒**を食っていた。
→ `WaveformCanvas` を `getContext('2d', { willReadFrequently: true })`＝CPU 描画へ。

**辿り方が要点なので順を残す**（道具は `npm run bench -- --cpu` / `--cpu-deep`）:

```
1 内訳     レイアウト・スタイル・JS を足して 15%。**残り85%が「その他」**
2 標本器を切る  それでも85%。**自分の測り方が作った数字ではない**と確かめる
3 記録     `Layerize` と名前で出る
4 数える   **800枚→赤 / 160枚→緑 が完全一致**
5 正体     `canvas ×427` ＋ 巻き添えの `div.audio-clip ×347`
```

**「JS が重いはず」で探すと絶対に見つからない。** React は28秒中2.8秒だった。

### 次にやるなら

1. **`tv` の残り3件**（`light` は全部緑）。上の直しの後の数字は `やること.md`:
   - クリップを掴む 95% 91.8ms ／ 再生してみる 95% 104.2ms
   - 全部選んでまとめて動かせない
   → 次に大きいのは `GPUTask`/`CommandBuffer` 系 約11秒と `RunPaintLifecyclePhase` 7.6秒。
     **レイヤーの話ではない**ので、また内訳から取り直すこと
2. `やること.md` の残り（プレテストで出た直し・追加）

~~`probe` が素材の所まで送る~~ **済（08-04）**／
~~`useSegClock` を実データで測る~~ **済（08-04）。問題なし**／
~~`bench-limits` が平均しか返していない~~ **済（08-04）。95%＋長いドラッグへ**／
~~本体側の測定漏れ3件~~ **済（08-04）。3件とも測定側だった**

版は **v0.1.21**（08-04 に公開済み。0.1.20 は同日の安定化ぶん）。

---

## どこに何が書いてあるか

| | |
|---|---|
| `引き継ぎ-心臓の分け直し.md` | **リファクタはこれ1本。**配線を剥がした経緯／500行の根拠／入れた検査／受け口の型（`Wired`）／**測る道具の穴8つ**／**撤回した判断**（2026-07-31〜08-04・5本を統合） |
| `引き継ぎ-秘書エージェント計画.md` | **「それ、もうある？」に答える係。**目次・正典台帳・エージェント本体・見送りの鮮度 |
| `引き継ぎ-プレビューとタイムラインの大きさ.md` | 画面の大きさ・操作バーの高さを決めた経緯 |
| `引き継ぎ-2026-08-02-ネスト（組）.md` | 組（ネスト）の仕様と、**わざとやっていない事** |
| `やること.md` | プレテストで出た直し・追加。**着手前にここを見る** |
| `リリース手順.md` | 版を上げて出すまで |
| コードの真上のコメント | **なぜそう作ったか**。ここが一次情報 |
| `.company/engineering/docs/` | 設計の背景・製品の方針（git に乗らない） |
| `.company/engineering/debug-log/` | 調べた経緯・行き止まり（git に乗らない） |

### 「428行」を目標にしないこと

`428` はこのリポジトリの**90%点**（＝定義上いつも約31本が超える）。
追いかけても終わらない。**効く基準は2つだけ**:

1. **1,250を超えない**（AIが1回で読めなくなる。機械で止まっている）
2. **1つのファイルが2つ以上の話題を持たない**（これは人が判断する）

大きくても話題が1つなら、それは正しい大きさ。
