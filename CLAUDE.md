# GiftCut

切り抜き動画に特化したテロップ編集アプリ。Electron + React + TypeScript + FFmpeg。

**このファイルは AI が毎回自動で読む。** 長くしないこと（長いと読み飛ばされる）。
詳しい話は各リンク先にある。

---

## 最初に：組織がある

このプロジェクトは `.company/` に**秘書室と開発部**を持っている。
`.company/` は自動では読まれないので、**この2行だけ覚えて入口を踏むこと**:

| いつ | 何を読む |
|---|---|
| コードを触る前 | 下の表から**担当部門の CLAUDE.md** |
| 相談・TODO・記録・雑談のとき | `.company/secretary/CLAUDE.md`（秘書室） |

どちらか1つを読めば `.company/CLAUDE.md`（組織のルール）も一緒に付いてくる。

**オーナーが「秘書」と言ったら秘書室のこと。** `/company` でも入れるが、
打ち忘れても抜けないようにここへ書いてある。

### 作業の終わりに必ず確かめること

**`.company/` は .gitignore されている**（clone しても付いてこない）。
コードと一緒に残すべき記録は**リポジトリ側**に置くこと:

- **なぜそう作ったか** → そのコードの真上のコメント
- 何をどう変えたか・測った数字 → `引き継ぎ-*.md`
- これからやること → `やること.md`

2026-07-31〜08-02 の作業は `.company/` に1行も残らなかった（秘書のメモは 07-29 で
止まっていた）。**実害が無かったのは、travel すべき物が全部リポジトリ側にあったから。**

---

## 書く前に：**もうあるか**を見る

**`npm run index`**（数百ms）。399ファイルの「パス＋そのファイルが自分で名乗っている1行」が出る。

2026-08-03 に直した11件のうち**5件が「正典・仕組みはあるのに、その道を通っていなかった」型**
だった（`overwriteCues` は移動だけ／`EMPTY_DRAG_IMG` は素材ビンだけ／全体表示の率が3か所バラバラ…）。
**どれも探せば見つかった。** 目次はそのために作ってある。

```
掴んだときカーソルの絵を消すには？  → lib/dragChip.ts  「カーソルへ何を握らせるか」
テロップが重なったら？              → shared/overwrite.ts「置いた物を勝たせる」
素材を落とす所は？                  → state/useMediaDrop.ts
```

**git に入れていない**（呼ぶたびに作る＝腐らない）。中身は各ファイルの冒頭コメントの
1行目そのままで、書き起こしではない。詳しくはそのファイルを開くこと。
計画と、この先の段階は `引き継ぎ-秘書エージェント計画.md`。

---

## 触る前に：担当を決める

**触ったファイルのパスで、持ち主が一意に決まる。**「直すところが違う」を構造で消すため。

| パス | 部門 | 決まり |
|---|---|---|
| `src/renderer/src/state/**` | 心臓・配線部 | `.company/engineering/departments/state/CLAUDE.md` |
| `src/renderer/src/components/**` | 画面部 | `.company/engineering/departments/ui/CLAUDE.md` |
| `src/renderer/src/lib/**`<br>`src/shared/**`<br>`src/main/**` `src/preload/**` | 計算・書き出し部 | `.company/engineering/departments/core/CLAUDE.md` |
| `e2e/**` `scripts/**` `**/*.test.ts` | 検査部 | `.company/engineering/departments/qa/CLAUDE.md` |

**持ち場を持たない部門が1つある。** 割る・整理する・重複を消す指示は**開発部ではなく
こちらが出す**（パスで分けると、部門をまたぐ整理が誰の物にもならないため）:

| | |
|---|---|
| リファクタリング部 | `.company/engineering/departments/refactor/CLAUDE.md` |

**目的は行数ではない。** 不具合が来たとき・要件が増えたときに**的確に直せる／足せる**こと。
測り方は「①場所が一発で分かる ②**直し漏れが出ない** ③足す場所が分かる」。
08-03 の不具合8件は**全部②**（片方だけ直した型）。だから**②が最優先**。

**取説（`// ## 中身`）が書けない形が1つある。** ファイル全体が1つの関数で
包まれていると、並ぶ名前が1〜2個になって「これしか無い」という嘘の案内になる。
そのときは**先に割る**（`exportRun` が実例。1,229行中1,012行が1コールバックだった）。

**2つ以上にまたがる作業は、着手前にどちらへ置くか決める**
（またがったまま進めると、両方に半分ずつ書かれて二重実装になる）。

※ `.company/` は .gitignore なので、clone しただけでは無い。無ければこのファイルの決まりだけで進めてよい。

---

## 決まりには検査が付いている

**書いただけの決まりは守られない**ので、機械で止めている。破ると `npm run verify` が赤くなる。

| 止めている失敗 | 検査 |
|---|---|
| ファイルが気づいたら1万行 | `src/shared/fileSize.test.ts`（上限1,250行＋借金の据え置き） |
| **上限に当たってから慌てる** | 同上。**1,200行を超えたら名前を出して知らせる**（止めない）。<br>出たファイルは**次に触るときに、まず出す物を決める** |
| 同じ物を別の場所に二重に書く | `src/shared/noDuplicate.test.ts`（shared の作り直し／12行そっくり） |
| **500行超なのに取説が無い** | `src/shared/readability.test.ts`。**500 を超えると AI は通しで読まず<br>grep に切り替わる**ので、割るか「`// ## 中身`」を付けるかを迫る |
| **取説が腐る**（引っ越したのに説明が残る） | 同上。並べた名前と top-level の実体を照合する。<br>実例: `telopAnim.ts` に本体の無い説明が残っていた（誰も気づかなかった） |
| 冒頭コメントが無いファイルが増える | 同上。**コメントが無いファイルほど話題が多い**（実測） |
| **心臓の受け口が `any` で開く** | `src/shared/ctxTypes.test.ts`。区画は心臓を自分で見に行くので、<br>受け口が `any` だと**存在しない物を触っても型検査が素通りする**。<br>型は手で書かず `Wired<'キー'>` で実体から引く（`state/wiredValue.ts`） |
| 使っていない物がたまる | tsconfig の `noUnusedLocals` |
| 時間計算がバラバラになる | `src/shared/timeline.test.ts` |
| **一時フォルダが際限なく貯まる** | `e2e/lib/e2eFixture.mjs` の `cleanBigTemp`（**5GB を超えたら捨てる**）。<br>通し・測定・音の確認の入口で毎回通る。**82GB まで貯まった実績あり** |
| 画面と書き出しで絵がズレる | `shared/clipMotion`・`shared/filterGraph` の試験 |

**検査が無い決まりは「願望」**として `.company/engineering/CLAUDE.md` に分けて書いてある。
決まりを増やすときは検査とセットで。

---

## やってはいけないこと（実際にやらかした物だけ）

1. **通しの e2e（`npm run e2e`、約12分）を勝手に回さない。** 普段は `--only=<語> --fast` で絞る。
   知らないフラグを渡すと**無視されて全件走る**（`e2e/run.mjs` は `includes` で拾うだけ）
2. **`--only` の赤／緑を通しの結果と同じだと思わない。** 順番に依存する項目がある
3. **赤を見たら、まず変更前と比べる。**
   `git stash push -u` → `npm run build` → 同じ `--only` → `git stash pop` → `npm run build`。
   既存の不具合を自分のせいだと思って直しにいかない
4. **大きいファイルを機械的に割らない。** 割る直前に「境目をまたぐ名前」を数える（目安40個）。
   数え方と、`useAppWiring` が割れないと出た実測は `引き継ぎ-App分割.md` の「段階4・5」
5. **一括置換をしない。** `(prev) =>` のような形を機械で置き換えると、
   関係ない所（別の setState のコールバック）まで巻き込む（実際に2回起きた）
6. **コードを heredoc で書かない。** バックスラッシュが1つ消えて、試験ごと壊れて素通りする
7. **消えた物を `?? ''` や `if (count())` で守らない。** 落ちない代わりに**黙って死ぬ**。
   拡大UIを下のバーへ移した日から、e2e と負荷チェックの**4か所が測らないまま緑**だった
   （`.tl-zoom input[type=range]` が消えたのに気づけなかった）。
   測る側は「**成立しなければ落ちる**」に倒すこと
7. **e2e を手前で待たない。** 絞った確認でも数十秒〜数分あり、その間ずっと返事ができない。
   **裏で走らせて、終わったら結果を見る**（待っている間も話が続けられる）
8. **ファイルをスクリプトで切り貼りしない。** 2026-08-03 に3回失敗した:
   `やること.md` を Python で置換して**0バイトにした**（開いた時点で中身が消え、
   書き込みが文字コードで失敗）／`node -e` の中でバッククォートがシェルに食われて
   **説明文が空になった**／閉じ括弧の探し方が甘く**関数の途中で切った**。
   **切り出すときは、行番号を目で確かめてから1か所ずつ編集する。**
9. **測る e2e を裏で走らせている間に、ファイルを編集しない。**
   このリポジトリは**編集のたびにフックが `npm run verify`（型検査＋925件）を自動で回す**ので、
   絵の止まり・音の抜けを測っている裏で CPU を丸ごと使うことになる。
   2026-08-03 に実際に赤を1つ作り、**それを本物と誤判定しかけた**
   （経緯は `やること.md` の「間欠的に落ちる確認」）
10. **`git checkout <ファイル>` で一時的な変更を戻さない。** 戻る先は**コミット済みの版**なので、
   その日の作業ごと消える（2026-08-03、受け口の型で実際に消した）。しかも**気づきにくい**——
   続けて走らせた検査が「対象が無い」ので素通りして緑になり、
   **歯を確かめたつもりで何も確かめていなかった。**
   → わざと壊すときは **`cp` で控えを取って、控えから戻す。**
   git は「コミット済みへ戻す」道具で、「さっきの状態へ戻す」道具ではない

---

## よく使うコマンド

```
npm run index                         **目次**（399ファイル・約43k字）。「それ、もうある？」に最初に使う
npm run measure                       **測り直しの口**。verify が出さない数字（any の残り・
                                      正典を呼んでいる場所の数・借金）。**見送りの根拠を確かめるとき**
npm run verify                        型 + 単体（数秒。編集のたびにフックが自動で回す）
npm run build                         e2e の前に必ず要る
npm run e2e -- --only=<語> --fast     絞った確認（**裏で走らせる**。手前で待たない）
npm run e2e                           通し224項目・約12分（指示があったときだけ）
npm run dev                           開発起動
```

---

## いまの状態（2026-08-04 未明）

- **通し 255 / 256（13.4分）。赤は1件で、これが最優先。**
  `動きをコピーしたあとでも、クリップのコピー＆貼り付けは普通に効く` が落ちる
  ——**貼り付けてもテロップが増えない**（「貼ったのに出てこない」＝黙って消える型）。
  → **二分探索で `39e4e22`（前回の貼り付け・重なり修正）が原因と確定。**
    今日の作業とは無関係。`useCopyPaste` の上書きの呼び出しを外すと緑になる。
  → **ただし数が合わない**ので、まだ直していない（推測で直さない）。
    調べた事・次の一手は `やること.md` の先頭

- **右パネルの束81個を、タブ5つのフックへ割った**（`RightPanelArea` 410 → **114行**、
  心臓から受け取る物 **81 → 8個**）。`useAppWiring` は1行も増えない
  ——タブのフックは**自分で心臓を見に行く**ので、配線の大元を経由しない。
  → **`useAppWiring` は割れない。3つの測定で確定した**（横に切ると300〜506個またぐ／
    60行以上の連続範囲を全部試して40の内側は1つだけ／**束を外へ出すと200〜400個返す**）。
    前の「106〜413」は**過小**だった（省略記法のプロパティを数え落としていた）。
  → **「束を小さくすれば配線も分かれる」は成り立たなかった。** 束の入力を作っているのが
    他の全部の束にも配っている共通のフックなので、**網は消費側からはほどけない**。
    だから `dialogs` `menus` に同じ手は当てていない。
    **「良い切り口を探す」はもうやらなくてよい。全部試して無い**
  → 経緯・測り方・踏んだ穴は `引き継ぎ-心臓の分け直し.md`
- **★を押すとプロジェクト由来まで焼き付く件（⑦の残り）を直した。**
  「アプリ側とプロジェクト由来を別々に持つ」つもりだったが、**保存する物を変えて済んだ**
  ——`saveFavorites(画面の一覧)` を `persistFav(押した1件, on/off)` に。
  画面の一覧を渡さないので**混ざりようがない**。検査は `lib/telopTemplates.test.ts`（9件）。
  → **直したあと grep で数えて、同じことをする2つ目の道が1件見つかった**
    （`useLabelsPresets`）。**片方だけ直す型**が今日3回出ている
- **`state/` の `any` を 293 → 19件にした。** 残る19件は**全部「正しい `any`」**
  （ディスクから読んだJSONを解く所）。心臓の受け口（`*Context.tsx`）は **0 で固定**。
  → **受け口と deps では手が違う。** 受け口は実体から引ける（`Wired`）が、
    deps は輪になるので引けない。代わりに「**呼ぶ側が実物を渡すので、手で書いても
    ズレたら呼び出し側で落ちる**」を使う。型は推測せず compiler API で実引数を読んだ。
  → **本物を2件見つけた。どちらも「setState の updater に手で書いた注釈が実体と違う」。**
    受け側が `any` である限り永久に見つからない型
  → 経緯は `引き継ぎ-受け口の型.md`。残数は `npm run measure`
- **測り直しの口（`npm run measure`）を作った。** verify が出さない数字だけを出す
  （`any` の残り・正典を呼んでいる場所の数・借金）。作った初日に
  **借金の上限を下げ忘れていた**のと **`npm run audit` が存在しない**のを見つけた
- **心臓の受け口 341件の `any` を潰した。** 区画は心臓を自分で見に行くのに、
  その受け口が全部 `any` だった＝**存在しない物を触っても型検査が素通りする**。
  手で書き直さず、**詰めている実体から機械で引く**形にした（`state/wiredValue.ts`）。
  **363件が本物の型／`any` は 0。** `any` が隠していた本物を1件見つけた
  （`TimelineArea` の手書き注釈に `kind` が無く、書き直した瞬間に段の種類が消える形）。
  検査は `ctxTypes.test.ts`（3件・歯も確認）。経緯は `引き継ぎ-受け口の型.md`
- **秘書エージェント（段階0〜3）ができている。** 目次・正典台帳・本体・見送りの鮮度。
  検証で3件当て、こちらの見落としを3つ指摘した。段階4（測り直しの口）は未着手。
  `引き継ぎ-秘書エージェント計画.md`
- **通し 251 / 252 で赤ゼロ**（2026-08-03 夜・13.0分）。残る1件は落ちたのではなく飛ばされていた
  ——`orderDependent` が要らない所に付いていて、**書かれてから一度も走っていなかった**。
  詳しくは `やること.md` の末尾
- **`npm run verify` 緑（925件）**
- **通しで落ちていた赤4件は、アプリの不具合ではなかった。** 直したのは検査側1か所。
  `掴んだまま右端まで引っぱる` が**寄せたまま終わっていた**ので、後続4件が
  「2.5倍に寄せて4,862px 送った画面」を測っていた（帯は**見えている範囲にしか
  作らない**ので `3 → 1`、左外へ出て `x = -2074`）。`--only=掴んで` **19件緑**。
  → **「変更前と比べる」が効かない型だった**（原因の確認が、戻し先にも既に入っていた）。
    経緯と教訓3つは `やること.md` の「通しで落ちていた4件」
- 読める形の検査（5件）と、**式で見る重複の検査**（5件）を足した。
  詳しくは `引き継ぎ-読める形.md`
- **リファクタリング部を作った。** 目的は行数ではなく
  「**不具合が来たとき・要件が増えたときに的確に直せる**」。
  測り方は ①場所が一発で分かる ②**直し漏れが出ない** ③足す場所が分かる。
  この日の不具合8件は**全部②**（片方だけ直した型）だったので、②が最優先
- **500 が境目**（実測）。そこを超えると AI は通し読みをやめて grep に切り替わる。
  **超えたら「割る」か「取説（`// ## 中身`）を付ける」かのどちらか。**
  取説は機械で照合するので、引っ越して説明だけ残すことはできない
- **書き出しの実害を1件消した。** `exportRun` が `shared/timeline` を手書きして
  いて、正典にある `Math.max(0, …)` が抜けていた（画面は0で止まるのに
  書き出しだけ負の長さになる）。`--only=書き出` **22件緑**で確認
- **8本を割る／取説を付けた**（`projectFiles` `AppMenus` `useSessionMemory`
  `telopStyle` `useLibraries` `useProjectFile` `StylePanel` `useTimelineEdit`）。
  **`StylePanel` 以外は全部またぐ名前0**
- **`exportRun` を3つに割った（最後の1本）。1,229 → 479行。**
  「まず取説を作る」つもりが、**実測したらその道は塞がっていた**——top-level の
  callable が2つしか無く（1,012行が `export:run` の中の1コールバック）、
  取説を書いても2行で「これしか無い」という**嘘の案内**になる。
  → 話題で3つ: `exportRun`（受け口・479）／`exportGraph`（組み立て・893・取説18項目）／
  `exportSpawn`（走らせる・258・取説8項目）。**依存は一方向**で、組み立てと実行は
  互いを知らない。経緯と危なかった所は `引き継ぎ-読める形.md`

### 前（2026-08-03 昼）

- **通し 246 / 248**（12.8分）。前回 222/224 から項目が24件増えて赤は2件のまま。
  **前に落ちていた2件（プレビューで複数のテロップをまとめて操作）は緑になった。**
  いま落ちている2件も直したので、次の通しは全緑のはず
- **本物のプロジェクトで測れるようになった**（`npm run bench -- --project=<path>`／
  `npm run stutter -- --project=<path>`）。1.0MB・248テロップ・段11本で
  **19 良好 / 0 問題あり**、絵の止まり 0ms・音の抜け 0回
- **リファクタ: 11本を割った。** 判断は行数順ではなく
  「**頭のコメントが2つ以上の話題を宣言している物**」から。7本が導管 0 / 0 だった
- ~~割らないと数えて決めた6本~~ … **この判断は夜に撤回した。**
  「またぐ名前 40個以下なら割る」は**書く側の手間しか測っておらず、
  読む側の負担を1ミリも測っていなかった**（導管 +5 は1回、部分読みは毎回）

---

## 前の状態（2026-08-02）

- **v0.1.12 を出した（2026-08-02）。** https://github.com/kyohei0612/GiftCut/releases
- `npm run verify` 緑（**701件**）。**`npm run e2e` の通しは 08-02 昼が最後**（222 / 224）。
  そのあと入れた分は絞った確認だけで見ている（本人の判断で通しは見送り）。
  **次に通しを回すときは、まずこの2件が緑になっているはず**（原因は直してある）
- **書き出しが速くなった**（テロップの重ねを連番1本に。実機で ffmpeg 6.1秒 → 1.2秒）。
  測った数字と、まとめられる条件は `やること.md` の「書き出し」
- **更新はこちらから再起動を促さない**（次に閉じたときに黙って当たる）。
  0.1.11 → 0.1.12 が、その挙動を実際に確かめられる最初の更新
- **リファクタの前後で通しの結果が1件も変わっていない**（落ちる2件も同じ2件・同じ数値）
- **上限（1,250行）を超えるファイルは0。借金（DEBT）も空。**
  308ファイルの中央値は121行
- 落ちている e2e は**すべてリファクタ前からの既存不具合**（`やること.md` に控えてある）

### 「428行」を目標にしないこと

`428` はこのリポジトリの**90%点**（＝定義上いつも約31本が超える）。
追いかけても終わらない。**効く基準は2つだけ**:

1. **1,250を超えない**（AIが1回で読めなくなる。機械で止まっている）
2. **1つのファイルが2つ以上の話題を持たない**（これは人が判断する）

大きくても話題が1つなら、それは正しい大きさ。
例: `main/exportRun.ts` 1,133行は「書き出し」1つ。渡す物を数えたら0個だった。

## どこに何が書いてあるか

| | |
|---|---|
| `引き継ぎ-読める形.md` | **AI が読める形に保つ話。**500 という数字の根拠／撤回した基準／入れた検査／割った8本／順番に依存する所（2026-08-03） |
| `引き継ぎ-心臓の分け直し.md` | **右パネルの束81個をタブ5つへ割った話。**配線側が割れないことの測り直し／省略記法で数を間違える穴／次の順序（2026-08-04） |
| `引き継ぎ-受け口の型.md` | **心臓の受け口 341件の `any` を実体から引く形にした話。**測ってから決めた／`= W` の1行にしなかった理由／見つけた本物1件／`git checkout` で作業を消した話（2026-08-03） |
| `引き継ぎ-秘書エージェント計画.md` | **「それ、もうある？」に答える係。**段階0〜3（目次・正典台帳・エージェント本体・見送りの鮮度）と検証（2026-08-03） |
| `引き継ぎ-App分割.md` | App.tsx を 11,404 → 198行にした経緯。測った数字と、やってはいけない理由 |
| `やること.md` | プレテストで出た直し・追加 約30件。**リファクタが終わってから着手**と書いてある |
| コードの真上のコメント | **なぜそう作ったか**。ここが一次情報 |
| `.company/engineering/docs/` | 設計の背景・製品の方針（git に乗らない） |
| `.company/engineering/debug-log/` | 調べた経緯・行き止まり（git に乗らない） |
