# 引き継ぎ：プレビュー画面とタイムラインの大きさ

最終更新 2026-08-01。**この件はほぼ片付いた。** 残りは1つ（下記「残っていること」）。

---

## いま置いてある場所（触るならここ）

大きさの決まりは **App.tsx から出してある**ので、探し回らなくてよい。

| 何を | どこに |
|---|---|
| 掴める境目の限界（左右・タイムライン高さ） | `src/renderer/src/state/panelSize.ts` |
| 幅・高さの状態と、境目を掴んで動かす所 | `src/renderer/src/state/usePanelLayout.ts` |
| 段（レーン）の高さ | `src/renderer/src/state/useLaneHeights.ts` |
| 横の拡大率と、その限界 | `src/renderer/src/state/useView.ts` |
| 段の縦位置の計算・落とし先の判定 | `src/shared/lanes.ts` |
| 目盛りの高さ・上下の余白 | `App.tsx` の `RULER_H` / `TRACK_PAD_ROWS` |

試験は `panelSize.test.ts`（6件）、`useLaneHeights.test.ts`（5件）、
`useView.test.ts`（4件）、`lanes.test.ts`（11件）。
**画面を起動せずに直せる**ので、数字をいじるだけなら `npm run verify`（5秒）で足りる。

---

## いまの数字

```
左パネル      既定 250   （170 〜 520）
右パネル      既定 300   （200 〜 560）
タイムライン   既定 370   （150 〜 620）

段の高さ      既定 26（最小）  上限 160
  ただし A1（本編の音）だけ 44
目盛り        24
上下の余白    映像の段 × 2

横の拡大率     既定 24    （6 〜 240）
```

### なぜその数字なのか（消さないこと）

- **タイムライン既定 370** … 以前は 420 で、画面のほぼ半分を占めていた。
  実測で「枠 845px に対して映像 326px」＝プレビューが枠の4割しか使えていなかった。
  切り抜きは「プレビューを見ながらテロップを詰める」作業なので、映像側を優先した。
- **タイムライン上限 620** … 超えると映像が画面の3割程度になり、作業が成立しない。
- **A1 だけ 44** … 波形が読めないと、どこで喋っているか分からず無音カットの当たりも
  付けられない。ただし音声レーンを全部高くすると段の合計が伸びて縦に溢れるので、
  要る1本だけにしてある。**62 に上げる案は見送り、44 で確定**（2026-08-01）。
- **拡大の上限 240** … 以前は 120。テロップの出入りの動き（0.05〜2秒）を合わせるとき、
  限界まで寄せても帯が細いままだった。
- **拡大の下限 6** … これより引くとクリップが線になって掴めない。
  全体を見たいときは「↔（フィット）」がある。

---

## 済んでいること

- 幅・高さ・段の高さを **App.tsx から出して試験を付けた**（数字の意味に名前が付いた）
- **段ごとの高さ**にした。以前は「映像なら映像の段が全部」まとめて変わっていて、
  波形を見たい1本のために他まで太り、画面が足りなくなっていた
- 落とし先の判定を `shared/lanes.ts` へ。**外したときに本編を上書きしない**を試験で固定
- 保存の読み直しで、範囲外の高さを捨てる（潰れた段・溢れる段を作らない）。
  拡大率も同じ扱いにした（`clampZoom`。0 や NaN だと中身が全部消える）
- **拡大の限界を1か所へ**（`ZOOM_MIN` / `ZOOM_MAX`）。以前はバー・ホイール・フィットの
  3か所に直接書いてあり、別々に直すと「バーは端まで行ったのにホイールではまだ寄れる」
  が起きる。**上限は倍の 240 にした**
- **プレビュー下の操作バーを1段にした**（下記）
- 通し（e2e）に縦まわりの確認あり：
  「タイムラインを縮めると縦に送れる」「段の見出しが行についてくる」
  「境目を掴んで高さを変えられる」「縮めると上下が一緒に小さくなる」

### プレビュー下の1段化（2026-08-01・**完了**）

押さない物（画質・fps・尺＝`TransportInfo`）を、操作バーからパネルの見出し行の
右端（`.panel-tabs-right`）へ出した。残った時刻とボタンだけなら、左右を同じ幅で
挟んだまま1段に収まる＝**一番よく押す再生ボタンは中央のまま**。

実測（プレビュー幅1012・普段の幅）:

| | 変更前 | 変更後 |
|---|---|---|
| 操作バー | 71px | **41px** |
| 見出し行 | 33px | 33px（据え置き） |
| **映像** | 273px | **303px（+30）** |
| 再生ボタンのずれ | 0.0px | 0.0px |
| ボタンの行数 | 1 | 1 |

**見出し行は入らなければ折り返す（`flex-wrap`）。** 詰める作りにしたら、
左右のパネルを限界まで広げたとき（プレビュー幅482）に「オーディオミキサー」タブが
押し出されて消えた。折り返しなら何も失わない。その幅でも操作バーは 103→73px。

やり方を選ぶ前に**一度「見出しへ出さずに1段化」を試して、逆に 71→73px 増えた**。
右側が 534px 要るのに 505px しかなく、ボタンが折り返したため。
左右対称をやめれば入るが、それは再生ボタンの中央維持を捨てることになる。
＝**「押す物」と「押さない物」を分けるのが要点**で、詰め方の工夫では入らない。

---

## 残っていること

### 「動かしてないのに画面がぶるぶるする」（**未調査・このままでは進めない**）

起動し直すと直る。大きさの計算が毎フレーム揺れている可能性があるが、
**測る手段が無い**ので手が付けられない。`やること.md` の
「測定報告ボタン（製品版に不具合報告の口が無い）」を先に作るのが早い。
そちらができたら、この症状もその口から出す。

---

## 測り方（次も同じ手が使える）

数字は当てずっぽうにせず、本物のアプリで測った。手順は：

```
npm run build                      # out/ を動かすので先にビルドが要る
node <使い捨てスクリプト>          # playwright で out/main/index.js を起動して
                                   # getBoundingClientRect を読むだけ
```

使い捨てスクリプトは `e2e/.cache/`（gitignore 済み）に置いて、終わったら消す。
比べるときは **`git stash` で変更前も同じ手で測る**と、差がそのまま出る
（`git stash push -- src/` → `npm run build` → 測る → `git stash pop` → もう一度ビルド）。

**枠を絞ってから測ること。** 素で `querySelector('.panel-tabs')` すると
左パネルのタブ帯を拾う（同じ class が3つある）。`.panel.monitor` の中で探す。

---

## 注意

- リファクタリング中だが、**帯（クリップ）は全部 `components/timeline/` へ出し終えた**。
  まだ App.tsx にあるのは再生エンジン（`startVideoSegClock` ほか）とロジックの塊で、
  大きさの計算とは絡んでいないので、この件では気にしなくてよい。
- 大きさを変えたら **e2e の目で見る確認が当たり所ずれで赤くなることがある**。
  実際、映像が30px 広がったときに「数値欄は押して振ると…」が赤くなった。
  絵は動いていて、固定の 28×20 窓の当たり所がずれただけだった（窓を文字の箱に
  合わせて直した）。**赤を見たら、まず絵が本当に動いていないかを確かめる。**
