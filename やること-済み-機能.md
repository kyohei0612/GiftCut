## プレビュー

- [x] **拡大機能**を付ける
      → **作った（2026-08-03）。** モニタの帯に **−／％／＋**。％を押すと全体表示へ戻る。
        枠ごと大きくして、はみ出した分はモニタ区画を送って見る。
      ※ **プロジェクトには入れない。** 見るためだけの物で、書き出しにも保存にも
        関わらない（クリップの拡大＝リフレームとは別物。あちらは絵が変わる）。
        覚えさせないのは、開き直したときに「なぜか寄っている」状態から始まらないため。
      確認は e2e「プレビューを寄せられて、等倍に戻せる」（戻せることまで見る）
- [x] **テロップのスタイルを D&D でテロップへ**持っていけるように
      → **タイムラインの帯へも落とせるようにした（2026-08-03）。**
        プレビューの文字の上には元から落とせたが、帯には落とせなかった。
        同じ物を同じように扱えないと「どこへ落とせるのか」を毎回思い出す羽目になる。
        見本帳とアイコンの両方。当てる中身はプレビュー側と**同じ関数**を通る。
      確認は e2e「見本帳をタイムラインの帯へ落とすと、その文字に当たる」
- [x] **モーションのリセットがプレビューから消えている**
      → **調べた結果、消えてはいなかった**（2026-08-02）。履歴を `resetVideoZoom` と
        「リセット」で追ったが、消された跡は無い。**いまも出ている**——ただし
        **動画・画像・映像レイヤーを選んだときだけ**（プレビュー下の枠のバーに
        「リセット（◯個）」。中身は等倍へ戻す＋打った動きも消す）。
      → **本当に無いのはテロップ側。** テロップを選んでプレビューで動かしても、
        プレビューには戻す口が無い（モーションタブの各行の戻すボタンしかない）。
      → **テロップ側も足した**（2026-08-02）。テロップを選ぶと映像の上寄りに
        バーが出て、「位置と動きを戻す（◯個）」で置き場所・大きさ・打った動きを
        まとめて元へ戻せる。**見た目の設定（色・縁・字の大きさ）は消さない**——
        戻したいのは置き場所と動きで、そこまで戻すと作り込みが消える。
        形は動画側のバーと揃えた（同じ役目の物が2つの見た目を持つと毎回思い出す羽目になる）。
        ※ z-index を付け忘れるとテロップの層に潜って**押せないボタン**になる（実際になった）。
        確認は e2e「プレビューで動かした文字を、その場で元へ戻せる」
- [x] **書き出しとプレビューで、画像の位置がズレる**（本人・2026-08-03）。
      → **済（2026-08-03。v0.1.17）。変形を当てる順が画面だけ逆だった。**
        目印を置いた画像を5通りの変形にかけ、画面（CSSの行列）と書き出し
        （同じ ffmpeg の並びを実際に流した絵）で重心を比べた:
        動かす・寄せるは一致、**反転で 0.497（左右が真逆）／回転で 0.228** ズレ。
        CSS は右から当たるので translate を左端へ移した。スクショ（canvas）も
        同じ間違いをしていたので揃えた（6通り×四隅で行列が一致することを計算で確認）。
        押さえに `lib/clipXform.test.ts` を10件（**元に戻すと5件赤**）。
      **「見た物と出来た物が違う」型**なので優先度が高い（同じ型で、色のズレと
      音量のズレを 08-03 に直している）。
      ※ 調べる順:
        1. **どちらが正しいかを先に決めない。** 実データで両方を撮って、
           ズレの向きと量を測る（`e2e/lib/measure.mjs` の similarity と
           bench の「[目]」の作りがそのまま使える）
        2. 画面側は `lib/clipXform`（CSS transform）と `PreviewLayers`、
           書き出し側は `shared/filterGraph` の overlay。**同じ式か**を突き合わせる
        3. テロップは `shared/clipMotion` で「画面と書き出しが同じ絵になる」ことを
           試験で押さえてあるが、**画像には同じ押さえが無い**。直したら足すこと
- [x] モーションと、プレビューで手で動かしたときの**連動を確認**
      → **済（2026-08-03）。連動は正しく効いていた。**
        印を打ってある項目をプレビューで手で動かすと、**固定値ではなく
        その時刻に印が打ち直される**（＝打った動きが消えない）。
        仕組みは書けていたが**確認が1つも無かった**ので、e2e に1件足した
        （09-モーション「印を打った物をプレビューで動かすと、その時刻に印が付く」）。
      ※ **歯があることも確かめた。** `useTelopBox` の putKey を止めると赤くなる。
        なお最初は `usePreviewManip`（動画・画像側）を壊して確かめようとしたが
        赤くならなかった——**テロップの掴みは別の道**（`useTelopBox`）なので、
        壊す場所を間違えると「歯が無い」と誤解する。
- [x] 再生バーの下の UI が**2段**なのが気に入らない。まとめられる物はまとめて1段にする
      （そのぶんプレビュー画面が大きくなる）
      → 済（2026-08-01）。画質・fps・尺を見出し行へ出して1段に。実測で映像が 273→303px。
        経緯は `引き継ぎ-プレビューとタイムラインの大きさ.md`
- [x] **打ち直しの欄が、左右のパネルを押しただけで閉じてしまう**（2026-08-02）。
      打ちながら色やフォントを直しに行くのは同じ一続きの作業で、そこで閉じると
      **打ちかけの文字と「変えたかった選択そのもの」が消える**
      （左パネルの「その文字だけ変える」は、まさにその選択を見ている）。
      → 左右のパネルの一番外側に `data-editor-safe` を付け、「外」から外した。
        タイムライン・プレビュー・再生は今までどおり押したら完了。
      ※ **閉じる方も一緒に確認する。** 片方だけ見ると「どこを押しても消えない」に
        してしまっても気づけない（＝Enter を押すまで終われない状態に戻る）。
        確認は e2e「打ち直しの欄は、左右のパネルを押しても消えない（タイムラインでは消える）」
- [x] テロップをダブルクリックすると文字を打てるが、**他をクリック・再生したときに閉じない**。
      Enter を押したときと同じ挙動で閉じる。
      **「どこか他を押した瞬間に完了」と同じ扱いにする**（押した先を問わない）。
      → 済（2026-08-02）。**素直に click を見張っても直らない**（タイムラインの
        クリップは pointerdown を自分で止めるので window まで届かない）。
        capture で拾い、押した場所が `.telop-editor` の中かどうかを自分で見る形にした
        （`state/useDismissOnOutside` に `inside` を追加。閉じ方は1か所のまま）。
        再生を始めたときは押した先が無いので、別に見張りを足した
- [x] **複数選んだテロップを、プレビュー上でまとめて操作できていない**（e2e が2件落ちていた）。
      → 済（2026-08-02）。**まとめ操作の側は最初から正しかった。**
        原因は手前の1行で、**テロップを Ctrl+クリックで足せていなかった**
        （何個押しても常に1個だけ選ばれる）。だから「選択中: 1」だった。
        `state/useTimelineDrag.ts` が `clearAll` を **`clearSegSel` という名前で
        受けていた**のが元。実際はテロップの選択も消すので、すぐ下の
        Ctrl+クリックが「同じバッチで空になった配列」に足していた。
        効果音・画像・映像レイヤーは同じ罠を `toggleSelect`（絶対値）で
        避けており、テロップだけ関数updater のまま取り残されていた。
        → 名前を `clearAll` に戻し、テロップも `toggleSelect` に揃えた。
        ※ **型検査にも `noDuplicate` にも掛からない型。**「名前の付け間違い」は
          いまも機械で止まらない（`engineering/CLAUDE.md` の「まだ検査が無い」）

## タイムライン

- [x] **Ctrl+ホイールでも拡大**できるように（限界まで拡大したいことがある）
      → 既にできていた（Alt でも可）。限界が足りていなかっただけ
- [x] 拡大バーの範囲を**いまの倍くらい**に広げる（テロップ設定などで広く使いたい）
      → 済（2026-08-01）。120 → 240。限界は `state/useView.ts` の ZOOM_MIN / ZOOM_MAX
- [x] **拡大の UI を、下のバーそのものに変える**（2026-08-02 追加）。
      いまの拡大 UI はやめる。下のバーの**左右に丸いボッチ**を付けて、
      左右に動かすと拡大・縮小。**最小の大きさは決める**（潰れないように）。
      同時にそのバーが**タイムラインの移動**も兼ねる（プレミアと同じ形）
      → 済（2026-08-02）。真ん中を掴めば移動、左右の●で拡大・縮小。
        道具立てからつまみを外した（**2か所で操れると、「どこを見ているか」と
        「どれだけ寄っているか」が別々の道具になって迷子になる**）。
        「↔（フィット）」は押す物なので残した。
        割合と拡大率の行き来は `shared/zoomBar`（試験11件）。

      **作ってみて分かったこと3つ**（どれも確認が捕まえた）:

      1. **つまみの細さに下限を置いてはいけない。** 拡大の上限と2つの制限が
         ぶつかり、短い素材では**上限まで寄れなくなる**（Ctrl+ホイールは別の道
         なので、そちらでは寄れて食い違う）。制限は拡大率だけにして、
         潰れたつまみは**描くときに最低幅**を持たせる（CSS の min-width）
      2. **幅が新しくなってから位置を当てる。** 先に当てると、まだ古い（狭い）
         中身の幅で切り詰められ、掴んでいない側の端まで動く。
         Ctrl+ホイール側も同じ理由で1コマ待っていた
      3. **横に送っただけでは描き直しが起きない。** 拡大率が変わらない操作
         （掴んで動かす・ホイールで横へ送る）では何も再計算されず、
         **つまみだけ置いていかれる**。バー自身がスクロールを見張るようにした
      ※ つまみを外したことで `zoomAroundPlayhead` が誰からも呼ばれなくなったので
        消した（**死んだコードを残さない**。前に同じ物を残して事故っている）
      確認は e2e「下の拡大バーで、拡大縮小と移動ができる」
- [x] **リップル削除で再生ヘッドが画面の外へ飛んだら、タイムラインも合わせて動かす**
      （2026-08-02 追加）。画面の端に来る場合は**少し内側**に置いて見やすくする。
      ※ 飛んだときだけ。飛んでいないときは動かさない
      → 済（2026-08-02）。**その動きをする物は既にあった**（`revealPlayhead`。
        見えていれば何もしない／外なら真ん中へ連れてくる。端ぎりぎりに置かないよう
        余白も見ている）。リップル削除の2か所から呼ぶだけだった。
        ※ 立たせるのは**次の描き直しのあと**（`setTime` の結果を待つ）。
          すぐ呼ぶと、動く前の位置を見て「もう見えている」と判断してしまう
- [x] **レーンからレーンへ移動**したい
      （本人「置いたテロップや画像が**レーン固定で他レーンに動かせなかった**」）
      → **原因が分かって直した（2026-08-03）。** 効果音・画像・映像レイヤーは
        「動き出したか」を**横のずれだけ**で見ていた（`Math.abs(ev.clientX - sx) < 3`）。
        **テロップだけ縦も見ていた**（`|dx| + |dy| < 3`）ので、テロップは動くのに
        他は真上・真下へ振っても「まだ動いていない」と判定され、
        **段を変える所まで一度も進まなかった**＝置いた段から動かせない。
        斜めに振れば動いていたので「たまに動く」ようにも見えたはず。
      → 3か所とも縦も見るように揃えた（`state/useClipDrag`）。
      確認は e2e「テロップを縦に振ると、別の段へ移せる（往復とも）」「画像も別の段へ移せる」
      （直す前は画像が `V2 が 0 → 0`。テロップは直す前から緑＝片方だけ壊れていた）
- [x] テロップが下まで行ってから次を展開すると、**1からではなく途中から**表示される
      → **原因が分かって直した（2026-08-03）。場所は本人の答えで確定＝右パネルのテロップタブ。**
        `VirtualBlock`（見えている分だけ描く箱）が、**スクロールの中で自分がどこから
        始まるかを「測る前は 0」と決め打っていた**。一覧を下まで送った状態で別の節を
        開くと、その節の1回目の描画で `scrollTop - 0` ＝ 送った量そのものが
        「この箱の中でのスクロール量」になり、**一覧の途中から描き始める**。
      → 直し: 測れるまで窓を掛けない（高さ `rowH` は元からその扱いだったのに、
        位置だけ当て推量になっていた）。`useState<number|null>(null)` にして揃えた。
      → あわせて、節を開いたあとの位置合わせも粘らせた（前は2コマ待って1回だけ。
        中身の高さが落ち着くまで合わせ直す。`useLibraries` の `alignSecTop`）。
      ※ **検査では捕まえられていない。** e2e「下まで見てから次の節を開くと、その節の
        頭から出る」を足したが、**わざと前の形へ戻しても緑のまま**。見本を120個
        入れて一覧を伸ばしても再現しなかった。**直しは読んで確かめた物**で、検査は
        「節を開いたのに頭から出ない」を丸ごと落とさないための見張りにとどまる。
      **次に確かめたいこと**: 実際に使っている見本の数で直ったかどうか（本人の目で）
- [x] テロップ同士が隣り合っているとき、**間を調整すると両方が反応**する
      → **原因が分かって直した（2026-08-02）。** 帯の幅の式が**6か所に書き写されていて、
        テロップだけ 1px を引き忘れていた**。他の帯はどれも `長さ×拡大 - 1` で
        1px 空けているのに、テロップだけ時間どおりに描いていたので、
        **前の尻と次の頭がぴったり接し、両方の持ち手が同じ画素に重なっていた**
        ＝どちらを掴んだのか決まらない。
      → 式を `lib/bandGeom` の `bandWidth` に1つへ寄せ、6か所ともそこを通した
        （下限だけ種類ごとに渡す。下限を大きくすると隣に食い込むので、
        そこは種類ごとに決める必要がある）
- [x] **ネスト**が欲しい。動画を複数選んで（Ctrl で複数選択）右クリック →「ネストする」。
      テロップなら1つのテロップとして、動画なら1つの動画として扱う。
      ※ **Ctrl で複数選ぶ方は既にできる。** 動画・効果音・画像・映像レイヤーは
        元から効いており、テロップだけ効いていなかったのを 2026-08-02 に直した

      → **作った（2026-08-02）。** 決めたとおり「組」の形。経緯・測った数字・
        わざとやっていない事は **`引き継ぎ-2026-08-02-ネスト（組）.md`**。
        `npm run verify` 791件 緑／絞った e2e は 17+35 件とも緑。

      **決めた中身（2026-08-02）。「組」にする。**

      「ネスト」は2通りに割れる。プレミア本来の意味（別のシーケンスへ押し込んで
      1本のクリップとして出し、ダブルクリックで中へ入る）は、**新しい入れ物・再生・
      書き出しの平坦化まで要る**——この列で一番大きい。
      **本人の狙いは「毎回選び直すのが面倒」**なので、そこまで要らない。

      → **採る形**: 選んだ物に「組」の印を付け、**掴む・動かす・消す・写すが常に
        組ごと効く**ようにする。中身はタイムラインに見えたまま。右クリックで
        「組を解く」。**書き出しには一切影響しない**（印を見ない）。

      **作った順（全部済み）**:

      1. `shared/group.ts` に純粋な判定＋試験（38件）
      2. 持ち物に `group?: number`（テロップ・効果音・画像・映像レイヤー）。
         読み直しで壊れていたら捨てる。**本編の切片（V1）は入れない**——
         切片は隙間なく連続する決まりなので単独で動かせない
      3. 右クリックに「ネストする」「組を解く」（テロップの品書きにも同じ物を出す）
      4. 選ぶ所を組ごとに広げる。**入口は `useNestSelectSync` 1か所**（効果1つ）

      ※ **段をまたぐ組を許す。** 動画とテロップと音をまとめて1つ、が本来やりたいこと。
        許さないと「まとめたいのにまとまらない」で終わる

      **わざとやっていない（作り忘れではない）**:
      ・端のトリムは組ごとに効かない（長さは物ごとに違う）
      ・上下の段を変えるのは掴んだ物だけ（全員同じ段へ寄せると重なって壊れる）
      ・本編の切片（V1）は組に入らない

      **検査**: e2e に3件（`--only=ネスト,組`）。わざと壊して赤くなるのも確かめた。
      「複製した組は元と繋がらない」は単体だけ（`remapGroups`）
- [x] 置いたときに上書きしない設定：**Shift を押しながら離すと、置いてあるクリップが
      上書きされず後ろへずれる**（いまは Ctrl+Shift でずれる）
      → 済（2026-08-02）。Shift だけでも割り込みになる。Ctrl も残した（慣れている手を壊さない）。
        決め事は shared/dragMode の dragModeOf（Alt が勝つ＝複製しながら割り込む動きは無い）
- [x] **吸着（マグネット）が弱い。範囲を広げる。**
      → 済（2026-08-02）。**8px → 18px**（state/useSnap.ts の SNAP_PX）。
        **画面pxで持つのが肝。** 秒で持つと拡大するほど吸い付きが強くなり、
        「近づけただけで勝手に付く」＝細かく置けなくなる。見た目の距離で決めれば
        どの拡大率でも手応えが同じ。
        これ以上広げると隣の端まで巻き込んで**狙っていない所へ付く**

## テロップの重なり・吸着・レーン（2026-08-02 追加）

**まとめて出た6件。持ち場が近いので、まとめて着手する。**

- [x] **重なったテロップは、重なった分を上書きして消す。**
      1つ目の尻が2つ目の頭に食い込んだとき、いまは**重なったまま**になっていた
      （動画クリップは上書きされるのに、テロップだけ違う）
      → 済（2026-08-02）。落とした瞬間に、**同じ段で重なった分は置いた側が勝つ**。
        判定は `shared/overwrite` の `subtractRanges`（試験9件）。
        ※ **真ん中を抜かれたら左右2つに割れる。** ここで片方を捨てると、
          残せたはずの文字が黙って消える。テロップは元の素材を持たない（文字だけ）
          ので、割っても失う物は無い。
        ※ **新しい id は updater の外で作る。** 中で idCounter を回すと
          StrictMode の2回走りで番号がずれる。作りきってから絶対値で入れている。
        ※ 端が接しているだけは「重なっていない」（重なりと見ると、隙間なく並べた
          テロップが置き直すたびに削られていく）。
        確認は e2e「テロップを重ねて置くと、重なった分が消える（上書き）」
        （**殺すと本当に赤くなることを確かめてある**）
- [x] **複数選んだときの吸着が、掴んだ1つにしか効かない。**
      複数なら**選んだ全体の頭とケツ**に吸着があるべき
- [x] **【要調査】吸着がケツに効いていないのでは。** 頭だけ効いている疑い
      → **そのとおりだった（2026-08-02）。** テロップの移動だけ `snapTime`
        （**時刻を1つだけ**寄せる道具）を通っていて、掴んだ物の**頭しか見ていなかった**。
        効果音・画像・映像レイヤーは `snapClipStart`（頭と尻の両方＝`shared/snap`
        の `nearestSnap`）を通っており、**テロップだけ取り残されていた**
- [x] **上下に動かすと、横位置がズレる。**
      動かす前の位置を覚えておき、**その位置自体にも吸着**を置く
      → 上の3件はまとめて済（2026-08-02）。**寄せるのは「掴んだ1つ」ではなく
        「動かしている束の全体」**（左端〜右端）にした。1つだけ選んでいるときも
        同じ道を通るので、ケツにも効くようになる。
        あわせて**元の位置（束の左端・右端）を寄せ先に足した**ので、上下に運ぶ
        だけのつもりのときは元の位置へ戻ってくる。
        置き場は `state/useSnap.ts` の `snapClipStart`（`more` で「動かしている物を
        外す」「その場かぎりの寄せ先を足す」を渡せるようにした）。
        ※ **判定そのものは shared/snap のまま。** 画面側に式を増やしていない
- [x] **まだ作っていないレーンへテロップを動かせない。**
      1段上まで持っていったら**そこで勝手に作られる**ようにする
      → 済（2026-08-02）。一番上より更に上へ運ぶと `addVideoTrack()` で1本足す。
        **足すのは1回の掴みにつき1本まで**——足した瞬間に行が1つ下へずれて
        「もう一番上ではない」状態になるので普通は止まるが、掴み損ねて画面の外まで
        走らせたときに段が増え続けるのだけは避ける（増えた段は手で消すしかない）。
        ※ 段の並びは**掴んでいる最中に読み直す**（`tracksRef`）。掴んだ時の並びの
          ままだと、足した段が使えない＝足しても行けない
- [x] **新しく作ったレーンで効かないものが多い。**
      例: レーンを選んでもトラック選択が機能しない
      → **原因が分かって直した（2026-08-02）。** トラック選択の道具が、テロップの段を
        **`V2` と `V3` の決め打ち**で見ていた。足した段（V4以降）に載せたテロップは
        一度も選ばれない。「V1 以外の映像の段」に直した（移動の並びと同じ決まり）。
        ※ **同じ型がまだ他にもあるかもしれない。** `'V2'` `'V3'` を名指ししている所は
          他に数か所ある（既定値として使っているぶんは問題ない）。
          新しい段で何か効かないと気づいたら、まずそこを疑う

## トランジション

- [x] **間（クロス）に入れても付かない／ケツに入れると背景が見えながら切り替わる**
      → 調べた（2026-08-02）。**どちらも作りどおりで、見え方の問題だった。**
      ・「間に付かない」… 間のクロスは**次のクリップの「頭より前」の素材を重ねて**
        切り替える。次のクリップが素材の先頭から始まっている（頭を切っていない）と
        重ねる余地がゼロ＝長さ0。しかも  のときしか帯を描いていなかったので、
        **付いているのに何も見えない**状態になっていた。
        判定は  の （ で頭打ち）。
        → **効いていない帯を斜線で出す**ようにした（消さない。あとで頭をトリムすれば
          効き出すので、消すと置き直しが要る）。押すと理由が出る。
      ・「ケツで背景が見える」… 最後のクリップの尻は「次」が無いので、
        送り出した先には背景しかない。**仕様どおり**。
      ※ 本人が「今やったら直った」のは、2本目の作り方が違っただけ
        （Ctrl+K で切った2本目は srcStart > 0 なので効く）。

## 効果音（SE）

- [x] SE は音声トラックに入るのはよいが、**レーンまで固定しないで**ほしい
      （いまは一番上のレーンに固定）。判定は mp3 かどうかだけなので、置き場所は自由に
      → 済（2026-08-02）。**掴んで落とす方は元から狙った段へ行っていた。**
        固定されていたのは**素材をダブルクリックして置く道**で、`'A2'` 決め打ちだった。
        → 段を選んであればそこ、無ければ**その時刻が空いている一番上の段**へ。
        判定は `shared/lanes` の `pickAudioLane`（試験4件）。
        ※ **本編の音（A1）は渡さない。** 渡すと元の音の上に置いてしまうので、
          包み（`audioLaneFor`）の側で外している——呼ぶ側ごとに外すと忘れる
        ※ `useAppWiring` を**太らせない**（当時 1,254行で検査に止められた）。
          判定を shared へ出して呼び出しを1行にした。
          ※ いまは 362行だが、方針は同じ——**フックを足すなら囲いを作る**
            （`state/wiringSize.test.ts` が見張っている）
- [x] 映像トラックにマウスがあるときだけ出る影は、そこにテロップ等が入っていたら1段ずらす
      → 済（2026-08-02）。狙った段がその時刻に埋まっていれば**1段ずつ上へ避ける**
        （判定は `shared/lanes` の `avoidBusyLane`・試験4件）。
        見るのはその段に乗り得る物すべて——テロップ・画像・重ねた映像。
        ※ **影と実際の置き先は必ず同じ判定を通す**（別々にすると影が嘘をつく）。
          `useMediaDrop` の `imgLaneAt` 1つに寄せて、影・落とし・端で受けた分の
          3か所ともそこを通した。
        ※ 上まで詰まっていたら狙った段のまま（置かないより、上書きでも置く方が
          分かりやすい。段を足すかどうかは人が決める）

## モーション・キーフレーム

- [x] ~~**付けた動きが、保存して開き直すと消えている**~~ **不具合ではなかった**
      （2026-08-02 に確認）。保存する側は正しい。
      通しでは元々緑で、**絞って回したときだけ赤くなる確認**だった:
      ・その確認は動きを自分で付けず、前の項目が付けた物に寄りかかっていた
      ・自分で付けるようにしたら、今度は準備の最後で `.mo-head` を押していて、
        そこが「動きを消す」ボタンの当たり所だった＝打った印を自分で消していた
      → 確認を自分で完結するようにし、手を離すのは blur に変えた（押さない）。
      **「保存に動きが入っていない」という文言は、保存漏れにも準備の失敗にも
      同じように出る。** 赤を見たら、まず準備が成立しているかを見ること
- [x] **キーフレームを付けていないとモーションをコピペできない**。関係なくコピーさせてほしい
      → 済（2026-08-02）。印が無い項目は**いまの値だけ**を写し、貼る先の行へ流し込む。
        印が無い行の値は、テロップなら pos/scale、クリップなら zoom… と持ち主が
        ばらばらで、写す側からは辿れない。**行そのものが読み書きを持っている**ので、
        行を控えて（motionRowsRef）そこから値をもらう形にした。
        ※ MotionTab が行を外へ渡す effect は**中括弧で包むこと**。
          `() => onRows?.(...)` だと行の配列が effect の戻り値になり、React が
          「後片付けの関数ではない」と見なして**タブごと落ちる**（実際に空になった）
- [x] モーションを**1つずつコピー**したいときもある
      → **元からできていた**（2026-08-02 に確認）。モーションタブの行を選んでから
        Ctrl+C すると、その行だけが写る（Ctrl で足し引き／Shift でまとめて）。
        見出しを押すとその組がまとめて選ばれる。上の直しで、
        **印が無い行も1つずつ写せる**ようになった
- [x] **アンカーを出すボタン**が欲しい（どこへ向かって拡大するのかを決められない）
      → **済（2026-08-02）。** リフレーム枠のバーに「◎ 拡大の中心」。押すと ◎ が出て、
        掴んで置いた所へ向かって寄るようになる。もう一度押すとしまう。

        **決めたとおり、アンカーは画面だけの道具にした**（下の「やってはいけない作り」）。
        置いた結果は今までどおりの x/y に入るので、**書き出し側の式は1つも増えていない。**
        計算は `shared/clipMotion` の `zoomOffsetForAnchor` / `anchorOfZoom`。
        確かめ方は「組み立てた ffmpeg の列を読み直して、基準点の画素が寄る前後で
        同じ元画像の点を指すか」（式の形ではなく行き着く先で見る）。

        **作ってみて足した物が3つある**（決めた時に見えていなかった所）:

        1. **四隅を掴んだときだけでは足りない。** 拡大が変わる道は他にもある
           （モーションタブの数値欄・属性の貼り付け）。そこを通ると中心へ寄ってしまい、
           基準点が「掴んだときだけ効く飾り」になる。→ 出している間は**どこで拡大が
           変わっても引き直す**見張りを置いた（`usePreviewManip` の useEffect）。
           輪にならないのは、引き直した先がその式の**不動点**だから
        2. **等倍のときは何もしない。** 等倍では基準点がどこでも位置は 0 なので、
           合わせに行くと「等倍のまま横へずらす」が 0 に引き戻されてできなくなる
        3. **掴んで動かしたら基準点も付いていかせる。** 絵をずらせば止まっている点は
           別の場所になる。付いていかせないと、見張りが動かした先から引き戻す
           ＝**パンできなくなる**
        ※ 基準点は**誰に付いているか**も一緒に持つ。持たないと、別のクリップを
          選んだ瞬間にその子の位置を勝手に書き換える
        ※ 元に戻す（Ctrl+Z）は、拡大の変更と位置の引き直しで**2回ぶん**積まれる
        確認は e2e「拡大の中心を決めると、そこへ向かって寄る」
        （**見張りの1行を殺すと本当に赤くなることを確かめてある**）

      **やってはいけない作り**（＝採らなかった道。同じ話が出たときのため残す）:
      拡大の基準点を新しい持ち物として足し、
      画面と書き出しの両方でそれを見るようにする。
      画面は CSS の `transform-origin`、書き出しは `zoompan` の式——
      **同じことを2つの別々の式で書く**ことになり、この部門で一番危ない型
      （画面では正しく見えるのに書き出すとズレる）にまっすぐ入る。

      **採るべき作り**: アンカーは**画面だけの道具**にして、動かした結果を
      **いまある位置（x/y）へ書き込む**。数式は1本:

          x = (0.5 - ax) * (s - 1)      ax は 0..1 の基準点、s は拡大率

      こうすると書き出し側は**今までどおりの x/y を見るだけ**で、
      新しい式が1つも増えない＝ズレようがない。

      **気をつける所**: 拡大に印（キーフレーム）が付いているときは、
      s が時間で変わるので x/y も同じ時刻に印を打つ必要がある
      （打たないと、寄りの途中で基準点がずれていく）。

      置き場: 画面は `state/usePreviewManip`（枠のバーに「拡大の中心」）、
      計算は `shared/clipMotion` に純粋な関数として足して試験を付ける。
- [x] **キーフレームの削除**ができない。触っていることが分かるマウスの形を作って、
      テロップや動画に付いた印を消せるように
      → 済（2026-08-02）。タイムラインの◆を**右クリックで1つ消せる**。
        乗せると形が指に変わり、白く光る（触れると分かるように）。
        テロップ・動画切片・画像・映像レイヤーの4種類とも同じ操作。
        ※ **消すのは右クリック。** 左にすると、クリップを選ぼうとして印に当たった
          瞬間に消える（打った物が黙って消えるのが一番困る）。
        ※ ◆は「その時刻に印がある項目」をまとめて1つ出しているので、消すときも
          同じ時刻の印を全項目から外す（見えている◆と一致させる）。
        心臓は `state/useMotion.ts` の `removeKeyAtTime`。
        確認は e2e「タイムラインの印を右クリックすると、その印だけ消える」

## 入力・操作まわり

- [x] XY などの**無限マウス調整で、触った瞬間に数字が飛ぶ**ことがある
      → 済（2026-08-02）。原因は**ポインタをロックした瞬間の1回**。ロックが効くと、
        その最初の1回だけ「元のカーソル位置から画面中央まで」の距離が movementX として
        まとめて届く（数百px）。**その1回だけ捨てる**ようにした。
        ※ 「大きい値を全部捨てる」やり方にしてはいけない。速く振った1回まで捨てて
          しまい、上限まで一気に持っていく操作ができなくなる（実際に確認が落ちた）。
        置き場は `components/ScrubNumber.tsx`。
        **戻すと本当に落ちることを確かめた確認**を2件足してある
- [x] **クリックが多い。** 右パネルの要素をタイムラインへ入れるとき、クリックは据え置きで
      **D&D でも持ってこられる**ように（アプリ全体でこの方針）
      → **見本帳とアイコンを、タイムラインのテロップ帯へ落とせるようにした（2026-08-03）。**
        素材ビン・効果音・トランジション・出入りの動きは元から掴めていたので、
        残っていたのはここだった。
      ※ **動きの見本（トランジションタブの「動き」）だけ、まだクリック専用。**
        掴めるようにするには「いま掴んでいる物」の札を1本増やす必要があり、
        それを配る `useAppWiring.tsx` が上限ぎりぎり（残り0行）だった。
        **リファクタで場所を空けてから**入れる
- [x] テロップや動画に D&D で要素を入れたら、**それを選択状態にする**
      → 済（2026-08-02）。見本帳（テンプレート）・アイコン・出入りの動きの3つとも、
        落とした先を選んだ状態にする。落とした直後は微調整に入るので、選ばれて
        いないと右パネルが別の物を出したままで、もう一度クリックが要る

## 音

- [x] 調整していないなら **Windows の音量と揃える**
      → **既にそうなっていた**（2026-08-02 に確認）。既定は 1.0＝アプリ側で1ミリも
        絞らない＝Windows で聞こえる大きさそのまま。Windows 側の数値を読みに行くのは
        別の話（ネイティブが要る）なので、やっていない
- [x] **音量バーをアプリの中**に置く
      → 済（2026-08-02）。プレビューの見出し行に 🔊＋つまみ。**ミキサーのマスターと
        同じ値**なので片方を動かすと両方動く。🔊 で消す／消す前の大きさに戻す

## 波形

- [x] **音声に色を付けると波形が見にくい。波形は既定の色で固定してほしい**
      → 済（2026-08-02）。**波形の色は元から固定**（#2196f3）だった。
        見にくくなるのは、帯の背景が付けた色そのものになるため（青いラベルに青い波形）。
        → 波形の下に**半透明の暗い敷き**を1枚置いた。付けた色は暗くなって残るので、
          色分けは効いたまま波形が読める。
        ※ 背景に合わせて波形の色を変える手もあるが、それだと
          「どの音がどの波形か」を色で覚えられなくなる。**波形の色は固定が正しい。**

## 素材ビン

- [x] **素材をまとめて選んで、まとめてタイムラインへ置けるようにする**（2026-08-02 追加）。
      **段階1・2とも済み**（下の各行のとおり。e2e もある）。チェックだけ付け忘れていた。
      いまは動画が複数あってもまとめて選べない。
      ・**Ctrl+クリックで複数選択**できるように → **済（2026-08-02）**
      ・選んだ物をタイムラインへ D&D すると、**押した順**にクリップが続けて並ぶ
        → **済（2026-08-02）**
      ・**全選択・ドラッグで囲んで選んだとき**は「名前順で数字が小さい方から」
        選んだのと同じ扱いにする（1・2・3 を適当に囲んでも 1→2→3 の順で並ぶ）
        → **Shift+クリックの範囲を先に入れた（2026-08-02）**。
          並びは**画面に出ている順**（素材は種類ごとに分けて並べているので、
          渡された順のままだと見た目とずれる。`KIND_ORDER` から組み直している）

      **段階2（まとめて置く）も入った（2026-08-02）。**
      1つ置くごとに、次の置き場所を「置いた物の終わり」へ進める。
      置き方は `useMediaDrop` の `placeDropped` 1つに寄せ、タイムラインへ落とした分と
      端で受けた分の**両方をそこへ通した**（別々に書くと片方だけ直る）。
      ※ **1つだけのときは今までと同じ道**（長さの測り直しもしない）。
        束のときだけ測る——画像は決め打ち5秒、音と映像は測り、
        測れなければ2秒（**止まるより、短くても置いて手で直せる方がよい**）。
      ※ **掴んだ物が選択の中に居るときだけ束で運ぶ。** 選択と関係ない物を掴んだら
        それ1つ——選んであることを忘れて別の物を掴んだときに、
        覚えのない素材まで置かれるのが一番困る。
      確認は e2e「まとめて選んだ素材は、その順に続けて並ぶ」
      （**続けて並ぶ＝重ならないことまで見る**。同じ所へ2つ落とすと上に乗るだけで、
      置いたつもりが1つしか無いように見えるため）

      **段階1（選ぶ所）**:
      選択は**押した順を覚える配列**にしてある（`selectedMediaIds`）。
      1つだけ扱う所のために `selectedMediaId`（最後に押した物）も残してあるので、
      使う側を全部書き換えずに済んでいる。
      **段階2（まとめて置く）は未着手。** 動画は長さを測ってからでないと次の
      置き場所が決まらない（非同期）ので、落とす側を順に流す形へ作り直す必要がある。
      確認は e2e「素材はまとめて選べる（Ctrl で足し引き・Shift で範囲）」

## 右パネル

- [x] テロップと同じで、**触っていない所は畳む**方式にする（お気に入りは除く）
      → **節そのものは元から畳めた。** 足りなかったのは**覚えておくこと**——
        組み立て直すたびに既定へ戻るので、選ぶ物を変えるたびに開き直す羽目になっていた
        （＝触った所まで畳まれる）。開け閉めを覚えるようにした（2026-08-03）。
        **触っていない所は畳んだまま、開いた所は開いたまま。**
      ※ 画面の都合なのでプロジェクトには入れない（localStorage）。

## 段（トラック）

- [x] **音声の段は、波形を出す物以外は一番細いのを既定にする**（2026-08-02 追加）
      → **既にそうなっていた**（`useLaneHeights` の `DEFAULT_LANE_H` は `{ A1: 44 }` だけで、
        他は最小26px）。効いていなかったのは**保存済みの古い値が勝っていた**から——
        高さは1つずつ localStorage に覚えているので、既定を変えても
        前に触ったことのある画面は前のまま。
      → **戻す口を付けた**（見出し列のいちばん上の「⤒」）。下の「引っ越しが効いていない」
        と同じ根で、そこで「やるなら初期化ボタンの方が筋がよい」と決めてあった通り。
        **黙って書き換えない**——その太さに慣れた人の画面が理由もなく変わるため
- [x] **段の名前を V1 / A1 の形で揃える**（2026-08-02 追加）。
      「音声」「SE」などの**注釈は要らない**。V シリーズと同じ見た目にする
      → 済（2026-08-02）。既定を番号だけにした（`lib/trackState` の DEFAULT_TRACKS）。
        **名前はプロジェクトに保存される**ので、既定を変えるだけでは開き直した人の
        画面が前のままになる。**前の既定の注釈（「V2 テロップ」など）に限って**
        読み込み時に番号へ戻す（`normalizeTrackName`）＝自分で付け替えた名前には触らない。
        アイコンの割り当て窓にも V2 だけ注釈を足す所があったので外した
- [x] **段の太さの「前の既定からの引っ越し」が効いていない。**
      前の既定（映像34/音声52）がそのまま保存されている人を、新しい既定
      （一番細い）へ移すつもりのコードが App.tsx にあったが、**誰も呼んでいない
      死んだコードだった**（読む場所が state/useLaneHeights へ移ったときに
      置き去りになった）。2026-08-02 の切り出しで消してある。
      いま復活させると、その太さに慣れた人の画面が黙って変わる。
      **やるなら「初期化」ボタンを足す方**が筋がよい。
      → **その口は既に作ってあった（2026-08-03 に確認）。** 段見出しの左上、
        ＋の隣の **⤒**（title「段の高さを既定へ戻す」）。押すと覚えている高さを
        丸ごと捨てて既定へ戻る（`useLaneHeights` の `resetLaneH`）。
        **黙っての引っ越しはやらない**方針のまま、決着している。
      → **検査が無かったので足した**（2026-08-03）。
        e2e「段の高さは『既定へ戻す』で戻せる」

## ~~通しで落ちている3件~~ → 片付いた（2026-08-04）。**アプリではなく素材だった**

赤3件（無音カット2件・ダッキング）は**全部1つの原因**で、コードには何も無かった。

```
確認用の素材は「Downloads にある いちばん小さい動画」から作る
  ↓ この日 src(5)〜src(8).mp4 が増えて、選ばれる物が入れ替わった
  ↓ 新しい方は 20秒に無音が 1か所も無い（旧 giftcaut_output は 0.305秒が1か所）
無音カット「切る所が無い（0秒）」→ 2件が赤
useSilenceDuck「無音0か所 → 曲線は空」→ 書き出しに volume=eval=frame が載らず 1件が赤
```

**作り物のフォールバック（途切れない 440Hz のサイン波）にも無音は無い**ので、
この3件は**最初から Downloads の中身の運で通っていた**。

直した所（`e2e/lib/e2eFixture.mjs`）:

- 素材の**決まった3か所（2.5-3.0 / 6.0-6.6 / 10.0-10.4秒）を黙らせてから**渡す。
  絵も音も本物のまま、`-c:v copy` なので焼き直さない
- 仕込んだ後に **数えて、足りなければ落とす**（素材が短い日にまた
  「アプリが壊れた」の顔をして出てくるのを防ぐ）
- `02b` の `cutSecs()` が読めないとき **0 を返していた**のをやめた。
  「本当に0秒」と「文が読めていない」が区別できず、切り分けに半日かかった

切り分けの手順（同じことが起きたとき用）: `--only=無音カット,ダッキング --fast` を
**いまの `src/`** と **`git checkout <その日の頭> -- src/`** で1回ずつ。
同じ `--only` どうしなら順番の条件がそろうので比較になる。今回はどちらも同じ赤3件で、
`src/` は無罪と分かった。

