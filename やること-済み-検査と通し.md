## リファクタ（2026-08-03 夜に大きく進めた）

**目的は行数ではない**——不具合が来たとき・要件が増えたときに的確に直せること。
詳しくは `引き継ぎ-心臓の分け直し.md`。

- [x] **読める形の検査を入れる**（`shared/readability.test.ts` 5件）。
      冒頭コメント／500行超は割るか取説／**取説が腐っていない**
- [x] **式で見る重複の検査を入れる**（`noDuplicate.test.ts` に5件）。
      名前が違っても捕まる。**入れた最初の1回で本物を1件見つけた**
- [x] **書き出しが時間計算を手書きしていたのを直した**（実害）。
      `exportRun` に正典の `Math.max(0, …)` が抜けていた
- [x] **8本を割る／取説を付けた。** `StylePanel` 以外は全部またぐ名前0
- [x] **`exportRun`（1,229行）を3つに割った**（2026-08-03 夜）。
      → **「まず取説を作る」は実測で塞がっていた。** top-level の callable が
        `isExporting` と `registerExportHandlers` の**2つしか無く**（1,012行が
        `export:run` の中の1コールバック）、取説を書いても2行で
        「これしか無い」という嘘の案内になる。**だから先に割った。**
      → 話題で3つ: `exportRun` **1,229 → 479**（受け口。500未満なので取説不要）／
        `exportGraph` 893（組み立て・取説18項目）／`exportSpawn` 258（走らせる・取説8項目）。
        **依存は一方向**で、組み立てと実行は互いを知らない
        （「グラフが悪いのか ffmpeg が悪いのか」を切り分けられる形の条件）。
      → 経緯・危なかった所・見つけた本物3件は `引き継ぎ-心臓の分け直し.md`。
        確認は `--only=書き出` 22件緑、`--only=書き出,トランジション,ディゾルブ` 23件緑。

- [x] **`exportGraph`（946行）をさらに4つに割った**（2026-08-04）。
      → **18項目の取説が、話題を4つ宣言していた**（小道具／重ねる段／切片／音）。
        本文もその順に連続して並んでいた。**切り口は 08-03 に書いてあり、
        読み返さなかっただけ。**
      → 測る道具の**5つ目の穴**を踏んだ: `interface` / `type` の宣言を
        「連れて行く文」に数えていた。**両側に置ける物は数えない**（import と同じ）。
        直したら各群の「返す」が 2〜27 → **0〜1** になった。
      → `exportTypes` 169 ／ `exportFilters` 53 ／ `exportOverlays` 316 ／
        `exportSegments` 336 ／ `exportAudioMix` 142。**全部500未満・依存は一方向。**
      → `readability.test.ts` も1つ直した（取説に型を並べると嘘の赤が出た）。
        **並べる義務は関数だけ／型と目印は「並べてもよい」。** 型まで必須にすると
        フックの `*Deps` が全部並んで目次が読めなくなる（実測12ファイルが赤）。
      → 確認: `npm run verify` 緑／`--only=書き出 --fast` **22件緑**。
        経緯は `引き継ぎ-心臓の分け直し.md`
      ※ **いちばん危なかったのは `xfOf`。** 映像側と音声側が1つの閉じ込みを
        共有していて、そのまま切ると片方だけ別の式を持つ＝音と絵の尺がずれる。
        top-level の純関数 `xfadeDurOf` にして両方が同じ物を通るようにした。
      ※ **借金を2種類返した**: 取説なしで500行超 8→7本、正典のある式の写し 18→16件
        （`vcLen` を `shared/timeline` へ置いた。**残り11か所は画面側**）
- [x] **`useAppWiring` 1,229 → 362行**（2026-08-04）。**下の「割れない」は全部覆った。**
      → **道は縦だった。** フックを1本ずつ囲い（`state/*Context.tsx`）へ上げると、
        配線を経由しなくなるので**またぐ数は関係なくなる**。順番は `npm run passthrough`。
        太り直しは `state/wiringSize.test.ts` が見張る（フック12本・450行）。
      → **測った数字は全部正しかった。間違っていたのは「測った物」。**
        測っていたのは「この1,229行を横線1本でどこで切れるか」で、
        決めたかったのは「この1,229行をどうやって小さくするか」。
      → 以下は**当時の記録**（同じ誤りを繰り返さないために残す）:
      → **2026-08-04 に測り直した。またぐ名前は 300〜506個**（前の「106〜413」は
        **過小**だった——省略記法のプロパティを数え落としていた）。**横には切れない。**
      → 60行以上の連続範囲を全部試して、40の内側は**1つだけ**（`769〜831`・37個）。
        ただし**63行を出すのに37個受け取る**ので採らなかった
        （「40個以下なら割る」は撤回済み＝書く側の手間しか測っていない）。
      → **やったのは記録が指す本筋**: 束を小さくする。
        `rightPanel` 81個 → タブ5つのフックへ（`RightPanelArea` 410 → 114行）。
        **`useAppWiring` は1行も増えない**（タブのフックは自分で心臓を見に行く）。
      → **その先の順序（dialogs → menus → 測り直す）は、測ったら要らなかった。**
        「束を小さくすれば配線も分かれる」は**成り立たない**——どの束も外へ出すと
        **200〜400個を残る側へ返す**（束の入力を作っているのが、他の全部の束にも
        配っている共通のフックだから）。**網は消費側からはほどけない。**
      → ~~**「良い切り口を探す」はもうやらなくてよい。全部試して無い。**~~
        ——**試していなかった。** 探していたのは「横線を引く場所」だけで、
        当時すでに書いてあった本筋（**a) 各フックが自分の心臓へ直接書く形へ変える**）を
        「大きいから」と後回しにしていた。実際にやったら1日で終わった。
      → 経緯・測り方・踏んだ穴（省略記法）は `引き継ぎ-心臓の分け直し.md`

**借金**（増やさない。減らしたら検査の数字も一緒に下げる）。**置き場だけ書く**:

```
取説なしで500行超   readability.test.ts の DEBT_INDEX
冒頭コメント無し    readability.test.ts の DEBT_HEAD
正典のある式の写し  shared/canon.ts の CANON[].debt（照合は noDuplicate.test.ts）
```

※ **数はここに書かない。** 前は「16件」と書いてあったが、`vcLen` を返した日に
  嘘になった。**同じ物を2か所に持たない**——コードでやっていることを記録でもやる。

- [x] **`vcLen` の残り11か所を `shared/timeline` へ寄せる**
      → **全額返済（2026-08-03）。生の式は 0 件**（実際は13か所あった）。
      **寄せてみたら2つの型に割れていた**:
      ・**もう手に持っていた**（deps で `vcLen` を受けているのに、その行だけ生で書いた）
        … `useClipDrag` `useMediaDrop` `useTimelineDrag`×2 `useTimelineEdit` の**5件**。
        **同じ関数の別の行では受け取った `vcLen` を使っている**ので、
        「知らなかった」のではなく**書くときに思い出さなかった**だけ。
        → 探す前に「もう手に持っていないか」を先に見ると速い（1回踏んで学んだ）
      ・持っていなかった（import を足した）… 残り6件
      ※ `LeftPanel.tsx` は前者の極端な形で、**ローカル定数を `vcLen` と名づけて
        正典の名前を奪っていた**。`vcSec` に改名したら、**型検査がその場で
        残り2か所の使用箇所を名指しした**（影が消えた証拠）。
      ※ 画面側の元締め `useTrackGeom` も自前の式をやめて正典を import して配っている。
        ここが正典を通るので、deps で受けている側は**自動的に正典を使う**。

---

## **最優先**: 貼り付けたテロップが出てこないことがある（2026-08-04 の通しで発覚）

- [x] **`動きをコピーしたあとでも、クリップのコピー＆貼り付けは普通に効く` が赤**
      （`e2e/checks/09-モーション.mjs`）。**→ 解決（2026-08-04）。通し 256 / 256。**

      **アプリは正しかった。直したのは検査側。**

      「数が合わない」の答え: **貼り付け先に、まったく同じ範囲を占めるテロップが
      既に居た**（`貼=6.0-8.0@V2 元=1.0-3.0@V2,6.0-8.0@V2 結果=2`）。
      `overwriteCues` は負けた側を割るが、**完全に覆われた物は割れないので消える**。
      だから 2個 → 2個。上の「本来は4個」は**素材の位置を取り違えていた**
      （1〜3秒と5〜10秒だと思っていたが、実際は 1.0-3.0 と 6.0-8.0）。

      → **検査が「貼り付けは上書きしない」に黙って寄りかかっていた**（`39e4e22` 以前は真）。
        直したのは3点:
        ・空いている所（`seekTo(3.5)`）へ貼る。**なぜそこが空いているかをコメントで書いた**
        ・数える前に横スクロールを戻す（`toLeft()`）——**帯は見えている範囲にしか作らない**
        ・**成立しなければ落ちる**判定を2つ足した（`.mo-row.mo-picked` が0本、
          `.telop-clip.clip-selected` が1本）。前は「消えても緑」だった
      → 調べ方は**アプリ内に一時的な表示を出す**（e2e から値が見える）。
        `subtractRanges` を単体で叩くより速く、実際の値が取れた。

      **調べたこと（当時の記録）:**

      | | |
      |---|---|
      | いつ入ったか | **`39e4e22`**（前回の「貼り付け・複製の重なり」修正）。**二分探索で確定** ——`76a250e` では緑、`39e4e22` で赤 |
      | 今日の作業のせいか | **違う。** セッション開始時のコミットでも同じ赤 |
      | 絞っても出るか | **出る**（`--only=動きをコピーしたあとでも --fast`）。順番依存ではない |
      | 何を外すと直るか | `useCopyPaste` の `setCues(next ?? merged)` を **`setCues(merged)` にすると緑**（＝上書きの呼び出しが原因） |

      **症状**: 貼り付けてもテロップの数が増えない（`2個 → 2個`）。
      **これは確認の作りの問題ではなく、実害の可能性が高い**——
      「貼ったのに出てこない」は**黙って消える**型。

      ※ **`cuesRef.current` から読む形も試したが直らなかった**（閉じ込みの古さではない）

      **この件でいちばん効いた教訓:**
      **「推測で直さない」を守ったので、アプリを壊さずに済んだ。**
      `useCopyPaste` の上書き呼び出しを外せば緑になったが、それは
      **正しい振る舞いを消す直し**だった。数が合わないまま直していたら、
      「貼り付けだけ重なりを無視する」を静かに作っていた。

---

## 間欠的に落ちる確認が1つある（2026-08-03 深夜に測った）

- [x] **`文字を重ねても、流したときに絵が止まらない` が間欠的に赤くなる**
      → **済（2026-08-04）。数え始める位置が悪かった。**

      `Space` を押しても `play()` は非同期で、最初の絵が出るまでに数百ms かかる。
      その間を数えていたので、**合計＝最長＝250ms（5サンプル連続）が3秒に1回だけ**
      という形の赤が出ていた（主スレッド最長 0ms＝JS は詰まっていない＝本物ではない）。

      **絵が1コマ進むまで待ってから数え始める**ようにした（`stutterScan`）。
      始まらなければ「測れていない」で落とす。**しきい値は緩めていない。**

      ```
      直す前  4回中2回が赤（必ず 250ms ちょうど）
      直した後 4回連続で 止まり 0ms / 内訳も全部 0
      ```

      以下は当時の記録（**測る側の教訓として残す**）:

      | 回 | 状態 | 結果 |
      |---|---|---|
      | ① | 変更後（**裏で編集していて汚染**） | 止まり 250ms 赤 |
      | ② | 変更後（クリーン） | 止まり 250ms 赤 |
      | ③ | **変更前**（クリーン） | 0ms 緑 |
      | ④ | 変更後（クリーン） | **0ms 緑** |

      落ちるときは決まって **合計＝最長＝250ms**（`everyMs=50` なので**5サンプル連続**）で、
      3秒の測定中に**1回だけ**。しかも **主スレッド最長 0ms**＝JS は詰まっていない。
      → **再生開始の立ち上がりを数えている**のが疑い（`stutterScan` は Space を押して
        すぐ測り始める）。ただし**落ちた回の内訳（`r.why`）を取り損ねた**ので未確認。
      → 次に赤を見たら、まず `文字多め: … 内訳 {...}` の行を残すこと。
        `絵がまだ来ない` が立っていれば立ち上がり、そうでなければ本物。
      ※ **閾値を緩めて済ませない。** 100ms は「1コマ落ちても気づかない」の線で、
        緩めると本物の止まりを見逃す。直すなら**立ち上がりを測り始めから外す**方。

      ※ **今日ここで判断を1回間違えた。** ②と③だけを見て「変更前は緑＝私の変更が原因」と
        結論しかけた。**赤2回のうち1回は自分で汚していた**（測定中に編集すると、
        フックの `npm run verify` が同時に回る）ので、実質1対1でしかなかった。
        → **測る確認は、標本を1回ずつ比べて判断しない。**
        → **測っている間は編集しない**（このリポジトリは編集のたびに verify が自動で回る）。

---

## 通しで落ちていた4件 → **②確認の作りが脆かった。アプリは壊れていない**（2026-08-03 夜に決着）

- [x] **① 掴んで動かしても動かない** → **そんな事実は無かった**
- [x] **② 通しのときだけクリップが1つ消える** → **消えていない。窓の外に居ただけ**

```
✗ 掴んで右へ動かすと、その位置へ移動する
✗ 元の場所はただの空きになる（他のクリップは動かない）
✗ Ctrl+Z で元の位置に戻る
✗ 離すと丸ごと消えるクリップが、離す前に赤く縁取られる
```

**4件とも緑になった**（`--only=掴んで` 19件緑）。直したのは検査側の1か所。

### 正体

`掴んだまま右端まで引っぱると、タイムラインが送られる` が、
**Ctrl+ホイールで10回寄せ、端まで送った状態のまま終わっていた。**
画面の状態を戻すのは **`resetProject()` の中だけ**（`run.mjs` の `viewDrift` /
`restoreView`）で、次にそれが呼ばれるのは**赤4件より後**。
だから4件は「2.5倍に寄せて 4,862px 右へ送った画面」を測っていた:

```
理由: クリップの数が変わった（3 → 1）              ← 帯は見えている範囲にしか作らない
理由: 後ろのクリップが動いてしまった（実際: -2074）  ← 画面の左外へ送られただけ
画面の状態を戻します（見ている場所: 0 → 4862 / 拡大率: 5664px → 14400px）  ← 赤4件の"後"に出ている
```

`clipLayout()` は `boundingBox()` の**画素**を返すので、寄せ・送りがそのまま効く。
**消えても動いてもいない。**

### 「元からある不具合」という判断が成り立っていなかった

`9b5e70e`（リファクタ前）へ戻しても同じ赤が出たので、そう見えていた。
だが**原因の確認そのものが 08-03 15:44（`975c30e`）に足された物**で、
`9b5e70e` にも既に入っていた。**戻し先が原因より後だった。**

→ **教訓: 「変更前と比べる」は、比べる先が原因より前にあるときだけ効く。**
  新しく足した確認が原因のときは、どこまで戻しても同じ赤が出る。

### 「単独でも再現」も証拠になっていなかった

`--only=掴んで右へ` の `128 → 128` は、**その check が自分では何も動かしていない**から。
実際の drag は1つ前の check（`dragBy(v1Clips().nth(0), W * 0.35)`）にあり、
絞ると drag が起きないまま `before` と比べる＝**必ず同じ値**になる。

### 直した所

1. `e2e/checks/02-タイムライン編集.mjs` — 寄せる確認が**自分で `resetProject()` して終わる**
2. `e2e/run.mjs` — **画面を寄せたまま終わった項目を名指しする**
   （`viewDirtyRef`。窓の閉じ忘れと同じ扱い＝黙って直さず、誰の後始末かを出す）。
   **わざと外して、本当に名前が出ることを確かめてある**
3. `e2e/run.mjs` のコメント「後始末は各項目の合間に走る」が**実体と違っていた**
   （`resetProject()` の中だけ）。これで読み違えたので直した

---

## 通し 251 / 252（2026-08-03 夜・13.0分）**赤ゼロ**

上の4件は消えた。残る1件は落ちたのではなく**飛ばされていた**。
そして**入れたばかりの名指しが、その場で原因を指した**（2件とも1行で分かった）。

### 見つけた2件（どちらも「効かない見張り」になりかけていた）

- [x] **一度も走らない見張りが1つあった**（`細すぎる演出の帯は作らない`）。
      `orderDependent: true` が付いていて**絞ると見ない**うえ、通しでも
      右パネルのタブが既定へ帰っていて `.fx-item` が無く、`skipHere` で飛んでいた。
      → **自分でトランジションタブと「💬 テロップ」の節を開く**ようにして印を外し、
        `skipHere` を `assert` に変えた（逃げると緑のまま一生走らない）。
      → **走らせた初回に本当に落ちた**（`寄せ直しても演出の帯が戻らない`）。
        原因は今日と同じ型——**ホイールの寄せ／引きはマウス位置が中心**なので
        30回往復すると横位置がずれ、テロップが窓の外へ出る。帯は見えている範囲に
        しか作らないので「戻らない」と「見ていない」の区別が付かなかった。
        → 戻すのを「↔ 全体表示」1回にして、**先に帯そのものが在ることを assert** した。
      ※ **印（`orderDependent`）は、要らない所に付くと直したかどうかを
        永久に確かめられなくなる。** 同じファイルの別の所で 08-03 に1件外した実績もある。
      → **歯も確かめた（2026-08-03）。** `TelopAnimBand.tsx` の
        `if (want < MIN_BAND_PX) return null` を殺すと
        `引いても細い帯が消えない（寄せた 1 → 引いた 1）` で本当に赤くなる。
        **書かれてから今日まで一度も走っていなかった**ので、
        「緑だが効くかどうかは誰も知らない」状態だった。

- [x] **嘘の「戻し切れなかった」が1回出ていた**（`拡大率=1471.62px（起動時 5664px）`）。
      拡大率は `.track-inner` の幅で見ているが、**幅は寄せ具合と中身の長さの両方**で
      決まる。SRT で中身を入れ替えた直後は、全体表示に戻しても幅が一致しない。
      直後にプロジェクトを開き直せば揃うので、**数えるのは中身を戻したあとの1回だけ**に
      した（`restoreView` の `final`）。**鳴りっぱなしの警告は読み飛ばされる。**

- [x] **測る側の「黙って緑」を2つ塞いだ**（2026-08-04）。
      → `npm run stutter` が **1つも測れていないのに「どの画質も、絵は止まらず
        音も抜けませんでした」と出していた。** 下書きに動画が無いと各画質が
        「プレビューに映像がありません」で continue し、results が空のまま
        まとめへ落ちる。**測っていないのに問題なしは、測って問題なしより悪い**
        （信じて先へ進む）。CLAUDE.md の7番そのもの。
      → `e2e/bench.mjs` にも同じ形があった（「0 良好 / 0 問題あり」）。両方
        「1件も無ければ落とす」に倒した。歯も確かめた（終了コード 1）。
      → **見つけた経緯**: useSegClock を出したあと、再生が本当に滑らかかを
        確かめようとして走らせたら緑だったが、上の行に「映像がありません」と
        出ていた。緑を信じていたら、いちばん timing に効く分割を
        「測って確かめた」と誤って記録するところだった。

- [x] **再生の分割（useSegClock）の絵の止まり・音の抜け** → **測った（2026-08-04）。問題なし。**
      → テレビ基準（カット50・テロップ100・5分）で `npm run stutter --project=`:

      ```
      1080p / 720p / 360p すべて ○ 滑らか
        絵の止まり 0ms（合計 0/5750ms）／ 音の抜け 0回
        主スレッドを塞いだ処理 0回 ／ 流れる速さ 1.04倍
      ```

      ※ 段差（プツッ）は 1〜2回出るが**判定に使わない**（素材由来が混ざる）。
      ※ **測れていることも確かめた**（5,750ms ぶん標本が取れている）。
        08-04 に「1つも測れていないのに問題なし」と出る穴を塞いだばかりなので、
        緑そのものより「何ミリ秒ぶん見たか」を先に見ること。

- [x] **配線（useAppWiring）を割る作業** → **完了（2026-08-04）。1,229 → 362行。**
      → 心配していた「囲いを足して App の木が深くなり、効果の走る順が変わる」は
        **起きなかった**。通し **256 / 256**（13.3分・初の全緑）で確認済み。
      → 経緯・道具の使い方・踏んだ穴は `引き継ぎ-心臓の分け直し.md`。

