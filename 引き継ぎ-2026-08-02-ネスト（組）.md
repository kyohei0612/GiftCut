# ネスト＝「組」を作った（2026-08-02）

前のコミット（`6f14346`）で中身だけ決めて、1行も作っていなかった物を作りきった。
**なぜそう作ったかはコードの真上のコメントが一次情報**（`src/shared/group.ts` の頭が入口）。
ここには「何をどう変えたか・測った数字・わざとやっていない事」だけを置く。

---

## 何ができるようになったか

複数選んで右クリック →「🔗 ネストする（組にしてまとめて動かす）」。以後、

- **掴んで動かす**と、種類をまたいで組ごと動く（テロップ＋効果音＋画像＋映像レイヤー）
- **選ぶ**と、どれか1つを押しただけで組の全員が選ばれる
- **消す**と組ごと消える（キーの Delete も、右クリックの削除も）
- **写す**（コピー／複製）と、写した側は**新しい組**になる（元とは繋がらない）
- 帯の左上に 🔗 が出る

右クリック →「⛓️‍💥 組を解く」で解ける。**書き出しには一切影響しない**（`group` を見ない）。

---

## 触ったファイル（19本）

| どこ | 何 |
|---|---|
| `src/shared/group.ts`（新・245行） | 純粋な判定。**種類の名前を知らない**形にしてある |
| `src/shared/group.test.ts`（新・245行） | 38件 |
| `src/renderer/src/state/useNest.ts`（新・195行） | 作る／解く／選択を広げる効果／掴んだ相手をずらす |
| `lib/srt.ts`・`lib/projectTypes.ts` | `group?: number` を4種類へ |
| `lib/projectLoad.ts` | 読み直しで壊れた値を捨てる（4か所） |
| `state/useAppWiring.tsx` | `useNestSelectSync()` と `openClipMenu` の直し |
| `state/useTimelineDrag.ts`・`useClipDrag.ts` | 掴んだときに相手を控え、動いた分ずらす（4か所） |
| `state/useCopyPaste.ts`・`useTimelineEdit.ts` | 写した物に新しい番号を振る |
| `components/AppMenus.tsx`・`timeline/ClipBand.tsx` ほか | 品書きの2項目と、帯の 🔗 |

---

## 測った数字

- `npm run verify` **791件 緑**（着手前 788／新しく足したのは 38 − 差し替え分）
- `npm run build` 通る
- 絞った e2e（`--fast`）
  - `--only=まとめて,複数,選択` … **17 / 17 緑**（1分未満）
  - `--only=右クリック,品書き,メニュー,削除,複製,貼り付け` … **35 / 35 緑**（1.5分）
  - `--only=ネスト,組` … **新しく足した3件とも緑**（11秒）
  - **通しは回していない**（指示が無いので。最終確認は絞らずに1回要る）
- e2e は **232 → 235項目**
- `useAppWiring.tsx` は **1,248行**（上限1,250）。**残り2行しかない。**
  次にここへ何か足すときは、先に何か出すこと

---

## つまずいた所（同じ穴を踏まないために）

### 1. `it.group ?? 0` で全件が拾われた

`membersOfGroups(pool, [0])` が「組に入っていない物」を全部返した。
`?? 0` は「組に入っていない」を「組0」に化けさせる。
**`sanitizeGroupId` が 0 を捨てているのはこのため。** 試験が先に捕まえた。

### 2. 右クリックが選択を1つに潰していた

`openClipMenu` は**常に**「押した1つだけ」を選び直していた。
そのままだと「まとめて選ぶ → 右クリック → ネストする」が成立しない
（右クリックした時点で選択が1つになるので、組にする物が無い）。

→ **押した物が既に選ばれていたら、選択はそのまま残す**に変えた。
テロップ側（`onClipContextMenu`）は元からこの形だったので、揃えただけ。

### 3. 掴んだ相手は「掴んだ時」に控える

動かしている最中に読み直すと、**自分がずらした後の位置を新しい起点にしてしまい**、
毎フレーム少しずつ流れていく。控えた位置からの**絶対値**で置くこと。

### 4. `Required<Cue>` の往復試験が効いた

型に `group` を足した瞬間、`projectRoundTrip.test.ts` が5件とも落ちた。
「読み込む側に書き足すまで通らない」という順番が実際に働いた
（この試験が無ければ、保存はされるのに開き直すと組だけ消えていた）。

---

## わざとやっていない事（作り忘れではない）

- **本編の切片（V1）は組に入らない。** 切片は常に隙間なく連続する（リップル前提）ので
  単独で動かせず、組でまとめて動かす意味が無い。動画を組に入れたいときは
  映像レイヤー（V2以降）に置く
- **端のトリムは組ごとに効かない。** 長さは物ごとに違うので、揃えると必ず壊れる
- **上下の段を変えるのは掴んだ物だけ。** 全員を同じ段へ寄せると重なって壊れる
- **組の相手が0秒より前へ出るときは0で止まる。** 束の形が左端で潰れる
  （掴んでいる方は自分で0止めするので、そこだけ間隔が縮む）
- **レザーで割ると、両方が同じ組に残る。** 割った物は同じ場面の物なので、そのままでよい

---

## e2e（`e2e/checks/11-編集の残り.mjs` の章「ネスト（組）」）

3件足した。**「解くと動かなくなる」を必ず対にすること**——
これが無いと、全部が常に一緒に動くだけの壊れ方でも1件目が通ってしまう。

| 見ている物 | なぜ要るか |
|---|---|
| 組にすると、テロップを掴んだだけで効果音も一緒に動く | 種類をまたぐのが売り |
| 組を解くと、一緒に動かなくなる | 上の1件目が空振りしていないことの担保 |
| 組は、保存して開き直しても残っている | 読み込む側の書き忘れは静かに消える |

### 赤くなることを確かめた（検査部の決まり）

- `shiftPartners(partners, delta)` → `0` に変えて建て直し
  → 1件目だけ赤（`効果音が付いてこない（317 → 317）`）。2・3件目は緑のまま
- `loadCues` の `group` を落とす → **単体が3件即赤**（e2e を待つまでもない）

どちらも戻して緑に復帰済み。

## 次に誰かがやるなら

1. `useAppWiring.tsx` の**残り2行**（1,248 / 上限1,250）。ここへ足す前に何か出す
2. 通しの e2e（約12分）。**このリファクタの後、まだ1回も通していない**
3. 「複製した組は元と繋がらない」は**単体だけ**（`remapGroups`）。
   画面の配線は4か所の1行ずつなので e2e にはしていない
