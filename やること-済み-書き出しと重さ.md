## 書き出し

- [x] **テロップPNGの切り詰めを通した**（2026-08-04）。**重ねる段が 2.85倍。**
      前回「得が確認できていない」として見送った2つが、どちらも片付いた。

      **① ばらつき（71秒と126.8秒）はアプリ側だった。** ffmpeg だけ取り出して
      同じ物を3回ずつ回すと **±1%**（3,629 / 3,698 / 3,678ms）。
      → 書き出しの速さを比べるときは**アプリを通さずに測る**こと。

      **② PSNR 43.8dB の理由は「重ねるときの色空間」だった。**
      `overlay` は既定で `format=yuv420`＝**重ねる絵の色差を 2x2 に間引いてから混ぜる**。
      間引きの網は絵の大きさで変わるうえ、swscale は 2x2 の外まで見るので、
      **偶数に揃えても一致しない**（実測 41.4dB）。`:format=rgb` にすると:

      ```
      1 全画面・yuv420（いま）  3,668ms      1と2の差  46.09dB
      2 全画面・rgb             3,019ms      1と3の差  46.09dB  ← 2と同じ
      3 切詰め・rgb             1,288ms      2と3の差  **inf**  ← 完全一致
      ```

      **絵の変化はすべて色空間ぶんで、切り詰めは1ミリも足していない。**

      → では色空間を変えてよいのか。**手で答えの出る形で確かめた**:

      ```
      青(0,0,255) に 赤(255,0,0) を透明度50%で重ねる
      canvas と同じ計算なら          128  0 127
      yuv420 で重ねる（いままで）    122  1 126   ← 赤が 6 ずれる
      rgb で重ねる                   127  0 127   ← 一致（丸めぶん）
      ```

      **プレビューは canvas ＝ rgb で重ねている。** つまり今までの書き出しは
      画面と違う計算をしていた。`format=rgb` は**そのズレを消す方向**なので通した。
      ※ `yuv444`（間引きだけ止めた物）に近いのは yuv420 の方（37.4 対 33.0）。
        差は間引きではなく**混ぜる色空間そのもの**なので、そちらを基準にしないこと。

      → 少ないときに損しないことも見た（1枚 909→877ms / 5枚 988→889ms）。
      → 見張りは `main/exportOverlays.test.ts`（**全部の段が rgb か・段ごとに違わないか**）。
        段ごとに色空間が違うと、境目で rgb ⇄ yuv の変換が段の数だけ挟まる。
      → 動きのあるテロップ（連番）は**まだ全画面**。大きさを揃える必要があるので、
        揃えるなら「その連番ぜんぶの和集合で切る」形になる。**まだやっていない。**

- [x] **書き出しを速くする**（2026-08-04）。**1.27倍。画質もファイルの大きさも変わらない。**
      → 遅さを分解したら**ほぼエンコーダだけ**だった。立てた仮説は全部外れ:

      ```
      -hwaccel cuda        6.2秒 → 6.2秒   読む側は詰まっていない
      scale/pad/fps 除去   6.2秒 → 6.2秒   解像度合わせはタダ
      カットの数           1個も50個も 3.6秒
      入力の重複           元動画は既に1回しか開いていない
      **preset**           p4 3.5秒 / **p5 6.2秒**（同じ cq18・同じ 2.1MB）
      ```

      → `h264_nvenc` の `preset` を **p5 → p4**。**PSNR 66dB・ファイルの大きさ同一**
        ＝画質は落ちていない（40dB を超えたら目では区別が付かない）。
        p3 以下は速いが**ファイルが1.4倍**に太るので採らない。
      ※ 見張りは `main/encoders.test.ts`（**p5 に戻すと赤**）。数字で決めた設定なので、
        変えるときは測り直すこと。
      ※ 遅いと言われたら `%APPDATA%\GiftCut\last-export-args.txt` を見る
        （引数と「同じ素材を何回開いたか」が残る）。

- [x] **テロップPNGの切り詰め** → **済（2026-08-04 夜）。重ねる段が 2.85倍。**
      見送っていた2つ（ばらつき／PSNR 43.8dB）は**この節の頭**に片付いた形で書いてある。
      要点だけ: ばらつきは**アプリ側**（ffmpeg だけなら ±1%）、43.8dB は
      **`overlay` の既定 `format=yuv420`**（色差を 2x2 に間引く）が原因で、
      `:format=rgb` にすると切り詰めあり／なしが **完全一致（inf）**になる。

- [x] **書き出しの窓で、名前などの入力欄にマウスを乗せてもカーソルが文字入力の形に
      ならない**（2026-08-02 追加）。押せる／打てることが見た目から分からない
      → 済（2026-08-02）。**見た目（`.pq-select`）を `<select>` と `<input>` で
        使い回していて、`cursor: pointer` 固定だった**。打つ欄だけ I 形にした
- [x] **「参照」のボックスが少し中心からずれている**（2026-08-02 追加）。真ん中に置く。
      あわせて、**参照先が長いときは尻を決めて残りを `…`** で出す（窓を押し広げない）
      → 済（2026-08-02）。ずれの正体は**中身を出すだけの `<span>` に同じ見た目を
        当てていた**こと（箱の高さに対して文字が上へ寄る）。真ん中に置き直した。
        長い道は**尻を残して頭を `…`** にする——道で知りたいのはどのフォルダに
        入るか（尻）で、頭はどれも同じ。全部は吹き出しに出る
- [x] **同じ名前があっても上書きしない**（2026-08-02）。
      名前の後ろに `(1)` `(2)` … を付けて避ける。消えるのは「前に何分もかけて
      作った物」で、しかも**消えたことに気づくのは探しに行ったとき**なので、
      黙って上書きしてはいけない。決めるのは書き出しを始める直前
      （窓を開けてから押すまでの間に、同じ名前が増えていることがある）。
      作り方は `shared/exportDefaults` の `uniqueName`、使う所は `main/exportRun`。
      ※ 選択の窓を通ったときは避けない（その窓が既に「上書きしますか」を
        聞いており、そこで「はい」と答えた人の意思まで覆すと狙って上書きできない）

- [x] **書き出しが遅い。犯人は「テロップ1枚＝ffmpeg の入力1つ＋重ね1段」の作り。**
      → 済（2026-08-02）。**等間隔で全区間を刻んでいるテロップを連番1入力にまとめた。**
        重ねる段が1本で済むので、枚数が増えても重くならない。
        **実機で ffmpeg 6.1秒 → 1.2秒**（e2e の素材・テロップ121枚・同条件）。

        まとめられるのは等間隔の物だけ（頭と尻の演出だけのテロップは真ん中に
        長い静止区間があり、連番にすると尺が合わない）。**判定は
        「刻みの並びが本当に等間隔か」を見て決める**——`animBreakpoints` の中の
        条件を書き写すと、同じ規則が2か所になって片方だけ直したときに気づけない。

        置き場: 画面側が `telopSeqs` を組んで（`state/useExport.ts`）、
        `main/exportRun.ts` が `-framerate` 付きの連番入力1つ＋重ね1段にする。

        ※ **`MAX_TELOP_PNGS = 2000` の理由が変わった。** コマンドライン長の崖は
          消えたが、画面側が base64 で抱える量が残る。増やすなら、貯めずに
          1枚ずつ書き出す形へ変えてから。

      **測った数字（次に触るときはこれと比べる）**

      GPU は元から効いていた（この機械は NVENC が通る。QSV と AMF は一覧に
      あっても実際は失敗する＝「1枚焼いて確かめる」が正しく働いている）。
      1080p30・60秒を、重ねる枚数だけ変えて測った（枚数以外は同条件）:

      | 重ねる枚数 | GPU（NVENC） | CPU（x264 medium） |
      |---|---|---|
      | 0枚 | 5.1秒 | 6.1秒 |
      | 60枚 | 6.8秒 | — |
      | 200枚 | 10.7秒 | 12.9秒 |
      | 600枚 | **23.3秒** | — |

      **1枚あたり約 0.03秒** ずつ増える。焼く物を GPU→CPU に変えても
      1〜2秒しか動かない＝**エンコーダは犯人ではない。**

      **連番にしたあと**: 同じ600枚を「1本の連番入力＋重ね1段」で渡すと
      **5.2秒**（重ねない 5.1秒とほぼ同じ）。実機でも ffmpeg 6.1秒 → 1.2秒。

      ※ 内訳は書き出し完了時に出る（`テロップ◯枚の画像化 ◯秒 ／ ffmpeg ◯秒`）。
        **次に「遅い」と思ったら、まずこの行を見て切り分ける。**
        この機械の画像化は1枚 約16ms（121枚で1.3秒）

- [x] **音だけを mp3 で書き出す**（2026-08-02。急ぎの依頼）。
      書き出しの窓の入れ物で  を選ぶ。192kbps・48kHz。
      音の道すじ（切片の並び・効果音・BGM・ダッキング・音量を揃える）は**映像のときと
      同じ物をそのまま通す**ので、聴いた音と書き出した音が必ず一致する。
      ・**映像を1コマも焼かない**ので、テロップの画像作りは丸ごと飛ばす（待ち時間の無駄）
      ・ただし映像の枝は  で**繋いでから捨てる**。繋がないと ffmpeg が
        「Filter has output (v) unconnected」で止まる（実際に確かめた）。
        枝ごと組み立てない形にもできるが、組み立ては映像と音が絡んでおり、
        分けると**音の道すじまで別物になる**危険がある
      ・同梱 ffmpeg に  が入っていることを確認済み
      確認は e2e「音だけを mp3 で書き出せる」（読めるか＋音が入っているかまで見る）
      → **0.1.13 は遅かった（0.1.14 で直した）。**
         は「出さない」だけで、**映像は最後まで作られてから捨てられる**。
        実測（1080p・60秒）で **0.8秒 → 10.4秒＝13倍**。8分の素材なら分単位で効く。
        直し方: 欲しい出口（音）から逆にたどって**要る枝だけ渡す**
        （ の 。試験5件）。
        組み立ては映像と音が絡んでいるので**分けずに、最後に落とす**——
        分けると音の道すじまで別物になりうる（聴いた音と違う物が出る）

- [x] **【要調査】プレビューでテロップをダブルクリックした状態が、書き出しにも
      乗っているかもしれない。**
      → **乗らない（2026-08-03）。道すじを最後まで辿った:**
        `useExport` → `renderCueToPng(cue, …)` → `cueToSvgDataUrl` → `innerHtml(cue, …)`
        → `buildTelopSVG(style, text, runs)`。**受け取るのはテロップの持ち物だけ**で、
        画面を一度も読まない。編集枠 `.telop-editor` は `PreviewOverlays.tsx` にある
        別の要素で、この道すじからは触れない。
      ※ **機械の検査は付けていない。** 「画面を読んでいないこと」を試験で押さえるには
        書き出しを本当に走らせて絵を見るしかなく（`buildTelopSVG` は canvas で
        文字を実測するので、jsdom では degenerate な出力になり意味を持たない）、
        1項目のために数分の書き出しを通しへ足す価値が無いと判断した。
        **将来「画面を撮って書き出す」方式に変えるなら、ここが真っ先に壊れる。**

- [x] 書き出すと画像がデフォ位置に戻る（v0.1.9 で直した）。
      **拡大していないと X/Y が効かなかった。** 移動を「大きくした絵のどこを
      切り抜くか」で表していたので、等倍だと切り抜く余地がゼロ＝位置が 0 に
      丸められていた。動き（モーション）の位置キーも同じ理由で止まっていた。
      → 先に余白ごと台紙を広げてから切り出す形にした。動き側は足りるぶんだけ
        広げる（広げる必要が無ければ今までどおりの重さ）。
      → 判定は `shared/clipMotion` の `zoomPanChain` と `zoompanFilter`。
        画面（CSS の translate+scale）と同じ画素を指すか、試験で突き合わせてある。

## テロップ

- [x] **縦書きを選べるように**（2026-08-02 追加）。
      テロップ追加の横のプルダウンで「横書き／縦書き」を選ぶ
      → **作った（2026-08-03）。** ＋テロップの隣に「横書き／縦書き」を置いた。
        **選んでいるテロップにも当たる**（向きは作ってから決めることが多く、
        選んで押せないと作り直す羽目になる）。既定は横書きのまま。
      → **横書きの道すじには一切触っていない。** `buildTelopSVG` の中で
        変えたのは**文字の置き方（tspan の x/y）と枠の縦横**だけ。
        縁・影・グラデ・部分装飾は同じ物がそのまま効く＝
        **画面と書き出しで別々の式にならない**（両方この関数を通る）。
      → 組版そのものは `writing-mode: vertical-rl` に任せた。
        1文字ずつ座標を置きに行くと、約物・合字・拡大文字が全部自前計算になり、
        **横書きと縦書きで組版を2つ持つ**ことになる。
      ※ **見つけた取りこぼし**: 返り値の `textW` だけ実測値のままで、
        縦にしても幅が横書きのままだった（当たり判定と絵の大きさが食い違う）。
      ※ **部分装飾の背景ハイライトは縦書きでは敷かない。** あの計算は
        「行の左端から文字幅を足す」横書き専用で、縦に当てると見当違いの所へ色が付く。
        **間違った箱を描くくらいなら描かない。**
      検査: 単体6件（枠の縦横が入れ替わる／既定は横書き）と
        e2e「横書き／縦書きを切り替えると、文字の並びが縦になる」（戻せることも見る）

- [x] **プレビューの枠を大きくし過ぎるとテロップがズレる**（2026-08-02 追加）。
      もらった画面では、下段の大きいテロップが**右へはみ出して切れている**。
      → **原因が分かって直した（2026-08-03）。書き出しには乗らない・画面だけの話。**
        映像の枠（`.screen`）が `height: 100%` ＋ `max-width: 100%` ＋ `aspect-ratio` で、
        **区画が縦長になると幅だけ頭打ちになり、高さは 100% のまま**＝比率が崩れる。
        実測: 区画の幅 1200px で 1.778（正しい）→ 600px で 1.711 → **320px で 0.885**。
      → 崩れた枠の中で、映像は `object-fit: contain` で letterbox されるから
        **映像だけ小さくなる**。テロップは**枠の高さ**（`cqh`）を基準に置いているので、
        そのぶん大きく・外側にずれる＝「右へはみ出して切れる」。
      → 直し: 高さを「入る方」で決める。`height: min(100%, calc(100cqw / var(--ar)))`。
        `100cqw` は区画の幅（`.monitor-stage` を container にした）。縦長でも横長でも比率が保たれる。
        比率の**数**は `--ar` で画面側から渡す（`aspect-ratio` の文字列では割り算できない）。
      確認は e2e「モニタを細くしても、映像の枠の縦横比は保たれる」
      （直す前は `1200px→1.778、600px→1.711、320px→0.885` で赤）
- [x] **字間が全体的に広すぎる**。もっと狭くしてよい
      → 済（2026-08-02）。既定の字間を 0 → **-40**（1/1000em＝字の大きさの4%ぶん）。
        `lib/telopStyle.ts` の `defaultTelopStyle`。**保存済みのテロップは変わらない**
        （それぞれ自分の字間を持っている）ので、変わるのはこれから作る分だけ
- [x] 書き出した動画でテロップがチカチカした（v0.1.8 で直した）。
      表示窓の終わりを半フレーム詰めていたせいで窓と窓の間に隙間ができ、
      出力フレームがそこへ落ちるとテロップが消えていた。半開区間に変更。
      併せて、動きの刻みを 15fps 固定から書き出し fps に合わせた。
      → 判定は `shared/filterGraph` の `overlayEnableExpr` と、その試験にある。
      ※**書き出しは長くなる**（60fps なら画像は 15fps の4倍焼く）。
        遅すぎるという話が出たら、刻みに上限を付けるのが次の手。

## 更新（自動アップデート）

- [x] **更新のたびに再起動を促され、セットアップ画面まで出るのが面倒**（2026-08-02）。
      原因は2つあった:
      ・落とし終わると「5秒後に再起動します」と**こちらから迫っていた**
      ・当てるときに `quitAndInstall(false, …)` を呼んでおり、**NSIS が対話モードで
        立ち上がっていた**（置き場所を選ぶ画面まで毎回出る）
      → **こちらからは促さない**（`shared/updatePolicy` は必ず「次に閉じたとき」を返す）。
        当てるのは黙って走る（`quitAndInstall(true, true)`）。
        帯は「用意しました」と出るだけで、待てない人のために
        「今すぐ更新して再起動」を置いてある（押されたときだけ動く）。
      ※ 初回の導入は今までどおり置き場所を選べる（`oneClick: false` のまま）。
        **黙らせたのは更新のときだけ。**

## 重さ（実素材で出た。2026-08-02 追加）

- [x] **編集すればするほど重くなる。**
      1本ぶん編集し終わる頃には目に見えて重い。**特にタイムライン**。
      → **本人の体感で「解消している」（2026-08-03 深夜）。閉じる。**
        大きい所（下の各行）を直したあとの実素材での感触。
        **再発したら「📈 測定を書き出す」を押してから開き直すこと**
        （下に測り方と、これまでに効いた直しが全部残してある）。

      **本人のヒントで症状が特定できた（2026-08-03）:**
      「**再生ヘッドがカクつく／タイムラインだけ／エフェクトを足すと早い段階で**」。
      **プレビューは平気**というのが決め手——アプリ全体の描き直しなら両方遅くなる。

      → **掴んでいる間はタイムラインが描き直されるので、要素の数が直に効く。**
        「見えていないのに作っていた物」を3つ止めた（v0.1.18）:

      | 止めた物 | 中身 |
      |---|---|
      | 時間の目盛り | 全体ぶん作っていた（寄せると4,538個） |
      | 出入りの演出の帯 | 全体表示で約2px。1つにつき6個 |
      | 端のつまみ | 帯が12pxなのに片側7px×2。**出しても掴めず、動かす邪魔だった** |

      実測（451秒・テロップ248・エフェクト248・全体表示）: **DOM 3,166 → 1,228**。

      **次は実機の体感待ち。** 数字は2.6分の1になったが、本人が「軽くなった」と
      感じるかは別。**感じないなら、下の「本丸」へ進む判断材料になる**。
      ※ 手がかりに実物をもらってある: `C:\Users\kyohei\Downloads\project.gcproj`
        （本人が実際に1本編集した物）。**まずこれを開いて測る**

      **中身を数えた（2026-08-02）。数の多さが原因ではない:**

      | | |
      |---|---|
      | ファイル全体 | 0.87MB |
      | テロップ | 242個（限界は4000枚。**桁が違う**） |
      | 切片 29 / 効果音 21 / 素材 3 | どれも限界のはるか手前 |
      | **iconAssign** | **0.469MB**（PNG を dataURL で3枚。1枚165KB） |
      | **laneIconAssign** | **0.125MB**（同じく1枚） |

      → **プロジェクトの68%が絵4枚。** 数ではなく、この持ち方の方を疑う。

      **見つけた物1: `lib/telopStyle.ts` の `inkCache` は際限なく増えていた。**
      鍵に**文字の中身**（1行目と最終行の先頭40字）が入っているので、
      **1文字打つたびに新しい鍵ができ、古い物は一度も捨てられない**。
      1件は小さいが、編集し続ける限り増え続ける＝「編集した分だけ重くなる」
      という症状と形が合う。
      → **済（2026-08-02）。上限2000件で、しばらく使っていない物から捨てる**
        （`lib/lru.ts`・試験4件）。2000は実物（テロップ242個）の8倍なので、
        1本ぶん編集しても測り直しは起きない。
        ※ **入れた順のまま捨ててはいけない。** 1つのテロップを直し続けている間に
          他が流れると、その控えが押し出されて毎回測り直しになる。
          取り出すたびに最後尾へ回す形にしてある。
      ※ 同じファイルの `baselineCache` はフォント名が鍵なので増えない。

      **見つけた物2: 「未保存か」を見るのに毎回プロジェクト全体を文字列にしている
      ——が、これは犯人ではない（2026-08-03 に測った）。**
      中身が変わるたび300ms後に `markUnsaved` が走り、`projectJson()` で全体を
      文字列にして保存済みと比べている。0.87MB の 68% が絵のdataURLなので
      「ここが重いのでは」と疑ったが、**実データで測ったら1回 1.5ms**
      （文字列の比較は 0.0001ms）。編集1回につき1.5msなら症状の説明にならない。
      → **作り替えなくてよい。** ここを参照比較へ直す案は、間違えると
        「未保存なのに保存済みに見える」＝作業が消える型なので、**測らずに触らないこと。**

      **見つけた物3: 目盛りをタイムライン全体ぶん作っていた（2026-08-03。直した）。**
      実データ（451秒）で寄せると**目盛りだけで 4,538 個の要素**になり、
      帯（クリップ）は23個しか描いていないのに DOM が 1,679 → **10,412** に
      膨らんでいた。**帯は元から窓の分だけだったのに、目盛りだけが全部**。
      → 見えている範囲＋前後1画面ぶんだけ作る（`shared/rulerTicks`・試験14件）。
        実測: 寄せた状態で DOM **10,412 → 986**、目盛り 4,538 → 506、
        再生ヘッドを掴んで動かす 649 → 461ms。

      **見つけた物4: 再生ヘッドが1px動くたびにタイムライン全体が描き直されていた
      （2026-08-03。半分だけ直した）。**
      `TimelineArea` が `currentTime` を受け取って再生ヘッドへ渡していたので、
      帯277個ぜんぶが毎回描き直されていた。時刻が要るのは再生ヘッド1本だけなので、
      **あちらが自分で見に行く**形にした（`components/timeline/Ruler` の `Playhead`）。
      → 実測: 全体表示で掴んで20回動かす **1,135 → 903ms**（約2割）。

      **まだ残っている本丸（次はここ・要注意）:**
      `state/useAppWiring` そのものが `currentTime` を受けている
      （`useNowShowing` / `useVClipEls` / `useCurrentLook` がプレビューのために要る）。
      **配線が組み直されると、心臓の値も全部作り直されるので、帯を `memo` で
      包んでも止まらない**（毎回新しい関数を受け取るため）。
      → 直すなら「**プレビューだけが要る値を、タイムラインが見ない心臓へ分ける**」。
        これは心臓の分け直しなので、**測ってから・立ち会えるときに**やること。
        いまの数字: 全体表示で20回 903ms（1回45ms）、寄せた状態は461ms。
        帯22個で461ms／277個で903msなので、**帯1つあたり約0.09ms/回**。

      **まだ見ていない所**:
      ・履歴は100件で頭打ち＋中身は参照を持つだけなので、たぶん犯人ではない
        （`useHistory` の `pushUndo` / `snapNow` を読んで確認済み）
      ・タイムラインの描き直しの回数（掴んでいる間に App 全体が描き直される話は
        `.company` の測定に残っている。分割後に測り直していない）
      ・波形とサムネの持ち方
      ・**消したクリップの `<video>` が残っていないか**（`useVClipEls` の持ち方）。
        残ると、消したはずの映像を裏で読み続ける＝**編集した分だけ増える**という
        症状の形と合う。**次はここから測る**（2026-08-03 の見立て。未確認）
- [x] **掴んだまま枠の外まで引っ張ったら、タイムラインも動く**（ウェブページと同じ）。
      → 済（2026-08-02）。テロップ・効果音・画像・映像レイヤー・本編の切片、全部。
        速さの決め方は `shared/edgeScroll`（試験5件）。再生ヘッド側に同じ物が
        直書きされていたので、そちらもここを通るようにした
        （別々だと掴む物ごとに手つきが変わる）。
        ※ **返すのは「実際に送れた量」。** 掴んだ物は「掴み始めからどれだけ動いたか」で
          場所を決めているので、景色が送られたぶん掴み始めを戻す必要がある。
          端に着いてもう送れないときに戻すと、**物だけが滑っていく**

## 吸着（マグネット）の抜け（2026-08-02 追加）

- [x] **【要調査】あちこちで抜けている。効果音もタイムラインの素材も、全種類まとめて見る。**
      → **一覧にして直した（2026-08-02）。** どの操作がどちらを通っているかを数えたら、
        **端を摘む所は全部正しく**（`snapTime`＝動かす端は1つなので、これでよい）、
        **動かす所だけが3つとも同じ形で抜けていた**:

      | | 抜けていたこと |
      |---|---|
      | 効果音・画像・映像レイヤー（動かす） | ①掴んだ1つの端しか見ない ②**一緒に動いている仲間が寄せ先に残っている**（隣に吸い付いて動けない）③元の位置が寄せ先に無い |
      | 端を摘む（全種類） | 抜けていない。動かす端は1つなので `snapTime` で正しい |
      | 動画切片（V1） | **触っていない。** 元から自分の位置が寄せ先に入る形で、欲しかった動きになっている。並びが繋がっている（上書き・割り込みがある）ぶん作りが別なので、まとめて直すと事故る |

      → 3つとも**束の全体**（左端〜右端）で寄せ、**束の仲間を寄せ先から外し**、
        **元の位置を寄せ先に足した**。テロップと同じ決まりに揃えてある。
        束の範囲を出すのは `state/useClipDrag` の `spanOf`（1つだけ掴んだときも
        同じ道を通る＝そのクリップの頭と尻の両方に効く）

## 出ているはずの物がズレる（2026-08-02 追加）

- [x] **まだ来ていないテロップが、動画の頭でプレビューに出ている**（同期のズレ）。
      もらった画面では**再生ヘッドが 00:00:00:00** なのに、V3 の
      「夏休みシーズンになりましたが」（1秒あたりから始まる帯）が既に映っている。
      本人の見立てでは**動画の最初だけ**かもしれない。
      → **原因が分かって直した（2026-08-03）。同期のズレではなかった。**
        `useNowShowing` が「**先頭が 0.5秒 以内から始まるなら、頭の隙間を埋める**」
        として、**画面側だけ 0秒 から出していた**（聞き取り字幕の頭の空白を
        隠すため）。見立てどおり**動画の最初だけ**起きる。
      → **書き出しは引き延ばしていない**（`overlayEnableExpr` は `c.start` のまま）＝
        **プレビューに出ている物が製品に無い**。部門の決まり「画面と書き出しで
        別々の式を持たない」を破っていた。
      → 判定を `shared/cueWindow` の `isCueShowing` 1つに寄せ、頭埋めをやめた。
        突き合わせの試験（画面の判定 vs 書き出しの式を1コマずつ）も足した。
      → **代わりに戻ってくる物**: 自動字幕が 0.1秒 あたりから始まると、
        頭のその一瞬だけ文字が出ない。**それが書き出しの実物**なので、
        邪魔ならテロップの開始を動かすのが正しい直し方（画面で嘘をつかない）。
      確認は e2e「動画の頭で、まだ始まっていないテロップは出ない」
      （頭埋めを戻すと `始まる前なのに 1 枚出ている（開始 0.30秒）` で赤くなるのを確認済み）
- [x] **タイムラインから消えている画像が、プレビューには出る。**
      本人「プロジェクトを開き直したらこうなったかも」。
      → **中身を見た（2026-08-02）。データは壊れていない。**
        `a4651cc2-0802s.gcproj` の画像は1枚で、
        `track: "V2"`（段は V5〜V1 まで実在）・`tStart 32.25`・`長さ 2.95`。
        再生ヘッドは 32:56 ＝ **出る時刻に居るのでプレビューは正しい。**
      → **実際に開いて確かめたが、再現しなかった（2026-08-02）。**
        使い捨ての道具でそのプロジェクトを開き、`.img-clip` の数を数えた。
        帯は**見ている範囲に入ると出て、外れると消える**（そういう作り＝
        見えない帯を作らない）。テロップの帯もまったく同じ絞り方を通っており、
        **画像だけが特別扱いされている所は無かった。**
      → **原因が分かって直した（2026-08-03）。縦に隠れていたのでもなかった。**
        **「再生ヘッドは32秒、タイムラインは先頭」という食い違い**が残っていた。
        帯は**見えている範囲にしか作らない**ので、先頭を見ている限り32秒の帯は
        存在しない。プレビューは再生ヘッドで決めるので出る。両方とも正しい。
      → **食い違いの作り方は2つ重なっていた**:
        1. 前回の横スクロール（sx）を**起動直後に1回だけ**書いていた。その時点では
           中身の幅がまだ無いので**ブラウザが黙って0へ丸める**（例外も出ない）。
           右パネルの縦スクロールでは既に同じ穴を踏んで「届く高さになってから」に
           直してあったのに、横は直っていなかった
        2. **「素材を読み込んだ直後に一度だけ全体表示」が、戻した拡大率と横位置を
           上書きしていた**（`fitTimelineZoom` は拡大率を変えて横位置を0へ戻す）。
           一方**再生ヘッドは別口で戻る**ので、ヘッドだけ32秒に残る
      → 直し: sx は**届く幅になるまで見張ってから**戻す（5秒で諦める）。
        前回の見ていた場所を戻したときは**全体表示をしない**（`takeRestoredView`。
        1回だけ答えて降りるので、あとから別の動画を開けば全体表示は効く）。
      確認は e2e「前回の続きを開くと、タイムラインの横位置も戻る」
      （直す前は `横位置が戻っていない（7613 → 0／送れる幅 12689 → 4262）`）

## スクショ

- [x] **【要調査】スクショを撮っても画像が保存されないことがある**（2026-08-02）。
      → **こちらでは再現できていない。** 確認を1つ足して（e2e「スクショが保存でき、
        映像がちゃんと写っている」）変更前のコードで回したが、**緑**だった
        ——ファイルはでき、映像も写っている。
      → 疑ったのは「映像を描いた canvas が汚れて `toDataURL()` が例外を投げる」型
        （そうなると**保存の窓すら出ず、押しても無反応に見える**）。
        `gcfile` に CORS を足して `<video crossOrigin>` を付ければ塞げるが、
        **再現していない物に、動画が読めなくなりうる変更は当てない**ことにして戻した。
      → 代わりに**途中でこけたら必ず理由を画面に出す**ようにした
        （`usePreviewManip` の captureScreenshot を包んだ）。次に起きたときは
        文言が出るので、そこから原因を決められる。
      → **正体が分かった（2026-08-03）。本人「画像のみだとスクショできない」。**
        `captureScreenshotInner` の頭が **`if (!videoSrc || !v) return`** で、
        動画を読み込んでいないと**何もせず断っていた**（「先に動画を読み込んでください」）。
        「撮っても保存されない」＝**そもそも撮っていなかった**。
      → **もっと悪いのはこっち**: 撮っていたのは**本編の映像とテロップだけ**で、
        **画像も重ねた動画も一枚も描いていなかった**。動画があっても、
        画像で作った画面は写らない（本編が黒い所は真っ黒で保存される）。
      → 直した:
        ・動画が無くても撮る。断るのは**その時刻に本当に何も出ていないとき**だけ
        ・**画像と映像レイヤーも重ねる。** 変形はプレビューと同じ並びをなぞる
          （rotate → 反転 → translate → scale、原点は中心）。明るさ・切り抜きも同じ物を通す
        ・映像レイヤーは**画面に出ている実物の `<video>`** から描く（読み直すと違うコマになる）。
          拾えるように `data-vcid` を付けた
      確認は e2e「本編の映像を隠しても、画像はスクショに写る」
      （画像を描かないと `明暗の幅 0`＝真っ黒で赤くなるのを確かめてある）

## 測定・報告（製品版）

- [x] **製品版に不具合報告の口が無い。** 開いている間ずっと測り続けて、
      「測定を書き出す」ボタンを邪魔にならない所に置く
      → **測る方は既に全部あった（2026-08-03 に確認）。** `useDiagnostics` が
        起動した瞬間から測り続け、**配布ビルドでも走る**。5分ごとに
        `userData/perf` へ書き、画面の例外・受け止め損ねた失敗もそこへ残る。
        いま何をしているか（再生中／画質／原本か焼き直しか／切片とテロップの数）も
        一緒に記録している。
      → **足りなかったのは「押せる場所」だけ。** Ctrl+Shift+P は知らないと辿り着けず、
        5分ごとの自動書き出しは置き場所を説明するのが面倒。
        一番下の帯（ステータスバー）に **「📈 測定を書き出す」** を足した。
        **出し先はダウンロード**（そのまま渡せる場所。確認の窓も出さない）。
        押した時点より前の分も入っている（起動から測り続けているので）。
      ※ **どこへ出したかを必ず言う。** 場所を言わないと「押したけど何も起きない」に見える。
        書けなかったときも黙らない（黙ると、知らせる口があること自体が信用されなくなる）
- [x] 画面が重いのか、**動かしていないのにぶるぶる震え続ける**ことがある
      （起動し直すと直る）。この手の映像側の症状も測れるようにする。
      → **本人の体感で「解消している」（2026-08-03 深夜）。閉じる。**
        **原因を特定して直したわけではない**ので、再発したら開き直すこと。
      ※ **測る手段は用意してある。** 震えている最中に一番下の帯の
        「📈 測定を書き出す」を押せば、その前後がそのまま出る。
        **再発したら、まずこれを押してもらう**（起動し直すと消えるので、
        直してから聞いても何も残っていない）。
- [x] 要するに、**測定報告ボタン1つであらゆる事象の状態を出せて、それを解析できる**形にする
      → **済（2026-08-04）。「出す」は前からあった。足したのは「解析」の側。**

      前は数字と「読み方」を並べていたので、**読む人が自分で当てはめないと
      原因に辿り着けなかった**。送ってくるのは困っている本人なので、そこを人にやらせない。

      報告の頭に **「いちばん疑わしいもの」** を疑わしい順で出す（`lib/perfMonitor` の
      `verdicts`）。あわせて **「環境」**（版・画面の大きさと倍率・素材の規模）も出す
      ——自動更新は黙って入れ替わるので、**「直したはずが直っていない」の大半は
      新旧の取り違え**だった。数字より先にそこを見る。

      言い当てる形（症状の言葉で書く。「videoLag が大きい」では自分の症状と結び付かない）:

      ```
      計算が重い            音がぶちぶち切れる直接の原因
      デコードが重い        画質を下げるか焼き直しを待てば直る類
      画面の作りが重い      間引きが効いていない
      絵が遅れている        テロップだけ先に走って見える
      **どれにも当てはまらないのに遅い**  ← 描画側（合成レイヤーの組み直しなど）
      ```

      最後の1つは 2026-08-04 に実際に踏んだ形。**JS でもデコードでも作り直しでも
      なかった**（JS は28秒中2.8秒）。ここで黙ると「測ったが何も出なかった」で終わる。

      ※ **何も無ければ何も言わない。** 必ず犯人を1つ出す形にすると、健康なときまで
        何かのせいにして、本当に出たときに信用されなくなる。
      ※ しきい値は `perfMonitor.test.ts` が境目ごと固定している（勘で動かさない）。

---

