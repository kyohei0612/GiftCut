# 300行を目指すリファクタ（2026-08-03 着手・途中）

**まだ終わっていない。** 何をどこまでやったか、次に何をするかを残す。

---

## 目標の位置づけ（ここを取り違えない）

| | 数字 | 何の線か |
|---|---|---|
| `MAX_LINES` | 1,250 | **AI が1回で読めるか**の実測。1,295で読めず、1,182は読めた。破ると赤 |
| `WARN_LINES` | 1,200 | 上限が近いと**名前を出す**（止めない） |
| `GOAL_LINES` | **300** | **直すときに全部を頭に入れていられる大きさ**（本人の指定）。止めない |

**300は「読めない対策」ではない。** 1,205行のファイルは実際に1回で読めている。
300を狙う理由は別で、今日見つけた不具合の多くが**「同じ物が2か所」型**だったこと
（`trackGain` / `DEFAULT_LABEL` / run.mjs と lib/measure）。小さいほどこの型は起きにくい。

---

## いまの数字

```
300行超   71本 / 全354本（21%）  はみ出し合計 16,267行
500行超   32本                    7,172行
800行超    6本                    2,019行
中央値    125行
```

上位: `useAppWiring.tsx` 1,250 / `exportRun.ts` 1,227 / `e2e/run.mjs` 1,114 /
`09-モーション.mjs` 1,031 / `e2e/bench.mjs` 977

---

## 割った物（すべて中身を変えていない）

| ファイル | 前 | 後 | 出した先 | またぐ名前 |
|---|---|---|---|---|
| `e2e/run.mjs` | 1,250 | 1,114 | `lib/runMeasure.mjs` | 2個 |
| `usePreviewManip.ts` | 731 | 533 | `state/useScreenshot.ts` | 8個 |
| `09-モーション.mjs` | 1,205 | 1,031 | `checks/09b-Premiere取り込み.mjs` | — |
| `exportRun.ts` | 1,246 | 1,227 | `shared/filterLabels.ts` | **0個** |

あわせて**死んだコードを削除**: `lib/people.ts` まるごと（`fileToDataUrl` 以外
すべて未使用だった）＋ `iconLibrary` のレイアウト2関数。

### 割る前に必ず数えた

決まりは `引き継ぎ-App分割.md`:「**境目をまたぐ名前が40個を超えたら割らない**」。
数えるのに**読むだけの下調べを2本並列**で走らせ、根拠を出してから切った。

`filterLabels` は外から持ち込む名前が**0個**だったので迷わず出せた。
しかも純粋な文字列処理なので **`shared` へ置いて試験10件を付けられた**——
今までは「同じ入力を2か所から参照するとグラフが壊れる」を確かめるのに
**実際に書き出すしか無かった**（画面では分からず、書き出して初めて出る型）。

---

## 次にやること（この順番で）

### 1. `MAX_LINES` を 1,000 へ下げる

赤くなるのは4本。それぞれの手は下に書いてある。
**`SPLIT_NOTES`（`fileSize.test.ts`）に、割れない理由と数字を控えてある。**

| ファイル | 手 |
|---|---|
| `exportRun.ts` 1,227 | **重ね合わせ**（L755–963・209行・またぐ名前23個）を出す。`filter` を書き換えて返す形なので、**戻り値で組み直す**必要がある。次点は切片の組み立て（21個） |
| `e2e/run.mjs` 1,114 | 素材づくりと後片付け（`resetProject` まわり）が次の候補 |
| `09-モーション.mjs` 1,031 | 途中の塊（C＝コピー＆貼り付け・3件）を出せる。**印を付け終わったので、もう静かにずれない**（下記） |
| `useAppWiring.tsx` 1,250 | **割らない。DEBT へ。** 境目をまたぐ名前が 106〜413個（2026-08-02 実測）で目安の40個を大きく超える。先に**受け取る側（context の束）を小さくする**のが順序 |

### 2. 500 → 300 と続ける

各段で赤くなった物だけ、**数えてから**割る。
**割れない物は理由と数字を付けて DEBT に載せる**——無理に割って壊すより、
割れない理由が残る方が価値がある。

---

## 途中で分かったこと

### 印の付いていない順番依存が3か所あった（直した）

`09-モーション.mjs` の 85 / 833 / 968行目は、**手前の項目が残した状態に
寄りかかっているのに印が無かった**。絞って回すと必ず赤くなるのに、
「自分が壊したのか」を毎回 stash して確かめる羽目になる。
`{ orderDependent: true }` を理由つきで付けた。

**途中の塊を出すのは、この印を付けてから。** 印が無いまま塊を動かすと、
前提が静かにずれて、どこで壊れたか分からなくなる。

### 見た目が同じだからとクラス名を使い回さない（2回やらかした）

- 測定ボタンが `status-pop` を使い回し →「別ウィンドウから戻すチップ」の**数を
  数えている確認**を壊した
- 縦書きの切替が `ratio-group` を使い回し → 画面比の chip を数えている確認を壊した

どちらも `status-perf` / `wm-group` へ分け、**理由をコメントに残した**。
CSS は1つの規則にまとめてあるので見た目は変わらない。

### `lint-checks.mjs` が優秀

章を切り出した直後に「09b が `existsSync, readFileSync` を受け取っていない」と
即指摘してくれた。**切り出しの取りこぼしは、これで全部拾える。**

---

## 続き（同じ日の後半・11本を割った）

前半は「大きい物を軽くする」だったが、後半は**判断の順番を変えた**。
行数の大きい順ではなく、**「頭のコメントが2つ以上の話題を宣言している物」**から
片付けた。結果、割った11本のうち**7本は導管（またぐ名前）が 0 / 0**だった。

### 割った物（すべて中身は変えていない）

| 元 | 前 | 後 | またぐ名前 |
|---|---|---|---|
| `state/useTelopEdit` → `useTelopAnim` | 335 | 158 + 193 | 5 / 0 |
| `state/useCopyPaste` → `useAttrCopy` | 540 | 299 + 292 | 3 / 0 |
| `panels/TransitionsTab` → `MotionPresetList` | 395 | 252 + 213 | 6 / 0 |
| `state/useTracksAdmin` → `useLaneResize` + `lib/faderDrag` | 399 | 312 + 99 + 26 | 6 / 0、0 / 0 |
| `main/index` → `subtitles` / `mediaProbe` / 新 `dialogs` | 793 | 440 + 143 | **0 / 0 ×3** |
| `components/PanelChrome` → `PanelTabs` / `PaneWindow` / `TabSortList` | 591 | 222 + 277 + 118 | **0 / 0 ×3** |
| `components/LeftPanel` → `MotionPanel` | 769 | 339 + 461 | **0 / 0** |
| `components/FillPicker` → `ColorArea`（＋ `shared/color`） | 643 | 306 + 303 + 151 | **0 / 0** |
| `state/useProjectFile` → `useTelopTemplate` | 793 | 666 + 178 | 5 / 0 |

### 割る前に「頭のコメント」を読むと、ほぼ当たる

7本で、**ファイル自身が2つ以上の話題を宣言していた**:

- `PanelChrome` … 1行目が「タブ帯・**並び替え**・別ウィンドウ」と3つ挙げていた
- `LeftPanel` … 1行目が「左パネル（**プロパティ／モーション**）」
- `useCopyPaste` … 「**3種類ある**」と見出しで書いていた
- `useTelopEdit` … 見出しが2つ（「足す・書き換える」と「出入りの演出」）
- `telopSvg` … 「`_` で始まる関数は…**呼びたくなったら shared へ出す**」と
  自分で出し方まで書いていた（＝既に呼ばれていた）

逆に、**冒頭コメントが無いファイルほど話題が多い**という関係もはっきり出た:
`main/index.ts`（宣言なし・実際は7話題）、`FillPicker.tsx`（宣言なし・3話題）。

### 割ったら重複が出てきた（1件）

`FillPicker` を2つに分けた瞬間、`noDuplicate` が
**「浮かせ窓の掴んで動かす見出し」がまったく同じ25行で2つある**と拾った。
1ファイルの中に収まっていたせいで、検査にも人目にも掛かっていなかった。
`ColorArea.FloatingPicker` に1本化した。

**これが「1ファイル1話題」を守る本当の理由。** 行数ではなく、
同じ物が2つあることに気づけるかどうか。

### 割らないと判断した物（数えた結果）

切っても deps が両側に重複するだけで総数が減らないもの。**理由を数字で残す**:

| ファイル | 行 | 割らない理由 |
|---|---|---|
| `state/useClipDrag` | 533 | 3つに割ると同じ導管が27本になる（総数が減らない）。共有ヘルパ `spanOf` のため4つ目が要る。頭のコメントに「バラバラに置いて片方だけ直していた」事故の記録がある |
| `state/useTimelineEdit` | 890 | 話題は1つ。切ると deps が 24 → 29（+5重複）。呼ぶ側（`useKeyboard`・`AppMenus`）は常に両側を1組で使う |
| `state/useTimelineDrag` | 597 | 大きい塊を出すと12個が両側に重複 |
| `state/useSegmentDrag` | 381 | deps 9個のうち7個が両側に要る |
| `panels/PropertiesPanel` | 375 | 話題は1つ。6つに割ると分岐だけの30行が残る |
| `lib/telopSvg` の `buildTelopSVG` | 447 | 1話題。影の塊を出すだけで17個＋書き換えながら積む変数3つ |

### 検査を1つ増やした

`tsconfig.node.json` に `noUnusedLocals` が**無かった**（画面側には元からある）。
そのせいで本体側に死んだ import が43個たまっていた。
**消す前に検査を付けた**——消すだけでは同じことがまた起きる。
