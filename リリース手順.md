# リリース手順（新しい版を配る）

GiftCut は **起動したときに GitHub の Releases を見に行き、新しい版があれば
勝手に落として当てる**。使う人に更新作業をさせない。

出す側の手順は、下の3つだけ。

## 1回だけ: 置き場所を用意する（2026-07-29 に済んだ）

置き場所: https://github.com/kyohei0612/GiftCut （公開）

自動更新は「配る相手が誰でも読める場所」に置く必要がある。
非公開のリポジトリだと、配る相手全員に読み取り用のトークンを渡すことになり、
配布物にトークンを埋める羽目になる。**公開リポジトリを使う。**

置き場所を変えるときは `electron-builder.yml` の `publish:` を直す。

**公開する前にやったこと**（次に別のリポジトリを作るときも同じ判断が要る）:

履歴から `.company/`（Geba の解析ツール）と e2e の画面写真（実素材が写り込む）を
消してから公開した。バックアップを取ってから `git filter-branch` で抜き、
GitHub 側で本当に無いことまで確認した。**コピーしただけでは履歴から消えない。**

## 毎回: 新しい版を出す

1. `package.json` の `version` を上げる（例: 0.0.1 → 0.1.0）
2. コミットする
3. 出す:

```bash
GH_TOKEN=$(gh auth token) npm run release
```

`npm run release` は
**検査 → ffmpeg のライセンス確認 → ビルド → インストーラ作成 → Releases へ添付**
までを通しでやる。検査が落ちたらそこで止まる（壊れたものは出ない）。

### 手で出すとき（トークンを渡したくないとき）

`gh` は自分で認証を持っているので、トークンを環境変数で渡さずに出せる。
0.0.1 はこの手順で出した。

```bash
npm run dist && npm run check:packaged
```

**ここで名前を必ず合わせること。** `latest.yml` は `GiftCut-Setup-0.0.1.exe`
（ハイフン）を指すが、出来上がるファイルは `GiftCut Setup 0.0.1.exe`（空白）。
**空白のまま上げると GitHub が名前を変えてしまい、更新が拾えなくなる。**

```bash
cd dist && cp "GiftCut Setup 0.0.1.exe" GiftCut-Setup-0.0.1.exe && cp "GiftCut Setup 0.0.1.exe.blockmap" GiftCut-Setup-0.0.1.exe.blockmap
```

```bash
gh release create v0.0.1 dist/GiftCut-Setup-0.0.1.exe dist/GiftCut-Setup-0.0.1.exe.blockmap dist/latest.yml --title "GiftCut 0.0.1" --notes-file notes.md
```

出したら、**更新が読む物を実際に取ってきて確かめる**（名前がずれていても
リリース自体は成功してしまうので、ここを見ないと気づけない）。

```bash
curl -fsSL https://github.com/kyohei0612/GiftCut/releases/download/v0.0.1/latest.yml
```

出したあとの Release は最初「下書き」ではなく公開になる。取り消したいときは
GitHub の Releases から消す。消せば、更新を見に行っても何も起きない状態に戻る。

## 身内に渡すとき（自動更新の対象外）

再配布が許可されていない効果音とテロップ素材を**入れたまま**渡せるのは身内だけ。
`npm run dist:home` で **exe 1つ**（解凍もインストールも不要）を作る。

```bash
npm run dist:home
```

出来上がりは `dist-home/GiftCut-家庭用-x.x.x.exe`。

**公開用の `dist/` とは置き場所を分けてある。** 同じ所へ出すと
`dist/win-unpacked/resources/` に SE と telop-presets が残り、次に公開用を
作ったときに**混ざって配ってしまう**（`npm run check:packaged` は気づくが、
`npm run release` はそこを通らない）。

**この版は自動更新しない**（更新の設定を入れていない）。新しい版は作り直して
手で渡す。**それでよい**理由: 更新すると本体はインストーラ版として別の場所に
入るので、同梱した素材が付いてこない。「更新したら素材が消えた」になる。

## 使う人が足した物は、更新しても消えない

更新はアプリ本体を丸ごと入れ替えるが、**`%APPDATA%\GiftCut\` の下は触らない**。

| 物 | 置き場 |
|---|---|
| 効果音・テロップ素材（自分で足したぶん） | `%APPDATA%\GiftCut\SE\` `telop-presets\` |
| 動きのプリセット（取り込んだぶん） | `%APPDATA%\GiftCut\motion-presets\` |
| テンプレート | `%APPDATA%\GiftCut\テンプレート\` |
| アイコン画像・設定・自動保存の下書き | `%APPDATA%\GiftCut\`（Local Storage ほか） |

**ファイルメニューの「〜のフォルダを開く」から全部開ける。** 開く道が無いと、
消えない場所があっても本人には無いのと同じなので、そこまでを1組にしてある。

## 使う人から見た動き

1. 起動して4秒後、新しい版があるか見に行く（無ければ何も出ない）
2. あれば裏で落とす。画面の上に細い帯で「⬇ 新しい GiftCut を用意しています… 42%」
3. 落とし終わったら
   - **手が空いていれば**「5秒後に再起動します」と出して、そのまま再起動
     （「今すぐ再起動」「あとで」も選べる）
   - **書き出し中・保存していない変更があるとき**は再起動しない。
     次にアプリを閉じたときに当てる
4. 更新のための再起動では、**落とす直前に今の状態を下書きに書く**。
   開き直したときは「復元しますか？」とは聞かず、そのまま続きから開く
   （自分で勝手に閉じておいて開き直しを頼むのは筋が通らないので）

## 署名について（やらないと決めた）

コードサイニング証明書は有料（年契約）なので**取らない**。
そのぶん、初めて入れる人に何が起きるかを分かった上で配る。

**起きること:**

- インストーラを実行すると「Windows によって PC が保護されました」が出る。
  **消えるのではなく、`詳細情報` → `実行` を押せば入る。**
  署名が無い物は「実績（ダウンロード数）」で警告が薄れる仕組みだが、
  配る数が少ないうちは毎回出ると思っておく
- ウイルス対策ソフトが誤検知することがある（署名が無い実行ファイルは疑われやすい）
- **更新は署名が無くても動く。** electron-updater は SHA512 で中身を確かめる

**やること（お金をかけずに減らせるぶん）:**

1. 配る場所に**入れ方を1行で書く**。「警告が出たら `詳細情報` → `実行`」。
   これが無いと、警告を見た人はだいたいそこで止まる
2. GitHub Releases から配る（知らない場所からのダウンロードより疑われにくい）
3. **ファイル名と発行元の表記を変えない**。同じ物を出し続けるほど実績が付く

**気が変わったら:** 証明書を取って `electron-builder.yml` の `win:` に
`certificateFile` を足すだけでよい（この設定のまま署名付きになる）。
EV 証明書なら警告は最初から出ない。OV 証明書は実績が付くまで出ることがある。
