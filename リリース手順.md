# リリース手順（新しい版を配る）

GiftCut は **起動したときに GitHub の Releases を見に行き、新しい版があれば
勝手に落として当てる**。使う人に更新作業をさせない。

出す側の手順は、下の3つだけ。

## 1回だけ: 置き場所を用意する（2026-07-29 に済んだ）

置き場所: https://github.com/kyohei0612/GiftCut （公開）

自動更新は「配る相手が誰でも読める場所」に置く必要がある。
非公開のリポジトリだと、配る相手全員に読み取り用のトークンを渡すことになり、
配布物にトークンを埋める羽目になる。**公開リポジトリを使う。**

置き場所を変えるときは `electron-builder.yml` の `publish:` を直す。

**公開する前にやったこと**（次に別のリポジトリを作るときも同じ判断が要る）:

履歴から `.company/`（Geba の解析ツール）と e2e の画面写真（実素材が写り込む）を
消してから公開した。バックアップを取ってから `git filter-branch` で抜き、
GitHub 側で本当に無いことまで確認した。**コピーしただけでは履歴から消えない。**

## 毎回: 新しい版を出す

1. `package.json` の `version` を上げる（例: 0.1.0 → 0.1.1）
2. コミットして push する
3. **タグを打って push する**（下の注意を参照）
4. 出す:

```bash
git tag -a v0.1.1 -m "v0.1.1"
git push origin v0.1.1
npm run release
# 赤くなったら、そのまま同じ行をもう一度（1回目で足りないのは既知）
```

### 鍵（`GH_TOKEN`）は、もう手で渡さなくてよい（2026-08-06）

ここには前から `GH_TOKEN=$(gh auth token) npm run release` と**正しく書いてあった**。
それでも v0.1.27 で抜けた。**書いてあることは、思い出さなければ無いのと同じ。**

  `gh`         ログイン済み（鍵は keyring の中）
  `GH_TOKEN`   **無い** ← electron-builder が見るのはこちらだけ

食い違いの厄介な所は**出るエラーが別の場所を指す**こと。
「タグが push されていないのでは」と言われたが、タグは上がっていて、
本当は**発行が一度も走っていなかった**。

→ `scripts/release.mjs` が、無ければ `gh auth token` から借りる。
  `scripts/check-release.mjs` の文面も、疑う順を実際に起きた順へ直した。
  CI など `gh` が居ない所では、今までどおり環境変数が本筋（在ればそちらを使う）。

`npm run release` は
**検査 → ffmpeg のライセンス確認 → ビルド → インストーラ作成 → Releases へ添付
→ 差し替え（bundle）を作って足す → 添付が揃っているかを数える** までを通しでやる。
検査が落ちたらそこで止まる（壊れたものは出ない）。

### 添付は**5つ**（2026-08-06 に2つ増えた）

```
GiftCut-Setup-<版>.exe            インストーラ
GiftCut-Setup-<版>.exe.blockmap   差分ダウンロード用
latest.yml                        自動更新が最初に読む
bundle-<版>.zip                   **JS だけの差し替え**（約 360KB）
bundle-<版>.json                  その荷札（版・土台の指紋・中身の指紋）
```

**差し替えが無くても更新は動く。** そこが危ない——使う側は「差し替えは出ていない」と
読んで、静かにインストーラの道へ落ちる。動いてしまうので**上げ忘れに気づく機会が
一度も無い**まま「更新が十数秒かかる」に戻る。だから `check:release` が名前で数える。

### **`npm run release` は2回回す前提。しかも自分の終了コードは嘘をつく**

1回目は**添付が足りないまま**終わることがある。**そのとき electron-builder の
終了コードは 0**（＝「成功した」と言ってくる）。ログにも `uploading file=...exe` と
出ているのに、実際には上がっていない。`publishing` の行が2回出る（発行が二重に走る）
のが目印。

  0.1.11 / 0.1.21 / 0.1.22 / 0.1.23 と**4回連続で同じ落ち方**をしている。
  落ち方は毎回同じではない（0.1.23 は blockmap だけ欠けた）。

**2026-08-05 に、ここを機械で数えるようにした。**
`npm run release` の最後に `npm run check:release` が繋いであり、
**3つ揃っていなければ赤で止まる**（何が上がっていないかを名前で言う）。

  判定の中身 … `src/shared/releaseAssets.ts`（試験付き。実際に起きた4つの形で固定）
  叩く所     … `scripts/check-release.mjs`（`gh release view` を読む）

**それまでは「必ず数えること」と、ここに人間向けに書いてあった。
4回とも書いてあるとおりに落ちている**ので、文章では止まらないと分かった。
——読む人が居なければ効かない決まりは、決まりではない。

### **中途半端なリリースは、使っている人にエラーとして見える**（2026-08-06 に分かった）

出す側の面倒事だと思っていたが、違った。v0.1.28 の1回目が
「blockmap と差し替えだけ上がった」状態で止まっていた時刻に、
**本人のアプリがちょうど見に行って** これを掴んでいる:

```
エラー: Cannot find latest.yml in the latest release artifacts
        (…/releases/download/v0.1.28/latest.yml): HttpError: 404
```

自動更新は30分おきに見に行くので、**1回目と2回目の間に当たった人は全員これを見る**。
壊れはしない（右下に小さく出るだけで、次の確認で直る）が、
**2回目を回すまでの時間は短いほどよい。**

※ `gh` が無い・未ログイン・タグが無いときも**赤にする**。
  「見に行けなかった」を「問題なし」に倒すと、出せていないのに緑で終わる。

### 落ち方は1種類ではない（0.1.24 で分かった）

入れた初日に、**想定していなかった落ち方**をした:

```
0.1.11 / 0.1.21 / 0.1.22 / 0.1.23   終了コード 0 で「成功」。添付が足りない
0.1.24 の1回目                       **electron-builder 自身が HTTP エラーで終了コード 1**。
                                     しかも exe だけ上がっていた（中途半端）
```

最初は `electron-builder … && npm run check:release` と繋いでいたので、
**後者は一度も走らなかった。** 手で叩いて初めて「blockmap と latest.yml が無い」と出た。

→ 繋ぎ方をシェルから外した（`scripts/release.mjs` が順に呼ぶ。`&&` も `;` も `&` も
  Windows か POSIX のどちらかで外れる）。**発行が落ちても必ず数える。**
  そうすれば「落ちた。しかも中途半端に上がっている」まで一度に分かる。
  どちらの落ち方でも、2回目を回せば揃う（0.1.24 も2回目で3つ揃った）。

### タグを先に打つこと（0.1.1 でここに落ちた）

タグが無いまま出すと、添付まで済んだあとで最後に落ちる:

```
⨯ 422 Unprocessable Entity
   "Published releases must have a valid tag"
```

**公開のリリースには、実在するタグが要る。** 止まるだけで壊れはしないが、
ビルドを丸ごとやり直すことになる。順番を守れば起きない。

※ 失敗しても添付済みのファイルは残っているので、タグを打ってから
`npm run release` をもう一度回せば「上書き」で通る。

### もう一度回せば通る（0.1.2 と 0.1.3 で毎回これ）

タグを先に打っていても、1回目が落ちることがある:

```
⨯ 422 Unprocessable Entity  "already_exists" (tag_name)
```

発行の処理が二重に走り、片方が先にリリースを作って、もう片方が同じ物を
作ろうとして落ちる。**そのまま `npm run release` をもう一度回せば通る**
（2回目は「上書き」になる）。

**このとき手で埋めないこと。** 1回目が落ちた時点では blockmap しか
上がっておらず、しかも手元の `dist/latest.yml` は**前の版のまま**になっている。
それを上げると、更新が前の版を指したまま壊れる（実際に 0.1.2 でやりかけた）。

**0.1.11 / 0.1.22 でも起きた。** 1回目は「blockmap だけ上がって、exe と latest.yml が
無い」状態で終わる（`gh release view` で添付が1つしか無いことで分かる）。
**そのまま `npm run release` をもう一度回して通った**（2回目は
`overwrite published file ... reason=already exists on GitHub` と出る）。
ビルドからやり直すので数分かかる。

※ **2回目はビルドし直すので、exe の中身が1回目と変わる**（署名の時刻が入るため、
  大きさも数十バイト動く）。**それでよい**——公開される `latest.yml` は
  2回目の exe を指しているので、揃っている。1回目の数字と見比べて慌てないこと
  （0.1.22 で 121,508,384 → 121,508,353 と動いた）。

### 2回回しても exe と latest.yml が上がらないことがある（0.1.21 で踏んだ）

**ログには「上書きする」と出ているのに、添付は blockmap だけ**という形。

```
• overwrite published file  file=GiftCut-Setup-0.1.21.exe reason=already exists on GitHub
• overwrite published file  file=latest.yml reason=already exists on GitHub
```

「上書き」は**消してから上げ直す**ので、消えた所で終わると添付が減る。
`publishing` の行が2回出ている（発行が二重に走っている）のと同じ原因に見える。

**このときは手で上げてよい。** 上の「手で埋めないこと」は**ビルドが落ちたとき**の
話で、あれは `dist/latest.yml` が前の版のまま残っているのが理由。
今回は**ビルドは成功している**ので、確かめてから上げれば前の版を指す事故は起きない。

```bash
cat dist/latest.yml    # version が上げた番号か／size が dist の exe と一致するか
```

両方を確かめてから、**ハイフンの名前に写して**上げる:

```bash
cd dist && cp "GiftCut Setup 0.1.21.exe" GiftCut-Setup-0.1.21.exe
gh release upload v0.1.21 GiftCut-Setup-0.1.21.exe latest.yml --clobber
```

※ 上げた直後は**ダウンロードURLが数十秒 404 を返す**（反映待ち）。
  `gh release view` で添付が見えていれば上がっている。取り直せば通る。

### blockmap が上がらないことがある（0.1.4 で踏んだ）

`uploading ... blockmap` と出ていても、**添付が exe と latest.yml の2つしか
無いことがある**。blockmap が無いと差分更新が使えず、毎回まるごと落ちてくる
（壊れはしないが、113MB を毎回配ることになる）。

手で足すときは**名前に気をつける**。GitHub は空白を `.` に変えるので、
そのまま上げると `GiftCut.Setup.0.1.4.exe.blockmap` になり、
更新が探しに行く `GiftCut-Setup-0.1.4.exe.blockmap` と食い違う。
**ハイフンの名前に写してから**上げること。

```powershell
Copy-Item "dist\GiftCut Setup 0.1.4.exe.blockmap" "dist\GiftCut-Setup-0.1.4.exe.blockmap"
gh release upload v0.1.4 "dist/GiftCut-Setup-0.1.4.exe.blockmap"
```

### 出したら必ず確かめる

```powershell
$r = Invoke-WebRequest "https://github.com/kyohei0612/GiftCut/releases/download/v0.1.3/latest.yml" -UseBasicParsing
[System.Text.Encoding]::UTF8.GetString($r.Content)
```

見るのは2つ。**version が上げた番号になっているか**と、
**添付が3つ（exe / blockmap / latest.yml）そろっているか**。
リリース自体は中途半端でも「成功」に見えるので、ここを見ないと気づけない。

### 手で出すとき（トークンを渡したくないとき）

`gh` は自分で認証を持っているので、トークンを環境変数で渡さずに出せる。
0.0.1 はこの手順で出した。

```bash
npm run dist && npm run check:packaged
```

**ここで名前を必ず合わせること。** `latest.yml` は `GiftCut-Setup-0.0.1.exe`
（ハイフン）を指すが、出来上がるファイルは `GiftCut Setup 0.0.1.exe`（空白）。
**空白のまま上げると GitHub が名前を変えてしまい、更新が拾えなくなる。**

```bash
cd dist && cp "GiftCut Setup 0.0.1.exe" GiftCut-Setup-0.0.1.exe && cp "GiftCut Setup 0.0.1.exe.blockmap" GiftCut-Setup-0.0.1.exe.blockmap
```

```bash
gh release create v0.0.1 dist/GiftCut-Setup-0.0.1.exe dist/GiftCut-Setup-0.0.1.exe.blockmap dist/latest.yml --title "GiftCut 0.0.1" --notes-file notes.md
```

出したら、**更新が読む物を実際に取ってきて確かめる**（名前がずれていても
リリース自体は成功してしまうので、ここを見ないと気づけない）。

```bash
curl -fsSL https://github.com/kyohei0612/GiftCut/releases/download/v0.0.1/latest.yml
```

出したあとの Release は最初「下書き」ではなく公開になる。取り消したいときは
GitHub の Releases から消す。消せば、更新を見に行っても何も起きない状態に戻る。

## 身内に渡すとき（素材は ZIP で後から）

**渡す物は公開用と同じ1つだけ。** 素材（再配布が許可されていない効果音と
テロップ）は、あとから ZIP を渡して取り込んでもらう。

1. 公開用のインストーラをそのまま渡す（Releases の物でよい）
2. 素材の ZIP を別に渡す
3. 受け取った側は ファイル →「素材パック（ZIP）を取り込む…」で選ぶだけ

**身内用のビルド（`npm run dist:home`）は 0.1.6 で無くした。**

同梱する形には無理があった:

- **自動更新できない。** 更新すると本体はインストーラ版として別の場所に入るので、
  同梱した素材が付いてこない。「更新したら素材が消えた」になる。
  そのため身内だけ更新の輪から外れ、新しい版のたびに手渡しが要った
- **混ざる危険が残る。** 作る途中で `dist/win-unpacked/resources/` に
  SE と telop-presets が残り、次に公開用を作ったときに紛れ込みうる
  （`npm run check:packaged` は気づくが、`npm run release` はそこを通らない）

ZIP で渡す形なら、**配る物が1種類**になってこの2つとも消える。
素材は `%APPDATA%\GiftCut\` の下に入るので、更新しても消えない。

※ 昔作った `dist-home/` が手元に残っていることがある。中に再配布禁止の素材が
入っているので、**そのまま人に渡さない**（記録にも入らないようにしてある）。

## 使う人が足した物は、更新しても消えない

更新はアプリ本体を丸ごと入れ替えるが、**`%APPDATA%\GiftCut\` の下は触らない**。

| 物 | 置き場 |
|---|---|
| 効果音・テロップ素材（自分で足したぶん） | `%APPDATA%\GiftCut\SE\` `telop-presets\` |
| 動きのプリセット（取り込んだぶん） | `%APPDATA%\GiftCut\motion-presets\` |
| テンプレート | `%APPDATA%\GiftCut\テンプレート\` |
| アイコン画像・設定・自動保存の下書き | `%APPDATA%\GiftCut\`（Local Storage ほか） |
| お気に入り・自作スタイル・人物・自分の動きの**控え** | `%APPDATA%\GiftCut\ユーザー設定.json` |

**ファイルメニューの「〜のフォルダを開く」から全部開ける。** 開く道が無いと、
消えない場所があっても本人には無いのと同じなので、そこまでを1組にしてある。

### 素材を渡すとき

**ZIP を1つ渡せばよい。** 受け取った側は
ファイル →「素材パック（ZIP）を取り込む…」で選ぶだけ（展開は要らない）。

ZIP の中は、この4つのフォルダのどれかを直下に置く:

```
SE/  telop-presets/  motion-presets/  テンプレート/
```

**この4つ以外は取り込まない。** 受け取った ZIP の中身を無条件に置き場へ
撒かないため。素材の入っていない ZIP を選んだときは、何も入れずに終わる
（途中で失敗した場合も、入れたぶんを消して元へ戻す）。

### 右パネルの設定（お気に入り等）は、なぜ消えないか

これらは画面側の保存領域（localStorage）に入っている。**紐づく先は
「画面をどこから読み込んだか」で決まる**。いまは `file://`＝場所を含まない
原点なので、入れ直しても引っ越しても同じ所を見る。

**ここを独自の仕組み（`app://` のような物）へ変えると、その更新の瞬間に
利用者の設定が全部消える。しかも黙って消える。**
自動チェックで釘を打ってある（「右パネルの設定が、更新をまたいでも同じ場所に紐づく」）。

同じ内容を `ユーザー設定.json` にも控えているので、別の機械へ移すときは
そのファイルを持っていけばよい（手元に値がある鍵は上書きしない）。

## 使う人から見た動き

1. 起動して4秒後、新しい版があるか見に行く（無ければ何も出ない）
2. あれば裏で落とす。画面の上に細い帯で「⬇ 新しい GiftCut を用意しています… 42%」
3. 落とし終わったら
   - **手が空いていれば**「5秒後に再起動します」と出して、そのまま再起動
     （「今すぐ再起動」「あとで」も選べる）
   - **書き出し中・保存していない変更があるとき**は再起動しない。
     次にアプリを閉じたときに当てる
4. 更新のための再起動では、**落とす直前に今の状態を下書きに書く**。
   開き直したときは「復元しますか？」とは聞かず、そのまま続きから開く
   （自分で勝手に閉じておいて開き直しを頼むのは筋が通らないので）
5. 落としてくるのは**差分だけ**（`.blockmap` を使う）。
   照合が合わなければ自動で全部落とすので、失敗して止まることはない
6. いま動いている版は**題名の横と右下の帯**に出る（`v0.1.1`）。
   「直したはずが直っていない」と言われたら、まずここを見る

**更新の印（`resumeAfterUpdate`）は使い切り。** 控え（`ユーザー設定.json`）の
対象からは外してある。控えて次の起動で戻すと、更新していないのに
「更新後の扱い」になり、落ちたときの下書きが黙って読み込まれる。

## 署名について（やらないと決めた）

コードサイニング証明書は有料（年契約）なので**取らない**。
そのぶん、初めて入れる人に何が起きるかを分かった上で配る。

**起きること:**

- インストーラを実行すると「Windows によって PC が保護されました」が出る。
  **消えるのではなく、`詳細情報` → `実行` を押せば入る。**
  署名が無い物は「実績（ダウンロード数）」で警告が薄れる仕組みだが、
  配る数が少ないうちは毎回出ると思っておく
- ウイルス対策ソフトが誤検知することがある（署名が無い実行ファイルは疑われやすい）
- **更新は署名が無くても動く。** electron-updater は SHA512 で中身を確かめる

**やること（お金をかけずに減らせるぶん）:**

1. 配る場所に**入れ方を1行で書く**。「警告が出たら `詳細情報` → `実行`」。
   これが無いと、警告を見た人はだいたいそこで止まる
2. GitHub Releases から配る（知らない場所からのダウンロードより疑われにくい）
3. **ファイル名と発行元の表記を変えない**。同じ物を出し続けるほど実績が付く

**気が変わったら:** 証明書を取って `electron-builder.yml` の `win:` に
`certificateFile` を足すだけでよい（この設定のまま署名付きになる）。
EV 証明書なら警告は最初から出ない。OV 証明書は実績が付くまで出ることがある。
