# GiftCut の UI/UX 見直し（2026-08-05 / v0.1.22）

**狙い: 初心者から上級者まで、感覚で分かる。**
モードで分けずに、**1つの画面**でそれを成立させる。

見たもの: 通常編集の画面（`e2e/last-run.png`）／負荷テストの画面／右パネル各タブ／
トラック見出しの拡大／`styles.css` の実寸。**触ってはいない**ので、
「押した感じ」に関わる項目は推定と書いてある。

---

## 0. 先に、良い所（壊さないために書く）

- **骨格がプレミアと同じ。** 左＝選んだ物の設定、中央＝プログラムモニタ、
  右＝素材、下＝タイムライン、トランスポートは中央下、最下段にステータスバー。
  **経験者は初日から迷わない。** ここは動かさない
- **空の状態が教えている。** 素材ビンの「まだ素材がありません／ここへ掴んで落とすか…」は
  3つの入り口を全部書いていて、初心者向けとして正しい
- **クリップに元の位置が出ている**（`test_video.mp4 00:05.0〜`）。プレミアでも上級者が欲しがる情報
- **上のタブが作業の順番になっている**（ホーム→編集→字幕→書き出し）。
  初心者の道筋がUIに刻まれている。**これは資産。もっと使える**

---

## 1. 使う原則（なぜその判断か、を後から辿れるように）

| 原則 | 何を決めるか |
|---|---|
| **Fitts の法則** | 押す時間は「距離÷大きさ」。小さい物・遠い物は遅く、外れる |
| **Hick の法則** | 選択肢が増えると決める時間が伸びる（対数）。**まとめると実質の数が減る** |
| **ゲシュタルト（近接・共通領域）** | 線で区切るより、**間隔と背景**でまとめる方が速く読める |
| **認識 > 想起**（Nielsen #6） | 覚えさせない。**見れば分かる**所に置く |
| **加速器**（Nielsen #7） | 上級者の近道は、**初心者の目に入らないが、初心者の道の上にある** |
| **段階的開示** | 最初は少なく、必要になった時に出す。**隠すのではなく、静かにする** |
| **空間記憶** | 「最適な位置」より**動かないこと**が効く。配置換えは高くつく |
| **応答時間**（Nielsen） | 0.1秒＝即座、1秒＝流れが切れない、10秒＝注意が切れる |

※ **「7±2」は使わない。** あれは短期記憶の話で、**メニュー項目数の根拠にはならない**
（よくある誤用）。項目数は Hick と「まとまりの数」で考える。

---

## 2. 実測で出た具体的な問題

### 2-1. 押す物が小さすぎる（**最優先・機械で直せる**）

`styles.css` から実測:

```
.th-btn（🔒 / 👁）      16 × 15 px
.th-ms（M / S / ♪＋）   16 × 15 px
.zoom-bar-knob（●）     12 × 12 px
```

**WCAG 2.2 の下限は 24 × 24 CSS px**（SC 2.5.8・レベルAA）。
「隣と 24px 以上離れていれば小さくてよい」という例外があるが、
**M と S は隣り合っていて 4px しか離れていない**ので、例外にも当たらない。

参考の目安: WCAG AAA は 44、Apple は 44pt、Material は 48dp。
**24 は床であって推奨ではない。**

→ **見た目を変えずに当たり判定だけ広げる**のが正解（`::before` で透明な枠を張る）。
   絵は 16px のまま、押せる範囲を 24px にする。ロックや M/S は
   **押し間違えると音が消える**類なので、ここは効果が大きい。

### 2-2. トラック見出しの列が揃っていない（**意図はあるのに効いていない**）

拡大して測った（`last-run.png` を4倍）:

```
V3 / V2 / V1   🔓 👁                  ← 先頭が x≈49
A1 / A2        🔓 M S                 ← 先頭が x≈69（**20px 右へずれている**）
A3             🔓 M S ♪＋            ← 先頭が x≈49（たまたま揃っている）
```

コードには**揃える意図が書いてある**:

> 映像には M/S が無いが、空きを置いて列を揃える。
> 揃っていないと、段によってボタンの位置がずれて毎回探すことになる。

映像側には M/S ぶんの空きが2つ入っているが、**音声側に 👁 ぶんの空きが無い**。
だから3枠と4枠が混ざって、右揃えの分だけずれる。A3 は ♪＋ があって4枠になり、
**偶然**揃っている。

→ 音声側にも 👁 の位置へ空きを1つ入れる。**3行の変更。**
   （これは今日1日で何度も出た「片方だけ直した」型そのもの）

### 2-3. ショートカット早見表が常時出ている

```
Ctrl+Z 元に戻す / Ctrl+C・Ctrl+V コピー貼付 / Ctrl+D 複製 / Ctrl+K 分割 / M マーカー / ホイール 横・Shift+ホイール 縦
```

**これは「認識 > 想起」の正しい実装ではない。** 覚えていない人は、
その瞬間に見ている場所（メニュー・右クリック・ボタン）でこそ知りたい。
常設の表は**初日で読まれなくなり、横幅だけ残る**。しかも古くなる
（今日 `=` / `-` を足したが、ここには載っていない）。

→ **加速器は初心者の道の上に置く**（Nielsen #7 の本来の形）:

```
ボタンのツールチップに   「分割（Ctrl+K）」
右クリックの品書きに     「複製          Ctrl+D」
メニューに               同上
```

こうすると、**初心者はメニューを使うたびにショートカットを覚える**。
上級者は表を見ない。表は消してよい。

### 2-4. 同じ物が2か所に出ている

| | 上のツールバー | ステータスバー | モニタ下 |
|---|---|---|---|
| 比率 16:9 | ✓ | ✓ | |
| 再生ヘッドの時刻 | | ✓ | ✓ |

**片方が古くなる型**（今日コードで何度も潰したのと同じ）。
プレミアでは、時刻＝モニタ下、ステータス＝ツールと選択、比率＝設定。

### 2-5. 時間の書き方が2種類

モニタ右上 `01:00.0` ／ モニタ左下 `00:00:00:00`。
**同じ画面で流儀が2つ**あると、読むたびに切り替えが要る。
プレミアはタイムコードで統一。

### 2-6. 空きが埋まらない領域が2つある

- **左パネル**: 何も選んでいないと「タイムラインでクリップを選択してください」だけ。
  **幅250 × 高さ450 が常時死んでいる**。プレミアは同じ場所が
  「エフェクトコントロール」だが、**他のパネルとタブで同居**しているので死なない
- **プレビュー**: モニタの左右と下に暗い余白が広い。
  **一番見る場所**なので、パネルいっぱいまで使う価値がある

### 2-7. 右パネルのタブが既定幅で入りきらない

タブ帯に `‹` `›` `»` の**3種類の「まだ先がある」表示**が同時に出ている。
5つ（プロジェクト／テロップ／アイコン／SE／トランジション）が入らない。

→ 選択肢: ①ラベルを短く（トランジション→効果）②アイコン＋文字
   ③既定幅を広げる。**「送りボタン」で解決しようとしない**のが要点——
   送りは「入っていない」ことの証拠であって、解決ではない。

### 2-8. 引いたときのタイムラインが読めない（**負荷テストの絵で顕著**）

- **めじるしの線が全画面高・高彩度の緑**で並ぶ。150本あると「柵」になる
  → 情報量の小さい物が、一番強い視覚的重さを持っている
- **全部の段が同じ重さ**。本編（V1/A1）が編の背骨なのに目立たない
  → プレミアは V1/A1 を既定で高くして、他を静かにする

### 2-9. 透明部分が市松模様

素材が枠を埋めないとき、モニタに市松（透明）が出る。
**書き出すと黒になる**ので、見えている物と出来上がる物が違う。
プレビューは「出来上がりの予告」であるべき。→ 黒で埋める。

### 2-10. 名前のないつまみ

モニタ上の帯に、白い四角と横スライダーがラベル無しで並んでいる。
**何のつまみか、押すまで分からない。** 最低でもツールチップを。

---

## 3. 「初心者から上級者まで」を1つの画面で成立させる設計

ここが本題。**モードを分けない**のが結論。理由と、そのための4つの規則。

### 規則1: 上級者の道は、初心者の道の**上**に置く

```
初心者      メニュー／右クリック／ボタンを使う
              ↓ そこにショートカットが併記してある
上級者      キーだけで同じことをする
```

**別の場所に「上級者向け設定」を作らない。** 作ると、初心者はそこへ辿り着けず、
上級者は2か所を覚えることになる。2-3 の早見表を消して併記へ移すのが、この規則の実践。

### 規則2: 難しい物は**隠さず、静かにする**

隠す（メニューの奥・別画面）と、存在に気づけない。
**同じ場所に置いたまま、彩度と大きさを落とす**のが正しい。

例: キーフレーム（◆）はクリップ上に出ているが、使わない人には模様でしかない。
**消さない。薄くする。** 触った瞬間に濃くなればよい。

### 規則3: **直接操作を第一、数値は第二**

「感覚で分かる」の中身は、ほぼこれ。掴む・引く・落とすで完結し、
数値欄は**確認と微調整**のために添える（逆にしない）。

いまの GiftCut は既にこの形になっている（掴んで動かす・端を引く・落として配置）。
**強い。** 数値欄も `ScrubNumber`（押して振ると増減）があり、
**掴む操作と数値が地続き**になっている。これは良い設計。

### 規則4: 配置を動かさない

**「最適な位置」より「いつもの位置」**。空間記憶は速く、意識を使わない。
今日の「拡大の軸を入口ごとに変える」も、**入口ごとに一貫していれば**手が覚える。

---

## 4. 手を付ける順番（費用対効果）

| 順 | 何を | なぜこの順か | 規模 |
|---|---|---|---|
| 1 | ~~**当たり判定を24pxへ**（2-1）~~ | **済（08-05）。ただし横は 20px 止まり**（下に理由） | 小（CSS） |
| 2 | ~~**トラック見出しの列を揃える**（2-2）~~ | **済（08-05）。3行では済まなかった**（下に理由） | 小 |
| 3 | ~~**重複表示と時間書式を1つに**（旧4・2-4/2-5）~~ | **済（08-05）** | 小 |
| 4 | ~~**早見表を消す→ツールチップ併記**（2-3）~~ | **済（08-05）** | 中 |
| 5 | ~~**プレビューを広げる／市松を黒に**（2-6/2-9）~~ | **済（08-05）** | 中 |
| 6 | ~~**めじるし線を静かに／本編を太く**（2-8）~~ | **済（08-05）** | 中 |
| 7 | **右パネルのタブを収める**（2-7） | **見送り（本人の判断・08-05）。**数字は下 | 小〜中 |
| 8 | ~~**左パネルの空きを埋める**（2-6）~~ | **済（08-05）** | 大 |

---

## 4-1. 1〜3 をやってみて分かったこと（2026-08-05）

**見積もりが3件とも外れている。** 外れ方に型があるので残す。

### 「当たり判定を24pxへ」→ **24 にすると悪くなる**

隣との間隔（pitch）は **16px（絵）＋ 4px（すき間）＝ 20px**。
24 にすると**隣の判定と4px重なり**、重なった所は後ろの要素が勝つので
**M の右端を押すと S が反応する**——小さいままより悪い。

→ **20 × 24** にした（面積 240 → 480px² で倍）。24 × 24 を取るには
  見出しを 128 → 140px へ広げる要があり、それは別の判断。

**本当の決まりは「24px 以上」ではなく「大きさ ≦ 隣との間隔」だった。**
検査（`src/shared/hitArea.test.ts`）も両方を CSS から読んで突き合わせている
——数字だけ固定すると、あとから gap を詰められて黙って壊れる。

拡大バーの●も同じ形で詰まった。判定を広げると**左右の●が重なる**ので、
つまみの下限を 22 → 28px へ上げた。それでも 24 にしないのは、
**真ん中の「掴んで動かす」が 8px しか残らない**ため。

### 「列を揃える・3行」→ **役割ごとに5枠だと名前が消える**

素直に5枠（🔒 / 👁 / M / S / ♪＋）にすると、見出しは 128px 固定なので
**名前に残るのが 41px → 18px**。「A1」すら出なくなる。

→ **👁（映像だけ）と ♪＋（音声だけ）は同じ段に同時に出ない**ので、
  1つの列を分け合わせて**全段4枠**にした。よく押す 🔒 / M / S が揃うことを優先。

### 「重複を消す」→ **消したら配線が1本減った**

下の帯から時刻を外したら、`App.tsx` が `currentTime` を受け取る理由が消えた。
**再生中は毎コマ変わる値**なので、画面の骨格が毎コマ描き直される線が1本無くなっている。
重複表示は見た目の問題だと思っていたが、**配線の問題でもあった。**

### おまけ: e2e の見張りが死んでいたのを見つけた

確認を回したら `v1Clips is not defined` で1件落ちた（**私の変更とは無関係**。
600行超を割った日の受け取り忘れ）。それ自体より問題なのは、
**`e2e/lint-checks.mjs` がそれを「書き忘れなし」と緑で言っていた**こと。

原因は名前の集め方が**行頭固定**で、道具はほぼ全部が関数の中＝字下がりのため
**75個中25個しか見ていなかった**。字下がりを許すと今度は run.mjs の局所変数まで
拾って**37ファイル全部が赤**。→ **`const C = { … }` の鍵だけを読む**形に作り直した
（それが確認ファイルとの契約そのもの）。ついでにコメントと文字列も落とした。

**CLAUDE.md 7番「測る側は成立しなければ落ちるに倒す」の実例が、また1つ増えた。**

---

## 4-2. 4〜8 をやってみて分かったこと（2026-08-05）

### 4 早見表 →「併記」は**もう出来ていた**

道具立てのボタンは前からツールチップに `(Ctrl+Z)` を持っていたし、
右クリックの品書きも `コピー（Ctrl+C）` と書いてあった。**足りなかったのは形**。

名前の途中にキーが混ざっていると、**キーを探すのに一行ずつ読む**ことになる。
右端に揃える列（`ContextMenu` の `combo`）へ移した。Windows もプレミアもこの形で、
名前を追う目とキーを追う目が別々に動ける。**早見表はこれで要らなくなった**ので消した。

**掴んでいる最中の案内は残した。** 「いま離すとどうなるか」「Ctrl を足すと何が変わるか」
を操作の最中に出すもので、常設の表とは別物。読まれる案内はこちらの形。

### 5 プレビュー → **余白より「嘘」の方が問題だった**

市松模様は画像編集の作法としては正しいが、このアプリでは**嘘になる**。
素材が枠を埋めない所は、書き出すと必ず黒になる。**見えている物と出来上がる物が
違っていた**ので、下地を黒にした（プレビューは「出来上がりの予告」であるべき）。

余白の方は 18px → 10px。ただし**モニタ左右の黒帯の大半はレターボックス**
（区画と映像の縦横比の差）で、余白ではない。そちらは詰めようがない。

### 6 めじるし → 線を 0.5 → 0.18 に落とし、V1 を既定で高く

線は「ここに印がある」しか言っていないのに、**全画面高・高彩度の緑**で
画面の一番強い視覚的重さを持っていた。消さずに薄く（規則2）。旗は今までどおり。

本編の段は A1 が既に 44px だったが、**V1 だけ最小の 26px のまま**だった。
V1 も 44 に。**2本だけにする**——高い段が3本4本と増えたら「本編だけ高い」が消える。

### 7 右パネルのタブ → **送りボタンを置いたままでは、何をしても入らない**

測ったら答えが出た（`.tab` の padding 10×2・font 12 から）:

```
タブの合計          約 344px（プロジェクト/テロップ/アイコン/SE/トランジション）
送り ‹ › ≫          **60px（常時）**
右パネルの幅        300px（内側 292px）
```

**ラベルを詰めても、padding を削っても、60px の常設ノイズがある限り入らない。**
「送りは『入っていない』ことの証拠であって解決ではない」が数字で裏づけられた形。

解ける道は3つしかない: ①折り返して2段 ②語彙を削る（トランジション→切替）
③パネルを広げる（＝プレビューを削る）。**どれも別の物を差し出す**ので、
本人の判断で**見送り**とした（08-05）。いまは ≫ から1クリックで届く。

※ 送りの常時表示は「出たり消えたりで押す場所がずれる」という**意図した仕様**で、
  e2e に6件の確認が付いている。動かすならそこも書き換えることになる。

### 8 左パネル → **埋める中身の選び方の方が難しかった**

空いた 250 × 450 に何を置くかで、**その日の午前中の作業を台無しにしかけた。**
比率・再生ヘッド・テロップ数を並べたくなるが、**どれも午前中に「重複だから」と
外したばかり**の物。置いたら数時間で元へ戻すことになる。

→ 決め方を先に決めた: **「他所に出ていない事」だけを出す。**
   残ったのは**書き出しの中身**（解像度・fps・画質）だった——素材から自動で決まる
   仕様なので、**窓を開くまで何が出るのか見えない**。4K を読んだつもりが 1080p、は
   出来上がってから気づく類の事故なので、常に見えている価値がある。

**「空きを埋める」は、置ける物を探す作業ではなく、置いてはいけない物を除く作業だった。**

---

## 5. 測り方（やりっぱなしにしないため）

このリポジトリの流儀に合わせて、**直したら機械で押さえる**:

- 当たり判定 … `styles.css` の実寸を試験で固定（24px を下回ったら赤）
- 列揃え … トラック見出しの各行で、先頭アイコンの x が同じことを e2e で見る
- 重複表示 … 「同じ値が2か所に出ていない」は人が見るしかない。**決めた場所を
  コメントに書く**（`比率はステータスバーだけ` のように）

※ **見た目の良し悪しは機械で測れない。** 測れるのは「決めたとおりか」だけ。
  だから**決めた事を書き残す**方が、検査を増やすより効く。
